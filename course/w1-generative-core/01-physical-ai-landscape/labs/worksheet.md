# W1-M1 워크시트 — 제출물

> **이 파일이 W1-M1의 제출물입니다.** [`README.md`](README.md)의 5단계를 따라가며 채우세요.
> 시작 전에 사본을 만들어 쓰는 것을 권합니다: `cp worksheet.md worksheet-$(date +%F).md`
>
> 🔴 **채우는 순서가 중요합니다.** ①②는 **lesson을 닫고** 기억만으로 먼저 채운 뒤, 그다음에 §3.3·§7.1을 열어 채점합니다.
> 🔴 **모르는 칸은 `?`로 남기세요.** 억지로 채우면 이 워크시트가 무의미해집니다. `?`가 팀 질문의 원재료입니다.

| 항목 | 값 |
|---|---|
| 작성일 | |
| 실행 환경 (인스턴스 / 로컬) | |
| Python 버전 (`python3 --version`) | |
| 한글 폰트 사용 여부 (`[font]` 줄) | 한글 사용 / 영문 폴백 |
| 총 소요 시간 | |

---

## ① 5계층 이름 + 주파수 (백지)

**lesson을 닫고** 채우세요. 주파수는 범위로 적습니다.

| 계층 | 이름 | 동작 주파수 | 입력 → 출력 (한 줄) |
|---|---|---|---|
| L5 | | | |
| L4 | | | |
| L3 | | | |
| L2 | | | |
| L1 | | | |

**대역폭 분리 — 인접 경계 중 어디가 가장 넓은가?** (Step 3.4)

| 경계 | 대표 비율 | 판정(OK / 약함) |
|---|---|---|
| L1 / L2 | ×____ | |
| L2 / L4 | ×____ | |
| L4 / L5 | ×____ | |

가장 넓은 경계: **____** · 그 자리에 L3가 놓인 이유를 한 줄로:

```
(여기에 작성)
```

`약함`으로 나온 경계가 있다면, 그 경계는 대역폭 분리가 아니라 무엇의 분리인가?

```
(여기에 작성)
```

### ① 자기 채점

| 대조 대상 | 결과 |
|---|---|
| **lesson §3.3** (5계층 블록도) — 계층 이름 | ___ / 5 정답 |
| **lesson §3.3** — 주파수 | ___ / 5 정답 |
| **lesson §3.3** — 입출력 | ___ / 5 정답 |
| **`01` 블록 [2]** stdout — 분리비 3개 | ___ / 3 정답 |

틀린 항목 메모:

```
(여기에 작성)
```

---

## ② 회사 스택 5요소 배치 (백지)

**lesson을 닫고** 채우세요. 확실하지 않으면 `?`.

| 계층 | 회사 스택 요소 | 이 요소를 깊게 다루는 모듈 | 확신도 (확실 / 애매 / 모름) |
|---|---|---|---|
| L5 | | | |
| L4 | | | |
| L3 | | | |
| L2 | | | |
| L1 | | | |
| 병렬(데이터 수집) | | | |

### ② 자기 채점

| 대조 대상 | 결과 |
|---|---|
| **lesson §7.1** (배치도 Mermaid) — 계층별 요소 | ___ / 5 정답 |
| **lesson §7.2** (계층 × 스택 × 모듈 매핑 표) — 모듈 번호 | ___ / 6 정답 |

> §7.2 표의 **"확인 상태" 열**을 같이 보세요. `팀 확인 필요`가 붙은 항목은 **내가 틀린 게 아니라 아무도 모르는 것**입니다.
> 그 항목을 `?`로 남겼다면 정답으로 칩니다.

틀린 항목 메모:

```
(여기에 작성)
```

---

## ③ Step 2 시나리오별 계산 결과

> 🔴 **"손계산" 열을 먼저 채우고 나서 스크립트를 돌리세요.** 순서를 바꾸면 이 표는 받아쓰기가 됩니다.

기준선: $f_2 = 50$ Hz, $f_4 = 5$ Hz, $\tau_{\text{infer}} = 100$ ms, $\tau_{\text{comm}} = 20$ ms → $H_{\text{required}} = 16$, 최악 반응 지연 320 ms.
(Step 2는 전부 `--smoke` **없이** 실행합니다.)

### 시나리오 ① `--f2 200`

| 항목 | 손계산 (먼저) | 스크립트 (나중) | 일치? |
|---|---|---|---|
| $H_{\text{required}}$ | | | |
| 최악 반응 지연 [ms] (H_required 기준) | | | |
| stale head [스텝] | | | |

$f_2$를 4배로 올렸는데 최악 반응 지연이 어떻게 됐고, 왜 그런가? (식으로)

```
(여기에 작성)
```

### 시나리오 ② `--tau-infer 0.300`

| 항목 | 손계산 (먼저) | 스크립트 (나중) | 일치? |
|---|---|---|---|
| $H_{\text{required}}$ | | | |
| 최악 반응 지연 [ms] | | | |
| stale head [스텝] | | | |
| `[5]` 표 `부족` 행의 공백률 [%] | | | |

$\tau_{\text{infer}}$가 3배가 됐는데 $H$는 몇 배가 됐는가? 3배가 아닌 이유는?

```
(여기에 작성)
```

`부족` 행의 공백률이 그 값이 나온 이유는? (stale head와 $H$를 비교해서)

```
(여기에 작성)
```

### 시나리오 ③ `--h-chunk 8`

| 항목 | 손계산 (먼저) | 스크립트 (나중) | 일치? |
|---|---|---|---|
| 공백률 [%] | | | |
| 최악 반응 지연 [ms] | | | |
| ASCII 타임라인의 반복 주기 [스텝] | | | |

손계산과 스크립트 값이 다르다면 **어느 쪽이 틀린 것인가?** (힌트: 둘 다 틀리지 않았습니다)

```
(여기에 작성)
```

### 마무리 질문

> **L4 추론을 300 ms로 만드는 대신 청크를 늘리는 선택의 대가는 무엇인가?**
> (lesson §4.3 트레이드오프 + MPC 예측 지평 대응을 반드시 포함할 것)

```
(여기에 작성 — 4~6문장)
```

### ③ 자기 채점

| 대조 대상 | 결과 |
|---|---|
| **lesson §4.1** 부등식 — 손계산 3개 | ___ / 3 일치 |
| **lesson §4.3** 트레이드오프 문단 — 마무리 질문에 "반응성" 언급 | 있음 / 없음 |
| **lesson §4.3** 제어 대응 문단 — 마무리 질문에 "receding horizon / 예측 지평 $N$" 언급 | 있음 / 없음 |
| **lesson 셀프 체크 퀴즈 7번** 정답과 대조 | 일치 / 불일치 |

---

## ④ Step 4에서 `?`로 남긴 칸

Step 4에서 채우지 못했거나 확신이 없어 `?`를 붙인 칸을 전부 옮겨 적습니다.

| # | 어느 칸인가 (구역 / 계층) | 무엇을 몰랐나 | lesson에 답이 있었나 | 팀 질문으로 승격? |
|---|---|---|---|---|
| 1 | | | 있음 / **없음(내부 구현)** | 예 / 아니오 |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |

**14칸 처리 현황**

| 구역 | 칸 수 | 채움 | `?` |
|---|---|---|---|
| 동작 주파수 (L5·L4·L3·L2) | 4 | | |
| 우리 회사 스택 (L5~L1) | 5 | | |
| 화살표 라벨 (4개 경계) | 4 | | |
| 데이터 수집 박스 | 1 | | |
| **합계** | **14** | | |

> ✅ 합이 14여야 합니다. ✅ `?`가 **3개 이상**이어야 합니다(0개면 베낀 것).

**산출물 경로**: `artifacts/W1-M1/my_stack.png` — 저장 확인 [ ]

### ④ 자기 채점

| 대조 대상 | 결과 |
|---|---|
| **lesson §3.3** 블록도 — 주파수 4칸 + 화살표 4개 | ___ / 8 정답 |
| **lesson §7.1** 배치도 — 회사 스택 5칸 + 데이터 수집 1칸 | ___ / 6 정답 |
| lesson에도 답이 없어 `?`가 정답이었던 칸 | ___ 개 |

---

## ⑤ Step 5 — SONIC 데모 감상

### 5.1 접근 기록

| 항목 | 값 |
|---|---|
| `nvlabs.github.io/GEAR-SONIC/` 접속 | 성공 / 실패 |
| 데모 영상 재생 | 성공 / 실패 |
| 실패했다면 대체 자료 | arXiv:2511.07820 Figure / GR00T-WholeBodyControl 문서 / 리포 README GIF |

### 5.2 세 가지 관점 메모 (각 한 줄 이상)

**① 하나의 정책이 VLA·텔레옵·게임패드를 모두 받는다는 게 화면에서 어떻게 보이는가?**

```
(여기에 작성)
```

**② 모션이 "자연스럽다"는 게 구체적으로 뭘 말하는가?** (구체적 동작으로)

```
(여기에 작성)
```

**③ 이 영상에서 L3 인터페이스는 어디에 숨어 있는가?**

```
(여기에 작성)
```

### 5.3 한 문단 — "회사가 지향하는 최종 그림"

> 3~5문장 · **자기 말로** · 5계층 중 **최소 3개 계층 이름 등장** · **마지막 문장은 "그런데 내가 아직 모르는 것은 ~다"로 끝낼 것**

```
(여기에 작성)
```

**자기 점검**

- [ ] lesson 문장을 복사하지 않았다
- [ ] 계층 이름이 3개 이상 등장한다 → 등장한 계층: ____________
- [ ] 마지막 문장이 "모르는 것"으로 끝난다
- [ ] Step 4의 다이어그램과 모순되지 않는다 (모순이 있다면 아래에 기록)

모순이 있었다면 무엇이었고 어느 쪽이 틀렸나:

```
(여기에 작성)
```

---

## ⑥ 셀프 체크 퀴즈 10문항 — 틀린 것 기록

lesson **「셀프 체크 퀴즈」**를 풀고 **그 `<details>` 정답**과 대조합니다. 맞은 것은 비워두고 **틀린 것만** 적으세요.

| # | 주제 | 결과 | 틀린 이유 (내 답 vs 정답) | 다시 볼 절 |
|---|---|---|---|---|
| 1 | LLM과의 차이 3가지 / 진짜 병목 | O / X | | §2.1 |
| 2 | 5계층 이름 + 주파수 | O / X | | §3.3 |
| 3 | "승부처는 L3"의 근거 | O / X | | §3.5 |
| 4 | 계층 = 대역폭 분리 (제어 용어) | O / X | | §3.4 |
| 5 | $H_{\text{chunk}}$ 계산 (답 41) | O / X | | §4.1 |
| 6 | Action chunking = MPC 예측 지평 / 대가 | O / X | | §4.3 |
| 7 | 데이터 피라미드 4층 | O / X | | §5.1 |
| 8 | IDM / latent action / world model 차이 | O / X | | §5.2 |
| 9 | 조직별 베팅 계층 / Gemini Robotics 2의 의미 | O / X | | §6.3, §7.4 |
| 10 | 회사 스택 5요소 배치 + 모르는 것 3가지 | O / X | | §7.1, §7.2 |

**점수**: ___ / 10

**7점 미만이면** 다음 모듈로 넘어가기 전에 §3.3·§3.4·§4를 다시 읽으세요. 이 세 절은 W2~W4 내내 재사용됩니다.

---

## ⑦ 내가 발견한 불일치 (문서 vs 실제 출력)

랩을 돌면서 문서와 실행 결과가 어긋난 곳을 적습니다. **이걸 찾는 것도 랩의 목적입니다.**

| # | 어디 | 문서는 뭐라고 하나 | 실제 출력은 | 어느 쪽이 맞나 |
|---|---|---|---|---|
| _예시_ | _`practice/README.md` §5_ | _"L1과 L2 사이가 가장 넓다"_ | _`01` 블록 [2]: L1/L2 = ×8.9, **L2/L4 = ×50**_ | _출력. 집필 중 발견돼 이미 수정됨_ |
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |

---

## ⑧ 팀 질문으로 승격할 항목 ★

> **온보딩의 핵심 산출물입니다.** 여기 적은 것을
> [`notes/questions-for-team.md`](../../../../notes/questions-for-team.md)의 **W1-M1 표에 새 행으로 복사**합니다.
> 기존 P0 1~5, M1-1~M1-6과 **중복되지 않는 것**으로 최소 3개.

| # | 질문 | 왜 중요한가 (이 답이 없으면 무엇을 못 하는가) | 기존 질문과 중복? | 우선순위 |
|---|---|---|---|---|
| 1 | | | 아니오 / M1-__와 유사 | P0 / P1 |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |

**중복 확인 결과**: 내가 스스로 도달한 질문 중 기존 리스트(M1-1~M1-6)와 겹친 것은 ___ 개.
겹쳤다면 그것도 성과입니다 — 어느 것이 겹쳤는지 적어두세요.

```
(여기에 작성)
```

- [ ] `notes/questions-for-team.md`에 복사 완료 (추가한 행 번호: ______)

---

## ⑨ 용어집 적립 (5개)

[`notes/glossary.md`](../../../../notes/glossary.md) W1 섹션에 추가할 용어 5개.
**정의 한 줄 + 제어/ML/LLM 대응 비유**를 반드시 같이 적으세요(비유가 없으면 나중에 안 떠오릅니다).

| 용어 | 정의 (한 줄) | 제어·ML·LLM 대응 비유 |
|---|---|---|
| | | |
| | | |
| | | |
| | | |
| | | |

- [ ] `notes/glossary.md`에 복사 완료

---

## ⑩ 실행 로그 (→ `docs/progress.md`로)

| 항목 | 값 |
|---|---|
| 총 소요 시간 (계획 120분 대비) | |
| GPU 사용 | **없음** |
| 인스턴스 비용 | |
| 가장 오래 걸린 Step | |
| 막힌 지점 + 해결 방법 | |
| 발생한 에러 (README의 E1~E7 중) | |
| 다음 모듈로 넘길 미해결 항목 | |

- [ ] [`docs/progress.md`](../../../../docs/progress.md)에 기록 완료

---

## 최종 제출 체크

- [ ] ①~⑩ 전부 기입 (빈칸 없음 — `?`와 "모름"도 기입으로 인정)
- [ ] `artifacts/W1-M1/my_stack.png` 존재
- [ ] `notes/questions-for-team.md`에 새 질문 3개 이상
- [ ] `notes/glossary.md`에 용어 5개
- [ ] `docs/progress.md`에 실행 로그
- [ ] `docs/course-plan.md`의 W1-M1 산출물 5번(실행검증) 체크박스 갱신
