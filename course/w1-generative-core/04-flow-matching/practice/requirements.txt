# W1-M4 practice 의존성 — 버전 고정
# 집필 환경에서 실제로 설치·실행 검증한 버전입니다.
#   Python 3.12.12 / Ubuntu on WSL2 / RTX 3080 12GB (capability 8.6) / torch 2.13.0+cu130 / 2026-08-04
#
# ⚠️ **W1-M3와 완전히 같은 의존성입니다. 추가 설치가 하나도 없습니다.**
#    W1-M2·M3·M5에서 만든 venv를 그대로 쓰면 됩니다.
# ⚠️ 이 모듈은 W1-M3 practice의 세 파일을 **모듈로 로드해서 재사용**합니다.
#    따라서 `course/w1-generative-core/03-diffusion-ddpm-dit/practice/` 가 리포에 있어야 하고,
#    `02`는 `artifacts/W1-M3/02_results.csv`(W1-M3 실습 산출물)를 읽습니다.
#    없으면 경고만 하고 FM/RF 곡선만 그립니다 — README §5.2 참고.
# ⚠️ **세 스크립트 전부 CPU로 완주합니다.** 실측 시간은 README.md §2.1.

# --- 학습 ---
# CUDA 13.0 빌드입니다. PyPI 기본 인덱스가 아니라 PyTorch 인덱스에서 받아야 합니다:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu130
# CPU만 있는 환경이면 대신:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
# (버전 번호는 같고 빌드만 다릅니다. 스크립트는 --device cpu 로 자동 폴백합니다.)
torch==2.13.0

# --- 데이터와 수치 ---
numpy==2.5.1
scikit-learn==1.9.0       # 01·02의 2D toy 분포. W1-M3 make_data()가 쓰므로 여기서도 필요합니다
                          # — **같은 make_moons(16384, noise=0.06, seed=0) 호출**이어야
                          #   02의 겹쳐 그리기가 성립합니다(01 §7에서 bit 단위로 대조).

# --- 물리 엔진 (03에서만, D=29를 읽는 용도) ---
# 03은 G1의 model.nu 와 actuator_ctrlrange 만 읽습니다. 렌더는 하지 않습니다.
# menagerie가 없으면 03이 경고만 하고 D=29 상수로 계속 진행합니다(W1-M3 03과 같은 처리).
mujoco==3.11.0

# --- 그림 저장 (뷰어 금지, 결과는 전부 파일로) ---
matplotlib==3.11.1        # 세 스크립트 모두 Agg 백엔드 고정

# --- 노트북 동기화 (.py <-> .ipynb) ---
jupytext==1.19.5

# ─────────────────────────────────────────────────────────────────────────────
# 로봇 모델은 pip 패키지가 아니라 git clone입니다 (약 2.3 GB, W1-M2/M3에서 이미 받았다면 재사용):
#   git clone --depth 1 https://github.com/google-deepmind/mujoco_menagerie.git repos/mujoco_menagerie
#
# 이 모듈이 **쓰지 않는** 것:
#   · torchdiffeq / torchcfm / flow_matching 등 FM 라이브러리
#     — §5.2의 손실이 세 줄이고 Euler 샘플러가 네 줄이라는 것이 실습의 요점입니다.
#       라이브러리를 부르면 그 세 줄이 안 보입니다.
#   · diffusers — W1-M3와 같은 이유. DDIM 대조군도 W1-M3 코드를 그대로 재사용합니다.
#   · torchvision — 이미지 데이터셋을 쓰지 않습니다(2D toy와 G1 액션 청크뿐).
#   · scipy — 에너지 거리·sliced W₁을 torch로 직접 구현한 W1-M3의 함수를 씁니다.
# ─────────────────────────────────────────────────────────────────────────────
