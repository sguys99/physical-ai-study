# W2-M2 워크시트, 제출물

> **이 파일이 W2-M2의 제출물입니다.** [`README.md`](README.md)의 8단계를 따라가며 채우세요.
> 시작 전에 사본을 만들어 쓰는 것을 권합니다. `cp worksheet.md worksheet-$(date +%F).md`
>
> 🔴 **⑥과 ⑦이 이 모듈의 본체입니다.** ①~⑤는 전부 그 둘을 쓰기 위한 재료입니다.
> ⑥은 lesson [§4.2](../lesson.md)의 실행 방식 비교표를 **자기 기기 숫자로 다시 세우는 것**이고,
> ⑦은 lesson 상단의 **완료 기준** 그 자체입니다.
>
> 🔴 **모르는 칸은 `?`로 남기세요.** 특히 회사 내부 구현은 lesson §7.3에도 답이 없습니다(4개 항목 전부 미확인).
> **그 `?`가 팀 질문의 원재료입니다.** 채워진 칸보다 `?`가 온보딩의 산출물에 더 가깝습니다.

---

## ⓪ 기본 정보

| 항목 | 값 |
|---|---|
| 작성일 | |
| 총 소요 시간 (계획 2시간 20분 대비) | |
| 선수 모듈 완주 여부 | **W2-M1 랩 ☐** · **W2-M2 practice ☐** (`01` ☐ `02` ☐ `03` ☐ `04` ☐) · W1-M1 ☐ |
| lesson 정독 완료 | ☐ |
| **롤아웃 mp4를 실제로 재생해 봤는가** ★ | ☐ 예 ☐ 아니오 |

### 실행 환경

| 항목 | 값 | 확인 명령 |
|---|---|---|
| **가상환경** | `.venv-lerobot` ☐ / 다른 것 ______ | `verify` 출력 `[1]` 표의 python 경로 |
| Python | | `.venv-lerobot/bin/python --version` |
| `lerobot` | | `[1]` 표 |
| `torch` + cuda | 버전 ______ / `cuda_available` = ______ | `[1]` 표 |
| `diffusers` / `gym-pusht` / `pymunk` | ______ / ______ / ______ | `[1]` 표 |
| GPU | 모델 ______ / VRAM ____ GB | `[1]` 표 |
| `ffmpeg` | 있음 ☐ / **없음(E4 폴백)** ☐ | `which ffmpeg` |
| **디스크 여유 (시작 전)** ★ | ______ GB | `df -h .` |
| **디스크 여유 (Step 5 직후)** ★ | ______ GB | 차이가 3 GB 넘게 나면 E6 |

**집필 검증 환경과 다른 점** (README §0.5는 WSL2 / Python 3.12.12 / lerobot 0.6.1 / torch 2.11.0+cu130 / diffusers 0.39.0 / RTX 3080 12GB / ffmpeg 없음 / 2026-08-11):

```
(여기에 작성. 없으면 "동일")
```

### Step별 실측 소요 시간

| Step | 계획 | 명령 실행(초) | 실측 소요 | 집필 실측 |
|---|---|---|---|---|
| Step 1. 설치와 대조 | 10분 | | | `--quick` 6.5초 |
| Step 2. 데이터셋 | 7분 | | | `--with-dataset` 7.6초 |
| Step 3. 학습 스모크 300스텝 | 12분 | | | 학습 루프 64초 |
| **Step 4. 논문 레시피 ★** | 15분 | | | 27.7초 |
| **Step 5. 평가와 롤아웃 ★★** | 20분 | | | `eval_s` 13.1초 |
| **Step 6. 사전학습 체크포인트 ★** | 15분 | | | 두 번 다 실패로 끝남 |
| **Step 7. §4.2 재계산 ★★** | 25분 | | | `practice/04` 31초 |
| **Step 8. 완료 기준 ★★** | **35분** | (코드 없음) | | |
| **합계** | **약 2시간 20분** | **약 6분** | | |

> Step 7+8이 전체의 **43%** 여야 정상입니다. 명령만 돌리고 끝냈다면 이 비율이 무너집니다.

---

## ① Step 1. 설치와 환경

### 1.1 무엇이 새로 들어왔는가

W2-M1의 `.venv-lerobot`에 얹은 것입니다. 🔴 **새 venv를 만들었다면 그것부터 되돌리세요.**

| extra | 무엇이 들어오나 | 왜 필요한가 |
|---|---|---|
| `pusht` | `gym-pusht` · `pymunk` · `pygame` · `shapely` · `scikit-image` | Step 5의 평가 환경 |
| `diffusion` | `diffusers` | **없으면 정책 생성이 죽습니다** (E3) |
| `evaluation` | 평가 유틸 | `lerobot-eval` |

**실제로 들어온 버전** (`verify [1]` 표에서):

```
diffusers   ______
gym-pusht   ______
pymunk      ______
gymnasium   ______
```

### 1.2 W1 환경과 분리돼 있는가

```bash
.venv/bin/python -c "import lerobot" 2>&1 | tail -1
```

| 기대 | 내 출력 | 판정 |
|---|---|---|
| `ModuleNotFoundError: No module named 'lerobot'` | | ☐ 정상 ☐ **섞였음(E7)** |

---

## ② Step 1. 설치본 대조 결과

### 2.1 🔴 먼저 채우고 나서 돌리세요

lesson §6.1을 읽고 **기억으로** 채우세요. 돌린 뒤에 채우면 이 문항의 의미가 없습니다.

| 필드 | 라이브러리 기본값 (예상) | 논문 레시피 (예상) |
|---|---|---|
| `horizon` | | |
| `n_action_steps` | | |
| `crop_shape` | | |
| `use_group_norm` | | |
| `pretrained_backbone_weights` | | |
| `use_separate_rgb_encoder_per_camera` | | |
| `batch_size` | | |

**몇 개를 맞혔습니까? ______ / 7**

### 2.2 대조 결과

```bash
.venv-lerobot/bin/python course/w2-policy-vla/02-diffusion-policy/labs/verify_dp_install.py --quick
```

| 항목 | 집필 실측 | 내 결과 |
|---|---|---|
| 첫 줄 "기대값 출처" | `lesson.md` 경로 | ☐ lesson.md ☐ **폴백 상수** |
| `[2]` 드리프트 | **14/14 PASS** | ______ / 14 |
| `[3]` 소스 주석 | `configuration_diffusion.py:118` | 줄 ______ |
| `[4]` `horizon=9` 시도 | `ValueError` | |
| `--quick` 종합 | **PASS 33 / FAIL 0 / WARN 0** | PASS ___ / FAIL ___ / WARN ___ |
| exit code | **0** | |

### 2.3 FAIL과 WARN을 가른 근거

FAIL이나 WARN이 났다면 여기에 적고 **어느 쪽 문제인지 판정**하세요.

| 항목 | 기대 | 실제 | **설치 문제인가 문서 문제인가** |
|---|---|---|---|
| | | | |
| | | | |

**Q. "기대값 출처"가 lesson.md인 것이 왜 중요합니까?** (한 문장)

```

```

> 힌트. 이 스크립트가 lesson의 값을 상수로 들고 있었다면 무슨 일이 생기는지 생각해 보세요. README §1.3에 사연이 있습니다.

---

## ③ Step 2와 3. 데이터셋과 학습 스모크

### 3.1 데이터셋 메타

| 항목 | 집필 실측 | 내 결과 |
|---|---|---|
| 에피소드 | 206 | |
| 프레임 | 25,650 | |
| fps | 10 | |
| 관측 해상도 | 96×96 | |
| 평균 에피소드 | 124.5 프레임 = 12.5초 | |
| `observation.state` 범위 | min [13.5, 32.9] max [496.1, 511.0] | |

**Q. `observation.state`의 대역이 왜 0~512입니까? 96도 384도 아닌 이유를 한 문장으로.**

```

```

**세 해상도를 구분해 적으세요.**

| 숫자 | 무엇의 크기인가 |
|---|---|
| 96×96 | |
| 384×384 | |
| 512×512 | |

### 3.2 학습 스모크 300스텝

| step | 50 | 100 | 150 | 200 | 250 | 300 |
|---|---|---|---|---|---|---|
| 집필 실측 `loss` | 0.883 | 0.204 | 0.108 | 0.080 | 0.074 | **0.064** |
| **내 `loss`** | | | | | | |

| 항목 | 집필 실측 | 내 결과 |
|---|---|---|
| `num_learnable_params` | 262709026 (263M) | |
| 속도 | 6.3~7.0 step/s | |
| `mem_gb` | 약 5.0 | |
| **체크포인트 크기** | **3.0 GB** | |
| ↳ `model.safetensors` | 1,003 MB | |
| ↳ `optimizer_state.safetensors` | 2.0 GB | |

**Q. ★ 손실이 0.883에서 0.064로 14배 내려갔습니다. 이것을 "잘 배웠다"로 읽어도 됩니까?**

```

```

> 🔴 이 손실이 무엇의 오차인지부터 적으세요. lesson §3.2의 식을 다시 보면 됩니다.
> 그리고 **Step 5의 성공률을 본 뒤에 이 칸으로 돌아와** 답을 고칠지 판단하세요.

### 3.3 저장된 설정

```bash
.venv-lerobot/bin/python -c "
import json,sys
d=json.load(open(sys.argv[1]))
for k in ('horizon','n_action_steps','crop_shape','use_group_norm','pretrained_backbone_weights','use_separate_rgb_encoder_per_camera','drop_n_last_frames'):
    print(f'{k:38s} {d.get(k)!r}')
" outputs/train/w2m2_dp_smoke/checkpoints/000300/pretrained_model/config.json
```

| 필드 | 내 체크포인트 값 | §6.1 표의 어느 열인가 |
|---|---|---|
| `horizon` | | ☐ 기본값 ☐ 논문 레시피 |
| `n_action_steps` | | ☐ 기본값 ☐ 논문 레시피 |
| `crop_shape` | | ☐ 기본값 ☐ 논문 레시피 |
| `use_group_norm` | | ☐ 기본값 ☐ 논문 레시피 |
| `pretrained_backbone_weights` | | ☐ 기본값 ☐ 논문 레시피 |
| `use_separate_rgb_encoder_per_camera` | | ☐ 기본값 ☐ 논문 레시피 |
| `drop_n_last_frames` | | 이 설정에 공식을 넣으면 ______ |

**Q. `drop_n_last_frames`가 공식과 어긋납니다. 원인이 무엇입니까?**

```
🔴 이 칸의 정답은 "확인되지 않았습니다"입니다. lesson §6.2도 그렇게 적었습니다.
   추측을 쓰지 마세요. 대신 어떻게 확인할 수 있을지를 적으세요.
```

---

## ④ ★ Step 4. 논문 레시피 7항목 대조

### 4.1 학습 로그에서 뽑은 여섯 줄

```bash
grep -E "'(horizon|n_action_steps|crop_shape|use_group_norm|pretrained_backbone_weights|use_separate_rgb_encoder_per_camera)':" /tmp/dp_paper.log
```

| # | 필드 | Step 3 (기본값) | Step 4 (논문 레시피) | 바뀌었나 |
|---|---|---|---|---|
| 1 | `horizon` | | | ☐ |
| 2 | `n_action_steps` | | | ☐ |
| 3 | `crop_shape` | | | ☐ |
| 4 | `use_group_norm` | | | ☐ |
| 5 | `pretrained_backbone_weights` | | | ☐ |
| 6 | `use_separate_rgb_encoder_per_camera` | | | ☐ |
| 7 | `batch_size` | | | ☐ |

### 4.2 ★ 일곱 번째는 어디에 있었는가

**Q. 여섯 개는 `'policy':` 블록에 있는데 `batch_size`는 없습니다. 어디에 있고 왜 거기 있습니까?**

```

```

**같은 로그에 `batch_size`가 세 번 나옵니다. 셋을 구분해 적으세요.**

| 어디에 나오나 | 값 | 무엇인가 |
|---|---|---|
| 최상위 딕셔너리 | | |
| `'eval':` 안 | | |
| §6.1 표가 말하는 기본값 | | (로그에는 안 나옵니다. 어디서 확인했나: ______) |

### 4.3 ★ 파라미터 수가 그대로다

| 항목 | Step 3 | Step 4 | 같은가 |
|---|---|---|---|
| `num_learnable_params` | | | ☐ |

**Q. `horizon`을 64에서 16으로, `n_action_steps`를 32에서 8로 줄였는데 파라미터 수가 한 자리도 안 바뀌었습니다. 왜입니까?**

```

```

**Q. ★★ 그렇다면 "263M짜리 Diffusion Policy 기준으로 몇 % 개선"이라는 문장은 무엇을 특정하지 못합니까?**

```

```

### 4.4 손실 비교

| | Step 3 (기본값) | Step 4 (논문 레시피) |
|---|---|---|
| 20스텝 근처 `loss` | | |

**Q. 두 값을 나란히 놓고 우열을 말할 수 있습니까? 없다면 왜입니까?**

```

```

---

## ⑤ ★★ Step 5와 6. 평가, 롤아웃, 그리고 막힌 자리

### 5.1 E1을 만났는가

```bash
# 기본값으로 한 번 (일부러)
.venv-lerobot/bin/lerobot-eval --policy.path=... --env.type=pusht --eval.n_episodes=2 ...
```

| 항목 | 내 결과 |
|---|---|
| 에러 이름 | ☐ `NamespaceNotFound` ☐ 다른 것 ______ ☐ 안 났음 |
| `import gym_pusht`를 부모에서 하면 | ☐ 성공 ☐ 실패 |
| 넘은 방법 | ☐ `--eval.use_async_envs=false` ☐ `--eval.batch_size=1` ☐ 다른 것 |

**Q. 이 에러가 "설치 문제가 아니다"라는 근거를 한 문장으로.**

```

```

### 5.2 평가 결과

| 항목 | 집필 실측 (300스텝 체크포인트) | 내 결과 |
|---|---|---|
| **`pc_success`** | **0.0** | |
| `avg_max_reward` | 0.008472 | |
| `avg_sum_reward` | 0.247310 | |
| `eval_s` / `eval_ep_s` | 13.08 / 6.54 | |
| **mp4 개수** | **2** | |
| mp4 속성 | 384×384 · 300 프레임 · 10 fps · 30초 | |

### 5.3 ★★ `pc_success 0.0`을 어떻게 읽는가

**아래 표를 채우면서 판정하세요.**

| 무엇을 확인했나 | 이번 실행으로 확인됐나 |
|---|---|
| 체크포인트가 로드되는가 | ☐ |
| 정책이 환경에서 액션을 내는가 | ☐ |
| 에피소드가 끝까지 굴러가는가 | ☐ |
| 보상과 성공 판정이 집계되는가 | ☐ |
| 롤아웃이 mp4로 저장되는가 | ☐ |
| **정책이 태스크를 푸는가** | ☐ |

**Q. 마지막 줄만 ❌입니다. 이것이 파이프라인의 실패입니까 학습량의 문제입니까? 숫자로 근거를 대세요.**

```
(힌트. 본 학습은 10만 스텝입니다. 300스텝은 그 몇 %이고, 데이터셋을 몇 바퀴 돈 것입니까?
 로그의 epch 값이 답을 줍니다.)
```

**Q. ★ ③.2의 "손실이 14배 내려갔다"와 여기의 "성공률 0"은 모순입니까?**

```

```

### 5.4 ★★ 롤아웃 영상을 보고 한 문단

🔴 **이 칸이 Step 5의 제출물입니다.** mp4를 실제로 재생하고 정책이 무엇을 하고 있었는지 쓰세요.

| 관찰 항목 | 내가 본 것 |
|---|---|
| 밀대가 움직이는가 | |
| 움직임이 매끄러운가 끊기는가 | |
| **재계획 경계(3.2초마다)에서 방향이 튀는가** | |
| T자 블록에 닿는가 | |
| 두 에피소드가 서로 다른가 | |

**한 문단으로 (5~8문장):**

```




```

> 🔴 "학습이 덜 돼서 잘 못했다"만 쓰면 영상을 본 것이 아닙니다.
> **몇 초에 한 번 궤적이 새로 시작되는지**를 세어 보고, 그것이 $T_a = 32$와 10 Hz에서
> 나오는 3.2초와 맞는지 확인하세요. 이것이 ⑥으로 이어지는 재료입니다.

### 5.5 Step 6. 사전학습 체크포인트

| 시도 | 결과 | 어디서 멈췄나 |
|---|---|---|
| `--policy.path=lerobot/diffusion_pusht`로 바로 평가 | ☐ 성공 ☐ **막힘** | |
| 에러가 시킨 경로 `src/lerobot/...` | ☐ 있음 ☐ **없음** | |
| 설치본 경로로 마이그레이션 스크립트 실행 | ☐ 성공 ☐ **막힘** | |
| `model.safetensors`가 나왔는가 | ☐ 예 ☐ **아니오** | |

**Q. 그래서 이 랩의 정공법은 무엇이고 왜입니까? (두 문장)**

```

```

**Q. ★ 저장 포맷이 바뀌면 남이 만든 체크포인트를 못 씁니다. 우리 스택에서 이 문제가 어디에 걸립니까?**

```
(GEAR-SONIC · HOMIE · 외부 VLA 체크포인트 중 하나를 골라 구체적으로.
 답이 안 나오면 그것 자체가 팀 질문입니다. ⑧으로 옮기세요.)
```

---

## ⑥ ★★ Step 7. lesson §4.2 표를 자기 숫자로 다시 계산

> **이 표가 이 랩의 첫 번째 제출물입니다.** lesson §4.2의 네 행을 자기 기기 값으로 다시 세웁니다.

$$
T_{\text{replan}} + \tau_{\text{infer}} + \tau_{\text{comm}} \;\le\; \frac{T_a}{f_2},
\qquad f_2 = 10\ \text{Hz},\quad \tau_{\text{comm}} = 0\ (\text{시뮬})
$$

### 6.1 내 기기의 지연

```bash
cd course/w2-policy-vla/02-diffusion-policy/practice
source ../../../../.venv-lerobot/bin/activate
python 04_dp_latency_bench.py --device cuda
```

| 설정 | NFE | lesson | **내 중앙값** | **내 최소~최대** | 비율 |
|---|---|---|---|---|---|
| DDIM 1, 바닥 | 1 | 9.4 ms | | | |
| DDIM 10 | 10 | 68.0 ms | | | |
| DDIM 16 | 16 | 107.1 ms | | | |
| **DDPM 100, 라이브러리 기본값** | 100 | **665.2 ms** | | | |
| **DDPM 100, 논문 PushT 설정** | 100 | **705.2 ms** | | | |

| 항목 | 기대 | 내 결과 |
|---|---|---|
| 단조 증가 | PASS | ☐ |
| 비율 밴드 0.3~3.0 | 전부 PASS | ☐ |
| 종합 | 19/19 PASS | ______ |

> 🔴 **몇 번 돌린 범위로 적으세요.** 한 번의 숫자는 판정 근거가 못 됩니다.
> 돌린 횟수 ______ 회, `load average` ______

### 6.2 ★★ 표 4행 재계산

**빈칸을 자기 숫자로 채우세요. 굵은 칸이 자기 값에서 나오는 것입니다.**

| 실행 방식 | $T_a$ | 재계획 주기 @10 Hz | **내 추론 실측** | **예산 안에 드는가** | **최악 반응 지연** |
|---|---|---|---|---|---|
| 스텝마다 재계획 | 1 | ______ ms | ______ ms (DDPM 100) | ☐ 예 ☐ 아니오, ____배 초과 | ______ ms |
| 논문 레시피 | 8 | ______ ms | ______ ms (DDPM 100) | ☐ 예, 여유 ____ ms ☐ 아니오 | ______ ms |
| 논문 실기 설정 | 8 | ______ ms | ______ ms (DDIM 16) | ☐ 예, 여유 ____ ms ☐ 아니오 | ______ ms |
| 라이브러리 기본값 | 32 | ______ ms | ______ ms (DDPM 100) | ☐ 예, 여유 ____ ms ☐ 아니오 | ______ ms |

🔴 **두 번째 행을 조심하세요.** 집필 기기에서 여유가 **95 ms**뿐이었습니다. 자기 기기의 `DDPM 100, 논문 PushT 설정` 중앙값이 800 ms를 넘으면 **그 행의 판정이 뒤집힙니다.**

**내 기기에서 두 번째 행의 판정:**

```
☐ lesson과 같다 (아슬아슬하게 들어감)
☐ 뒤집혔다 (초과) → 여유 ______ ms 부족

그렇다면 lesson §4.2의 "논문이 실기 실험에서 DDIM으로 갈아탄 이유가 이 산수"라는 문장을
내 숫자로 다시 쓰면:

```

### 6.3 200 ms 문턱과 대면

[W1-M1 §3.3](../../../w1-generative-core/01-physical-ai-landscape/lesson.md)의 "자세가 무너지기까지 200 ms"와 맞댑니다.

| 실행 방식 | 최악 반응 지연 | 200 ms의 몇 배 밖 |
|---|---|---|
| 논문 레시피 ($T_a=8$) | ______ ms | ______배 |
| 라이브러리 기본값 ($T_a=32$) | ______ ms | ______배 |

**Q. PushT에서는 이 값이 문제가 안 되는데 G1에서는 문제가 됩니다. 무엇이 다릅니까? (한 문장)**

```

```

### 6.4 계층 판정이 내 숫자에서도 유지되는가

| 계층 | 스텝 예산 | **내 NFE 100** | **내 NFE 1** | 판정 |
|---|---|---|---|---|
| L2 전신 제어 500 Hz | 2 ms | | | ☐ 가능 ☐ 불가능 |
| L2 전신 제어 50 Hz | 20 ms | | | ☐ 가능 ☐ 불가능 |
| PushT 10 Hz, 스텝마다 | 100 ms | | | ☐ 가능 ☐ 불가능 |
| L4 상위 지능 1 Hz | 1,000 ms | | | ☐ 가능 ☐ 불가능 |

**Q. ★ NFE 1이면 L2 하단 50 Hz에 들어갑니다. 그렇다면 lesson §7.1의 "L2 불가"는 틀린 것입니까?**

```
(힌트. 그 판정이 어느 설정에 대한 것인지, 그리고 NFE 1이 지연 말고 무엇을 잃는지.
 lesson §5.3의 단서와 practice/03 README §4.3의 `[5]`가 같은 자리를 다룹니다.)
```

### 6.5 검산

`practice/02`가 lesson 값으로 낸 표와 내가 손으로 계산한 표를 대조합니다.

```bash
python3 02_receding_horizon.py
```

| | 재계획 주기 | 여유 | 최악 반응 지연 |
|---|---|---|---|
| `02`의 출력 (lesson 값 기준) | | | |
| 내 손계산 (내 기기 값 기준) | | | |
| **차이가 나는 열** | | | |

**Q. 어느 열이 기기에 따라 바뀌고 어느 열이 안 바뀝니까? 왜입니까?**

```

```

---

## ⑦ ★★ Step 8. 완료 기준 한 문단 (최종 제출물)

### 7.1 조각별 재료 확인 (쓰기 전에)

| 조각 | 필요한 것 | 어디서 | 준비됐나 |
|---|---|---|---|
| (a) | 설정 **7개의 필드명**과 각각의 두 값 | ② · ④ | ☐ |
| (b) | **내 기기의** DDPM 100 중앙값과 100 ms 대비 배수 | ⑥.1 · ⑥.2 | ☐ |
| (c) | $T_a$, 재계획 주기, 최악 반응 지연의 거래 구조 | ⑥.2 · ⑤.4 | ☐ |

### 7.2 ★★ 한 문단, 백지로

> 라이브러리 기본값으로 PushT를 돌린 결과를 놓고 **(a)** 이것이 논문 재현이 아닌 이유를 설정 7항목으로 지목하고, **(b)** 자기 기기의 추론 지연으로 10 Hz 예산 대비 몇 배인지 계산하고, **(c)** 그럼에도 태스크가 도는 이유를 receding horizon으로 설명하는 **한 문단**

```






```

### 7.3 반드시 덧붙일 두 줄

| # | 내용 | 내가 쓴 것 |
|---|---|---|
| 1 | receding horizon이 **사는 것**과 **파는 것** | |
| 2 | PushT에서 성립한 값을 G1에 못 옮기는 이유 | |

### 7.4 자가 채점

- [ ] 일곱 항목을 **필드명으로** 댔다. "설정이 다르다"가 아니라 `horizon`, `n_action_steps`, `crop_shape`, `use_group_norm`, `pretrained_backbone_weights`, `use_separate_rgb_encoder_per_camera`, `batch_size`
- [ ] 배수 계산에 **자기 기기의 숫자**가 들어 있다
- [ ] "그럼에도 도는 이유"에 **최악 반응 지연이라는 대가**가 함께 적혀 있다
- [ ] lesson 문장을 옮겨 적은 곳이 없다

### 7.5 셀프 체크 퀴즈, 틀린 것 기록

lesson의 퀴즈 10문항을 풀고 `<details>` 정답과 대조하세요.

| 문항 | 맞았나 | 틀렸다면 어느 절로 돌아갔나 |
|---|---|---|
| 1~10 중 틀린 것 | | |

---

## ⑧ 팀 질문과 모르는 것 ★

### 8.1 lesson §7.3의 미확인 4건

전부 추측하지 않고 남겨둔 것입니다. 이 랩을 하면서 힌트가 생긴 것이 있으면 적으세요.

| 항목 | lesson의 상태 | 이 랩에서 생긴 힌트 |
|---|---|---|
| L4 상위 정책이 확산 계열인가 흐름 계열인가 | 미확인 | |
| L3이 연속 청크를 이산화하는가 처음부터 이산인가 | 미확인 | |
| L2가 받는 명령이 위치인가 속도인가 | 미확인 | |
| 우리 시연에 다봉성이 실제로 있는가 | 미확인 | |

### 8.2 ★★ 자기 스택에서 가장 급한 것 둘

lesson이 이미 적립한 것은 `W2M2-1` ~ `W2M2-6`입니다. **겹치지 않는 것**으로 두 개를 고르세요.

| # | 질문 | 왜 이것이 급한가 | 답이 오면 무엇이 정해지나 |
|---|---|---|---|
| 1 | | | |
| 2 | | | |

### 8.3 실습을 하고 나서야 생긴 질문 (이게 제일 값집니다)

Step 6에서 하나는 반드시 나옵니다. 저장 포맷과 체크포인트 호환 문제입니다.

```

```

### 8.4 심화 변형에서 나온 질문

README의 변형 A와 B는 둘 다 미검증입니다. 돌려봤다면 결과를, 안 돌렸다면 왜 안 돌렸는지를 적으세요.

| 변형 | 돌렸나 | 결과 또는 이유 |
|---|---|---|
| A. `n_action_steps`를 평가 시 오버라이드할 수 있는가 | ☐ | |
| B. NFE를 100에서 16으로 줄여 재평가 | ☐ | |

---

## ⑨ 내가 발견한 불일치 (문서 대 실제 출력)

`verify`가 FAIL이나 WARN을 냈거나, 이 랩의 숫자가 자기 출력과 달랐다면 여기에.

| # | 어디 | 문서가 말하는 것 | 실제 출력 | 어느 쪽이 맞나 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |

> 🔴 **설치본 출력을 믿습니다.** 문서가 틀렸다고 판단되면 그 근거를 함께 적으세요.
> 이것이 자료 개정의 입력이 됩니다.

---

## ⑩ 막힌 지점과 해결 방법 (→ `docs/progress.md`로 그대로 옮길 것)

| # | 어느 Step | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|---|
| 1 | | | | | |
| 2 | | | | | |
| 3 | | | | | |

**에러 표 E1~E9 중 실제로 만난 것:** ☐ E1 ☐ E2 ☐ E3 ☐ E4 ☐ E5 ☐ E6 ☐ E7 ☐ E8 ☐ E9

**표에 없던 에러:**

```

```

---

## ⑪ 용어집 (5개)

[`../../../../notes/glossary.md`](../../../../notes/glossary.md)에 추가할 것. 이 랩에서 처음 손으로 만진 것으로 고르세요.

| 용어 | 한 줄 정의 | 제어 또는 LLM 비유 |
|---|---|---|
| | | |
| | | |
| | | |
| | | |
| | | |

> 후보. receding horizon 실행, NFE, `drop_n_last_frames`, forkserver 워커, processor 포맷, 옵티마이저 상태, `pc_success`

---

## ⑫ 실행 로그 (→ `docs/progress.md`)

| 항목 | 값 |
|---|---|
| 날짜 | |
| 돌린 것 | 학습 스모크 ☐ · 논문 레시피 ☐ · 평가 ☐ · **롤아웃 mp4 확인 ☐** · `practice/04` ☐ |
| GPU 시간 | ______ 분 |
| **디스크 최대 사용량** | ______ GB (체크포인트 ______ 개) |
| 클라우드 비용 | 0원 (로컬) |
| 본 학습 10만 스텝을 돌렸는가 | ☐ 예(______ 시간) ☐ 아니오 |

---

## ⑬ 최종 제출 체크

- [ ] ⓪~⑤ 실측값이 채워졌다
- [ ] **⑥.2 표 4행이 자기 기기 숫자로 채워졌고, 두 번째 행의 판정을 자기가 내렸다** ★★
- [ ] **⑤.4 롤아웃 영상 관찰이 한 문단으로 적혔다** ★★
- [ ] **⑦.2 완료 기준 문단을 (a)(b)(c) 세 조각으로 썼다** ★★
- [ ] ⑧에 새 팀 질문이 최소 2개 있다
- [ ] ⑩을 `docs/progress.md`에 옮겼다
- [ ] ⑪ 용어 5개를 `notes/glossary.md`에 옮겼다
- [ ] 체크포인트를 지워 디스크를 회수했다
