# W1-M5 워크시트 — 제출물

> **이 파일이 W1-M5의 제출물입니다.** [`README.md`](README.md)의 6단계를 따라가며 채우세요.
> 시작 전에 사본을 만들어 쓰는 것을 권합니다: `cp worksheet.md worksheet-$(date +%F).md`
>
> 🔴 **⑥이 이 모듈의 본체입니다.** 나머지 칸은 전부 ⑥을 쓰기 위한 재료입니다.
> ⑥은 마스터플랜이 **W2-M5의 종합 과제**로 지정한 것이고, 여기서 초안을 잡아두면 2주 뒤에는 완성만 하면 됩니다.
> 지금 안 쓰면 그때는 실측이 아니라 기억으로 쓰게 됩니다.
>
> 🔴 **모르는 칸은 `?`로 남기세요.** 특히 회사 내부 구현은 lesson에도 답이 없습니다. `?`가 팀 질문의 원재료입니다.

---

## ⓪ 기본 정보

| 항목 | 값 |
|---|---|
| 작성일 | |
| 총 소요 시간 (계획 4시간 대비) | |
| 선수 모듈 완주 여부 | W1-M1 ☐ · W1-M2 ☐ |

### 실행 환경

| 항목 | 값 | 확인 명령 |
|---|---|---|
| **GPU** | 모델: ______ / VRAM ____ GB / **없음(CPU만)** | `nvidia-smi` |
| Python | | `python3 --version` |
| `torch` + 백엔드 | 버전 ______ / `cuda` · `cpu` | `python -c "import torch; print(torch.__version__, torch.cuda.is_available())"` |
| `mujoco` | | `python -c "import mujoco; print(mujoco.__version__)"` |
| G1 모델 경로 | | `echo $MENAGERIE_PATH` |

**집필 검증 환경과 다른 점** (README §0은 Python 3.12.12 / torch 2.13.0+cu130 / RTX 3080 12GB / 2026-08-02):

```
(여기에 작성 — 없으면 "동일")
```

### Step별 실측 소요 시간

| Step | 계획 | 실측 | 비고 |
|---|---|---|---|
| Step 1 — FSQ 손으로 확인 | 30분 | | |
| Step 2 — 데이터·베이스라인 | 30분 | | |
| **Step 3 — 사용률 비교 ★** | 55분 | | `02 --sweep` 벽시계: ____ 초 |
| Step 4 — 액션 토크나이저 | 45분 | | |
| **Step 5 — 설계 논증 ★** | 45분 | | |
| Step 6 — 마무리 기록 | 35분 | | |
| **합계** | **4시간** | | |

---

## ② Step 1 — FSQ를 손으로 확인

### 2.1 실행 결과

| 확인 항목 | 기대 | 내 결과 |
|---|---|---|
| assert 통과 개수 | 5종 | |
| 왕복오차 | `0.00e+00` | |
| `nn.Parameter` 개수 | **0개** | |
| Table 1 곱 (5개) | 240 / 1,000 / 4,375 / 15,360 / 64,000 | |
| $d{=}3, L{=}3$ 코드워드 수 | 27 | |

### 2.2 ★ 세 수식이 코드 어디에 있는가

`01_fsq_minimal.py`를 열어 **직접 찾아서** 줄 번호를 적으세요. (README 1.3)

| # | 수식 | 찾는 단서 | 줄 번호 |
|---|---|---|---|
| 1 | $f: z_i \mapsto \lfloor L/2 \rfloor \tanh(z_i)$ — **바운딩** | `tanh`가 나오는 줄 | |
| 1-b | 짝수 $L$ 보정 (`offset` · `shift`) | 위 줄 근처 | |
| 2 | `round_ste: x ↦ x + sg(round(x) - x)` — **STE** | `.detach()`가 나오는 줄 | |
| 3 | mixed-radix 열거 $\mathrm{idx} = \hat z_1 + L_1(\hat z_2 + \cdots)$ | `basis`를 **만드는 줄**과 **쓰는 줄** 두 곳 | / |

### 2.3 확인 질문

**Q1. `nn.Parameter`가 0개인 것이 왜 중요한가?** (lesson §5.2 · §4.2)

```

```

**Q2. 짝수 $L$에서 비대칭 오프셋이 필요한 이유는?** (양자화가 정수로 반올림된다는 점에서 출발하세요)

```

```

**Q3. 심화 — `[4,4,4]`와 `[8,6,5]`는 같은 $d{=}3$인데 무엇이 갈리는가?** 한 문장으로. ($L_i \ge 5$ 휴리스틱)

```

```

---

## ③ Step 2 — 데이터와 베이스라인

### 3.1 예상 먼저 (돌리기 전에 적을 것) 🔴

**29개 관절을 서로 독립으로 흔들면(`--synergies 0`) 사용률과 복원이 어떻게 될 것 같습니까?**

```
예상:
```

### 3.2 실측

| 구성 | 사용률 | val MSE | 무정보 베이스라인 대비 | RMSE(°) |
|---|---|---|---|---|
| `--synergies 6` (기본) | | | | |
| `--synergies 0` | | | | |

### 3.3 답할 것

**"압축은 데이터에 구조가 있을 때만 가능하다"를 위 두 실행의 숫자로 설명하세요.**

```

```

`02_data_samples.png`에서 본 것 (G1 포즈 6장):

```

```

---

## ④ Step 3 — 사용률 비교 ★★ 이 랩의 본체

### 4.1 예상 먼저 (돌리기 전에) 🔴

**코드북을 $2^8 \to 2^{12}$로 키우면 FSQ와 VQ의 사용률이 각각 어떻게 될 것 같습니까?**

```
FSQ 예상:
VQ  예상:
```

### 4.2 실측 표 — 본인 실행 결과

`artifacts/W1-M5/02_results.csv`에서 옮기세요.

| 구성 | \|C\| | 쓰인 코드 | 사용률 | val MSE | RMSE(°) | 양자화기 param |
|---|---|---|---|---|---|---|
| FSQ `[8,6,5]` | 240 | | | | | |
| VQ | 256 | | | | | |
| FSQ `[8,5,5,5]` | 1,000 | | | | | |
| VQ | 1,024 | | | | | |
| FSQ `[7,5,5,5,5]` | 4,375 | | | | | |
| VQ | 4,096 | | | | | |

> 참고 — 집필 환경 실측(시드 고정, 2회 재현): FSQ 100.0 / 96.8 / 71.9 %, VQ 83.6 / 21.1 / 4.5 %.
> **소수점까지 같을 필요는 없습니다.** 경향이 맞으면 정상입니다.

### 4.3 그림에서 읽은 것

**`02_usage.png`** — FSQ는 완만히 내려가고 VQ는 절벽으로 떨어집니다. **절벽이 몇 % 지점에 있었는가:**

```

```

**`02_code_hist.png`** — VQ의 죽은 코드가 어떻게 보였는가:

```

```

**`02_curves.png`** — **MSE가 교차한 코드북 크기는:** ________

이것이 lesson §5.5의 어떤 주장을 뒷받침하는가:

```

```

### 4.4 🔴 최대 함정 — 여기서 결론을 잘못 내리는 사람이 많습니다

**FSQ 사용률이 $|C|$가 커질수록 100 → 96.8 → 71.9 %로 내려갑니다. 이것을 "FSQ도 결국 collapse한다"로 읽으면 틀립니다. 왜 틀렸는지 쓰세요.**

(힌트: 논문의 ≈100%는 ImageNet 규모 데이터 기준입니다. 우리 데이터는 무엇인가?)

```

```

### 4.5 VQ 트릭 실측

| 설정 | 사용률 |
|---|---|
| 바닐라 | |
| `--vq-init data` | |
| `--vq-ema` | |
| 둘 다 | |

> 집필 환경 실측(|C|=1024): 21.1 / 19.5 / 18.8 / 18.3 % — **트릭이 도움이 되지 않았습니다.**

**"우리 팀이 VQ를 쓴다면 이 트릭 표를 누가 관리해야 하는가?"** 한 줄로:

```

```

---

## ⑤ Step 4 — 액션 토크나이저로 써 보기

### 5.1 실측

| 항목 | 기대(집필 환경) | 내 결과 |
|---|---|---|
| 480프레임 → 토큰 개수 | 30 | |
| 복원 RMSE 평균 | 5.88° | |
| 최악 관절 / 오차 | `right_shoulder_yaw` / 12.00° | |
| 최선 관절 / 오차 | `right_ankle_roll` / 0.98° | |
| 압축률 | 1,489.9배 | |
| 압축률 `assert` 통과 | ✅ | |

### 5.2 토큰 열 발췌

`artifacts/W1-M5/03_tokens.txt`에서 본인 결과를 붙이세요.

```

```

**같은 토큰이 연속으로 반복되는 구간이 있습니까? 그것은 무엇을 뜻합니까?**

```

```

### 5.3 ★ 판단 — 정해진 답이 없습니다

**평균 5.88°, 최악 12.00°. 이 정밀도로 G1을 제어할 수 있습니까?**

```

```

**하위 WBC가 흡수해야 하는 것은 무엇입니까?** (세 축으로 나눠 쓰면 ⑥ 논거 ③에서 그대로 씁니다)

```
①
②
③
```

### 5.4 압축률과 정밀도의 교환

| `--tokens-per-chunk` | 사용률 | RMSE(°) | 압축률 |
|---|---|---|---|
| 1 (기본) | | | |
| 4 | | | |

> 집필 환경 실측: `4`에서 사용률 100%, RMSE 3.04°, 압축률 372배.

**어느 지점이 "충분히 좋은" 토큰 레이트인가? 판단 기준을 한 문장으로.**

```

```

> 이 문장을 쓸 수 있으면 lesson §7.4의 팀 질문(토큰 레이트·시간축 처리)을 던질 준비가 된 것입니다.

---

## ⑥ ★★ Step 5 — "왜 FSQ 기반 계층 구조인가" 1페이지 논증

> **이 모듈의 제출물입니다.** 마스터플랜 §6이 W2-M5의 종합 과제로 지정한 것이며, 여기서 초안을 잡습니다.
> **④⑤에서 적은 본인 실측값을 3개 이상 인용하세요.** 인용 없는 논증은 남의 말을 옮긴 글입니다.

### 논거 ① 코드북 없는 인터페이스의 운영상 이점 (3~5문장)

*무엇이 사라지는가(보조 손실 2항·EMA 버퍼·사용량 카운터·재초기화 로직·코드북 텐서) / 공유 상태가 없다는 것이 상하위 계약에 무엇을 의미하는가 / ④의 트릭 실측이 이것을 어떻게 뒷받침하는가*

```

```

**인용한 실측**: 

### 논거 ② 주파수 접합에서 토큰 홀드가 하는 일 (3~5문장)

*L4 1~10 Hz ↔ L2 50~500 Hz 사이를 무엇이 메우는가 / ⑤의 반복 토큰 구간 / 연속 궤적 청크는 소진되면 어떻게 되는가, 토큰은 왜 다른가*

```

```

**인용한 실측**: 

### 논거 ③ 하위 정책 교체 가능성 (3~5문장)

*토큰의 의미를 누가 쥐고 있는가 / 같은 토큰 공간을 유지한 채 WBC를 재학습하면 무엇이 절약되는가 / **반대 방향**: 토크나이저를 재학습하면 토큰 ID 의미가 바뀐다 — FSQ가 여기서 유리한 점(격자 고정)과 그래도 남는 문제(인코더·디코더 매핑)*

```

```

**인용한 실측**: 

### 논거 ④ 🔴 반대 논거 — 연속 액션이 더 나은 경우

> **이 칸을 비우면 논증이 아니라 광고문입니다.** 첫 기술 토론에서 가장 먼저 받을 질문이 여기입니다.

*Diffusion Policy · π0의 Flow Matching 헤드가 유리한 태스크는? / 분해능 상한이 언제 실제 병목이 되는가(접촉이 풍부한 조작·고주파 보정·힘 제어) / **④의 교차점을 인용하세요** — $2^8$에서 VQ가 나았고 $2^{10}$에서 동률이었다는 사실이 "FSQ가 항상 낫다"를 반박합니다*

```

```

**인용한 실측**: 

### 종합 — 한 문단으로

```

```

---

## ⑦ ★ 막힌 지점과 해결 방법 (→ `docs/progress.md`로 그대로 옮길 것)

> 이 칸이 다음 주 자료의 입력이 됩니다. **지우지 마세요.**

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

**에러 표(README §흔한 에러)에 없던 새로운 에러:**

```

```

---

## ⑧ 내가 발견한 불일치 (문서 vs 실제 출력)

> 문서와 출력이 어긋나면 **출력을 믿고** 여기 적으세요. 자료 수정의 입력이 됩니다.

| 위치 | 문서의 서술 | 실제 출력 |
|---|---|---|
| | | |

---

## ⑨ 팀 질문 ★

lesson §9의 질문 6개 중 **본인이 실제로 물어볼 것**을 고르고, 실습 후 새로 생긴 질문을 추가하세요.
→ [`../../../../notes/questions-for-team.md`](../../../../notes/questions-for-team.md)로 옮길 것.

| # | 질문 | 왜 중요한가 | 우선순위 |
|---|---|---|---|
| M5-1 | 회사 FSQ가 **무엇을** 토큰화하는가 (관절각 / 속도 / 궤적 청크 / latent motion) | | |
| M5-2 | $\mathcal{L}$과 $d$는 무엇인가 | | |
| M5-3 | 토큰 레이트는 몇 Hz인가 / 시간축을 어떻게 다루는가 | | |
| M5-4 | | | |
| M5-5 | | | |

**실습을 하고 나서야 생긴 질문** (이게 제일 값집니다):

```

```

---

## ⑩ 용어집 (5개)

→ [`../../../../notes/glossary.md`](../../../../notes/glossary.md) W1-M5 절에 추가.

| 용어 | 내 말로 정의 | 제어·ML·LLM 비유 |
|---|---|---|
| | | |
| | | |
| | | |
| | | |
| | | |

---

## ⑪ 셀프 체크 퀴즈 — 틀린 것 기록

lesson §10의 10문항. **틀린 것만** 적으세요.

| # | 왜 틀렸나 | 다시 읽을 절 |
|---|---|---|
| | | |

---

## ⑫ 실행 로그 (→ `docs/progress.md`)

```
날짜:
돌린 것:
막힌 지점:
소요 시간:
GPU: (로컬이면 모델명 + 총 사용 시간 / 클라우드면 비용)
다음에 할 것:
```

---

## ⑬ 최종 제출 체크

- [ ] **G1** — `01`이 assert 5종 + `nn.Parameter : 0개` + 왕복오차 `0.00e+00`
- [ ] **G2** — `02 --smoke` 완주 + `02_data_samples.png`에서 G1 포즈 6장 확인
- [ ] **G3 ★** — `02 --sweep` 6구성 완주 + `02_usage.png`·`02_code_hist.png`에서 **VQ 죽은 코드 절벽을 눈으로 봄**
- [ ] **G4** — `03`이 토큰 30개 + 압축률 assert 통과, `03_tokens.txt`를 **열어서 읽음**
- [ ] **G5 ★** — ⑥ 논증 1페이지 완성, **본인 실측 3개 이상 인용**, 반대 논거(④) 비어 있지 않음
- [ ] **G6** — `docs/progress.md` · `notes/glossary.md` · `notes/questions-for-team.md` 갱신
- [ ] **[P1]** `notes/papers/`에 FSQ(2309.15505) 3-pass 노트 착수

> ⑥의 반대 논거가 비어 있으면 G5는 미완입니다. 그 칸이 논증을 논증으로 만듭니다.
