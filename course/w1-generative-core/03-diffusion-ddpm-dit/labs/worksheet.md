# W1-M3 워크시트 — 제출물

> **이 파일이 W1-M3의 제출물입니다.** [`README.md`](README.md)의 8단계를 따라가며 채우세요.
> 시작 전에 사본을 만들어 쓰는 것을 권합니다: `cp worksheet.md worksheet-$(date +%F).md`
>
> 🔴 **④와 ⑤가 이 모듈의 본체입니다.** ①②③은 전부 그 둘을 쓰기 위한 재료입니다.
> lesson [「실습으로 가기」](../lesson.md)가 못박은 그대로입니다 — **진짜 산출물은 노트북이 아니라 백지에 그린 DiT 블록도와 §5.2 표를 자기 손으로 다시 계산한 것.**
> ④는 마스터플랜 **W1 체크포인트 1번 문항** 그 자체이고, ⑤는 W1-M4로 넘어가는 근거를 숫자로 쥐는 칸입니다.
>
> 🔴 **모르는 칸은 `?`로 남기세요.** 특히 회사 내부 구현은 lesson §8.4에도 답이 없습니다(5개 항목 전부 미확인).
> **그 `?`가 팀 질문의 원재료입니다.** 채워진 칸보다 `?`가 온보딩의 산출물에 더 가깝습니다.

---

## ⓪ 기본 정보

| 항목 | 값 |
|---|---|
| 작성일 | |
| 총 소요 시간 (계획 2시간 대비) | |
| 선수 모듈 완주 여부 | W1-M1 ☐ · W1-M2 ☐ |
| lesson 정독 완료 | ☐ |

### 실행 환경

| 항목 | 값 | 확인 명령 |
|---|---|---|
| **device** | `cuda` (모델: ______ / VRAM ____ GB) · **`cpu`** | 스크립트의 `[device]` 첫 줄 |
| Python | | `python --version` |
| `torch` + 백엔드 | 버전 ______ / `cuda` · `cpu` | `python -c "import torch; print(torch.__version__, torch.cuda.is_available())"` |
| 인터프리터 경로 | | `which python` — `.../physical-ai-study/.venv/bin/python` 이어야 정상 |
| `mujoco` + menagerie | 있음 ☐ / **없음(D=29 상수로 진행)** ☐ | `ls repos/mujoco_menagerie/unitree_g1/scene.xml` |

**집필 검증 환경과 다른 점** (README §0.5는 Ubuntu on WSL2 / Python 3.12.12 / torch 2.13.0+cu130 / RTX 3080 12GB + i7-12700K / 2026-08-03):

```
(여기에 작성 — 없으면 "동일")
```

### Step별 실측 소요 시간

| Step | 계획 | 스크립트 실행(초) | 실측 소요 | 비고 |
|---|---|---|---|---|
| Step 1 — 환경 확인 | 3분 | — | | |
| Step 2 — `01 --smoke` | 4분 | | | 집필 실측 GPU 12.7s / CPU 20.1s |
| Step 3 — `01` 본 실행 + 그림 | 8분 | | | 집필 실측 GPU 97s / CPU 279s |
| **Step 4 — `02` NFE 판정 ★** | 8분 | | | 집필 실측 GPU 9s / CPU 60~100s |
| **Step 5 — `03` 항등 검증 ★** | 7분 | | | 집필 실측 GPU 68s / CPU 275s |
| **Step 6 — 백지 블록도 ★★** | **40분** | (코드 없음) | | |
| **Step 7 — NFE 재계산 ★★** | **35분** | (코드 없음) | | |
| Step 8 — 체크포인트 + 기록 | 15분 | — | | |
| **합계** | **약 2시간** | **GPU 약 3분 / CPU 약 11분** | | |

> Step 6+7이 전체의 **62%** 여야 정상입니다. 이 비율이 뒤집혔다면 스크립트만 돌리고 끝낸 것입니다.

---

## ① Step 2·3 — `01` 출력 검산

### 1.1 🔴 먼저 채우고 나서 돌리세요

**스크립트를 보기 전에** lesson [§3.3](../lesson.md)을 읽고 아래 빈칸을 채우세요. 출력을 보고 채우면 검산이 아니라 받아쓰기입니다.

| $t$ | $\beta_t$ | $\bar\alpha_t$ | $\sqrt{\bar\alpha_t}$ | $\sqrt{1-\bar\alpha_t}$ | SNR |
|---|---|---|---|---|---|
| 1 | 0.00010 | | | | |
| 100 | | 0.89702 | | | |
| 250 | 0.00506 | | | | |
| 500 | | | 0.28033 | | |
| 1000 | 0.02000 | | | 1.0000 | |

**SNR이 정확히 1을 지나는 $t$는?** (힌트: $\bar\alpha_t$가 얼마일 때인가) → **______**

**$t=1000$에서 신호가 몇 배 감쇠했나?** ($1/\sqrt{\bar\alpha_{1000}}$) → **______배**

### 1.2 실측 대조

`01`을 돌린 뒤 어긋난 칸만 적으세요.

| 항목 | 기대 | 내 결과 | 일치 |
|---|---|---|---|
| §3.3 assert | `✅ 5행 전부 일치` | | ☐ |
| SNR 교차 $t$ | **259** ($\bar\alpha$=0.50024) | | ☐ |
| 감쇠 배율 | **157.4배** | | ☐ |
| §4.4 assert | `✅ 6행 + 비율 전부 일치` | | ☐ |
| $\lambda_1/\lambda_{1000}$ | **49.00** | | ☐ |
| $\epsilon_\theta$ 파라미터 | **889,602개** | | ☐ |
| 최종 손실 (초기) | 약 **0.24~0.25** (0.56~0.58) | | ☐ |
| 생성 샘플 평균 / 표준편차 | ≈ [0,0] / ≈ [1,1] | | ☐ |

### 1.3 $t$ 구간별 평균 손실 — 출력에서 옮기세요

| 구간 | 0-100 | 100-200 | 300-400 | 500-600 | 700-800 | 900-1000 |
|---|---|---|---|---|---|---|
| 내 결과 | | | | | | |

> 집필 실측(본 실행): 0-100에서 **약 0.70**, 900-1000에서 **0.000**. 스모크(600 스텝)에서는 0.79~0.82로 조금 높게 나옵니다.

**Q1. 손실이 $t$에 대해 단조 감소하는 이유를 한 문장으로.**

```

```

**Q2. ★ 이 관찰이 §4.4에서 $\lambda_t$를 버린 결정과 어떻게 이어집니까?** (키워드: 어려운 $t$ · 49배 · 승격)

```

```

**Q3. 스모크(600 스텝)에서도 §3.3·§4.4 표 검산이 본 실행과 **완전히 같은** 값을 냅니다. 왜입니까?**

```

```

### 1.4 그림 두 장을 나란히 놓고

**`01_schedule.png` (c)의 $\lambda_t$ 곡선 모양은?** (단조 감소 / U자 / 단조 증가) → **______**, 최저점은 $t\approx$ **______**

**이것이 "큰 $t$일수록 가중치가 작다"라는 흔한 요약과 어떻게 다릅니까?**

```

```

**`01_forward_diffusion.png`에서 두 초승달이 아직 보이는 마지막 패널은 $t=$ ______.** 그 패널의 SNR은 ______ (§3.3 표에서).

---

## ② Step 4 — `02` NFE 판정 ★

### 2.1 🔴 예상 먼저 (돌리기 전에 적을 것)

**NFE를 1000에서 10으로 줄이면 ancestral과 DDIM 중 어느 쪽이 더 많이 나빠질 것 같습니까? 왜?**

```
예상:
```

### 2.2 실측 표 — 본인 실행 결과

`artifacts/W1-M3/02_results.csv`에서 옮기세요. **`energy` 열만** 씁니다(ms는 실행마다 흔들립니다).

| NFE | energy (ancestral) | energy (DDIM) | anc/DDIM 배율 |
|---|---|---|---|
| 1 | | | |
| 2 | | | |
| 4 | | | |
| **10** | | | |
| 20 | | | |
| 50 | | | |
| 250 | | | |
| 1000 | | | |

**노이즈 바닥** (스크립트가 블록 [1]에 찍습니다): **______**

> 참고 — 집필 환경 실측(시드 0, 샘플 4,096개): 노이즈 바닥 **0.00101**, 최대 격차는 **NFE=10에서 1.76배**.
> **소수점까지 같을 필요는 없습니다.** 경향이 맞으면 정상입니다.

### 2.3 ★★ 판정 — 어느 행부터 우열이 무의미한가

> 🔴 **이 랩에서 가장 잘못 읽기 쉬운 칸입니다.** NFE=1000 행만 보면 "ancestral이 이겼다"고 결론 내리게 됩니다.

**노이즈 바닥 ______ 을 기준으로, 위 표의 어느 행부터 두 샘플러의 우열을 말할 수 없습니까?**

```
행:
판정 기준(한 줄):
```

**그 기준으로 NFE=1000의 "역전"(0.84배)을 어떻게 읽어야 합니까?**

```

```

**노이즈 바닥이란 무엇의 값입니까? 왜 0이 아닙니까? 낮추려면 어떤 인자를 건드려야 합니까?**

```

```

### 2.4 ★★ NFE 1~2가 둘 다 실패하는 것

**NFE=1에서 두 샘플러의 energy가 정확히 같습니다. 왜입니까?**

```

```

**NFE 1~2가 둘 다 실패하는 것이 W1-M4(Flow Matching)로 넘어가는 근거인 이유를 두 문장으로.**
(키워드: 샘플러 교체 · 경로 설계 · 절단오차)

```
①
②
```

### 2.5 갈리는 구간

| 항목 | 내 결과 |
|---|---|
| DDIM이 1.1배 이상 우세한 NFE 구간 | ______ ~ ______ |
| 최대 격차가 나는 NFE / 배율 | NFE ______ / ______배 |
| lesson §5.1 비교표의 어느 행이 이것을 설명하는가 | |

**`02_samplers_grid.png`의 NFE=10 열에서 위(ancestral)와 아래(DDIM)가 어떻게 달라 보였는가:**

```

```

**`02_quality_vs_nfe.png` (c)의 x축은 무엇이고, 왜 로봇에서는 그 축이 (a)의 축보다 중요한가:**

```

```

---

## ③ Step 5 — `03` 항등 검증 이해 ★

### 3.1 실측

| 초기화 | 기대 | 내 결과 | 판정 |
|---|---|---|---|
| adaLN-Zero (조건 MLP = 0) | 정확히 `0.000e+00` | | ☐ |
| 랜덤 초기화 (대조군) | `1e-1` 수준의 **0이 아닌 값** | | ☐ |
| **랜덤이지만 $\alpha$만 0** | 정확히 `0.000e+00` | | ☐ |

| 항목 | 기대 | 내 결과 |
|---|---|---|
| `model.nu` | **29** (menagerie 없으면 상수 29) | |
| shape 관통 | `[4,32,29] → [4,16,58] → [4,16,128] → (4,32,29)` | |
| 파라미터 수 | **1,274,298개** | |
| 초기 출력 norm | `0.000e+00` | |
| cross-attn 관측 토큰 | `[4,144,512]` 통과 | |
| 관절 한계 위반 비율 | **0.00%** | |

### 3.2 ★ 항등을 만드는 것이 $\gamma\cdot\beta$가 아니라 $\alpha$인 이유

> 세 줄을 **함께** 읽어야 나오는 결론입니다. 특히 세 번째 줄(랜덤인데 $\alpha$만 0)이 결정적입니다.

**한 문단으로.** (키워드: 잔차 게이트 · 가지의 출력이 0 · $h_{\text{out}} = h_{\text{in}} + 0 + 0$)

```

```

**이것이 lesson §6.2 표의 어느 두 행 차이를 설명합니까?** → ______ (FID ______) → ______ (FID ______)

**제어 대응 한 줄** (힌트: 다단 캐스케이드를 닫는 순서):

```

```

**세 번째 줄이 없으면 무엇을 잘못 결론 내릴 수 있습니까?**

```

```

### 3.3 forward 벤치 — Step 7의 입력

여기 옮겨 적은 값이 ⑤의 재료입니다.

| 구성 | 파라미터 | 1 forward [ms] — **내 기기** | device |
|---|---|---|---|
| d=128, 4블록 *(이 실습)* | 1.3M | | |
| d=256, 6블록 | 7.4M | | |
| d=512, 12블록 | 57.7M | | |
| **d=1152, 28블록** *(DiT-XL)* | **673.7M** | | |

> 참고 — 집필 환경 실측 범위(3회): GPU 2.1~2.6 / 3.6~4.9 / 6.8~7.4 / **13~21** ms, CPU 1.2~2.4 / 3.9~5.2 / 22~27 / **200~205** ms.
> **범위가 넓은 것이 정상입니다**(README **E6**). 배치 1·토큰 16개는 커널 오버헤드가 지배하는 영역입니다.

**파라미터가 512배(1.3M → 673.7M) 늘 때 시간은 몇 배 늘었습니까?** GPU ______배 / CPU ______배

**그 차이가 뜻하는 것은?** (힌트: 작은 모델이 하드웨어를 채우고 있는가)

```

```

### 3.4 다봉성

**`03_action_chunk.png` (a)~(c)에서 같은 시작 포즈에서 뽑은 궤적들이 갈라졌습니까?** ☐ 예 ☐ 아니오

**회귀 헤드였다면 이 자리에 무엇이 나왔을까요?** (lesson §7.3)

```

```

---

## ④ ★★ Step 6 — 백지 DiT 블록도

> **마스터플랜 W1 체크포인트 1번 문항입니다.**
> *"DiT 블록 다이어그램을 그리고 conditioning이 들어가는 위치를 설명하라."*
>
> 🔴 **lesson을 닫고 그리세요.** 보면서 채우면 베끼기이지 백지 연습이 아닙니다.
> 워크시트 파일: [`../practice/dit_block_worksheet.excalidraw`](../practice/dit_block_worksheet.excalidraw) — [excalidraw.com](https://excalidraw.com)에서 열기

### 4.1 그리기 기록

| 항목 | 값 |
|---|---|
| lesson을 닫고 그렸는가 | ☐ 예 ☐ 아니오 |
| 소요 시간 | ______분 |
| 물음표로 남긴 칸 수 | ______개 |
| 채운 뒤 대조에서 틀린 칸 수 | ______개 |

### 4.2 ★ 채점 체크리스트 A군 — 6개 변조 지점

**이 6개가 이 문항의 핵심입니다.** 하나라도 틀렸으면 §6.3을 다시 읽으세요.

| # | 항목 | 맞음 | 내가 쓴 것 (틀렸을 때만) |
|---|---|---|---|
| A1 | $\gamma_1,\beta_1$ — **첫 LayerNorm 직후, self-attention 앞**. `⊙(1+γ₁) ⊕ β₁` | ☐ | |
| A2 | $\alpha_1$ — **self-attention 출력 뒤, 첫 잔차 덧셈 직전** | ☐ | |
| A3 | $\gamma_2,\beta_2$ — **두 번째 LayerNorm 직후, MLP 앞** | ☐ | |
| A4 | $\alpha_2$ — **MLP 출력 뒤, 두 번째 잔차 덧셈 직전** | ☐ | |
| A5 | 여섯 개 전부 `[B, 1, d]`이고 **토큰 축 $T$로 broadcast** | ☐ | |
| A6 | LayerNorm은 **`elementwise_affine=False`** — $\gamma,\beta$가 그 역할을 **대체**하므로 | ☐ | |

### 4.3 ★ 채점 체크리스트 B군 — 조건 경로

| # | 항목 | 맞음 | 내가 쓴 것 |
|---|---|---|---|
| B1 | $c = \mathrm{Emb}(t) + \mathrm{Emb}(y)$ — **두 임베딩 벡터의 합**, `[B, d]` | ☐ | |
| B2 | 경로가 **`SiLU → Linear(d → 6d)`** | ☐ | |
| B3 | 출력을 **6등분(chunk 6)** 해 $\gamma_1\beta_1\alpha_1\gamma_2\beta_2\alpha_2$로 | ☐ | |
| B4 | 이미지 DiT에서 $y$는 클래스 라벨, **액션 헤드에서는 $t$만 adaLN**, 관측은 cross-attention | ☐ | |

### 4.4 ★ 채점 체크리스트 C군 — zero-init 위치와 논리

| # | 항목 | 맞음 | 내가 쓴 것 |
|---|---|---|---|
| C1 | **0으로 초기화하는 것은 `Linear(d → 6d)` 하나** — 블록 본체 가중치가 아니다 | ☐ | |
| C2 | 초기에 $\gamma=\beta=\alpha=0$이고, **$\alpha=0$ 때문에** 두 잔차 가지가 닫혀 $h_{\text{out}}=h_{\text{in}}$ | ☐ | |
| C3 | 항등을 만드는 것은 **$\alpha$ 하나** — $\gamma,\beta$가 아니다 (③ 3.2와 같은 답) | ☐ | |
| C4 | 28층을 통과해도 초기 신호가 변형되지 않아 그래디언트가 폭주·소멸하지 않는다 | ☐ | |
| C5 | 제어 대응: **소프트 스타트.** 게인을 0에서 여는 절차를 옵티마이저에 위임 | ☐ | |

### 4.5 차원 셈 (워크시트 상단 칸)

| 항목 | 값 |
|---|---|
| 이미지 DiT-XL/2: 잠재 `[B,4,32,32]`, $p=$ ______ → $T=(I/p)^2=$ ______, $d=$ ______ | |
| 블록 수 $N=$ ______, 헤드 ______개, MLP hidden $=$ ______ $\cdot d$ | |
| 액션 청크판: `a_t [B, H=___, D=___]` → `patchify p_t=___` → `[B, ___, d]` → 출력 `[B, ___, ___]` | |
| 최종 레이어(마지막 블록 다음)에 $\gamma,\beta$는 붙는다. **$\alpha$는 있는가 없는가? 왜?** | |

### 4.6 ★★ 말로 설명하는 칸 — 이것이 문항의 절반입니다

문항은 "그리고 **conditioning이 들어가는 위치를 설명하라**"입니다. 그림만으로는 절반입니다.

**Q1. conditioning 주입 4방식을 FID 순으로 정렬하고, adaLN과 adaLN-Zero의 차이가 무엇 하나인지.**

```
순서:
차이:
```

**Q2. "조건이 시퀀스 전체에 균일하게 작용해야 할 때는 토큰보다 정규화에 태우는 것이 낫다" — 이 문장의 근거가 되는 §6.2 표의 숫자 두 개는?**

```

```

**Q3. 액션 헤드에서는 왜 $t$만 adaLN으로 가고 관측은 cross-attention으로 갑니까?** (§7.1 표 · §6.4 마지막 줄)

```

```

**Q4. ★ 종합 — 이 문항에 백지로 답한다고 생각하고 한 문단으로 쓰세요.** (그림 없이 말로만)

```

```

---

## ⑤ ★★ Step 7 — NFE 예산 재계산

> **lesson 「실습으로 가기」가 지정한 두 번째 산출물입니다.** §5.2·§8.3 표를 **자기 하드웨어 실측값**으로 통째로 다시 채웁니다.

### 5.1 산수 검산 — 먼저 lesson 표가 재현되는지

가정: $f_2=50$ Hz, $\tau_{\text{comm}}=20$ ms, $\tau_{\text{VLM}}=60$ ms, $T_{\text{replan}}=\tau_{\text{infer}}$.

$$
\tau_{\text{infer}} = \tau_{\text{VLM}} + \text{NFE}\times t_{\text{NFE}}
\qquad
\text{지연} = 2\,\tau_{\text{infer}} + \tau_{\text{comm}}
\qquad
H_{\text{chunk}} = \lceil f_2 \times \text{지연} \rceil
$$

**NFE=250, $t_{\text{NFE}}$=3 ms로 손으로 계산하세요.**

| 단계 | 내 계산 | lesson §5.2 |
|---|---|---|
| $\tau_{\text{infer}}$ | | 810 ms |
| 지연 | | 1,640 ms |
| $H_{\text{chunk}}$ | | 82 |

☐ 재현됨 → 5.2로. ☐ 안 됨 → README §7.1을 다시 보세요.

### 5.2 $t_{\text{NFE}}$ 고르기 — 판단이 필요한 칸

③ 3.3의 벤치 4행 중 **어느 행이 "우리 헤드"일 것 같습니까?** 정답이 없습니다.

| 항목 | 값 |
|---|---|
| 고른 구성 (d / 블록 수 / 파라미터) | |
| **$t_{\text{NFE}}$** | **______ ms** |
| device (`cuda` / `cpu`) | |
| **고른 근거** (한 줄) | |

**이 벤치 값을 그대로 예산에 넣으면 안 되는 이유는?** (힌트: 토큰 16개 · 배치 1 · 조건 토큰 없음)

```

```

### 5.3 §5.2 표 재계산 — 자기 숫자로

| NFE | $\tau_{\text{infer}}$ | 지연 | $H_{\text{chunk}}$ | lesson (@3 ms) |
|---|---|---|---|---|
| 1 | | | | $H$=8 · 160 ms |
| 10 | | | | $H$=10 · 200 ms |
| 20 | | | | — |
| 50 | | | | $H$=22 · 440 ms |
| 250 | | | | $H$=82 · 1,640 ms |
| 1000 | | | | $H$=307 · 6,140 ms |

> 참고 — $t_{\text{NFE}}$=14 ms(DiT-XL GPU)로 채우면: NFE 10 → $H$=21 · 420 ms / NFE 50 → $H$=72 · 1,440 ms / NFE 250 → $H$=352 · **7,040 ms**.

### 5.4 ★ 200 ms 선

지연을 NFE의 함수로 정리하면 $\text{지연} = 140 + 2\,\text{NFE}\cdot t_{\text{NFE}}$ (ms)이므로 $\text{NFE} \le 30 / t_{\text{NFE}}$입니다.

**[W1-M1 §3.2](../../01-physical-ai-landscape/lesson.md)이 "발이 미끄러지기 시작해서 자세가 무너지기까지 200 ms가 안 걸린다"고 못박았습니다.**

| $t_{\text{NFE}}$ | 지연이 200 ms를 넘지 않는 NFE 상한 |
|---|---|
| 3 ms *(lesson 가정)* | **10** |
| **내 $t_{\text{NFE}}$ = ______ ms** | **______** |

**이 숫자를 보고 든 생각을 한 줄로:**

```

```

### 5.5 §8.3 자리별 상한 재계산

| 자리 | 주기 예산 | 헤드에 남는 시간 | @3 ms *(lesson)* | **@내 $t_{\text{NFE}}$** |
|---|---|---|---|---|
| L2 안에 직접 · 50 Hz | 20 ms | 20 ms | 6 NFE | |
| L2 안에 직접 · 500 Hz | 2 ms | 2 ms | **0 NFE** | |
| **L4 청크 생성 · 5 Hz** | 200 ms | **140 ms** | **46 NFE** | |
| L4 청크 생성 · 10 Hz | 100 ms | 40 ms | 13 NFE | |

> 참고 — 집필 환경에서 DiT-XL(673.7M)을 그대로 액션 헤드에 쓰면 **L4 5 Hz 자리 상한이 46 → 6~10**으로 떨어집니다(GPU 13~21 ms/NFE 기준). CPU라면 **0**입니다.

**Q1. 가정 하나(3 ms → 내 값)가 상한을 몇 배로 바꿨습니까?** → ______배

**Q2. L2의 두 행(50 Hz · 500 Hz)은 $t_{\text{NFE}}$가 뭐든 거의 0입니다. 이 결론이 하드웨어를 바꿔도 안 뒤집히는 이유는?**

```

```

**Q3. 그래서 action chunking은 무엇과 무엇을 교환하는 거래입니까?** ([W1-M1 §4.3](../../01-physical-ai-landscape/lesson.md)의 MPC 지평 트레이드오프와 연결)

```

```

### 5.6 ★★ 결론 한 줄

**"우리 스택이 감당 가능한 NFE 상한은 얼마인가."** 가정과 함께 쓰세요 — 가정 없는 숫자는 다음 주에 쓸 수 없습니다.

```

```

**이 결론에서 아직 채우지 못한 칸은 무엇입니까?** (lesson §8.4의 다섯 항목 중)

```

```

---

## ⑥ 체크포인트 백지 답변

> 마스터플랜 [§5 W1 체크포인트](../../../../docs/physical-ai-4week-master-plan.md) 3문항 중 **1번 전체**와 **2번의 DDPM 절반**이 이 모듈 담당입니다.
> 3번(FSQ)은 [W1-M5](../../05-latent-discrete-fsq/labs/worksheet.md), 2번의 Flow Matching 절반은 W1-M4가 받습니다.
>
> 🔴 **lesson과 ④⑤를 닫고 쓰세요.** 다 쓴 뒤에 열어서 대조합니다.

### 6.1 체크포인트 1번 (전문)

> **DiT 블록 다이어그램을 그리고 conditioning이 들어가는 위치를 설명하라.**

**그림** — ④의 excalidraw를 첨부하거나 여기에 ASCII로 다시 그리세요. **한 번 더 그리는 것이 목적입니다.**

```




```

**설명** (④ 4.6의 Q1~Q4를 종합해 한 문단으로):

```

```

| 자가 채점 | ☐ 6개 변조 지점 위치 | ☐ 조건 경로 `SiLU→Linear(d→6d)` | ☐ zero-init 위치 | ☐ 항등 논리 | ☐ 액션 청크판으로의 전이 |

### 6.2 체크포인트 2번 — DDPM 절반

> **DDPM과 Flow Matching의 학습 objective 차이를 수식 수준으로 설명하라.** 로봇 제어에서 FM이 선호되는 실용적 이유는?

**DDPM 쪽 — 수식 수준으로.** ELBO → $L_{t-1}$ 제곱차 붕괴 → $\epsilon$ 재파라미터화 → $\mathcal{L}_{\text{simple}}$까지 백지에 재구성하세요.

```
① forward 닫힌 형태:

② ELBO 세 덩어리 중 학습되는 항:

③ 제곱차로 붕괴하는 데 필요한 가정:

④ 재파라미터화 후 남는 것:

⑤ L_simple:

⑥ 버린 것(λ_t)과 그 대가:
```

**"로봇 제어에서 FM이 선호되는 실용적 이유"의 근거 절반은 이미 이 모듈에 있습니다.** ②와 ⑤에서 얻은 것으로 쓸 수 있는 만큼 쓰세요.

```
근거 ① (② 2.4에서):

근거 ② (⑤ 5.4·5.5에서):

W1-M4에서 채울 것:
```

### 6.3 셀프 체크 퀴즈 — 틀린 것 기록

lesson [「셀프 체크 퀴즈」](../lesson.md)의 10문항. **틀린 것만** 적으세요.

| # | 왜 틀렸나 | 다시 읽을 절 |
|---|---|---|
| | | |
| | | |

---

## ⑦ 팀 질문 / 모르는 것 ★

lesson [「팀에 물어볼 것」](../lesson.md)의 6개 중 **본인이 실제로 궁금해진 것**을 고르고, 실습 후 새로 생긴 질문을 추가하세요.
→ [`../../../../notes/questions-for-team.md`](../../../../notes/questions-for-team.md)의 W1 섹션으로 옮길 것.

| # | 질문 | 왜 궁금해졌나 (어느 Step에서) | 우선순위 |
|---|---|---|---|
| M3-1 | L4에 액션 헤드가 있는가? DiT 계열인가, pi0처럼 언어모델 블록 재사용인가, 헤드 없이 FSQ 토큰 직접 예측인가 (§8.1 A·B·C) | | |
| M3-2 | **objective가 DDPM 계열인가 FM 계열인가? 추론 NFE는 몇이고 1회 forward는 몇 ms인가** | ⑤ 전체가 이 세 숫자를 못 채워서 가정으로 돌아갔음 | |
| M3-3 | 조건(관측)을 헤드에 어떻게 주입하는가 — cross-attention / in-context / adaLN | | |
| M3-4 | FSQ 토큰과 액션 헤드의 순서는? | | |
| M3-5 | 액션 청크 길이 $H$와 하위 소비 주파수 $f_2$의 실제 값은? | | |
| M3-6 | 학습 데이터에 다봉 시연이 실제로 있는가? | | |

**실습을 하고 나서야 생긴 질문** (이게 제일 값집니다):

```

```

**④⑤에서 `?`로 남긴 칸 중 팀 질문으로 승격할 것:**

```

```

---

## ⑧ 막힌 지점과 해결 방법 (→ `docs/progress.md`로 그대로 옮길 것)

> 이 칸이 다음 주 자료의 입력이 됩니다. **지우지 마세요.**

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

**에러 표(README §흔한 에러 6개)에 없던 새로운 에러:**

```

```

---

## ⑨ 내가 발견한 불일치 (문서 vs 실제 출력)

> 문서와 출력이 어긋나면 **출력을 믿고** 여기 적으세요. 자료 수정의 입력이 됩니다.
> 단, `01`의 스케줄 표만은 예외입니다 — **assert가 깨졌다면 코드가 아니라 lesson 표를 먼저 의심**하세요.

| 위치 | 문서의 서술 | 실제 출력 |
|---|---|---|
| | | |

---

## ⑩ 용어집 (5개)

→ [`../../../../notes/glossary.md`](../../../../notes/glossary.md) W1-M3 절에 추가.
후보: adaLN-Zero · patchify · NFE · ancestral sampling · probability flow ODE · $\epsilon$-prediction · classifier-free guidance · action chunk

| 용어 | 내 말로 정의 | 제어·ML·LLM 비유 |
|---|---|---|
| | | |
| | | |
| | | |
| | | |
| | | |

---

## ⑪ 실행 로그 (→ `docs/progress.md`)

```
날짜:
환경: (device / torch / GPU 또는 CPU)
돌린 것:
  - 01 --smoke →
  - 01 →
  - 02 →
  - 03 →
막힌 지점:
소요 시간: Step 1~5 ___분 / Step 6~8 ___분
GPU: (로컬이면 모델명 + 총 사용 시간 / CPU 단독이면 "CPU 단독")
미검증 배지 해제: 없음 (이 모듈에는 배지가 없습니다)
다음에 할 것:
```

---

## ⑫ 최종 제출 체크

- [ ] **G1** — `01`이 §3.3·§4.4 assert 2종 통과 + SNR 교차 `t = 259` 확인
- [ ] **G2** — `01_schedule.png` (c)와 `01_loss.png` (b)를 **나란히 보고** $\lambda_t$의 U자와 "작은 $t$가 어렵다"를 확인
- [ ] **G3 ★** — `02` 16구성 완주 + **노이즈 바닥 기준으로 "어느 행부터 무의미한가"를 ②에 직접 판정**
- [ ] **G4 ★** — `03` 세 초기화 결과 확인 + **"항등을 만드는 것은 $\alpha$"** 를 ③ 3.2에 자기 말로 씀
- [ ] **G5 ★★** — excalidraw 워크시트를 **lesson을 닫은 채** 채웠고, ④ 4.2의 **A군 6개를 전부 맞힘**
- [ ] **G6 ★★** — ⑤에 §5.2·§8.3 표를 **자기 $t_{\text{NFE}}$로 재계산**하고, 5.6에 **가정이 명시된 결론 한 줄**을 씀
- [ ] **G7** — ⑥ 체크포인트 1번을 **백지로** 답하고 자가 채점 5칸을 확인
- [ ] **G8** — `docs/progress.md` · `notes/glossary.md` · `notes/questions-for-team.md` 갱신
- [ ] **[P1]** `notes/papers/`에 DiT(2212.09748) 또는 Diffusion Policy(2303.04137) 2-pass 노트 착수

> ⑤ 5.6의 결론에 **가정이 안 적혀 있으면 G6은 미완입니다.** 그 가정이 M3-2 팀 질문을 던질 수 있게 만드는 것입니다.
> ④ 4.6이 비어 있으면 G5도 미완입니다 — 체크포인트 문항의 절반이 "설명하라"이기 때문입니다.
