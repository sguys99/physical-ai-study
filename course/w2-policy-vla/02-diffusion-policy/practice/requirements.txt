# W2-M2 practice 의존성
#
# ┌──────────────────────────────────────────────────────────────────────────────┐
# │  A층 (01, 02, 03) 의존성 0. 설치할 것이 아무것도 없습니다.                    │
# │      python3 01_mean_collapse.py                                             │
# │  표준 라이브러리만 씁니다:                                                    │
# │      argparse, csv, os, random, re, statistics, time, unicodedata, pathlib    │
# │  numpy 도 matplotlib 도 쓰지 않습니다. 그림은 CSV 와 ASCII 로 냅니다.         │
# └──────────────────────────────────────────────────────────────────────────────┘
#
# 집필 환경 (여기서 실제로 실행 검증한 값):
#   WSL2 (Linux 6.6.87.2) / RTX 3080 12GB / 2026-08-11
#   A층: 시스템 python3 3.12.3, 실행검증 완료 (의존성 0)
#   B층: .venv-lerobot 의 python 3.12, --device cuda 실행검증 완료
#
# A층 세 스크립트는 lesson.md 를 읽어 표의 숫자를 다시 계산합니다.
# 그래서 practice/ 폴더 안에서 실행해야 합니다 (상대 경로 ../lesson.md).

# ─── B층 (04_dp_latency_bench.py) 만 필요한 것 ─────────────────────────────────
# 실제 DiffusionPolicy 를 인스턴스화해 추론 지연을 직접 잽니다.
# torch 나 lerobot 이 없으면 04 는 크래시하지 않고 설치 안내를 낸 뒤 정상 종료합니다.
#
# 설치는 개별 pin 이 아니라 extra 를 통째로 거는 쪽이 안전합니다:
#   python3.12 -m venv .venv-lerobot && source .venv-lerobot/bin/activate
#   uv pip install "lerobot[pusht,diffusion,evaluation]"
#
# diffusion extra 가 빠지면 정책 생성 시점에 diffusers 부재로 죽습니다
# (lesson 「실습으로 가기」에 기록된 함정).
#
# 아래는 집필 환경에서 실제로 확인된 조합입니다. 재현이 필요하면 이 버전으로 고정하세요.
lerobot==0.6.1
torch==2.11.0
diffusers==0.39.0
numpy==2.2.6

# CUDA 빌드가 필요하면 인덱스를 지정합니다 (집필 환경은 torch 2.11.0+cu130):
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu130
#
# GPU 없이도 04 는 돕니다. --device cpu 로 내려가고, NFE 100 두 설정은 한 번에 몇 분씩
# 걸리므로 --smoke 를 함께 주세요 (반복 3회, NFE 100 건너뜀).

# ─────────────────────────────────────────────────────────────────────────────
# lerobot-train, 데이터셋 다운로드, 롤아웃 mp4 는 여기 없습니다.
#
#   practice/ 는 "lesson 의 숫자를 검산하고 정책 객체를 만져보는" 층이고,
#   labs/ 는 "PushT 학습과 평가와 롤아웃을 완주하는" 층입니다.
#   마스터플랜이 끝까지 지키라고 명시한 LeRobot 완주는 labs/ 의 몫입니다.
#
# 이 모듈은 jupytext 로 .py 와 .ipynb 를 동기화합니다 (생성물도 커밋):
#   pip install jupytext==1.19.5
#   jupytext --sync 01_mean_collapse.py
# ─────────────────────────────────────────────────────────────────────────────
