# W1-M2 워크시트 — 제출물

> **이 파일이 W1-M2의 제출물입니다.** [`README.md`](README.md)의 6단계를 따라가며 채우세요.
> 시작 전에 사본을 만들어 쓰는 것을 권합니다: `cp worksheet.md worksheet-$(date +%F).md`
>
> 🔴 **막힌 지점을 지우지 마세요.** 이 워크시트의 ⑦(막힌 지점)이 가장 값진 칸입니다.
> 그대로 [`../../../../docs/progress.md`](../../../../docs/progress.md)로 옮길 수 있게 써두세요 — **여기 쌓인 실측값이 W3 자료의 입력**이 됩니다.
> 🔴 **모르는 칸은 `?`로 남기세요.** 특히 회사 내부 구현은 lesson에도 답이 없습니다. `?`가 팀 질문의 원재료입니다.

---

## ⓪ 기본 정보

| 항목 | 값 |
|---|---|
| 작성일 | |
| 총 소요 시간 (계획 6시간 대비) | |
| **1.5일 모드로 넘어갔는가** | 아니오 / 예 — 이유: |

---

## ① 내 인스턴스 사양

| 항목 | 값 | 확인 명령 |
|---|---|---|
| 제공자 | RunPod / Lambda / AWS / 회사 클라우드 / 기타: ______ | — |
| 인스턴스 타입 | | 콘솔 |
| **GPU** | 모델: ______ / **없음** | `nvidia-smi` |
| 드라이버 · CUDA 버전 | | `nvidia-smi` 우상단 |
| vCPU / RAM | | `nproc` · `free -h` |
| **퍼시스턴트 볼륨 경로 (`$WORK`)** | | `df -h` |
| 볼륨 크기 / 여유 | ____ GB / ____ GB | `df -h` |
| OS | | `lsb_release -a` |
| **Python** | | `python3 --version` |
| `mujoco` | | `python -c "import mujoco; print(mujoco.__version__)"` |
| `playground` | | `pip show playground \| head -2` |
| `jax` + 백엔드 | 버전 ____ / `cpu` · `cuda` | `python -c "import jax; print(jax.__version__, jax.devices())"` |

**검증 환경과 다른 점** (README §0.4는 Python 3.12.12 / mujoco 3.11.0 / playground 0.2.0 / 2026-08-01):

```
(여기에 작성 — 없으면 "동일")
```

---

## ② Step별 실측 소요 시간

| Step | 내용 | 계획 | **내 실측** | 차이의 원인 |
|---|---|---|---|---|
| 1 | 인스턴스 · venv · 렌더링 관문 | 60~120분 | | |
| 2 | `01` MuJoCo 기초 | 40분 | | |
| 3 | `02` G1 해부 + 워크시트 | 60분 | | |
| 4 | `03` G1 구동 + 영상 내려받기 | 60분 | | |
| 5 | `04` playground 스모크 | 30분 | | |
| 6 | 마무리 기록 | 40분 | | |
| | **합계** | **약 6시간** | | |

**가장 오래 걸린 Step**: ____ · 그 이유:

```
(여기에 작성)
```

---

## ③ Step 1 — 렌더 백엔드

### 3.1 백엔드 결정 경로

README §1.4 순서도에서 내가 지나온 경로를 표시하세요.

| 시도 | 결과 | 비고 |
|---|---|---|
| `MUJOCO_GL=egl` | ✅ 통과 / ❌ 실패 | 실패 시 에러 첫 줄: |
| `libegl1` 등 설치 후 재시도 | 해당 없음 / ✅ / ❌ | |
| `MUJOCO_GL=osmesa` | 해당 없음 / ✅ / ❌ | |
| `Xvfb` + `glfw` | 해당 없음 / ✅ / ❌ | |

**최종 사용 백엔드**: ______________

### 3.2 렌더링 관문 통과 기록

| 항목 | 기대값 | **내 출력** |
|---|---|---|
| `backend :` | `egl` | |
| `frame :` | `(240, 320, 3) uint8` | |
| `render_gate.png` 크기 | 0 바이트가 아닐 것 | ____ bytes |
| 종료 시 EGL traceback | 나와도 무해 | 나옴 / 안 나옴 |

### 3.3 tmux

- [ ] `tmux new -s pai` → `Ctrl+b` `d` → `tmux attach -t pai` 왕복 성공

---

## ④ Step 2·3 — MuJoCo 기초와 G1 해부

### 4.1 `01_mujoco_basics.py` 판정

| 확인할 것 | 기대 | **내 출력** |
|---|---|---|
| 운동방정식 잔차 | `0.000e+00` | |
| 수렴 차수 (Euler) | ≈ 1 | |
| 수렴 차수 (RK4) | ≈ 3~4 | |
| 렌더 shape | `(240, 320, 3) uint8` | |
| `artifacts/W1-M2/` 파일 수 | `01_*` 3개 | |

**timestep 스윕 그림에서 읽은 것** (README §2.3의 4개 질문)

1. `nq = nv = 1`, `nu = 0`인 이유 — 그리고 G1에서 셋이 달라질 것으로 예상한 이유:

```
(여기에 작성 — Step 3을 하기 전에 먼저 쓰세요)
```

2. (a) 패널이 궤적이 아니라 **편차**를 그린 이유, 그리고 오차가 진폭이 아니라 **위상**으로 누적된다는 것이 그림에서 어떻게 보였나:

```
(여기에 작성)
```

3. (b) 패널의 에너지 오차가 **전부 수치 오차**인 이유:

```
(여기에 작성)
```

4. 세로 점선 두 개(classic 0.002 / MJX 0.004) — MJX가 **어느 방향**으로 갔고 왜인가:

```
(여기에 작성)
```

### 4.2 ★ G1 인덱스 퀴즈 — CSV를 보기 **전에** 손으로

> 🔴 **먼저 손으로 채우고**, 그 다음에 `joints_g1.csv`를 열어 대조하세요. 순서를 바꾸면 받아쓰기가 됩니다.

**규칙** (lesson §5.4): `ctrl id = i` → `qpos adr = i + ___` → `qvel adr = i + ___` → `jnt id = i + ___`

| 관절 | `jnt` id | `qpos` adr | `qvel` adr | `ctrl` id |
|---|---|---|---|---|
| **`left_knee_joint`** | | | | |
| `right_knee_joint` | | | | |
| `waist_yaw_joint` | | | | |
| `left_elbow_joint` | | | | |
| `right_wrist_yaw_joint` | | | | |

**오프셋이 네 개 다 다른 이유를 한 줄로**:

```
(여기에 작성)
```

**CSV와 대조 결과**: ___ / 5 정답. 틀린 것:

```
(여기에 작성)
```

### 4.3 `02_g1_inspect.py` 판정

| 확인할 것 | 기대 | **내 출력** |
|---|---|---|
| `scene.xml` `nq` / `nv` / `nu` | 36 / 35 / 29 | |
| 세 변형 비교 | `✅ lesson §5.2 표와 전부 일치합니다` | |
| **인덱스 규칙 검증** | `✅ 구동 관절 29개 전부 규칙을 만족합니다` | |
| `wc -l joints_g1.csv` | 31 (헤더 1 + 30행) | |
| `kv` 범위 | `[4.553, 43.011]` | |
| `M_eff` 최대/최소 비 | **89배** | |
| `forcerange` | `[0. 0.]` | |
| `nsensor` / `sensordata` 길이 | 4 / 12 | |

**`kv`가 관절마다 다른 이유를 식으로**:

```
(여기에 작성 — k_v = ______ 에서 무엇이 관절마다 다른가)
```

`left_elbow_joint`의 `kv = 9.42912002`, `kp = 500`에서 $M_{\text{eff}}$ 역산 = ______ kg·m²

**`forcerange = [0, 0]`이 sim2real 갭의 씨앗인 이유**:

```
(여기에 작성)
```

### 4.4 킨매틱 트리 워크시트

[`../practice/g1_kinematic_tree_worksheet.excalidraw`](../practice/g1_kinematic_tree_worksheet.excalidraw) 채우기.

| 체인 | 노드 수 | 채움 | `?` |
|---|---|---|---|
| 왼다리 | 6 | | |
| 오른다리 | 6 | | |
| 허리 → 몸통 | 3 | | |
| 왼팔 | 7 | | |
| 오른팔 | 7 | | |
| **합계** | **29** | | |

> ✅ 채움 + `?` 의 합이 **29**여야 합니다. 빈 노드가 남아 있으면 미완성입니다.

- [ ] 상단 "확인할 규칙" · "차원 셈" 상자 채움
- [ ] 하단 확인 문항 채움
- [ ] lesson §5.4 표와 대조 완료

**대조에서 틀린 칸**:

```
(여기에 작성)
```

### 4.5 선택 심화 — `scene_with_hands.xml`

`python 02_g1_inspect.py --variant scene_with_hands.xml --smoke` 를 돌렸다면:

| 항목 | 값 |
|---|---|
| 규칙을 벗어난 관절 수 | ____ 개 |
| 어떤 관절인가 | |
| 원인 (한 줄) | |

**여기서 배운 것 — 산술 규칙을 믿으면 안 되는 이유**:

```
(여기에 작성)
```

---

## ⑤ Step 4 — G1을 움직이기 ★

### 5.1 실행 판정

| 확인할 것 | 기대 | **내 출력** |
|---|---|---|
| `--joints arms`의 `ctrl id` 범위 | `15~28` | |
| PD 서보 assert 잔차 | `~1e-13` | |
| `ctrlrange 클립 발생` | 0회 | |
| `arms` 골반 높이 (시작 → 끝) | 0.790 → 0.792 m | |
| `arms` mp4 프레임 수 | **180프레임 @ 30fps** | |
| **`legs` 골반 높이 (시작 → 끝)** | **0.790 → 0.062 m** | |
| `legs` 넘어진 시각 | `t ≈ 0.88 s` | |
| 실행 시간 (`--joints arms`) | 약 27초 | |

### 5.2 ★ 추종 오차 그래프에서 읽은 위상 지연

`03_tracking_arms.png`의 (d) 패널과 stdout `[3]` 표를 보고 채우세요.

| 관절 | $M_{\text{eff}}$ [kg·m²] | $\omega_n$ [Hz] | RMS 오차 [mrad] | **지연 실측 [ms]** | 지연 eq.(4) [ms] |
|---|---|---|---|---|---|
| `*_shoulder_pitch` (관성 큰 쪽) | | | | | |
| `*_shoulder_roll` | | | | | |
| `*_wrist_yaw` | | | | | |
| `*_wrist_roll` (관성 작은 쪽) | | | | | |

**관찰 3가지**

1. **관성이 클수록 지연이 ______ 다.** 손목 대비 어깨의 관성 비 ____ 배 → 지연 비 ____ 배. $\omega_n \propto 1/\sqrt{M}$ 이므로 지연이 $\sqrt{M}$에 비례한다는 예상과 맞는가?

```
(여기에 작성)
```

2. **`지연 실측`과 `지연 eq.(4)`가 정확히 같지 않다.** 그 이유를 eq.(4)의 가정으로 설명하세요.

```
(여기에 작성 — 힌트: 1자유도 근사 / M(q)가 자세 의존 / 관절 커플링)
```

3. **좌우 대칭 관절인데 값이 다르다** (`left_shoulder_pitch` vs `right_shoulder_pitch`). 내 실행에서 얼마나 차이 났나? 왜 그런가?

```
내 값: left ______ ms / right ______ ms
이유:
```

**kp=500 위치 서보의 대역폭 감각**: $\omega_n = \sqrt{500/M_{\text{eff}}}$ [rad/s].
$M_{\text{eff}} = 0.14$ → ______ rad/s = ______ Hz. 이 값이 W3에서 정책이 **50 Hz**로 명령을 뱉는 것과 어떻게 이어지는가:

```
(여기에 작성)
```

### 5.3 `legs`가 넘어지는 것을 어떻게 읽었는가 ★

> 29개 관절이 각각 mrad 수준으로 완벽하게 목표각을 추종하는데도 전신은 넘어집니다.
> **이것을 자기 말로** 설명하세요. lesson 문장을 복사하지 마세요.

```
(여기에 작성 — 3~5문장)
```

**조건**

- [ ] "PD 서보가 무엇을 모르는가"를 명시
- [ ] WBC와 W3-M1을 언급
- [ ] 회사 스택의 **L2 (GEAR-SONIC / HOMIE)** 를 언급

### 5.4 mp4를 봤는가 (통과 기준 G4)

| 항목 | 값 |
|---|---|
| 사용한 방법 | scp / VS Code Remote / 제공자 파일 브라우저 / HTTP 서버 |
| `g1_sin_arms.mp4` 재생 | ✅ 팔이 흔들리고 로봇은 서 있다 / ❌ |
| `g1_sin_legs.mp4` 재생 | ✅ 로봇이 무너진다 / ❌ |
| 걸린 시간 | ____ 분 |

**영상을 본 소감 한 줄** (이 모듈의 감정적 중심입니다. 형식 없이 그냥 쓰세요):

```
(여기에 작성)
```

### 5.5 심화 ① `--freq` 대역폭 실측

> 🔴 **먼저 예상하고** 표의 "예상" 열을 채운 뒤 돌리세요.

| `--freq` [Hz] | 어깨 RMS 오차 [mrad] (예상) | (실측) | 어깨 지연 실측 [ms] | 손목 RMS [mrad] |
|---|---|---|---|---|
| 0.5 (기준) | — | 14.57 (README 실측) | 32.7 | 3.71 |
| 1 | | | | |
| 2 | | | | |
| 5 | | 202.27 (README 실측) | 61.5 | 31.93 |
| 10 | | | | |

**어깨의 RMS 오차가 진폭 200 mrad의 절반(100 mrad)을 넘는 `--freq`**: ______ Hz
이 값이 실측 대역폭의 대용치입니다. 계산값 $\omega_n = 9.5$ Hz와 비교하면?

```
(여기에 작성)
```

**손목은 왜 여전히 멀쩡한가**:

```
(여기에 작성)
```

### 5.6 심화 ② `--joints waist` — 넘어짐은 이분법이 아니다

> 🔴 **돌리기 전에 예상을 먼저 쓰세요.** 넘어질까요? 왜?

```
예상:
```

**실행 결과** (`python 03_g1_sin_wave.py --joints waist --no-video`)

| 항목 | `arms` | **`waist`** | `legs` |
|---|---|---|---|
| 흔드는 관절 수 / ctrl id | 14 / 15~28 | 3 / 12~14 | 12 / 0~11 |
| 골반 높이 시작 → 끝 [m] | 0.790 → 0.792 | | 0.790 → 0.062 |
| **넘어진 시각** | 안 넘어짐 | | **t ≈ 0.88 s** |
| 최대 `M_eff` [kg·m²] | 0.1399 (어깨) | | — |
| 최소 `w_n` [Hz] | 9.5 | | — |

**예상이 맞았나요?** 틀렸다면 무엇을 놓쳤나:

```
(여기에 작성)
```

**관찰 ① 넘어지는 시각의 스펙트럼** — `arms` → `waist` → `legs` 순으로 빨리 넘어집니다.
이것을 "넘어진다/안 넘어진다"의 이분법이 아니라 **안정 여유(margin)** 의 문제로 다시 쓰면:

```
(여기에 작성)
```

**관찰 ② 허리가 이 모델에서 유효관성이 가장 큰 관절군**입니다. 어깨의 몇 배인가? 왜 그런가?

```
어깨 대비 ____ 배 /  손목 대비 ____ 배
이유:
```

같은 `kp = 500`인데 관절군마다 $\omega_n$ 이 ____ 배 차이납니다. 이것이 W3에서 뜻하는 바:

```
(여기에 작성)
```

**관찰 ③ 허리에서는 `지연 실측`과 `eq.(4)` 예측이 팔보다 잘 맞습니다.** 왜?

```
(여기에 작성 — 힌트: 팔에서 어긋났던 이유가 근사의 결함이었나 커플링이었나)
```

**왜 `arms`는 안 넘어지고 `waist`·`legs`는 넘어지는가** (지지 다각형 / 무게중심 / 접촉 유지로 설명):

```
(여기에 작성 — 세 경우가 각각 어떻게 다른지)
```

**이 관찰이 HOMIE ★ 의 하체 RL + 상체 텔레옵 분리 구조와 어떻게 이어지는가**
(주의: "상체는 흔들어도 안전하다"가 아닙니다. 실측은 그 반대를 보여줬습니다):

```
(여기에 작성)
```

---

## ⑥ Step 5 — playground 스모크

### 6.1 판정

| 확인할 것 | 기대 | **내 출력** |
|---|---|---|
| JAX 백엔드 | `cpu` 또는 `cuda` | |
| `jax.devices()` | `[CpuDevice(id=0)]` 또는 `[CudaDevice(id=0)]` | |
| `ALL_ENVS` 개수 | 19 | |
| G1·H1 태스크 수 | 4 | |
| **`action_size`** | **29** | |
| `ctrl_dt` / `sim_dt` | 0.02 / 0.002 | |
| **`n_substeps`** | **10** | |
| `episode_length` | 1,000 정책 스텝 = 20 s = 물리 10,000 스텝 | |
| `observation_size` | `state (103,)` / `privileged_state (216,)` | |
| 보상항 개수 | **24** | |
| lesson 대조 | `✅ lesson §7.3의 스펙과 전부 일치합니다` | |
| **종료 코드** | **`EXIT=0`** | |

### 6.2 JAX 컴파일 시간 (E8 관련)

| 항목 | 값 |
|---|---|
| `import mujoco_playground` | ____ s |
| `registry.load()` | ____ s |
| **`jax.jit(env.reset)` 첫 호출** | ____ s |
| `jax.jit(env.step)` step 1 (컴파일 포함) | ____ ms |
| `jax.jit(env.step)` step 2 (실행만) | ____ ms |
| 첫 호출 / 두 번째 호출 비 | ____ 배 |
| **캐시 상태** | 처음 실행(차가움) / 재실행(따뜻함) |
| step 1 `reward` | (기대 ≈ 0.029234) |

> 비가 1에 가깝게 나왔다면 캐시가 이미 따뜻한 것입니다 — **정상**입니다 (README E8).

### 6.3 확인 질문

1. **`n_substeps = 10`이 뜻하는 것** — 그리고 W1-M1의 action chunking과 어떻게 같은 구조인가:

```
(여기에 작성)
```

2. **`state (103,)`와 `privileged_state (216,)`가 나뉜 이유. 배포되는 것은 어느 쪽인가**:

```
(여기에 작성)
```

3. **보상 24개 중 `feet_air_time`·`feet_phase`가 특별한 이유** — 그리고 그게 왜 "MPC 대신 RL"의 진짜 근거인가:

```
(여기에 작성)
```

4. **가중치가 0인 항이 여럿 있다.** 이게 무엇을 뜻한다고 생각하는가:

```
(여기에 작성)
```

### 6.4 GPU 판단

| 항목 | 값 |
|---|---|
| 지금 GPU JAX를 설치했는가 | 예 / 아니오(W3까지 미룸) |
| 미뤘다면 그 이유 | |
| `pip install -U "jax[cuda12]"` 후 `jax.devices()` | 해당 없음 / |

---

## ⑦ ★ 막힌 지점과 해결 방법 (→ `docs/progress.md`로 그대로 옮길 것)

> **이 랩에서 가장 값진 칸입니다.** 다음에 인스턴스를 새로 띄울 때 이걸 보고 30분을 아낍니다.

| # | Step | 증상 (에러 메시지 첫 줄) | 원인 | 해결 방법 | 걸린 시간 | README 에러 번호 |
|---|---|---|---|---|---|---|
| 1 | | | | | | E__ / **표에 없음** |
| 2 | | | | | | |
| 3 | | | | | | |
| 4 | | | | | | |
| 5 | | | | | | |

**README 에러 표(E1~E10)에 없던 새 에러**가 있었다면 아래에 상세히:

````
증상:
```
(에러 메시지 전문)
```
원인:
해결:
````

**끝내 해결하지 못한 것** (있으면 다음 모듈로 넘김):

```
(여기에 작성 — 없으면 "없음")
```

---

## ⑧ 내가 발견한 불일치 (문서 vs 실제 출력)

랩을 돌면서 문서와 실행 결과가 어긋난 곳을 적습니다. **이걸 찾는 것도 랩의 목적입니다.**
**문서와 출력이 다르면 출력이 맞습니다** — 수치는 스크립트가 매번 다시 계산하지만 산문은 한 번 쓰면 굳습니다.

| # | 어디 (문서 / 절) | 문서는 뭐라고 하나 | 실제 출력은 | 원인 추정 |
|---|---|---|---|---|
| _예시_ | _labs README §5.2_ | _`reward` 0.029234_ | _0.029231_ | _버전 차이. done=0이면 정상_ |
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

**제공자 UI가 부록 A와 달랐던 점** (부록은 지도이고 정본은 공식 문서입니다):

```
(여기에 작성)
```

---

## ⑨ 팀 질문 ★

> **온보딩의 핵심 산출물입니다.** 여기 적은 것을
> [`../../../../notes/questions-for-team.md`](../../../../notes/questions-for-team.md)의 **W1 섹션에 `M2-n` 형식으로 복사**합니다.

### 9.1 lesson 「팀에 물어볼 것」에서 옮길 것 (최소 3개 — 아래 셋이 우선순위 최상)

| # | 질문 | 이 답이 없으면 무엇을 못 하는가 | 우선순위 | 복사함 |
|---|---|---|---|---|
| M2-1 | **팀 표준 학습 환경은? (시뮬레이터 · 클러스터 · 도커 이미지)** | Isaac Lab + 사내 이미지가 표준이면 W3 실습 경로가 통째로 바뀌고 GPU 요구사항(RT 코어)도 달라짐. 마스터플랜 §12가 "W1 중 반드시 확인"으로 못박음 | 🔴 P0 | [ ] |
| M2-2 | **회사 G1의 정본 로봇 모델 파일은? (menagerie 29 DoF vs 사내 MJCF/URDF)** | **액션 벡터 차원이 여기서 결정.** 이 답 전까지 이 랩은 "29 DoF 모델로 감각 익히기"이지 "우리 로봇 시뮬"이 아님 | 🔴 P0 | [ ] |
| M2-3 | **SDK 모터 번호 ↔ MJCF 관절 인덱스 매핑표가 사내에 있는가?** | 없으면 sim2sim에서 관절이 뒤바뀐 채 도는 버그를 각자 다시 발견. Step 3.6에서 손가락 4개가 규칙을 벗어나는 것을 이미 봄 | 🔴 P0 | [ ] |
| M2-4 | MJCF 물리 파라미터를 실기 측정값으로 보정한 사내 모델이 있는가? (`forcerange` / `armature` / `frictionloss` / 발 접촉 / 총질량) | 보정 모델이 있는지, 도메인 랜더마이제이션으로 흡수하는 방침인지에 따라 W3-M1의 랜더마이제이션 범위가 달라짐 | 🟡 P1 | [ ] |
| M2-5 | GPU 예산과 운영 규칙은? (주간 상한 / 스팟 사용 / 퍼시스턴트 볼륨 정책 / 사내 클러스터 큐) | 4주 실습 계획의 제약 조건. 부록 A 기준으로 A100/H100이 불필요하다는 점도 함께 확인 | 🟡 P1 | [ ] |

### 9.2 ★ 이 랩을 돌면서 내가 새로 만든 질문 (최소 1개)

기존 M1-1~M1-6, M2-1~M2-5와 **중복되지 않는 것**으로.

| # | 질문 | 어느 Step에서 생겼나 | 왜 중요한가 | 기존과 중복? |
|---|---|---|---|---|
| 1 | | | | 아니오 / M__-__와 유사 |
| 2 | | | | |
| 3 | | | | |

**킨매틱 트리 워크시트에서 `?`로 남긴 칸 중 팀 질문으로 승격할 것**:

```
(여기에 작성)
```

- [ ] `notes/questions-for-team.md`에 복사 완료 (추가한 행: ______)

---

## ⑩ 용어집 (5개)

[`../../../../notes/glossary.md`](../../../../notes/glossary.md) W1 섹션에 `### W1-M2`를 만들고 추가.
**정의 한 줄 + 제어·ML·LLM 대응 비유**를 반드시 같이 (비유가 없으면 나중에 안 떠오릅니다).

| 용어 | 정의 (한 줄) | 제어·ML·LLM 대응 비유 |
|---|---|---|
| | | |
| | | |
| | | |
| | | |
| | | |

후보: `mjModel`/`mjData` · MJCF · floating base · `n_substeps` · EGL·오프스크린 렌더 · 임계감쇠 `dampratio` · sim2sim · 비대칭 actor-critic · soft constraint(`solref`/`solimp`) · `implicitfast`

- [ ] `notes/glossary.md`에 복사 완료

---

## ⑪ 셀프 체크 퀴즈 10문항 — 틀린 것 기록

lesson **「셀프 체크 퀴즈」**를 풀고 그 절의 **`<details>` 정답**과 대조합니다. 맞은 것은 비워두고 **틀린 것만** 적으세요.

| # | 주제 | 결과 | 틀린 이유 (내 답 vs 정답) | 다시 볼 절 |
|---|---|---|---|---|
| 1 | `mjModel`/`mjData` 분리와 MJX 병렬 | O / X | | §3.1 |
| 2 | (계산) `scene_with_hands` hinge 수 / `nq−nv=1` | O / X | | §3.4, §5.2 |
| 3 | `left_knee_joint`의 네 인덱스 | O / X | | §5.4 |
| 4 | (계산) 에피소드 물리 스텝 수 / `n_substeps` | O / X | | §7.3, §7.4 |
| 5 | classic vs MJX 튜닝 5개 / sim2sim 축소판 | O / X | | §3.5 |
| 6 | `position` 액추에이터 힘 식 / `ctrl`의 의미 | O / X | | §5.5 |
| 7 | (계산) `dampratio=1`인데 `kv`가 다른 이유 / $M_{\text{eff}}$ 역산 | O / X | | §5.5 |
| 8 | `MUJOCO_GL` 세 백엔드 / import 순서 / EGL traceback | O / X | | §6.2 |
| 9 | sim2real 갭 씨앗 3개 + MJCF에 없는 요인 | O / X | | §5.6 |
| 10 | playground 배포명/import명 / 비대칭 actor-critic | O / X | | §7.1, §7.3 |

**점수**: ___ / 10

**7점 미만이면** 다음 모듈로 넘어가기 전에 §3.4 · §5.4 · §5.5를 다시 읽으세요. 이 세 절은 W3 내내 재사용됩니다.

---

## ⑫ 실행 로그 (→ `docs/progress.md`)

| 항목 | 값 |
|---|---|
| 날짜 / 모듈 | `____-__-__ (W1-M2 / simulator-bootcamp)` |
| 돌린 것 | Step 1~6 / 스크립트 4종 / 심화 ①② |
| 인스턴스 사양 | (① 표에서 옮기기) |
| 렌더 백엔드 | (③에서 옮기기) |
| **인스턴스 가동 시간** | ____ 시간 ____ 분 |
| **시간당 요금** | (제공자 공식 요금표 확인값) |
| **GPU 비용 합계** | |
| 볼륨(스토리지) 비용 | |
| **정지 시각** | |
| 가장 오래 걸린 Step | (②에서) |
| 막힌 지점 | (⑦에서 옮기기) |
| 다음으로 넘길 미해결 항목 | |

**주간 예산 상한 대비**: 정한 상한 ______ / 이번 모듈 소진 ______ / 남은 예산 ______

- [ ] [`../../../../docs/progress.md`](../../../../docs/progress.md)에 기록 완료

---

## ⑬ 최종 제출 체크

### 통과 기준 6개 (README §0.5)

- [ ] **G1** — `MUJOCO_GL` 렌더 성공, png 저장
- [ ] **G2** — `artifacts/W1-M2/`에 `01_*` 3개
- [ ] **G3** — `joints_g1.csv` 31줄, 인덱스 검증 29개 통과
- [ ] **G4** — `g1_sin_arms.mp4`를 **눈으로 봤다**
- [ ] **G5** — `04_playground_smoke.py --steps 2` → `EXIT=0`
- [ ] **G6** — `docs/progress.md` 기록 + **인스턴스 정지**

### 제출물

- [ ] 이 워크시트 ⓪~⑫ 전부 기입 (빈칸 없음 — `?`와 "모름"도 기입으로 인정)
- [ ] `../practice/g1_kinematic_tree_worksheet.excalidraw` 29노드 전부 처리
- [ ] `notes/questions-for-team.md`에 M2 질문 **4개 이상**(lesson 「팀에 물어볼 것」에서 3 + 본인 1)
- [ ] `notes/glossary.md`에 용어 5개
- [ ] `docs/progress.md`에 실행 로그
- [ ] `docs/course-plan.md`의 W1-M2 체크박스 갱신

### 🔴 마지막

- [ ] 퍼시스턴트 볼륨에 `mujoco_menagerie` · 리포 · `.venv`가 다 있다
- [ ] mp4를 로컬로 내려받았다 (인스턴스와 함께 사라집니다)
- [ ] **⚠️ 인스턴스를 정지(stop)했다** ← 이 랩의 마지막 명령
