# W1-M2 practice 의존성 — 버전 고정
# 집필 환경에서 실제로 설치·실행 검증한 버전입니다 (Python 3.12.12, 2026-08-01).
#
# ⚠️ Python 3.12 이상이 필요합니다. jax가 requires_python >= 3.12 이기 때문입니다.
# ⚠️ 01~03은 CPU만으로 완주합니다. 04(playground)도 CPU에서 스모크는 통과하지만
#    본 학습(W3-M1)에는 GPU가 필요합니다 — 맨 아래 주석 참조.

# --- 물리 엔진과 렌더링 ---
mujoco==3.11.0            # 물리 + 오프스크린 렌더러. pyopengl(3.1.10)을 함께 끌어온다
numpy==2.5.1              # mujoco가 함께 설치

# --- 영상·그림 저장 (뷰어 금지, 결과는 전부 파일로) ---
imageio==2.37.4           # mp4 저장 (기본 경로)
imageio-ffmpeg            # ffmpeg 바이너리 동봉 — 시스템에 ffmpeg가 없어도 mp4가 써진다
mediapy==1.2.7            # mp4 저장 폴백. MuJoCo 공식 예제가 쓰는 라이브러리
matplotlib==3.11.1        # 궤적·추종 오차 그래프 (Agg 백엔드 고정)

# --- RL 환경 스위트 (04에서만 사용) ---
# ⚠️ 이름 함정: PyPI 배포명은 `playground`, import 이름은 `mujoco_playground`입니다.
#    `pip install mujoco_playground`는 존재하지 않는 패키지입니다 (lesson §7.1).
#    이 한 줄이 jax(CPU 빌드) + mujoco-mjx(3.11.0)를 함께 끌어옵니다.
playground==0.2.0

# --- 노트북 동기화 (.py <-> .ipynb) ---
jupytext==1.19.5

# ─────────────────────────────────────────────────────────────────────────────
# GPU 인스턴스에서 추가로 (04의 본 학습 경로 · W3-M1)
#   pip install -U "jax[cuda12]"
#   export JAX_DEFAULT_MATMUL_PRECISION=highest    # Ampere 계열 공식 권장
# 위 requirements는 CPU JAX를 설치합니다. GPU 빌드로 덮어써야 병렬 학습이 됩니다.
#
# 로봇 모델은 pip 패키지가 아니라 git clone입니다 (약 2.3 GB):
#   git clone --depth 1 https://github.com/google-deepmind/mujoco_menagerie.git repos/mujoco_menagerie
# ─────────────────────────────────────────────────────────────────────────────
