# 팀 확인 질문 리스트

> **온보딩의 핵심 산출물.** 모듈마다 최소 1개씩 적립하고, 답을 받으면 답변란을 채웁니다.
> 여기 모인 질문이 캡스톤(W4-M5) "회사 스택 해설 문서"의 검증 질문 리스트가 됩니다.
> 회사 내부 구현은 **추측하지 않습니다.** 모르면 여기에 적습니다.

**상태 표기**: 🔴 미확인 · 🟡 부분 확인 · 🟢 확인 완료

---

## P0 — 빨리 물어볼수록 이득인 것

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 1 | 팀 표준 학습 환경은 무엇인가? (시뮬레이터 · 클러스터 · 도커 이미지) | Isaac 기반이면 4주 실습 경로를 그쪽으로 물려받는 것이 최선. **W1 중에 확인** | 🔴 | |
| 2 | 회사 파이프라인에서 SONIC 계열 WBC 정책의 **실제 입력**은 무엇인가? FSQ 토큰과 어디서 만나는가? | L3↔L2 인터페이스 = 시스템 설계의 승부처 | 🔴 | |
| 3 | FSQ 모델의 실제 구조는? (입력 차원 · 레벨 설정 · 학습 데이터 · 토큰 레이트) | W1-M5·W2-M5 실습의 방향을 결정 | 🔴 | |
| 4 | [마스터플랜 §2.3](../docs/physical-ai-4week-master-plan.md)의 추정 파이프라인 다이어그램이 실제와 맞는가? | 캡스톤 문서의 뼈대 | 🔴 | |
| 5 | 내가 담당할 영역이 좁혀졌는가? (VLA · WBC · 내비 · 통합) | 4주 이후 심화 트랙 선택의 입력 | 🔴 | |

---

## 모듈별 적립

### W1

**W1-M1 — Physical AI 개요와 산업 지형** ([lesson](../course/w1-generative-core/01-physical-ai-landscape/lesson.md))

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| M1-1 | **OpenHomie는 CC-BY-NC-SA 4.0(상업 이용 제한)인데, 별도 라이선스·상업 이용 승인을 받았는가? 아니면 논문만 참고한 자체 재구현인가?** | **법무 리스크.** 답에 따라 W3-M3에서 코드를 "그대로 쓸 수 있는가 vs 구조만 배우는가"가 갈림 | 🔴 | |
| M1-2 | GEAR-SONIC은 코드 Apache 2.0 / 가중치 NVIDIA Open Model License다. 우리는 공개 가중치를 파인튜닝하는가, SONIC 방식의 자체 학습인가? | 자체 학습이면 21,000 GPU-hours 규모 → 학습 인프라 질문이 따라붙음 | 🔴 | |
| M1-3 | 우리 G1은 어떤 구성인가 — 23 DoF 기본형 / 29 DoF EDU Plus / 43 DoF EDU Ultimate? Dex3 핸드가 있는가? | WBC 액션 벡터 차원과 텔레옵 매핑이 여기서 결정됨 | 🔴 | |
| M1-4 | L5 → L4 인터페이스의 실제 형태는? DualMap이 3D 좌표를 넘기는가, 시맨틱 맵 질의 API인가, 언어 목표를 그대로 넘기는가? | 스택 다이어그램의 최상단 경계 | 🔴 | |
| M1-5 | L4 상위 지능은 자체 VLA인가, GR00T N1.x 파인튜닝인가, 아직 미정인가? Gemini Robotics 2식 통합형 vs 우리의 L4/L3/L2 분리형 중 어느 쪽을 지향하는가? | 4주 학습의 W2 무게중심을 결정 | 🔴 | |
| M1-6 | L4 추론은 G1 온보드에서 도는가, 외부 워크스테이션인가? | 주파수 예산을 실측으로 채우려면 $\tau_{\text{infer}}$·$\tau_{\text{comm}}$ 필요 (W4-M4에서 재사용) | 🔴 | |

**W1-M2 — 시뮬레이터 부트캠프** ([lesson](../course/w1-generative-core/02-simulator-bootcamp/lesson.md))

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| M2-1 | **팀 표준 학습 환경은 무엇인가 — 시뮬레이터 · 클러스터 · 도커 이미지?** | 마스터플랜 §12가 **W1 중 확인**으로 못박은 항목. Isaac Lab + 사내 이미지가 표준이면 W3 실습 경로를 그쪽으로 옮기는 편이 낫고 GPU 요구사항(RT 코어)도 함께 바뀜. P0 1번과 같은 질문이라 **먼저 물어볼 것** | 🔴 | |
| M2-2 | **회사 G1의 정본 로봇 모델 파일은 무엇인가?** menagerie `g1_29dof_rev_1_0`을 그대로 쓰는가, 실기 구성에 맞춘 사내 MJCF/URDF가 있는가 | menagerie는 허리 3축 + 손목 3축(29 DoF), 실기 기본형은 허리 1축 + 팔 5축(23 DoF)로 **애초에 구성이 다름**. 여기서 액션 벡터 차원이 결정됨. M1-3의 확장 — DoF 개수뿐 아니라 "어느 XML이 정본인가"까지 | 🔴 | |
| M2-3 | **SDK 모터 번호 ↔ MJCF 관절 인덱스 매핑표가 사내에 있는가?** | `unitree_mujoco`는 "모터 번호가 실기와 대응"한다고만 하고 menagerie 인덱스와의 관계는 명시하지 않음. 이 표가 없으면 **sim2sim에서 관절이 뒤바뀐 채 도는 버그를 각자 다시 발견**하게 됨 | 🔴 | |
| M2-4 | **MJCF 물리 파라미터를 실기 측정값으로 보정한 사내 모델이 있는가?** `forcerange`(현재 무제한), `armature`·`frictionloss`(현재 전 관절 0.01 / 0.3), 발 접촉 형상·마찰(sphere r=5 mm, friction 0.6), 총질량(33.341 kg vs 실기 약 35 kg) | 보정 모델이 있는지 vs 도메인 랜더마이제이션으로 흡수하는 방침인지에 따라 **W3-M1의 랜더마이제이션 범위**가 달라짐 | 🔴 | |
| M2-5 | **GPU 예산과 운영 규칙은?** 주간 상한 · 스팟 사용 여부 · 퍼시스턴트 볼륨 정책 · 사내 클러스터 큐 사용법 | 부록 A 기준 이 4주에 A100/H100은 불필요하다는 점도 함께 확인할 것 (v1.2: 로컬 RTX 3080이 주력이 되며 우선순위는 낮아짐) | 🔴 | |

**W1-M3 — Diffusion 계보: DDPM → DiT** ([lesson](../course/w1-generative-core/03-diffusion-ddpm-dit/lesson.md)) — L4 액션 헤드가 실재하는가, 실재한다면 어떤 구조인가. M5-x가 L3(토큰) 쪽이라면 이쪽은 **그 위층**이다

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| M3-1 | **L4 상위 모델에 액션 헤드가 존재하는가? 있다면 DiT 계열인가, pi0처럼 언어모델 블록을 재사용하는가, 헤드 없이 FSQ 토큰을 직접 예측하는가?** | lesson §8.1의 설계 A·B·C 중 어느 배치인지가 **W2-M4 읽기 순서와 캡스톤 다이어그램을 바꾼다.** 세 갈래가 전부 열려 있는 상태로는 W2를 설계할 수 없음 | 🔴 | |
| M3-2 | **objective가 DDPM 계열인가 Flow Matching 계열인가? 추론 시 NFE는 몇이고 1회 forward는 몇 ms인가?** | 이 세 숫자로 지연 예산을 **실측으로 닫을 수 있다.** lesson §8.3 표가 3 ms/NFE 가정으로 비어 있는데, 실습 벤치로는 673.7M 헤드가 RTX 3080에서 13.2 ms라 가정보다 4배 이상 비쌌음 — 모델 크기에 따라 NFE 상한이 46에서 10으로 바뀜 | 🔴 | |
| M3-3 | **조건(관측)을 헤드에 어떻게 주입하는가 — cross-attention인가, in-context 토큰인가, adaLN인가?** | DiT 원논문 실측으로는 **이 선택 하나로 FID가 35.24에서 19.47까지 갈린다.** 하이퍼파라미터가 아니라 아키텍처 결정 | 🔴 | |
| M3-4 | **FSQ 토큰과 액션 헤드의 순서는?** 헤드가 연속 청크를 내고 FSQ가 이산화하는가, 상위가 토큰을 직접 예측하고 FSQ 디코더가 연속으로 되돌리는가 | **L3 계약의 실체.** P0 2번·M5-5의 앞단이고, 답에 따라 M5-1(토크나이저 입력)의 의미가 달라진다 | 🔴 | |
| M3-5 | **액션 청크 길이 $H$와 하위 소비 주파수 $f_2$의 실제 값은?** | lesson §5.2 표와 [W1-M1](../course/w1-generative-core/01-physical-ai-landscape/lesson.md) §4 부등식을 실수로 채우는 데 필요. M1-7의 `H_chunk` 재확인 겸 | 🔴 | |
| M3-6 | **액션 헤드 학습 데이터에 다봉 시연이 실제로 있는가?** | 단일 모드에 가깝다면 "생성 모델이어야 하는 이유"(다봉성)가 우리 데이터에서 약해지고, **회귀 헤드로 충분한지** 판단할 근거가 된다. 설계를 단순화할 수 있는 질문 | 🟡 | |

**W1-M5 — 잠재공간과 이산화: FSQ ★** ([lesson](../course/w1-generative-core/05-latent-discrete-fsq/lesson.md)) — P0 3번을 실행 가능한 수준으로 쪼갠 후속 질문

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| M5-1 | **FSQ 토크나이저의 입력은 정확히 무엇인가?** 관절 각도 벡터 / 각속도 포함 상태 / $T$스텝 궤적 청크 / 상위 모델의 latent motion 중 무엇이며, 정규화 규약(관절 range 기준? 표준화?)은 무엇인가 | W2-M5 설계 논증의 입력 차원을 결정 | 🔴 | |
| M5-2 | **시간축을 어떻게 다루는가 — 프레임당 1토큰인가, 청크당 N토큰인가? 토큰 레이트는 초당 몇 개인가?** 상위 L4가 몇 Hz로 새 토큰을 내고 하위가 그 사이를 어떻게 메우는가(마지막 토큰 홀드 / 보간 / 토큰 큐) | W1-M1의 지연 예산 부등식을 실수로 채우는 데 필요. practice `--tokens-per-chunk`로 압축률-정밀도 트레이드오프를 실측해봤으나(1→4에서 RMSE 5.88°→3.04°, 압축률 1,490배→372배) 회사가 실제로 어느 지점을 택했는지는 미확인 | 🔴 | |
| M5-3 | **레벨 구성 $\mathcal{L}$과 차원 $d$는 무엇인가?** FSQ 원논문 Table 1 권장값(`[8,5,5,5]` 등)을 그대로 쓰는가, 액션 도메인에 맞춰 재탐색했는가 | 코드북 크기를 정한 근거 확인 | 🔴 | |
| M5-4 | **토크나이저 학습 데이터는 무엇인가?** HOMIE 텔레옵 수집분 / 모캡 리타게팅 / 시뮬 롤아웃 / 혼합. 토큰 공간이 못 덮는 동작 영역(고속 동작·접촉 풍부 조작)이 알려져 있는가 | W2-M5 실습 데이터 설계 | 🔴 | |
| M5-5 | **토큰이 SONIC 계열 WBC 정책의 입력 어느 자리로 들어가는가?** 관측 벡터에 concat / 별도 조건 입력 / 연속 레퍼런스로 먼저 디코딩 후 투입 | P0 2번의 구체화. W3-M4 SONIC 정독 전에 답을 받아두면 속도가 달라짐 | 🔴 | |
| M5-6 | **토크나이저를 재학습하면 상위 모델도 함께 재학습하는가?** 토큰 ID 의미가 바뀌는 문제 관리 방식, 인터페이스 버전 존재 여부 | FSQ 계층 구조 설계 논증(worksheet ⑥)의 논거 ③ 핵심 | 🔴 | |

**W1-M4 — Flow Matching & Rectified Flow** ([lesson](../course/w1-generative-core/04-flow-matching/lesson.md)) — **M3-2("objective가 DDPM인가 FM인가")가 "FM 계열"로 답이 왔을 때 바로 이어서 물어볼 것**만 모았다. 그 답이 "DDPM 계열"이면 M4-1~M4-3은 무효가 되고 M4-4~M4-6만 남는다. M5-\*(FSQ 내부)와는 겹치지 않는다. **먼저 물어볼 순서: M4-5 · M4-6 → M4-4 → M4-1 → M4-2 · M4-3**

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| M4-5 | **액션 청크를 끝까지 실행하는가, 앞 몇 개만 쓰고 재계획하는가?** 겹치는 구간을 앙상블하는가(ACT식 temporal ensembling) | **L2 SONIC 계열 WBC가 실제로 무엇을 소비하는가의 문제라 P0 2번과 한 쌍.** M3-5가 $H$·$f_2$의 **값**이라면 이쪽은 **실행 정책**이고, lesson §8.1 "최악 반응 지연" 열의 값이 여기서 정해진다. pi0 설정(H=50 · 최대 50 Hz)을 끝까지 개방루프로 쓰면 최악 지연이 1초라 [W1-M1](../course/w1-generative-core/01-physical-ai-landscape/lesson.md) §3.3의 "자세가 무너지기까지 200 ms" 문턱 밖으로 나간다. 명령 공백을 하위가 어떻게 메우는지(마지막 토큰 홀드?)는 M5-2와 같은 질문 | 🔴 | |
| M4-6 | **설계 B(헤드가 연속 청크를 내고 FSQ가 이산화)라면 헤드가 FSQ 격자를 알고 학습되는가?** 양자화-인지 학습인가, 학습 후 반올림인가 | **L3 계약의 정밀도 문제이고 M3-4(FSQ와 헤드의 순서)의 다음 단계.** 후자면 **양자화 오차가 그대로 인터페이스 오차**가 되고 $\prod L_i$ 격자 밖 정밀도는 버려진다 — 그러면 헤드를 키우거나 NFE를 올려 얻은 정밀도가 L3에서 잘려나간다. P0 2번·M5-1·M5-3과 직결 | 🔴 | |
| M4-4 | **VLM 백본이 적분 루프 밖에 있는가?** 조건 토큰을 청크당 1회만 계산하는가, 매 스텝 다시 계산하는가 | **지연이 $\tau_{\text{VLM}}+K\tau_{\text{head}}$인가 $K(\tau_{\text{VLM}}+\tau_{\text{head}})$인가가 여기서 갈린다.** pi0 기준 300M 헤드와 3B 백본의 차이라 후자면 **lesson §8.2의 NFE 상한 표가 10배 틀린다.** M3-2(NFE·forward ms)와 M1-6(온보드인가 외부 워크스테이션인가)을 곱해야 지연 예산이 실측으로 닫힌다 | 🔴 | |
| M4-1 | **경로 스케줄과 $t$ 샘플링 분포는 무엇인가?** 직선(RF/CondOT)인가 VP 계열인가, $t$가 uniform인가 SD3식 logit-normal인가 | **M3-2가 "FM"으로 답이 오면 곧바로 이어지는 첫 질문.** lesson §5.4대로 $t$ 분포가 곧 암묵적 손실 가중이라 **재현·파인튜닝에서 가장 먼저 맞춰야 할 하이퍼파라미터**다. 모르면 SNR 대응(DDPM $t{=}259$ ↔ RF $t{=}0.5000$)을 우리 모델에 적용할 수 없고, W1-M3 실습 코드를 옮겨 재현 실험을 돌릴 수도 없다 | 🔴 | |
| M4-2 | **reflow나 증류를 적용했거나 검토했는가?** 몇 회이고 품질 손실을 무엇으로 측정했는가(성공률 · 궤적 매끄러움 · 다봉성 보존) | §6.3의 대가를 이미 지불했는지 확인하는 항목. reflow는 모델 자신의 출력을 target으로 삼아 오차를 상속하고 **큰 NFE의 품질을 팔아 작은 NFE의 품질을 사므로**, 적용했다면 "NFE를 올려도 품질이 안 오르는 구간"이 있다는 뜻이다. 실습 실측으로도 $S(Z)$ 0.9075 → 1.489e-05의 대가가 누적 학습 스텝 12,000 → 20,000 · 쌍 16,384개였다. 1-step 증류가 모드를 뭉갤 위험은 M3-6(다봉 시연이 실제로 있는가)과 함께 읽어야 판단이 선다 | 🔴 | |
| M4-3 | **ODE 적분기가 무엇이고 스텝 수는 고정인가 가변인가?** 고정 Euler · 고차 Heun · AdaFlow식 적응형 중 무엇인가 | 같은 NFE에서도 품질이 갈리는 항목이고, **가변이면 M3-2가 답하는 "NFE 몇"이 상수가 아니다** — lesson §8.2의 상한을 평균이 아니라 **최악값으로 다시 잡아야** 한다. 단봉이면 자동 1-step이 되는 AdaFlow식이라면 보행·모션 트래킹과 조작에서 예산이 달라진다 | 🔴 | |

### W2

**W2-M1 — 모방학습 기초 + ACT** ([lesson](../course/w2-policy-vla/01-imitation-learning-act/lesson.md)) — 기존 질문과 **축이 다른 것만** 모았다. 청크 실행 정책(끝까지 vs 재계획 vs 앙상블)은 `M4-5`, 청크 길이 $H$와 하위 소비 주파수 $f_2$의 **값**은 `M3-5`, G1 DoF 구성은 `M1-3`, L4 정체는 `M1-5`, 온보드 추론 여부는 `M1-6`이 이미 담당하므로 W2M1-\*는 그 질문들과 중복되지 않는다. **먼저 물어볼 순서: `W2M1-6` → `W2M1-1` → `W2M1-5` → `W2M1-3` · `W2M1-2` → `W2M1-4`** — `W2M1-6`의 답이 "있다"면 W2 전체가 재현에서 개선으로 성격이 바뀌므로 가장 먼저 물어야 하고, `W2M1-1`과 `W2M1-5`는 나머지 질문의 전제이며, `W2M1-4`는 `M1-6`(온보드 여부)과 곱해서 읽어야 의미가 생기므로 뒤로 간다

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| W2M1-6 | **ACT 또는 그에 준하는 BC baseline을 돌려본 적이 있는가?** 있다면 성공률과 실패 모드는? 특히 lesson §2.2가 설명한 "태스크 중반에 어정쩡한 자세로 멈추거나 같은 동작을 미세하게 반복하는" 분포 이탈 패턴이 관측됐는가 | **있으면 W2 전체가 재현에서 개선으로 성격이 바뀐다.** 그리고 실패 모드가 분포 이탈(compounding error)인지 다봉성(같은 상황에서 여러 정답)인지에 따라 W2-M2(Diffusion Policy)를 읽는 목적이 달라진다 — 전자면 청크 길이·데이터 커버리지 문제이고, 후자면 생성모델로 바꿀 근거가 된다. 이 4주 학습의 우선순위를 실측으로 재배치할 수 있는 유일한 질문 | 🔴 | |
| W2M1-1 | **시연 데이터를 실제로 무엇으로 수집하는가?** HOMIE 외골격 cockpit인가, 다른 텔레옵 리그인가, 모캡 리타게팅인가, 아직 수집 전인가? 지금까지 확보된 에피소드 수와 시간 규모는? | ACT는 초록 기준 **실세계 6개 태스크에서 10분 분량 시연으로 80~90% 성공률**, LeRobot 문서 기준 **50 시연**으로도 높은 성공률이 보고된다. 우리 규모가 그 스케일인지 자릿수가 다른지에 따라 W2 실습의 방향이 갈린다 — "적은 데이터에서 되게 하는" 문제인지 "많은 데이터를 관리하는" 문제인지. 데이터가 아직 없다면 W2는 공개 데이터셋 재현에 머물고, HOMIE 수집분이 있다면 W2-M2의 LeRobot 완주를 회사 데이터로 바꿔 붙일 수 있다 | 🔴 | |
| W2M1-5 | **우리 타깃 태스크의 가장 빠른 실패 모드는 무엇이고 그 시간상수는 얼마인가?** 고정 자세 조작인가, 이동 중 조작인가, 전신 균형이 걸린 동작인가 | lesson §6.2 표의 "허용 개방루프 창" 열을 채우는 값이고 **청크 길이의 상한을 정하는 물리량**이다. 표의 세 행이 각각 초 단위(고정 베이스 조작) · 수백 ms(이동 중 조작) · 200 ms 미만(전신 균형)인데, ACT 기본값 100스텝은 30 Hz에서 3,333 ms이므로 세 번째 행에서는 16배 초과다. `M4-5`가 "어떤 실행 정책을 쓰는가"를 묻는다면 이쪽은 **그 정책이 만족해야 하는 제약**을 묻는다. 답이 오면 `chunk_size`·`n_action_steps`를 우리 태스크에 맞게 재계산할 수 있다 | 🔴 | |
| W2M1-3 | **시연 데이터의 액션 벡터에 하체가 들어가는가?** HOMIE식으로 상체만 사람이 몬다면, 기록되는 $a_t$가 상체 관절만인가, 하체 RL 정책의 출력까지 함께인가, 보행 속도 명령 같은 상위 명령인가 | $D_{\text{action}}$의 정체가 여기서 결정되고 lesson §4.1 블록도의 출력 Linear 차원(`Linear(512 → D_action)`)과 디코더 쿼리가 담당하는 채널이 바뀐다. `M1-3`(G1 DoF 구성)이 하드웨어 쪽 상한을 묻는다면 이쪽은 **학습 타깃의 실제 구성**을 묻는다. 하체가 액션에 포함되면 낙상 모드가 청크 채널로 들어와 lesson §6.2 표의 "허용 개방루프 창"이 200 ms급으로 조여진다 — `W2M1-5`와 함께 읽어야 판단이 선다 | 🔴 | |
| W2M1-2 | **시연 기록 FPS와 배포 시 하위 소비 주파수 $f_2$가 같은가?** 다르면 액션을 리샘플링하는가, 청크 길이를 환산하는가 | lesson §3.6의 시간 환산이 전부 이 분모에 걸려 있다. `n_action_steps=100`이 30 Hz에서는 3,333 ms이고 50 Hz에서는 2,000 ms다. **학습 FPS와 배포 $f_2$가 어긋난 채로 청크 길이를 옮기면 개방루프 창의 실제 길이가 조용히 달라진다** — 스텝 수는 같은데 시간이 다르다. `M3-5`가 $H$·$f_2$의 **값**을 묻는 질문이라면 이쪽은 **두 값의 일치 여부와 불일치 시 처리 방식**을 묻는 별개 항목이다. **ACT/ALOHA 공개 데이터의 기록 FPS는 50 Hz로 확인됐다**(2026-08-09 labs 실행검증 — `aloha_sim_transfer_cube_human`·`aloha_mobile_cabinet` 둘 다). 그래서 이 질문은 이제 "우리 쪽 두 값이 서로 같은가"만 남았다 | 🔴 | |
| W2M1-4 | **카메라 구성은 어떻게 되는가?** 대수·해상도·손목 카메라 유무. 시각 백본은 ResNet-18급인가 사전학습 VLM인가 | lesson §4.1에서 계산했듯 카메라 2대 480×640이면 이미지 토큰이 600개인데 상태·latent 토큰은 각 1개다. **어텐션 비용의 거의 전부가 시각 특징이고 카메라 수에 선형으로 늘어난다.** 배포 지연을 줄일 첫 손잡이가 카메라 수와 해상도라서, 온보드 추론인지 외부 워크스테이션인지(`M1-6`)와 곱해서 읽어야 의미가 생긴다. 백본이 VLM이면 태스크당 정책 하나라는 ACT의 제약이 풀리고 W2-M3(VLA 계보) 쪽 이야기가 된다 | 🔴 | |

**W2-M2, Diffusion Policy** ([lesson](../course/w2-policy-vla/02-diffusion-policy/lesson.md)). 앞 모듈까지 적립된 질문과 **축이 겹치지 않는 것만** 골랐다. 청크 실행 정책은 `M4-5`, 함수 평가 횟수와 forward 시간은 `M3-2`, 액션 벡터 구성은 `W2M1-3`이 이미 담당한다. **먼저 물어볼 순서: `W2M2-1` → `W2M2-2` → `W2M2-3` → `W2M2-5` → `W2M2-4`, `W2M2-6`**. `W2M2-1`의 답이 "없다"면 이 계열을 쓸 근거 자체가 약해지므로 가장 먼저이고, `W2M2-2`는 §4.3의 위치 명령 논의가 우리 스택에 걸리는지를 가르며, `W2M2-3`은 팀과 성능 이야기를 시작하려면 전제가 되는 항목이다.

| # | 질문 | 왜 중요한가 | 상태 | 답변 |
|---|---|---|---|---|
| W2M2-1 | **우리 시연 데이터에서 다봉성을 측정해본 적이 있는가?** 같은 초기 상태를 반복 시연하는 프로토콜이 있어야 측정이 되는데 그것이 있는가 | **이 계열을 쓸 근거의 뿌리다.** lesson §2가 세운 논증은 "같은 관측에 정답이 여럿이면 제곱오차 최소해가 조건부 기댓값이라 어느 시연과도 닮지 않은 답이 나온다"이고, 다봉성이 없으면 이 문제 자체가 없어 회귀로 충분하다. `W2M1-6`이 "BC baseline의 실패 모드가 분포 이탈인가 다봉성인가"를 묻는데 이쪽은 **그 판단에 필요한 측정 프로토콜의 존재 여부**를 묻는 별개 항목이다. 반복 시연 프로토콜이 없으면 다봉성은 측정 불가이고, 그러면 W2-M2를 도입 근거가 아니라 보험으로 읽어야 한다 | 🔴 | |
| W2M2-2 | **하위 제어기가 받는 명령이 위치인가 속도인가?** HOMIE식 구성이면 상체와 하체의 명령 종류가 다를 수 있는데 그 경우 축이 어떻게 갈리는가 | 원 논문 §IV-B가 **위치 명령에서 일관되게 더 좋았다**고 보고했고 이는 대다수 행동 복제 연구가 속도 명령을 쓰는 관행과 정반대다. 저자 추정 둘 중 하나가 "위치 명령이 오차 누적의 영향을 덜 받아 액션 시퀀스 예측에 더 어울린다"이므로 **[W2-M1 §2.2]의 오차 누적 논의와 직결**된다. 우리가 속도 명령을 쓰고 있다면 논문 수치를 그대로 기대하면 안 되고, 상체와 하체가 섞여 있다면 청크 하나 안에서 두 종류가 공존한다는 뜻이라 정규화와 평가 지표를 따로 봐야 한다 | 🔴 | |
| W2M2-3 | **베이스라인 재현 레시피가 팀에 고정돼 있는가?** 어떤 라이브러리 버전에 어떤 설정을 기준으로 성능을 비교하는지 문서화돼 있는가 | lesson §6.1이 실측한 것은 **LeRobot 0.6.1 기본값이 논문 PushT 레시피에서 일곱 항목 벌어져 있다**는 사실이다(`horizon` 64 대 16, `n_action_steps` 32 대 8, `crop_shape` None 대 84, `use_group_norm` False 대 True, 사전학습 가중치 ImageNet 대 null, 카메라별 인코더 분리 True 대 False, `batch_size` 8 대 64). **아무 오버라이드 없이 돌리면 논문 재현이 아니다.** 팀이 "DP 대비 몇 퍼센트"를 말할 때 그 DP가 무엇인지 고정돼 있지 않으면 그 비교는 재현 불가능하다. 이 질문은 성능 이야기를 시작하기 전에 닫아야 하는 전제다 | 🔴 | |
| W2M2-5 | **체크포인트 선택 기준과 평가 프로토콜은 무엇인가?** 성공률의 정의, 시드 수, 보고 대상 체크포인트 | lesson §2.2가 짚은 것은 암묵 정책 계열이 **학습 손실은 매끄럽게 내려가는데 평가 성공률이 회차마다 진동해 체크포인트를 고를 근거가 사라진다**는 문제이고, 확산 정책이 비교 기준이 된 실질적 이유가 성능이 아니라 이 운용 예측 가능성이었다. 우리 쪽에서 학습 손실과 성능의 상관을 확인했는지, 마지막 체크포인트를 쓰는지 최고점을 쓰는지에 따라 보고되는 숫자의 의미가 달라진다. `W2M2-3`과 함께 읽어야 비교가 성립한다 | 🔴 | |
| W2M2-4 | **관측 프레임 2장이 우리 태스크에 충분한가?** 관측 구간 길이를 정한 근거가 있는가 | $T_o = 2$는 논문과 라이브러리가 일치하는 몇 안 되는 항목이라 **드리프트 목록에 안 잡히지만 그렇다고 검증된 값은 아니다.** 프레임 2장은 위치와 그 차분, 곧 속도까지만 준다. 이동 중 조작처럼 가속도나 접촉 상태가 걸리면 부족할 수 있고, 반대로 늘리면 조건 벡터 차원이 프레임 수에 비례해 커진다(lesson §5.1의 `global_cond_dim × n_obs_steps`). `W2M1-5`가 묻는 "가장 빠른 실패 모드의 시간상수"와 곱해서 읽어야 판단이 선다 | 🔴 | |
| W2M2-6 | **학습과 배포의 카메라 해상도가 같은가?** 다르다면 리사이즈를 어디서 하고 그 통계가 정규화에 반영되는가 | 공개 데이터셋은 lesson §6.2대로 학습과 평가가 **둘 다 96×96으로 어긋나지 않았다.** `PushtEnv`의 `observation_height/width`가 384라 충돌해 보이지만 그 두 필드는 환경에 전달되지 않고 384는 롤아웃 mp4 해상도일 뿐이다. **우리 쪽에서 이 대응이 깨지면 조용히 성능만 떨어진다.** 시각 정규화가 `MEAN_STD`이므로 리사이즈 위치가 통계를 바꾸고, 공간 소프트맥스 출력은 해상도와 무관해 **차원 오류가 나지 않아 더 안 잡힌다**. `W2M1-4`가 카메라 대수와 백본 종류를 묻는다면 이쪽은 해상도 일치 여부를 묻는 별개 축이다 | 🔴 | |

### W3

### W4

---

## 답을 받은 질문 (아카이브)

_(아직 없음)_
