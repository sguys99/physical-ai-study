# 교육자료 집필 규약 2차 개정 + W1-M1 시범 재작성

> **이 문서의 역할**: 2026-08-25 학습자 2차 피드백에 대응하는 개선 작업의 실행 계획이자 진행 추적표입니다.
> 작업이 진행되면 아래 체크박스를 `[x]`로 갱신합니다.
> 개정이 끝나면 규약 본문은 [`course-plan.md`](course-plan.md)에 남고 이 문서는 근거 기록으로 보존됩니다.

**진행 상태**: **Phase 8 진행 중** (2026-09-01 착수). Phase 0부터 7까지 닫혔고 **학습자 검토(6-5)가 「읽었고 괜찮다, 그대로 확산」으로 회신**돼 확산 착수 조건이 충족됐습니다. 이번 회차의 범위는 **W1-M2부터 W1-M5까지 넷**이고 W2-M1과 W2-M2는 다음 회차로 미룹니다

---

## 1. 왜 이 작업을 하는가

2026-08-09에 [`course-plan.md`](course-plan.md) §9.11로 한 차례 개정이 있었습니다. 학습자가 "체계가 없다, 너무 어렵다, 용어 설명이 부족하다"고 지적했고, 진단 결과 목차 부재와 용어 정의 규칙 부재와 표 과적용이 원인으로 확인돼 §3.7(문서 구조), §3.8(용어 도입), §3.9(이해 사다리), §3.10(문장부호)을 신설했습니다. W1-M1은 그 새 규약으로 **이미 재집필됐고 lint PASS 21, FAIL 0**입니다.

**그런데 같은 학습자가 같은 불만을 다시 제기했습니다.** 1차 개정이 잘못된 층을 고쳤다는 뜻입니다. 1차는 "정보를 어떻게 배열할까"를 고쳤고, 이번 불만은 "설명 자체가 독자를 데려가지 않는다"입니다.

### 학습자가 지적한 것 (2026-08-25)

1. 설명이 친절하지 않고 연결성이 부족하다
2. 경력을 너무 고려해 빗대어 설명하는데 그 비유가 이해되지 않는다
3. 너무 어렵다

요청은 "주니어에게 설명한다고 생각하고 상세하고 친절하고 차근차근" 쓰라는 것이고, 새 용어는 반드시 설명하고 다이어그램과 이미지를 적극 활용하라는 것입니다.

---

## 2. 진단으로 확인된 것

재집필본 `course/w1-generative-core/01-physical-ai-landscape/lesson.md`(801줄, 32,893자)를 전수 조사한 결과입니다.

### 2.1 비유가 문서 아키텍처에 하드코딩돼 있다

절 시작 용어표 7개가 전부 `| 용어 | 한 줄 정의 | 제어·LLM 비유 |` 3열 구조입니다. 비유가 보조 장치가 아니라 문서 골격의 일부입니다.

| 항목 | 수 |
|---|---|
| 용어표의 비유 셀 | 36 |
| 산문 내 명시적 비유 | 14 |
| **총 비유 지점** | **50** |
| 앵커 개념을 처음부터 설명한 것 | **3** |
| 배경 지식을 가정한 것 (산문 기준) | 11 / 14 (79%) |

문서 자신이 L55에서 "피드백 제어 루프와 대역폭은 보유 배경으로 가정"이라 선언합니다. 즉 버그가 아니라 **설계 전제가 틀린 것**입니다.

대표 사례입니다. L78 `캐스케이드 제어, 안쪽을 바깥보다 5~10배 빠르게 돌리는 그 관행`은 "그 관행"이라는 지시어로 독자가 이미 안다고 전제합니다. L89 `미분방정식 자리에 30억 파라미터 트랜스포머가 들어앉았습니다`는 플래너, 서보 루프, setpoint, 트랜스포머 네 개념을 한 문장에 미정의로 병치합니다. L657은 한 문장에 플랜트, 발산, 도메인 랜더마이제이션, 강인제어, 불확실성 집합 다섯 개를 미정의로 씁니다.

### 2.2 연결 조직이 없다

H2 일곱 개(L73, L119, L159, L324, L423, L476, L561)가 **전부 도입 산문 0자**이고 제목 바로 다음이 용어표입니다. "왜 이 절이 앞 절 다음에 오는가"를 말하는 문장이 문서에 한 곳도 없습니다. 산문 0자 섹션은 §6.1(41줄 Mermaid 계보도)과 §7.2를 포함해 아홉 곳입니다.

잘 된 연결 문장은 L467, L209, L302 세 곳뿐이고, 이 셋을 40여 개 전환 지점 전체로 확장하는 것이 처방입니다.

### 2.3 논거의 뼈대가 미정의다

| 용어 | 첫 등장 | 정의 위치 | 격차 |
|---|---|---|---|
| `FSQ` | L101 | L566 | **465줄** |
| `Flow Matching` | L100 | L480 | 380줄 |
| `latent` | L180 | 없음 | 문서 전체에서 미정의 |
| `다중 모드` | L305 | 없음 | §3.5 4대 근거의 핵심어 |
| `동특성 간섭` | L279 | 없음 | 대역폭 분리 논리의 전제 |
| `공백률` | L381 | 없음 | 산식 없이 숫자만 |

`DiT`, `DCT`, `ACT`, `Diffusion Policy`, `setpoint`, `롤아웃`, `sim2sim`, `VQ-VAE`도 정의가 없습니다. 48줄짜리 ASCII 블록도(L211~258)는 `RGB-D`, `LiDAR`, `오도메트리`, `grounding`, `IMU`, `RL`을 다이어그램 라벨로 처음 도입합니다.

### 2.4 독자를 도와주는 장치가 없다

- **워크드 예제 0개.** 계산은 전부 `practice/*.py`로 외주됩니다
- **이미지 파일 0개.** `img/` 폴더가 비어 있고 시각자료는 전부 Mermaid, 표, ASCII입니다
- **격려 문장 0회**, 독자 호명 3회
- **문체가 갈립니다.** 본문은 합니다체, 📌 박스 8개는 음슴체, 퀴즈는 명령형입니다

### 2.5 분량 신고가 사실과 다르다

frontmatter는 `est_reading_min: 39`인데 본문 L64는 "이론 3h"이고, 표 141행과 다이어그램 8개와 상호참조 185개를 포함한 초독 실측 추정은 55~75분입니다. **4.6배 불일치**입니다.

### 2.6 규약 자체가 이미 어긋나 있다

`scripts/lint-lesson.sh`의 분량 밴드와 집필 도구 3종의 밴드가 다릅니다. 2026-08-10 밴드 상향이 스크립트에만 반영됐습니다.

| 출처 | Tier A | Tier B | Tier C |
|---|---|---|---|
| `scripts/lint-lesson.sh` | 15,000~21,000 | 13,000~18,000 | 12,000~17,000 |
| `SKILL.md:177`, `lesson-template.md:4`, `authoring-checklist.md:96` | 12,000~18,000 | 10,000~15,000 | 9,000~13,000 |

### 2.7 렌더 버그

`$13,500` 표기 세 곳(L147, L619, L776)이 inline math로 잘못 해석됩니다. 데이터 피라미드 ASCII(L435~447)는 한글 전각 폭 때문에 우측 테두리가 어긋납니다. 청크 커버리지 다이어그램(L356~379)은 Mermaid `gantt`를 오용했고 색 설명이 실제 렌더와 다릅니다.

### 2.8 시각자료 판정

| 판정 | 개수 | 해당 |
|---|---|---|
| 설명에 기여 | 2 / 8 | sequenceDiagram(L394), 회사 배치도(L573) |
| 핵심이나 과부하 | 2 / 8 | 48줄 ASCII 블록도, 정렬 깨진 피라미드 |
| 장식, 중복, 오용 | 4 / 8 | 모듈 지도 완전 중복 2개, gantt 오용, 산문 0자 계보도 |

---

## 3. 확정된 방향

사용자와 8개 항목을 확정했습니다.

| 항목 | 결정 |
|---|---|
| 학습자 전제 | **ML, LLM, Agent 기본만 안다고 가정.** 제어, MPC, 상태공간, BPE, autoregressive는 모른다 |
| 비유 | **보조로 강등.** 본문은 비유 없이 완결되고 비유는 접이식 박스로 부가 |
| 분량 | **3층 문서로 분리.** `eli5.md`, `lesson.md`, `deep-dive.md` |
| 시각자료 | **Mermaid 강화 + SVG 생성.** 개념 그림은 `img/`에 SVG로 |
| 적용 범위 | **규약 개정과 W1-M1 시범 재작성까지.** 나머지 6개는 검토 후 |
| 수식 | **숫자 대입 예제 필수.** 모든 기호는 그 자리에서 정의 |
| 절 시작 용어표 | **폐지.** 본문 안 인라인 정의로 바꾸고 되짚기 표는 절 끝에 |
| 마스터플랜 | **필요한 부분 수정 승인** (커리큘럼 내용은 불변) |

---

## 4. 새 규약의 핵심

### 4.1 이해 사다리를 폐기하고 단일 전제로 교체 (§3.9 전면 재작성)

기존 4행 표(제어는 앵커, ML/DL은 압축, LLM 내부는 가정 금지, 로봇은 기초부터)를 두 줄 전제로 바꿉니다.

**안다고 가정하는 것**

- ML/DL 기본 (학습, 손실, 신경망, 과적합)
- LLM 기본 (프롬프트, 토큰이라는 말, 파인튜닝)
- Agent와 RAG 응용 실무

**모른다고 가정하는 것**

- 그 밖의 전부입니다
- 제어공학 (MPC, 상태공간, 캐스케이드 제어, PD 서보, 강인제어)
- 생성모델 내부 기제 (BPE, autoregressive, codebook, ELBO, KV 캐시)
- 로봇 전 영역 (시뮬레이터, VLA, WBC, SLAM, 미들웨어)
- 그리고 이 모듈이 다루는 개념 자체

수반 규칙입니다.

- **제어 개념을 설명의 근거로 쓸 수 없습니다.** 캐스케이드 제어, 대역폭 분리, MPC의 예측 지평은 이제 가르쳐야 할 대상이지 앵커가 아닙니다
- **"당신은 이미 안다" 류 문장 금지.** `lesson.md:85`의 헤딩 "당신은 이미 이 구조를 알고 있다"가 대표 사례입니다
- **설명 회피 문장 금지**는 유지하되 탐지 문구를 확장합니다
- `prereq`와 「선수 지식」은 새 전제와 일치해야 합니다

### 4.2 비유 강등

- **본문은 비유 없이 완결돼야 합니다.** 비유를 전부 지워도 이해에 구멍이 없어야 합니다
- 비유는 본문 뒤에 접이식 박스로 답니다

```markdown
<details>
<summary>💡 제어를 아신다면 (몰라도 됩니다)</summary>

캐스케이드 제어는 느린 바깥 루프가 목표를 정해 빠른 안쪽 루프에 넘기는 구조입니다.
안쪽을 바깥보다 5배에서 10배 빠르게 돌립니다. 여기 5계층이 같은 구조입니다.
</details>
```

- **박스 안에서도 앵커 개념을 한 문단으로 재설명합니다.** 지금은 "그 관행"처럼 지시어로 넘어갑니다
- 용어표의 `제어·LLM 비유` 열을 삭제합니다

### 4.3 3층 문서

| 파일 | 분량 | 성격 | 읽는 순서 |
|---|---|---|---|
| `eli5.md` | 산문 3,000~5,000자 (8~12분) | 그림 위주, 수식 0, 영문 약어 최소, arXiv 인용 0 | 1번 |
| `lesson.md` | 티어 밴드 | 본문. 개념을 처음부터 차근차근 | 2번 |
| `deep-dive.md` | 상한 없음 | 수식 전체 유도, 계보 세부, 출처 검증 메모 | 3번 (선택) |

`eli5.md` 골격입니다.

```
# [제목] 쉽게 먼저
> 한 문장 요약 (가장 평이한 문장)
## 이 모듈이 답하는 질문 하나
## 어떤 문제가 있었나          (이야기, 그림 1장)
## 그래서 무엇을 하나           (그림 2~3장, 각 그림에 "읽는 법" 캡션)
## 오늘 만날 개념 5개           (각 3문장, 수식 없이)
## 이제 lesson.md로             (본문이 무엇을 더 주는지)
```

`eli5.md`는 요약이 아니라 **다른 층위**입니다. 같은 내용을 짧게 쓴 것이면 읽기 부담만 늘어납니다.

### 4.4 절 구조 개정 (§3.7)

절 시작 용어표를 폐지하고 도입 산문을 필수로 만듭니다.

```
## N. [제목]

[도입 산문 3~5문장]                       ★필수, 표와 그림과 코드보다 먼저
  1. 앞 절에서 무엇을 봤는지 한 문장
  2. 그래서 무엇이 아직 안 풀렸는지 한 문장
  3. 이 절에서 무엇에 답할지 한 문장

### N.1 [소주제]
  ① 쉬운 말로 개념                        (용어는 첫 등장 자리에서 2~3문장 정의)
  ② 그림                                  (읽는 법 캡션 필수)
  ③ 구체 예시와 숫자
  ④ (필요하면) 수식과 숫자 대입 손계산      ★수식만 두고 넘어가지 않습니다
  ⑤ <details> 💡 비유 박스                (선택)

> ⚠️ **여기서 많이 헷갈립니다**            (선택, 오해 예방)

| 용어 | 이 절에서 나온 뜻 |                 ← 되짚기 표, 절 끝
> 📌 **여기까지 정리** (3줄)
```

기존 3부 구조(절 시작 용어표, 본문, 📌)가 4부 구조(도입 산문, 본문, 되짚기 표, 📌)가 됩니다. `notes/glossary.md` 롤업은 되짚기 표가 이어받습니다.

### 4.5 용어 도입 개정 (§3.8)

- **첫 등장 자리에서 2~3문장으로 풀어 씁니다.** 한 줄 정의는 되짚기 표에만 둡니다
- **역참조 금지를 FAIL로 승격합니다.** 정의보다 먼저 쓰면 정의를 앞으로 옮기거나 사용을 미룹니다. `FSQ` 465줄 격차 같은 것이 대상입니다
- **다이어그램 라벨에서 용어를 처음 소개하지 않습니다**
- 영문 약어는 최초 1회 전개를 유지하고, 전개 이후에도 첫 3회는 한국어 병기를 권장합니다

### 4.6 시각자료 개정 (§3.2)

- **모든 그림에 "읽는 법" 캡션 필수.** 그림 아래 1~3문장으로 무엇을 보라고 지시합니다
- **개념 설명 그림은 SVG로 만들어 `img/{module-id}-{slug}.svg`에 저장**하고 `../../../img/`로 참조합니다. `diagram-design` 스킬을 씁니다. Mermaid는 구조와 흐름과 계보에 계속 씁니다
- **큰 ASCII 블록도는 조각내 산문 사이에 배치합니다.** 한 블록 20줄 상한
- **한글 폭 정렬이 필요한 ASCII 그림은 만들지 않습니다**
- 「한 장 정리」의 모듈 지도 재게시는 §0과 완전 동일 복제를 금지하고 진행 표시나 요약 라벨을 더합니다

### 4.7 분량과 독해 시간 (§4)

- **스크립트와 문서의 밴드 불일치를 스크립트 쪽으로 통일합니다.** A 15,000~21,000, B 13,000~18,000, C 12,000~17,000
- **밴드는 이번에도 잠정입니다.** W1-M1 시범 결과로 1회 교정합니다. §9.11이 파일럿으로 밴드를 교정한 것과 같은 절차입니다
- **`est_reading_min`의 정의를 바꿉니다.** 지금은 산문만 세는 회귀식 값이라 실측의 절반입니다. 표와 그림과 수식과 퀴즈를 포함한 초독 시간으로 재정의하고, 회귀식 값은 밴드 비교용으로 lint 정보줄에만 남깁니다. §0의 「소요」와 반드시 정합시킵니다

### 4.8 문체 통일

본문과 📌 박스와 「한 장 정리」 표와 퀴즈를 전부 합니다체로 통일합니다. 줄표와 가운뎃점 금지(§3.10)는 그대로 유지합니다.

### 4.9 추가 보강

학습자 요청에 없었지만 교육 효과를 위해 더합니다.

- **워크드 예제 필수.** 절마다 최소 1개, 숫자를 실제로 대입해 손으로 따라가는 계산입니다. 현재 0개입니다
- **「여기서 많이 헷갈립니다」 박스 신설.** 「흔한 오해」는 문서 끝에 몰려 있어 정작 헷갈리는 자리에서 도움이 되지 않습니다
- **퀴즈를 3단 난이도로.** 지금 10문항이 전부 서술형 고난도입니다. 기억 확인 4, 적용 4, 종합 2로 나누고 앞 4개는 한 줄 답으로 풀리게 합니다
- **자기 설명 프롬프트.** 「한 장 정리」에 능동 회상 장치를 1개 둡니다
- **렌더 버그 수정.** `$13,500` 세 곳을 이스케이프합니다

---

## 5. 단계별 실행 계획

### Phase 0. 근거 기록과 계획서 이관

- [x] 이 계획서를 `docs/course-author-update-plan.md`로 기록
- [x] `docs/course-plan.md`에 **§9.19 신설**. 위 §2 진단 결과를 §9.11과 같은 형식으로 기록
- [x] §9.19에 **"1차 개정이 왜 못 잡았는가"** 를 명시. 1차는 배열을 고쳤고 2차는 설명 방식을 고칩니다

### Phase 1. 규약 SSOT 개정 (`docs/course-plan.md`)

- [x] **§3.9 전면 재작성.** 이해 사다리 4행 표를 삭제하고 §4.1(단일 전제)과 §4.2(비유 강등)로 교체
- [x] **§3.7 개정.** 절 4부 구조와 절 안 순서(개념, 그림, 예시, 수식, 비유 박스)
- [x] **§3.8 개정.** 절 시작 용어표 폐지, 첫 등장 2~3문장 정의, 역참조 FAIL 승격, 다이어그램 라벨 첫 소개 금지
- [x] **§3.2 개정.** 그림 캡션 필수, SVG 생성 경로, ASCII 20줄 상한, 한글 폭 정렬 그림 금지, 모듈 지도 재게시 차별화
- [x] **§4 개정.** 밴드를 스크립트 값으로 통일, `est_reading_min` 재정의, 잠정 표시
- [x] **§2 개정.** 산출물에 `eli5.md`를 필수로 추가하고 §2.1에 3층 구조 규정
- [x] **§1 절차표 개정.** Phase 3 산출물에 `eli5.md` 반영
- [x] **§3.11 신설.** 문체 통일, 워크드 예제 필수, 헷갈림 박스, 퀴즈 3단 난이도, 자기 설명 프롬프트

### Phase 2. 상위 문서 정합

- [x] `CLAUDE.md` **사용자 프로필 절 재작성.** "제어·ML 영역은 기초 생략하고 바로 심화" 문장 삭제
- [x] `CLAUDE.md` **작성 표준 절 갱신.** 비유 강등, 3층 문서, 그림 규약 반영
- [x] `docs/physical-ai-4week-master-plan.md` **§3 갭 분석 개정.** 보유 역량 레버리지 서술을 자료 난이도 전제와 분리. **커리큘럼 내용(모듈 구성, 논문 목록, 일정)은 손대지 않았습니다**
- [x] 마스터플랜 **§11.1 프롬프트 템플릿 개정.** 요구사항 8번(난이도)을 새 전제로 교체, 3층 문서와 비유 강등 반영
- [x] 마스터플랜 하단에 **v1.4 개정 이력** 추가

### Phase 3. 집필 도구 개정

- [x] `.claude/agents/course/pai-agent.md` (2026-08-26 완료, 223줄 → 300줄)
  - [x] 「학습자 프로필」과 「깊이 조절: 이해 사다리」를 새 전제로 교체
  - [x] 「비유를 적극 사용」 문단을 **「비유는 보조다」** 로 반전
  - [x] 산출물에 `eli5.md` 추가, 절 4부 구조 반영
  - [x] **분량 밴드 정정** (A 15,000~21,000, B 13,000~18,000, C 12,000~17,000)
  - [x] *(추가)* frontmatter `description` 개정. 「제어공학·LLM 비유 기반」과 산출물 목록의 `eli5.md` 누락을 정정
  - [x] *(추가)* §3.11 「독자를 데려가는 장치」 절 신설. 이 규약이 파일에 아예 없었습니다
  - [x] *(추가)* 시각자료 표에 SVG 행과 「그림 규약」 6개 추가, 이미지 경로 `.png` → `.svg`
  - [x] *(추가)* `est_reading_min` 초독 시간 재정의와 추정 기준 7종 반영
  - [x] *(방식 변경)* 밴드는 **포인터 + 정본 표시**로 실었습니다. 표는 편의용 사본이고 정본은 `scripts/lint-lesson.sh`와 §4 두 곳이며 어긋나면 lint가 이긴다고 명시했습니다. 숫자 중복이 3중 불일치를 두 번 재발시킨 원인이라 §4 박스의 선언을 따랐습니다
- [x] `.claude/skills/pai-course-author/SKILL.md` (2026-08-26 완료, 288줄 → 349줄)
  - [x] Phase 1 컨텍스트 로드에 §3.11 추가. §2.1과 `eli5-template.md`도 함께, 「§3.9 이해 사다리」 명칭을 「학습자 전제와 비유」로 정정
  - [x] Phase 2.5 재집필 블록에 **2차 피드백 실패 목록 9개** 추가 (§9.11 목록 8개와 병기)
  - [x] Phase 3 페이로드의 학습자 프로필 줄과 밴드 주석 정정, `eli5.md` 산출물 추가. 「강조해 전달할 것」 목록 11개로 전면 교체
  - [x] Phase 3에 **SVG 생성 단계** 추가 (`Phase 3-1` 신설, `diagram-design` 호출 지점과 검수 절차)
  - [x] Phase 7 롤업 소스를 절 시작 용어표에서 절 끝 되짚기 표로 변경. `notes/glossary.md`가 4열이라 `비유 / 메모` 열은 빈 칸 표시로 채우게 규정
  - [x] *(추가)* frontmatter `description`에 `eli5.md` 반영, 트리거에 "eli5만 다시" 추가
  - [x] *(추가)* Phase 4 육안 목록에서 폐기된 두 항목(절 안 순서, 앵커 재진술) 교체. `eli5.md`, 워크드 예제, 문체, `est_reading_min` 정합 검사 추가
  - [x] *(추가)* Phase 2.5 부수 갱신의 `est_reading_min`을 초독 시간 정의로 교체
  - [x] *(추가)* Phase 2.5 「기준 사례」(W1-M1 커밋 `1b76a65`)에 경고. **1차 개정 결과물이고 그것이 2차 불만을 받았으므로 절 구조와 용어 도입 방식은 따라 하지 말 것**. Phase 5 산출물로 교체 예정
  - [x] *(추가)* 「부분 재실행」 표에 "eli5만 다시", "그림만 다시" 행 추가
- [x] `.claude/skills/pai-course-author/references/lesson-template.md` **골격 전면 개정** (2026-08-26 완료, 293줄 → 451줄)
  - [x] 절 골격을 3부에서 **4부 구조**로 교체. 절 시작 용어표 자리에 도입 산문, 절 끝에 되짚기 표 2열
  - [x] 본문 순서를 개념, 그림, 예시와 숫자, 수식과 손계산, 비유 박스로. `- **제어 대응**:` 줄 삭제
  - [x] 워크드 예제 자리, `<details>` 비유 박스 자리, 헷갈림 박스 자리, 자기 설명 프롬프트 자리 신설
  - [x] 퀴즈를 3단 난이도 골격으로 재편, 모든 그림에 「읽는 법」 캡션 자리
  - [x] 「난이도」 소절을 「학습자 전제」로 교체하고 「비유는 보조입니다」와 「독자를 데려가는 장치」 신설
  - [x] 밴드는 포인터 + 정본 표시, 이미지 경로 `.svg`, `est_reading_min` 초독 시간 주석
  - [x] *(추가)* 한 줄 요약 아래 `eli5.md` 되돌이 링크 한 줄. §2.1의 읽는 순서를 본문 쪽에서도 안내
  - [x] *(정정)* **부록성 절(「흔한 오해」 이후)의 번호를 걷어냈습니다.** `lint-lesson.sh`가 `^## N.`로 본문 절과 부록 절을 가르고 번호 붙은 절마다 `📌`를 요구하는데, 구 템플릿은 출처까지 번호를 붙여 `📌` 6개 부족으로 FAIL이 났을 구조였습니다. §3.7 골격과 W1-M1 실물이 둘 다 무번호입니다
- [x] `.claude/skills/pai-course-author/references/eli5-template.md` **신규 작성** (2026-08-26 완료, 216줄)
  - [x] §2.1 골격 다섯 절을 그대로 쓰고 절마다 채워 넣을 자리와 지시 주석을 붙임
  - [x] 규격 명시: 산문 3,000~5,000자 독립 밴드, 수식 0, arXiv 0, 영문 약어 최소, frontmatter 없음
  - [x] 「요약이면 실패」를 머리말과 「작성 시 반드시」 양쪽에 못박고, **단순화한 자리마다 "정확히는 lesson.md §N에서" 링크**를 의무화
  - [x] 표를 쓰지 않는 것을 기본으로 규정. 표를 쓰고 싶어지면 요약을 하고 있다는 신호
  - [x] 비유 규정을 eli5용으로 분기. **전문 개념 앵커 비유는 금지, 일상 사물 비유는 본문에 허용**
  - [x] *(추가)* `lesson.md` 참조에 **절 번호와 절 제목 병기**를 의무화. 재집필로 번호가 밀려도 하류 참조가 조용히 깨지지 않게 하는 장치입니다
- [x] `.claude/skills/pai-course-author/references/authoring-checklist.md` C4 재작성과 신규 항목 추가와 밴드 정정 (2026-08-26 완료, 158줄 → 271줄)
  - [x] **C4를 「이해 사다리」에서 「학습자 전제와 비유」로 전면 재작성.** 기호는 그대로 두고 9항목 신설. 금지 문구 탐지 `grep -nE` 포함
  - [x] **Z군 신설.** `eli5.md` 13항목과 산문 카운터 명령. A(frontmatter) 앞에 두어 읽는 순서와 맞췄습니다
  - [x] **C6군 신설.** §3.11 독자를 데려가는 장치 9항목
  - [x] C2를 4부 구조와 도입 산문과 새 절 안 순서로 교체. **부록성 절에 번호를 붙이면 `📌` 부족 FAIL이 난다**는 것을 명시
  - [x] C3의 절 시작 용어표 3열 항목을 되짚기 표 2열로 교체, 역참조 FAIL 승격, 다이어그램 라벨 항목 추가
  - [x] C에 SVG와 「그림 규약」 5항목 추가, B의 이미지 경로 `.svg`와 GitHub 렌더·다크모드 확인 추가
  - [x] D의 밴드를 새 값으로 정정하고 정본 표시 병기, A에 `est_reading_min` 초독 시간과 §0 정합 항목 추가
  - [x] *(추가)* 머리말에 **lint 미개정 경고 2건**. 특히 C군이 폐지된 절 시작 용어표를 찾고 있어 그 판정을 믿으면 안 되고, **FAIL 메시지를 따라가면 폐지된 3열 표가 되살아난다**는 함정을 명시
  - [x] *(추가)* 「통과 후」에 신규는 Phase 5로, 재집필은 Phase 4에서 멈춘다는 갈림 추가

**Phase 3 부수 정합** (계획서에 없던 항목, 2026-08-26)

- [x] `docs/course-plan.md` **§4 도입문 정정.** Phase 1이 §3.9를 전면 재작성하면서 §4 첫 문장 「제어와 ML과 LLM은 기초를 생략하고 바로 심화로, 로봇 시뮬과 SLAM은 기초부터」를 놓쳤습니다. 폐기된 「이해 사다리」가 그대로 남아 있었고, Phase 3의 도구 5종이 전부 밴드를 위해 §4를 가리키므로 그 자리에서 구 전제를 다시 읽게 되는 구조였습니다
- [ ] **미해결로 남긴 것**: §4 티어표의 Tier C 근거 셀이 아직 「DDPM/FM/DiT는 복습」, 「RSSM은 학습된 상태공간 모델이므로 제어 배경이면 즉시 이해됨」입니다. 같은 구 전제이지만 **티어 재배정은 20개 모듈 전체에 걸리고 §9.15가 이미 제안만 남기고 승인을 기다리는 상태**라 손대지 않았습니다. 승인이 나면 그때 함께 고칩니다

### Phase 4. 검사 스크립트 개정 (`scripts/lint-lesson.sh`)

기계로 잡히는 것만 올립니다. 기존 검사군은 유지하고 아래를 더합니다.

> 🗂 **작업 단위와 순서** (2026-08-27 확정)
>
> 검사군 단위로 묶어 subagent 하나가 한 군을 통째로 고치고, 군마다 이 계획서 갱신과 커밋과 푸시를 거쳐 다음 군으로 넘어갑니다. 인접한 체크박스가 같은 코드 영역을 만지므로 체크박스 단위로 쪼개면 같은 자리를 두 번 고치게 됩니다.
>
> 순서는 **B → C → D → E → F와 A → I → H → 실측 기록 → 하류 문서 정합** 9단위입니다. 9단위는 2026-08-27에 추가됐습니다.

> ➕ **H군과 A군 정합 검사는 원래 계획에 없던 추가분입니다** (2026-08-27 결정)
>
> §3.11(독자를 데려가는 장치)이 `authoring-checklist.md`에는 C6군으로 들어갔는데 lint에만 빠져 있었습니다. 워크드 예제 0개와 음슴체 혼용이 2차 피드백의 진단 항목이었으므로 육안 검사에만 맡기지 않습니다.
>
> `est_reading_min`과 §0 「소요」의 정합은 §4가 「반드시 정합해야 합니다」라고 못박은 것인데 검사하는 곳이 없었습니다. W1-M1의 4.6배 불일치가 그렇게 통과했습니다.

- [x] **1. B군 추가** (2026-08-27 완료). H2 다음 첫 비어있지 않은 줄이 표나 코드펜스나 Mermaid면 FAIL (도입 산문 누락). W1-M1이 예측대로 7건
  - [x] *(추가)* 공용 헬퍼 `h2_blocks()` 신설. 코드펜스 인식 H2 블록 분할기이고 C군과 H군이 재사용합니다
  - [x] *(추가)* 불릿과 인용과 소제목으로 시작하는 절은 WARN. 표만큼 나쁘지는 않지만 도입 산문은 아닙니다
  - [x] *(추가)* 파일 머리 주석 SSOT 동기화. §3.9를 「학습자 전제와 비유」로 고치고 §2.1과 §3.11을 목록에 넣고 근거에 §9.19 추가
  - [x] 실측: `--all` 7개 파일 위반 7·6·8·9·6·6·7건, 전부 원인이 「제목 다음 곧바로 용어표」. C군이 용어표를 되짚기 표로 옮기면 같이 해소됩니다
  - [x] **이월분 해소** (8단위). 기존 📌 검사의 `body_h2`가 `grep`이라 코드펜스를 인식하지 못했습니다. 8단위에서 `h2_blocks()` 기반으로 통일했고 회귀 diff는 0입니다
- [x] **2. C군 개정과 추가** (2026-08-27 완료)
  - [x] 절 시작 용어표 존재 검사를 절 끝 되짚기 표 검사로 교체. 되짚기 표 0개면 FAIL, `| 용어 |` 표가 3열 이상이면 폐지된 절 시작 용어표로 보고 FAIL, `📌`보다 뒤에 있거나 사이에 소제목이 끼면 WARN
  - [x] 약어 전개 검사를 순서 인식으로. 첫 등장 줄에 전개가 없으면 FAIL이고 격차 줄 수를 보여줍니다. frontmatter와 코드펜스는 제외
  - [x] *(추가)* 약어 목록에 `FSQ ACT DiT DCT IMU VQ-VAE` 6개 추가. §3.8이 든 대표 사례 `FSQ`가 목록에 없었습니다. 뺀 항목은 없습니다
  - [x] *(추가)* 헬퍼 `term_table_rows()`와 `abbr_scan()` 신설
  - [x] 실측: **W1-M1의 `FSQ` 격차가 465줄로 정확히 잡힙니다.** §3.8 기록과 일치합니다. 7개 파일 전부 되짚기 표 0개이고 폐지된 3열 용어표가 합계 49개, 약어 역참조는 4개 파일에서 8건
  - [x] 발견: **3열 용어표가 역참조를 만들어내고 있었습니다.** W1-M3의 `DiT`는 첫 등장이 본문 L16이고 전개가 L365 용어표 셀입니다. 「표 셀에서 용어를 처음 소개하지 않는다」와 「역참조 금지」가 같은 뿌리입니다
- [x] **3. D군 추가** (2026-08-27 완료). 기존 검사 4개(Mermaid 개수, 표 대 불릿, 표 10행, 셀 120자)는 그대로 두고 5개를 더했습니다
  - [x] Mermaid나 이미지 뒤 3줄 안에 산문 캡션이 없으면 WARN
  - [x] ASCII 코드펜스 20줄 초과면 WARN. 대상은 무태그와 `text`와 `plain` 펜스뿐이고 `python` `bash` 같은 태그가 붙으면 코드지 블록도가 아니므로 제외합니다
  - [x] *(추가)* Mermaid `gantt` 오용은 FAIL. §2.7이 기록한 렌더 버그입니다
  - [x] *(추가)* §0과 「한 장 정리」의 첫 Mermaid가 완전 동일 복제면 WARN. §2.8의 「모듈 지도 완전 중복」입니다
  - [x] *(추가)* `![](...)` 로컬 이미지가 실재하지 않으면 FAIL. `img/`가 비어 있는 지금은 무출력이고 Phase 5에서 SVG가 들어오면 작동합니다
  - [x] *(추가)* 헬퍼 `figure_scan()`(캡션과 gantt와 이미지 참조를 한 번에), `ascii_blocks()`, `first_mermaid_body()` 신설. 기존 헬퍼 셋은 호출만 합니다
  - [x] 캡션 판정: 불릿과 인용은 캡션으로 인정하고 **📌 정리 박스만 인용에서 제외**합니다. 범례를 불릿으로 다는 것은 정당한 읽는 법이지만 📌는 §3.7이 정의한 절 마무리 구조물이라 캡션 자리를 대신할 수 없습니다
  - [x] 실측: **§2.7과 §2.8의 W1-M1 세 사례가 전부 잡힙니다.** gantt L357, 48줄 블록도 L211-258, 산문 0자 계보도 L486-526
  - [x] 실측: `--all` 7개 파일에서 **모듈 지도 완전 동일 복제가 7/7**, 20줄 초과 ASCII 블록도가 10개(25~56줄), 캡션 없는 그림이 4개 파일에서 6건. gantt는 W1-M1에만
  - [x] 발견: **§0과 「한 장 정리」의 모듈 지도는 7개 파일 전부 한 글자도 다르지 않습니다.** W1-M1만의 문제가 아니라 구 골격 템플릿이 재게시를 그대로 복제하게 뒀던 결과입니다. Phase 3에서 `lesson-template.md`와 `authoring-checklist.md`와 `pai-agent.md`가 이미 복제 금지로 고쳐졌으므로 신규 집필은 재발하지 않고, 기존 7개는 Phase 8 확산 때 해소됩니다
  - [x] 발견: 캡션 누락 6건 중 3건이 **그림 바로 뒤에 📌 정리 박스**가 오는 형태입니다. 그림을 던지고 절을 닫습니다
- [x] **4. E군 개정과 추가** (2026-08-27 완료)
  - [x] 설명 회피 문장 패턴에 `이미 .{0,10}알고 있`, `아실 겁니다`, `아실 것입니다`, `익숙하실`, `익숙하다면`, `이미 아실`, `당신은 이미` 추가. 기존 5패턴은 전부 보존하고 코드펜스 안은 제외
  - [x] 용어표에 `비유` 열이 남아 있으면 FAIL. 다음 줄이 구분행인 머리행만 대상이라 본문 셀의 「비유」라는 낱말에는 반응하지 않습니다
  - [x] 검사군 제목을 「이해 사다리」에서 「학습자 전제와 비유」로
  - [x] *(중요 정정)* **폐기된 전제를 강제하던 검사를 삭제**했습니다. 「선수 지식이 없음인데 제어 개념을 쓰면 FAIL」은 구 이해 사다리의 논리입니다. 새 §3.9에서 제어는 가정하는 것이 아니라 가르치는 대상이므로, 그대로 뒀으면 집필자를 제어를 prereq에 올리는 방향으로 밀어 §3.9를 정면 위반하게 만들었습니다
  - [x] *(추가)* 그 자리를 `prereq` 검사로 교체. 모듈 ID(`W숫자-M숫자`)가 아닌 항목이 있으면 FAIL. §3.9의 「앞 모듈에서 배운 것만 prereq에 올립니다」
  - [x] *(추가)* 💡 비유 박스가 한 대절에 2개 이상이면 WARN. §3.9의 「절당 최대 1개」
  - [x] *(추가)* 헬퍼 `dodge_scan()`, `analogy_col_rows()`, `prereq_items()` 신설
  - [x] 실측: **W1-M1의 두 사례가 정확히 잡힙니다.** 「당신은 이미 이 구조를 알고 있다」가 L85(계획서 §4.1 기록과 일치), `제어·LLM 비유` 머리행 7개(§2.1 기록과 일치). 목차 표 L32의 같은 문구도 함께 잡혔습니다
  - [x] 실측: 비유 열은 7개 파일 전부에 6~9개씩 **합계 49개**입니다. 회피 문장은 W1-M1에만 2건, prereq는 7개 파일 전부 통과, 💡 비유 박스는 아직 어느 파일에도 없습니다
  - [x] 발견: **삭제한 검사는 발화한 적이 없고 근거 없는 PASS를 7건 찍고 있었습니다.** 7개 파일이 전부 `**선수 지식**` 굵게 표기라 `선수 지식.*없음` 매칭이 0건이었습니다. 규약을 지킨 문서가 「선수 지식: 없음」이라고 쓰는 순간 FAIL을 맞는 구조였습니다
- [x] **5. F군 정정과 A군 추가** (2026-08-27 완료)
  - [x] F군: 밴드 주석에 「정본은 이 스크립트와 §4 두 곳뿐이고 고칠 때는 같은 커밋에서 함께」와 「이 밴드는 잠정이고 W1-M1 시범으로 1회 교정」 추가. 2026-08-10 주석은 보존. 밴드 숫자와 회귀식 계수는 무변경
  - [x] F군: `est` 변수를 `reg_min`으로 바꾸고 문구를 `회귀식 N분(산문만 세는 밴드 비교용 지표. 초독 시간은 frontmatter est_reading_min)`으로 네 분기 전부 통일. 회귀식 값이 초독 시간으로 오독되던 것을 끊었습니다
  - [x] A군: `est_reading_min`과 §0 「소요」 정합 검사 *(추가분)*. 차이가 `max(15분, 25%)`를 넘으면 FAIL. §0 구간 안에서 `**소요**` 굵은 표기만 찾아 본문의 「소요 시간을 기록」류 오탐을 막았습니다
  - [x] *(추가)* §4 추정 기준 7종으로 초독 시간을 계산해 정보줄로. 산문 550자/분, 표 1행 25초, Mermaid 90초, ASCII 블록 120초, SVG 45초, display 수식 90초, 퀴즈 15분
  - [x] *(추가)* `est_reading_min`이 그 추정값과 30% 넘게 어긋나면 WARN
  - [x] *(추가)* §0 목차 표 「읽는 시간」 열 합계가 `est_reading_min`과 20% 넘게 어긋나면 WARN
  - [x] *(추가)* 헬퍼 `reading_estimate()`, `sec0_duration()`, `toc_reading_min()` 신설
  - [x] 실측: **§0 「소요」 불일치가 7개 파일 전부에서 FAIL**입니다. W1-M1만의 문제가 아니라 `est_reading_min`은 회귀식 값이고 「소요」는 사람이 잡은 이론 시간이라 전 모듈이 같은 방식으로 어긋나 있습니다. 「소요」 파싱 실패는 0개
  - [x] 실측: 목차 「읽는 시간」 합계는 7개 파일 전부 편차 0%입니다. 절별 값이 회귀식 총합에 맞춰 역산돼 있어서, `est_reading_min`이 초독 시간으로 갱신될 때 **비로소 이 검사가 걸립니다.** 재집필 때 절별 값도 함께 갱신해야 합니다

> ⚠️ **미해결로 남긴 것: §4 추정 기준과 이 계획서 §2.5의 초독 추정이 서로 어긋납니다** (2026-08-27 발견)
>
> §4가 준 7종 기준으로 계산하면 W1-M1의 초독 시간이 **113분**입니다(산문 29 + 표 50 + 그림 13 + 수식 6 + 퀴즈 15). 그런데 이 계획서 §2.5는 같은 문서의 초독 실측 추정을 **55~75분**이라고 적었습니다. 둘 다 2026-08-25에 쓰였는데 값이 배로 벌어집니다.
>
> 원인은 **표 계수**입니다. W1-M1은 구분행을 뺀 표가 121행이고 1행 25초면 표만 50분입니다. 어떤 해석으로도 55~75에 닿지 않습니다. 표 기여가 2분이어야 하는데 불가능한 값입니다.
>
> **lint는 §4를 정본으로 보고 그대로 구현했습니다.** §4가 규약 SSOT이고 §2.5는 진단 기록이기 때문입니다. 다만 둘 중 하나는 틀렸습니다. 표 25초/행이 이 저장소의 표 밀도에 과대하거나, 55~75가 과소 추정입니다.
>
> **밴드 재교정과 같은 자리에서 함께 정합니다**(Phase 8). W1-M1을 재작성하면 표 행 수가 크게 줄어들 것이므로, 그때 실측하면 계수가 과대한지 아닌지가 드러납니다. 그전에는 추정값을 참고 지표로만 씁니다
- [x] **6. I군 신설** (2026-08-27 완료). `I. 3층 문서 (§2.1)` 검사군을 G군 뒤에 신설했습니다. 부재는 WARN 하나만 내고 나머지 12개를 건너뜁니다. 코드에 `TODO(Phase 8)` 주석을 남겨 확산이 끝나면 FAIL로 올리게 했습니다
  - [x] FAIL 8개: 산문 3,000~5,000자 독립 밴드(미달과 초과 둘 다), frontmatter 있음, 첫 줄 되돌이 링크 없음, 수식 존재, arXiv 인용 존재, 골격 5절 누락, 줄표와 가운뎃점 3회 이상, 「이미 아신다」류 문장
  - [x] WARN 5개: 표 2개 이상이거나 3행 초과, 「정확히는 lesson.md §N에서」 되돌이 링크 0개, `§N`에 절 제목 병기 없음, 캡션 없는 그림, 서로 다른 대문자 약어 3종 초과
  - [x] *(추가)* G군의 줄표·가운뎃점 판정을 헬퍼 `punct_counts()`로 공용화. **회귀 diff 0** (7개 파일 G군 출력 42줄이 개정 전과 완전 동일)
  - [x] *(추가)* 헬퍼 `eli5_scan()`, `show_hits()` 신설
  - [x] 실측: `--all` 총계가 PASS 147과 FAIL 47 그대로이고 WARN만 +7입니다. 7개 모듈이 eli5 부재 WARN 하나씩만 받습니다
  - [x] **이월분 해소** (7단위). `prose_metrics()`가 0바이트 파일에서 `ZeroDivisionError`를 냈습니다. 7단위가 `lint_one()`에 `[[ -s ]]` 가드를 넣어 막았고 함수 자체는 무변경입니다
- [x] **7. H군 신설** *(추가분)* (2026-08-27 완료). `H. 독자를 데려가는 장치 (§3.11)` 검사군을 G군과 I군 사이에 신설했습니다
  - [x] 워크드 예제 FAIL. `$$` 블록이 있거나 인라인 수식이 2개 이상인 절만 대상이고, `**숫자를 넣어봅니다.**` 류 굵은 리드가 없으면 위반
  - [x] 「여기서 많이 헷갈립니다」 박스가 문서 전체에 0개면 WARN
  - [x] 퀴즈 3단 구간 라벨(기억 확인, 적용, 종합) 누락 WARN
  - [x] *(추가)* 명령형 문항 WARN. §3.11이 「문항은 평서형 질문으로」라고 정했습니다
  - [x] 「한 장 정리」에 자기 설명 프롬프트가 없으면 WARN
  - [x] 문체 통일을 **FAIL**로 확정. 판정 등급은 실측으로 정했습니다. 1차 구현이 495건을 냈는데 오탐이 헤딩 22건 전부와 명사형 26건이라는 **구조적 두 갈래**에 몰려 있어서, 헤딩 제외와 명사 스톱리스트 두 규칙으로 걷어냈습니다. **표본 43건 전수 판정 결과 오탐 0%** 라 기준선 20%를 크게 밑돕니다
  - [x] *(부수)* 빈 파일 가드. `lint_one()`에 `[[ -s ]]` 검사를 넣어 `prose_metrics()`의 `ZeroDivisionError`를 막았습니다. 함수 자체는 무변경. 6단위에서 이월된 항목입니다
  - [x] *(추가)* 헬퍼 `worked_example_scan()`, `quiz_scan()`, `style_scan()` 신설
  - [x] 실측: **7개 파일 전부 6개 검사에 걸립니다.** 수식 절 35개 중 **35개 전부 워크드 예제 없음**, 문체 위반 445곳, 헷갈림 박스와 퀴즈 3단 라벨과 자기 설명 프롬프트는 전 파일 0개, 명령형 문항 42건(오탐 0)
  - [x] 발견: 문체 위반 385줄의 자리 분포가 인용과 📌 박스 174, **표 셀 143**, 불릿 60, 산문 8입니다. 표 셀 대부분이 되짚기 표의 정의 셀이라 재집필 때 손이 가장 많이 갈 자리입니다
  - [x] 발견: `$$` 한 줄 수식을 display 블록 시작으로 오인해 뒷부분을 통째로 삼키는 버그가 프로토타입에 있었습니다. 측정만 하고 넘어갔으면 검사가 조용히 새는 채로 커밋될 뻔했습니다
  - [x] **이월분 해소** (8단위). 「`body_h2`를 `h2_blocks`로 통일」은 B군 판정 로직 변경 금지 지시와 충돌해 7단위에서 미뤘고 8단위가 처리했습니다
- [x] **8.** 기존 7개 lesson에 `--all`로 돌려 새 검사가 무엇을 잡는지 기록 (2026-08-27 완료, 아래 「Phase 4 기준선 실측」). `course/` 아래는 한 파일도 고치지 않았습니다
  - [x] 이월분: 📌 검사의 `body_h2`를 `h2_blocks`로 통일해 코드펜스 인식을 맞췄습니다. 회귀 diff 0이고, 펜스 안에 번호 헤딩을 넣은 합성 파일에서 구 로직 9/9가 새 로직 8/8로 갈려 실제로 작동함을 확인했습니다
- [x] **9. 하류 문서 정합** *(2026-08-27 추가, 같은 날 완료)*. Phase 4가 lint를 바꿨으므로 lint의 적용 범위를 서술한 문서들이 낡았습니다
  - [x] `authoring-checklist.md` 머리말의 lint 미개정 경고 2건을 **「✅ 해소됨」 기록으로 전환**했습니다. 지우지 않고 왜 그 경고가 있었는지와 지금은 무엇이 다른지를 남겼습니다. 그대로 뒀으면 반대 방향으로 오도했을 자리입니다
  - [x] `authoring-checklist.md` C6군 머리말을 정정. H군이 잡는 6개와 육안으로 남는 3개(기호 정의, 기호표, 계산 외주)를 갈랐습니다
  - [x] `authoring-checklist.md` Z군 머리말의 「lint는 lesson.md 전용이라 이 절 전체가 육안입니다」를 정정. I군이 `lesson.md` 옆의 `eli5.md`를 검사합니다
  - [x] `docs/course-plan.md` §2.1, §3.2, §3.8, §3.9, §3.11, §4에 「검사는 lint N군이 합니다」 포인터 6줄 추가. **규약 본문은 한 글자도 바꾸지 않았습니다**
  - [x] 체크박스 44개에 `*(lint N군)*` 꼬리표. 부분적으로만 잡히는 것은 「…만」으로 범위를 적어 나머지가 육안임을 드러냈습니다
  - [x] 발견: 세 자리에서 **단순 표시를 달았으면 새 불일치가 됐을 뻔했습니다.** ① 체크리스트는 「H2 첫 줄 산문은 「한 장 정리」와 퀴즈도 예외가 아니다」라고 쓰는데 B군은 번호 붙은 대절만 봅니다 ② `비유` 열 항목은 체크리스트 배치가 C3인데 lint는 E군이 잡습니다 ③ G군의 줄표와 가운뎃점은 규약이 「쓰지 않습니다」인데 lint 임계가 3회라 1~2회는 통과합니다
  - [x] 발견: Z군의 「그림 라벨에 영문 약어」는 I군이 **구조적으로 못 잡습니다.** `eli5_scan()`이 코드펜스를 전부 제외해서 Mermaid 라벨의 약어가 계수에서 빠집니다. 육안으로 남기고 이유를 머리말에 적었습니다
- [x] **주의사항 준수 확인**: `prose_metrics()`의 산문 계산 알고리즘은 **한 글자도 바꾸지 않았습니다**. Phase 4 직전 커밋(`965ff7b`)과 함수 본문을 대조해 바이트 동일을 확인했습니다. §9.6 과거 실측값과의 비교 가능성이 보존됩니다

#### Phase 4 기준선 실측 (2026-08-27)

7단위까지 끝낸 lint를 기존 lesson 7개에 `bash scripts/lint-lesson.sh --all`로 돌린 결과입니다. **`course/` 아래 파일은 하나도 고치지 않았습니다.** 이번 단위는 측정이고, Phase 5(W1-M1 재작성)와 Phase 8(확산)이 여기 적힌 값과 대조합니다.

같은 자리에서 이월분 하나를 처리했습니다. B군의 📌 정리 박스 검사가 분모를 `grep -cE '^## [0-9]+\.'`로 세고 있어서 코드펜스 안의 번호 붙은 헤딩을 걸러내지 못했습니다. 바로 위 도입 산문 검사가 `h2_blocks()`로 센 값을 그대로 받게 바꿔 **B군 안의 분모 기준을 하나로 통일**했고, `📌` 개수도 같은 이유로 코드펜스를 인식하게 맞췄습니다. 7개 파일 출력 diff는 0입니다. 코드펜스 안에 `## 1. …`과 `📌`를 넣은 합성 파일에서는 개정 전 9/9가 개정 후 8/8로 갈려, 고친 것이 실제로 작동함을 확인했습니다.

**총계**

| 구분 | 개정 전 | 기준선 | 증감 |
|---|---|---|---|
| PASS | 145 | 147 | +2 |
| FAIL | 2 | 61 | +59 |
| WARN | 0 | 61 | +61 |

개정 전 값은 Phase 4 직전 스크립트(커밋 `7104eb9`)를 지금의 7개 파일에 그대로 돌려 재현한 것입니다. 대상 파일이 같으므로 두 열의 차이는 전부 검사 쪽에서 왔습니다.

**파일별 총계**

| 모듈 | 폴더 | tier | PASS | FAIL | WARN |
|---|---|---|---|---|---|
| W1-M1 | `01-physical-ai-landscape` | A | 19 | 11 | 9 |
| W1-M2 | `02-simulator-bootcamp` | A | 22 | 8 | 9 |
| W1-M3 | `03-diffusion-ddpm-dit` | C | 22 | 8 | 8 |
| W1-M4 | `04-flow-matching` | C | 21 | 9 | 8 |
| W1-M5 | `05-latent-discrete-fsq` | B | 21 | 9 | 9 |
| W2-M1 | `01-imitation-learning-act` | A | 20 | 9 | 9 |
| W2-M2 | `02-diffusion-policy` | A | 22 | 7 | 9 |
| **합계** | | | **147** | **61** | **61** |

**검사군별 위반** (칸은 `FAIL/WARN`)

| 모듈 | A | B | C | D | E | F | G | H | I | 합계 |
|---|---|---|---|---|---|---|---|---|---|---|
| W1-M1 | 1/1 | 1/0 | 4/0 | 1/3 | 2/0 | 0/0 | 0/0 | 2/4 | 0/1 | 11/9 |
| W1-M2 | 1/1 | 1/1 | 3/0 | 0/2 | 1/0 | 0/0 | 0/0 | 2/4 | 0/1 | 8/9 |
| W1-M3 | 1/1 | 1/0 | 3/0 | 0/2 | 1/0 | 0/0 | 0/0 | 2/4 | 0/1 | 8/8 |
| W1-M4 | 1/1 | 1/0 | 3/0 | 0/2 | 1/0 | 1/0 | 0/0 | 2/4 | 0/1 | 9/8 |
| W1-M5 | 1/1 | 1/0 | 4/0 | 0/3 | 1/0 | 0/0 | 0/0 | 2/4 | 0/1 | 9/9 |
| W2-M1 | 1/1 | 1/0 | 3/0 | 0/3 | 1/0 | 1/0 | 0/0 | 2/4 | 0/1 | 9/9 |
| W2-M2 | 1/1 | 1/0 | 2/0 | 0/3 | 1/0 | 0/0 | 0/0 | 2/4 | 0/1 | 7/9 |
| **합계** | **7/7** | **7/1** | **22/0** | **1/18** | **8/0** | **2/0** | **0/0** | **14/28** | **0/7** | **61/61** |

검사군은 A frontmatter와 독해 시간, B 문서 구조, C 용어 도입, D 시각자료와 표, E 학습자 전제와 비유, F 분량, G 문장부호, H 독자를 데려가는 장치, I 3층 문서입니다. 같은 순서의 PASS는 14, 49, 8, 37, 13, 5, 21, 0, 0입니다. **위반이 0인 검사군은 G군뿐이고, H군과 I군은 PASS가 0입니다.**

**7개 파일 전부에 걸린 항목**

FAIL 7종과 WARN 8종이 예외 없이 7개 파일 모두에 걸립니다. 총 위반 122건 중 105건이 여기에 해당합니다.

| 검사군 | 판정 | 항목 | 파일별 값 |
|---|---|---|---|
| A | FAIL | §0 「소요」 이론 시간과 `est_reading_min` 불일치 | 차이 74~141분 (허용은 15분과 25% 중 큰 값) |
| B | FAIL | 도입 산문 없는 대절 | 6~9개, 합계 49개. 49건 전부 첫 요소가 표 |
| C | FAIL | 절 끝 되짚기 표 0개 | 7개 파일 전부 0개 |
| C | FAIL | 폐지된 3열 절 시작 용어표 | 6~9개, 합계 49개 |
| E | FAIL | 용어표 「비유」 열 | 6~9개, 합계 49개 |
| H | FAIL | 워크드 예제 없는 수식 절 | 수식 절 35개 중 35개 |
| H | FAIL | 합니다체가 아닌 종결 | 41~92곳, 합계 445곳 |
| A | WARN | `est_reading_min`이 §4 초독 추정과 30% 넘게 어긋남 | 편차 57~70% |
| D | WARN | 20줄 초과 ASCII 블록도 | 1~2개, 합계 10개 |
| D | WARN | §0과 「한 장 정리」의 모듈 지도가 완전 동일 복제 | 7개 파일 전부 한 글자도 다르지 않음 |
| H | WARN | 「여기서 많이 헷갈립니다」 박스 0개 | 7개 파일 전부 0개 |
| H | WARN | 퀴즈 3단 난이도 라벨 누락 | 「기억 확인」, 「적용」, 「종합」 셋 다 없음 |
| H | WARN | 명령형 퀴즈 문항 | 2~8건, 합계 42건 |
| H | WARN | 「한 장 정리」에 자기 설명 프롬프트 없음 | 7개 파일 전부 0개 |
| I | WARN | `eli5.md` 없음 | 7개 파일 전부 없음 |

**일부 파일에만 걸린 항목** (나머지 17건)

| 검사군 | 판정 | 항목 | 파일 | 값 |
|---|---|---|---|---|
| B | WARN | 도입이 산문이 아닌 대절 | W1-M2 | 1/8개 (소제목) |
| C | FAIL | 역참조 약어 | W1-M1, W1-M3, W1-M5, W2-M1 | 합계 9건 |
| C | FAIL | 전개 없이 쓰인 약어 | W1-M1, W1-M2, W1-M4, W1-M5 | 합계 9개 약어 |
| D | FAIL | Mermaid `gantt` 오용 | W1-M1 | 1개 (L357) |
| D | WARN | 캡션 없는 그림 | W1-M1, W1-M5, W2-M1, W2-M2 | 합계 6건 |
| E | FAIL | 설명 회피와 「이미 안다」 문장 | W1-M1 | 2건 |
| F | FAIL | Tier 밴드 초과 | W1-M4, W2-M1 | 아래 참조 |

**개정 전 FAIL 2건의 정체와 현재 상태**

둘 다 F군 분량 밴드 초과였고, 기준선에도 그대로 남아 있습니다. Phase 4는 밴드 숫자도 `prose_metrics()`의 산문 계산도 건드리지 않았으므로 산문 문자 수가 글자 그대로 같습니다. 바뀐 것은 메시지 문구뿐이고, 회귀식 값이 초독 시간으로 오독되던 것을 끊으려고 5단위가 `독해 약 41분`을 `회귀식 41분(산문만 세는 밴드 비교용 지표)`으로 고친 결과입니다.

| 모듈 | 산문 | tier 밴드 | 개정 전 | 기준선 |
|---|---|---|---|---|
| W1-M4 | 18,387자 | C (12,000~17,000) | FAIL | FAIL 유지 |
| W2-M1 | 22,115자 | A (15,000~21,000) | FAIL | FAIL 유지 |

개정 전 스크립트는 이 둘 말고는 아무것도 잡지 못했고 WARN은 0이었습니다.

**이 기준선이 말하는 것**

총 위반 122건 중 105건이 7개 파일 전부에 걸립니다. 특히 B군 「도입 산문 없는 대절」과 C군 「폐지된 3열 용어표」와 E군 「용어표 비유 열」 셋은 파일별 건수가 7, 6, 8, 9, 6, 6, 7로 완전히 일치하는데, 대절 제목 다음에 곧바로 3열 용어표를 놓는 구 골격 하나가 서로 다른 세 규약을 동시에 어기기 때문입니다. 이런 분포는 개별 집필자의 실수로는 나오지 않고, 구 규약과 구 골격 템플릿이 그렇게 쓰라고 지시한 결과입니다. Phase 5는 W1-M1 하나(FAIL 11, WARN 9)만 재작성하므로 나머지 102건은 **Phase 8 확산 때까지 그대로 남습니다.** 그동안 `--all`의 FAIL은 0이 되지 않으므로, 재작성의 성패는 저장소 총계가 아니라 그 파일의 위반 건수로 판정합니다.

### Phase 5. W1-M1 시범 재작성

작성 순서가 중요합니다. 쉬운 층부터 씁니다.

- [x] **5-1. `eli5.md` 신규 작성.** "로봇에 지능을 넣는 일이 왜 층으로 나뉘는가"를 수식 없이 그림으로
- [x] **5-2. SVG 그림 제작** (`diagram-design` 스킬, `img/W1-M1-*.svg`)
  - [x] 5계층 스택 (지금 48줄 ASCII를 대체)
  - [x] 명령 하나가 내려가는 경로 (빨간 컵 서사)
  - [x] 지연과 청크 타이밍 (지금 gantt 오용을 대체)
  - [x] 데이터 피라미드 (지금 정렬 깨진 ASCII를 대체)
- [x] **5-3. `lesson.md` 재작성**
  - [x] H2 7개에 도입 산문 3~5문장
  - [x] 용어표에서 `비유` 열 제거, 절 끝 되짚기 표로 이동
  - [x] 미정의 논거 4개(`latent`, `다중 모드`, `동특성 간섭`, `공백률`) 정의 삽입
  - [x] `FSQ` 정의를 첫 사용 자리로 이동해 465줄 격차 해소
  - [x] 캐스케이드 제어, 대역폭 분리, MPC, PD 서보를 가르치는 대상으로 재작성
  - [x] 비유 14개를 `<details>` 박스로 격리
  - [x] 워크드 예제 신설 (최소 §3 주파수 비, §4 청크 길이 손계산)
  - [x] 문체를 합니다체로 통일 (📌 8개, 「한 장 정리」 표, 퀴즈 10문항)
  - [x] 퀴즈를 3단 난이도로 재편
  - [x] 중복 Mermaid(§0과 「한 장 정리」) 차별화
  - [x] `$13,500` 세 곳 이스케이프
  - [x] `est_reading_min`을 실측으로 바꾸고 §0 「소요」와 정합
- [x] **5-4. `deep-dive.md` 갱신.** 본문에서 덜어낸 것을 받고 절 번호 참조 정합
- [x] **5-5. 하류 참조 정합.** `grep -rn "W1-M1 §" course/ notes/ docs/` 와 `grep -rn "§[0-9]" course/w1-generative-core/01-physical-ai-landscape/labs course/w1-generative-core/01-physical-ai-landscape/practice` 로 확인 후 참조하는 쪽만 수정. `practice/*.py` 수정 시 `jupytext --sync`
  - [x] 이미지 상대 경로 규약 정정 (`../../img/` → `../../../img/`, 10개 파일 18곳). 사용자 승인 2026-08-28
- [x] **`practice/`와 `labs/`의 내용은 손대지 않습니다** (절 번호 참조 정정만)

### Phase 6. 검증

> 🗂 **작업 단위와 순서** (2026-08-29 확정)
>
> 항목 하나를 subagent 하나가 맡고, 항목마다 이 계획서 갱신과 커밋과 푸시를 거쳐 다음으로 넘어갑니다. 6-4와 6-5는 subagent가 수행할 작업이 아니라 각각 제약 확인과 사용자에게 드리는 요청이라 오케스트레이터가 직접 처리합니다.
>
> ➕ **6-2에 하류 산출물 정합 검증을 더했습니다** (2026-08-29 추가)
>
> Phase 5-5가 `practice/`와 `labs/`의 절 번호 참조와 이미지 상대 경로를 정정했는데 **그 결과를 검증하는 항목이 원래 Phase 6에 없었습니다.** jupytext 동기 상태와 `deep-dive.md` 링크 정합도 같은 자리에서 봅니다.

- [x] **6-1. lint FAIL 0** (2026-08-29 완료). `bash scripts/lint-lesson.sh course/w1-generative-core/01-physical-ai-landscape/lesson.md`가 PASS 51, FAIL 0, WARN 0입니다. Phase 4 기준선 19/11/9에서 PASS가 32 늘고 FAIL 11과 WARN 9가 전부 해소됐습니다
  - [x] 회귀 확인: `--all` 총계가 179/50/52이고 **나머지 6개 파일은 기준선 표와 한 칸도 다르지 않습니다.** 총계 변화분이 W1-M1 단독 변화분과 정확히 일치해 Phase 5의 부수 효과가 없었음이 확인됩니다
  - [x] *(추가)* **FAIL 0을 그대로 믿지 않고 주요 PASS 7건을 실물 대조했습니다.** Phase 4의 4단위가 「발화한 적 없이 근거 없는 PASS를 7건 찍던 검사」를 실제로 발견한 전례가 있어서입니다. 도입 산문 8/8과 되짚기 표 8개 2열과 약어 12개 첫 등장 전개와 워크드 예제 3/3과 합니다체 통일과 소요 시간 3중 정합이 전부 타당했고, **검사 조건이 실물과 어긋나 우연히 통과한 케이스는 없습니다**
  - [x] 실측: 구본에서 465줄 격차였던 `FSQ`가 첫 등장 `lesson.md:116`에서 바로 전개됩니다. 소요 시간은 frontmatter `est_reading_min: 120`과 §0 「소요」 120분과 목차 「읽는 시간」 열 합계 120분이 셋 다 같고 lint 초독 추정이 121분입니다. 편차가 1%와 0분과 0%라 관대한 임계값 덕에 통과한 것이 아닙니다
  - [x] 발견: E군 `prereq 0개가 모두 모듈 ID`는 **항목이 0개라 루프가 한 번도 돌지 않는 공허한 통과**이고 파일에 대한 근거를 담지 않습니다. W1-M1이 첫 모듈이라 빈 `prereq`가 §3.9에 맞고 `lesson.md:66`과 §0 선수 지식 표가 이를 보강하므로 상태 자체는 정상입니다. 다른 파일에서는 실제 항목을 세어 통과하므로 검사가 발화 불능인 것은 아닙니다
  - [x] 발견: C군 메시지 `되짚기 표 8개`는 본문 대절 수와 우연히 같아 절마다 하나씩 있는 것처럼 읽히지만 실제 분포는 §1과 §3에 2개씩이고 §0과 §6에는 없습니다. 신규 용어가 있을 때만 달라는 §3.8에 부합합니다
  - [x] 이월: **Mermaid 펜스 안 가운뎃점이 29개**입니다. §3.10이 코드블록 안을 예외로 두어 G군이 통과시키지만 렌더되면 독자에게 보이는 텍스트입니다. 6-2에서 함께 판단합니다
- [x] **6-2. 상대 링크 전수 확인, Mermaid 파싱 확인, SVG가 GitHub에서 렌더되는지 확인** (2026-08-29 완료)
  - [x] 상대 링크는 6개 마크다운에서 68개를 뽑아 로컬 66개를 전수 해석했고 **깨진 것이 0개**입니다. 앵커 링크는 저장소 전체에 한 건도 없어 슬러그 불일치 위험 자체가 없습니다. 외부 URL은 2개이고 arXiv 13건은 링크가 아니라 텍스트 표기인데 형식이 전부 맞습니다
  - [x] 이미지 경로 규약이 정착했습니다. `course/` 아래 이미지 참조가 `../../../img/` 8곳뿐이고 전부 루트 `img/`로 해석됩니다. SVG 4장은 각각 두 곳에서 참조돼 **고아 파일이 없습니다**
  - [x] **Mermaid는 구조 검증으로 대체하지 않고 실제 파서를 돌렸습니다.** nvm의 node v20으로 mermaid-cli 11.16.0(mermaid 11.17.2)을 스크래치패드에 설치해 5블록을 넣었고 **5/5 파싱 성공**입니다. 기본 securityLevel이 strict라 GitHub와 같은 조건입니다. 따옴표 없는 라벨 0건, gantt 0건, 노드 ID 중복 0건, 미정의 노드 0건이고 블록 3의 `<b>` 6개와 `<i>` 2개는 strict에서도 이스케이프되지 않고 서식으로 렌더됩니다
  - [x] **SVG도 실물을 눈으로 봤습니다.** mermaid-cli에 딸려 온 Chromium으로 4장을 라이트와 다크 배경에서 PNG로 뽑아 확인했고 **글자 잘림과 겹침과 넘침이 한 곳도 없으며 한글이 정상 렌더**됩니다. XML 적격 4/4, script와 이벤트 핸들러와 foreignObject와 원격 href 0건, 외부 폰트 의존 0건, viewBox 4/4 보유, 텍스트 전부 `<text>` 요소입니다. 다크 모드는 4장 모두 뷰박스를 덮는 불투명 배경 rect가 있어 안전하고 다크 스크린샷이 라이트와 같습니다
  - [x] *(추가)* 하류 절 번호 참조는 **어긋난 것이 0건**입니다. `practice/`와 `labs/`의 약 100건과 다른 모듈에서 들어오는 교차 참조 75건이 전부 유효하고, `lesson.md`에서 `deep-dive.md`로 나가는 24건과 되돌아오는 17건과 `eli5.md` 되돌이 링크 9건도 번호와 제목이 모두 일치합니다. **Phase 5-5의 참조 정정이 실제로 닫혔음이 확인됩니다**
  - [x] 발견: **`01_stack_frequency_budget`의 jupytext 쌍이 desync 상태입니다.** cell 17에서 `.py`는 `suffix = "_ascii" if args.ascii_labels else ""`로 영문 렌더를 따로 저장하는데 `.ipynb`는 그 두 줄이 없어 한글본을 덮어씁니다. 원인은 커밋 `09548b3`이 `.py`만 고치고 `.ipynb`를 재생성하지 않은 것이고 `.ipynb`의 git 로그에 그 커밋이 없습니다. **Phase 5가 만든 것이 아니라 그 전부터 잠복해 있던 결함**이고 나머지 두 쌍은 정상입니다
  - [x] 발견: 같은 결함이 문서 두 곳으로 번져 있습니다. `practice/README.md:80`은 고쳐진 새 동작을 적었는데 `labs/README.md:592-593`은 옛 동작을 적으면서 「practice/README.md §3 설명은 01에 해당하지 않는다」고 명시적으로 반박합니다. **지금 틀린 쪽은 labs/README.md입니다**
  - [x] 이월 판단: **Mermaid 펜스 안 가운뎃점 29개는 전부 렌더되면 보이는 라벨**이고 문법의 일부인 것은 없습니다. 줄표는 펜스 안에 0개입니다. 전부 큰따옴표 라벨 안이라 구분자로 기능하지 않고, 쉼표안과 공백안 두 변형을 파서에 다시 넣어 **10/10 파싱 성공**을 확인했습니다. 안전하게 바꿀 수 있으므로 순수한 문체 결정으로 남깁니다. 용법이 메타데이터 구분자와 식별자 연결과 순수 나열 세 갈래라 일괄 치환보다 갈래별 처리가 맞습니다
  - [x] 발견: `lesson.md:785`의 「더 깊이」 목록이 `deep-dive.md` §1부터 §9까지만 열거하고 §10(오해 4)과 §11(퀴즈 서술형 풀이)이 빠졌습니다. 본문 L625와 L725에서 따로 링크되므로 도달 불가는 아닙니다
  - [x] 발견: SVG 4장 모두 `font-family` 미선언이라 뷰어 기본 폰트에 의존합니다. 렌더 확인 결과 실사용 문제는 없었고 외부 폰트를 부르지 않아 GitHub 제약과는 무관합니다
  - [x] lint 회귀 없음: PASS 51, FAIL 0, WARN 0으로 6-1 값 그대로입니다

> 🔧 **6-2가 찾아낸 수정 대상** (별도 처리, 아래 6-2R)
>
> 6-2는 검증이므로 파일을 고치지 않았습니다. 나온 결함은 6-2R에서 사용자 결정을 받아 처리합니다.

- [x] **6-2R. 6-2가 지적한 5건 적용** *(2026-08-29 추가, 같은 날 완료)*. 사용자 승인 후 3개 파일에서 28줄이 들어가고 27줄이 빠졌습니다
  - [x] **`01_stack_frequency_budget.ipynb`를 `.py` 기준으로 재생성했습니다.** `.py`의 mtime을 올려 방향을 강제했고 `.py`의 md5가 실행 전후 동일해 역방향 덮어쓰기가 없었음이 확인됩니다. diff는 cell 17 한 곳이고 셀 19개와 metadata가 그대로이며 `outputs`는 원래 0개라 잃은 것이 없습니다. `02`와 `03`은 손대지 않았습니다
  - [x] **`labs/README.md`의 `*_ascii.png` 서술을 코드에 맞췄습니다.** 세 스크립트 모두 `suffix` 로직을 갖고 있어 옛 4줄이 전부 틀렸고, `practice/README.md`를 반박하던 문장과 백업 권고를 걷어내고 실제 파일명 넷을 적은 3줄로 바꿨습니다. 두 문서의 모순이 사라졌습니다
  - [x] **Mermaid 라벨 가운뎃점 29개를 갈래별로 걷어냈습니다.** 메타데이터 구분자 16개는 쉼표로, 계층 식별자 연결 6개는 공백으로, 나열 7개는 쉼표로 바꿨고 `오픈·중국 생태계`만 나열과 수식이 모호해 「오픈과 중국 생태계」로 붙였습니다. `lesson.md` 가운뎃점이 29개에서 0개가 됐고 바뀐 21줄이 전부 펜스 안입니다
  - [x] 실측: 치환 문자열이 원본보다 1자씩 짧아 **렌더 SVG가 42,010에서 41,972바이트로, 28,753에서 28,728바이트로 오히려 줄었습니다.** 노드 폭이 커져 레이아웃이 깨질 우려는 수치로 배제됐고 두 장을 2배 배율로 뽑아 잘림과 겹침이 없음을 눈으로도 확인했습니다
  - [x] **「더 깊이」 목록에 §10과 §11을 더했습니다. 다만 개별 항목 형식이 아니라 범위 형식입니다.** 제목을 병기했더니 산문이 21,032자가 되어 Tier A 상한 21,000을 넘겨 lint가 FAIL 1로 떨어졌습니다. **원본이 20,991자라 여유가 9자뿐이었습니다.** 후보 셋을 실측해 21,014자와 21,005자는 탈락했고 `오해와 퀴즈 풀이(§9~§11)`만 20,999자로 통과했습니다. 개별 형식을 고수하려면 이 줄 밖에서 산문 15자 이상을 덜어야 하는데 승인 범위 밖이라 하지 않았습니다
  - [x] **SVG `font-family` 추가는 불필요했고 6-2 보고를 철회합니다.** 4장 모두 최상위 `<g>`에 `'Noto Sans KR','Malgun Gothic','Apple SD Gothic Neo','Nanum Gothic',sans-serif`가 이미 있고 그 `<g>` 하나가 `<text>` 전량을 감싸며 개별 재정의가 없습니다. 전부 로컬 폰트이고 외부 참조가 0건이라 요구 조건이 이미 충족돼 **한 글자도 고치지 않았습니다.** 6-2의 「미선언」 판정은 검사 정규식이 여는 큰따옴표를 먹고 작은따옴표에서 빈 매치를 낸 버그였고 파일이 아니라 검사기가 틀렸습니다
  - [x] 재검증: lint **PASS 51, FAIL 0, WARN 0**으로 6-1 기준선과 같고, Mermaid 5블록이 다시 5/5 파싱되며 렌더 결과의 가운뎃점이 0개이고, SVG 4장은 XML 적격에 바이트 수 변화가 없습니다. 링크 68개와 절 번호 참조도 전부 재확인해 깨진 것이 없습니다. 오케스트레이터가 같은 항목을 독립 재확인했고 결과가 일치합니다

> ⚠️ **Phase 8 밴드 재교정에 넘길 실측: W1-M1 산문이 Tier A 상한에서 1자 남았습니다** (2026-08-29 발견)
>
> 재작성본이 20,991자로 상한 21,000에 9자를 남기고 통과했고, 6-2R이 「더 깊이」 한 줄을 보완하자 여유가 **1자**로 줄었습니다. 지금 이 문서는 **한 글자만 더해도 FAIL이 납니다.**
>
> 이것이 §4가 밴드를 잠정으로 선언한 이유를 그대로 보여줍니다. 계획서 §8이 예고한 「친절하게 쓰면 글자가 더 든다」가 실측으로 확인됐고, 3층 분리로 `deep-dive.md`에 54KB를 덜어낸 뒤에도 상한에 붙었습니다. **Phase 8의 밴드 재교정은 이 값을 입력으로 씁니다.** 그전까지 이 모듈에 산문을 더하려면 같은 양을 덜어내야 합니다.
>
> ✅ **2026-08-30에 해소됐습니다.** 사용자가 Tier A 상한을 21,000에서 23,000으로 올렸습니다(근거는 `docs/course-plan.md` §9.20). 위 서술은 상향 이전 기록이고 지금 여유는 1,098자입니다(6-3S 실측 21,902자). **다만 그 상향이 Tier A 8개 모듈 전체에 걸린다는 점은 아직 정리되지 않았습니다.** 6-3S 발견을 보세요.
- [x] **6-3. `authoring-checklist.md`로 육안 잔여 확인** (2026-08-29 완료). 체크박스 110개를 둘로 갈라 병렬 전수 판정했습니다. **통과 86, 위반 24**입니다. lint는 PASS 51에 FAIL 0으로 6-1과 같았고 **위반 24건은 전부 lint가 보지 않는 자리에서 나왔습니다**
  - [x] Z와 A와 B와 B2와 C와 C2와 C3: 체크박스 64개 중 통과 50, 위반 14. A와 B와 B2는 위반 0이고 위반이 C3(11개 중 8개)에 몰렸습니다
  - [x] C4와 C5와 C6과 D와 E와 F와 G와 H: 체크박스 46개 중 통과 36, 위반 10. E와 F와 G는 위반 0입니다
  - [x] **FAIL 급 6건**입니다
    - [x] **`eli5.md`가 `lesson.md`용 SVG 4장을 그대로 재사용합니다.** lint I군은 eli5 **산문**의 약어만 세어 1종으로 통과시키고 SVG 파일을 열지 않습니다. 실제 라벨에는 eli5 본문이 한 번도 쓰지 않은 `L5`부터 `L1`까지의 층 번호와 `Hz`와 `q_des`와 `H_chunk`와 `setpoint`와 `RGB-D`와 회사 도구 다섯 이름이 노출됩니다. §2.1이 eli5 독자를 「아직 아무 용어도 모르는 사람」으로 규정한 것과 정면으로 어긋납니다
    - [x] **다이어그램 라벨이 본문보다 먼저 용어를 소개합니다.** §3.8이 이 규칙을 만들 때 인용한 근거가 구본 W1-M1의 48줄 ASCII 블록도가 `RGB-D`와 `LiDAR`를 정의 없이 도입한 것인데, **재작성본은 블록도를 없애고도 같은 두 용어를 §7.1 Mermaid 라벨에 그대로 남겼습니다.** 컨테이너만 바뀌고 위반은 살아남았습니다. 같은 성격으로 stack-5layers.svg가 `DualMap`과 `GEAR-SONIC`과 `HOMIE`를 §7보다 330줄 먼저 보여주고 §6.1 계보도가 아홉 개를 설명 없이 도입합니다
    - [x] **`D_obs`가 §3.2 기호표에 없습니다.** 그림은 `관측 이력 [T_hist, D_obs]`인데 표는 `D_state`이고 `D_obs`는 문서 전체에서 grep 0건입니다. 이름을 고정하겠다고 선언한 표가 자기 그림의 이름을 놓쳤습니다
    - [x] **`labs/worksheet.md:331`에 폐지된 「제어·ML·LLM 대응 비유」 열이 살아 있습니다.** 바로 위 L329가 학습자에게 비유 작성을 강제까지 합니다. §3.9가 이 열을 폐지한 근거가 다름 아닌 W1-M1 실측(비유 지점 50개 중 36개가 이 열)이었는데 **당사자 모듈에 그대로 남았습니다.** lint E군은 `lesson.md`만 봅니다
    - [x] **`practice/`와 `labs/` 산문에 줄표 72회와 가운뎃점 56회가 남았습니다.** §3.10은 두 디렉토리를 적용 범위에 명시합니다. `lesson.md`와 `eli5.md`와 `deep-dive.md`는 셋 다 0인데 실습 쪽만 규약이 절반만 적용된 상태입니다
    - [x] **팀 질문 7번이 `notes/questions-for-team.md`에 적립되지 않았고 존재하지 않는 `M1-7`을 가리키는 참조가 2곳 살아 있습니다.** 적립은 처음부터 M1-1에서 M1-6까지 6개뿐입니다
  - [x] 개선 권고 15건. §0과 §2와 §7의 도입 산문이 하한 3문장에 못 미치는 2문장, §3.1과 §4.2가 2026-08-25 개정으로 앞당긴 그림 위치를 지키지 않고 수식 뒤에 그림, `latent`와 `코드북`과 `월드모델`의 정의가 한 문장 gloss, `≫` 미정의(같은 Phase 5의 `deep-dive.md:42`는 정의했습니다), 기호 6개인 PD 수식과 5개인 청크 부등식에 기호표 없음, §3.3 표의 「최선」과 「최악」 열 정의가 실습 코드 출력에만 있음, `미들웨어`와 `텔레옵`과 `궤적`과 `시맨틱 맵`과 `강화학습` 미정의, §1.1이 「왜 그 배수인지는 §3.3에서」라고 약속했는데 §3.3이 답하지 않음
  - [x] 발견: **데이터 피라미드 네 층이 본문 산문에 한 번도 열거되지 않습니다.** §5.1 표는 두 층만 담고 나머지 둘은 SVG에만 있어 **그림을 못 보는 독자는 퀴즈 2번을 풀 수 없습니다**
  - [x] 발견: FAIL 급 6건 중 **5건이 Phase 5 이전부터 잠복해 있던 것**입니다. 줄표와 가운뎃점은 커밋 `9b7a617`이 「문장부호 규약 적용」이라는 이름으로 같은 파일들을 손대고도 lesson과 deep-dive만 0으로 만든 뒤 멈춘 결과이고, 비유 열과 팀 질문 미적립은 `09548b3`과 그 이전부터입니다
  - [x] **재작성이 확실히 성공한 곳도 기록합니다.** `est_reading_min` 120분과 §0 「소요」와 목차 합계가 정확히 일치해 구본의 4.6배 불일치가 완전히 해소됐습니다. `lesson.md`의 그림 캡션 8개가 전부 「어디를 보고 무엇을 따라가라」는 진짜 지시문이라 §2.8이 구본 그림 8개 중 4개를 「장식, 중복, 오용」으로 판정한 상태에서 벗어났습니다. 제어 개념이 나오는 열두 자리를 전부 대조한 결과 `미들웨어` 하나를 빼고 전부 그 자리에서 처음부터 설명하고, 비유 신호어 전수 조사에서 본문 산문에 남은 비유가 없으며, 상태공간은 아예 쓰지 않았습니다
  - [x] E군 실행 검증: `practice/`를 스크래치패드로 복사해 세 스크립트를 스모크와 전체로 완주시켰고 전부 성공입니다. jupytext 3종 모두 `Unchanged`, 노트북 `outputs` 전부 0개, 뷰어 호출 0건입니다. **lesson의 9.6퍼센트와 78.7퍼센트와 41스텝이 실행 결과와 일치합니다.** 저장소는 원상복구됐습니다
  - [x] 발견: **부수로 두 문서의 어긋남을 찾았습니다.** 체크리스트 L140은 「선수 지식」 표를 §0에 요구하는데 §3.7 골격은 §1에 둡니다. 체크리스트 L159는 §3.8이 「권장」으로 쓴 한국어 병기를 필수처럼 적어 FAIL로 오판할 자리입니다

> 🚧 **고칠 때 걸리는 제약 둘** (6-3 실측)
>
> **본문 산문이 20,999자로 Tier A 상한 21,000에서 1자 남았고, 표 21블록과 불릿 21블록이 동수입니다.** 용어 정의를 산문으로 보태면 lint F군이, 기호표를 표로 만들면 D군이 곧바로 FAIL로 돌아섭니다. 정의 추가는 `deep-dive.md` 이관과 짝으로, 기호표는 불릿 형태로 넣어야 합니다.
>
> ✅ **분량 쪽 제약은 2026-08-30 밴드 상향으로 풀렸습니다.** 상한이 23,000이 되어 여유가 1,098자입니다. **표와 불릿이 동수라는 제약은 그대로**이므로 기호표는 여전히 불릿으로 넣어야 합니다.

- [x] **6-3R. 6-3 발견 FAIL 급 6건 처리** *(2026-08-29 추가, 2026-08-30 완료)*. 사용자가 **FAIL 급만** 고치기로 결정했고 개선 권고 15건은 학습자 검토 뒤로 미뤘습니다. 파일 소유가 겹치지 않게 세 갈래로 나눠 병렬 처리했습니다
  - [x] **`eli5.md` 전용 축약 SVG 4장을 신설했습니다.** 층 번호와 차원 기호와 제어 기호와 단위 약어와 회사 도구명과 데이터셋명을 전부 걷어내고 eli5 본문이 이미 쓴 말로만 라벨을 다시 붙였습니다. `0.5 ~ 5 Hz`는 「1초에 한 번에서 다섯 번」이 되고 `q_des [23~43]`은 「관절마다 하나씩, 스무 개가 넘는 숫자」가 됩니다. 데이터 피라미드는 네 칸에서 세 칸으로 줄이고 다섯 층은 번호 대신 하는 일로 이름을 붙였습니다. 기존 4장은 `lesson.md`가 계속 쓰므로 그대로 뒀습니다
  - [x] 실측: eli5 그림 4장의 캡션을 새 그림 구조에 맞춰 다시 썼습니다. **특히 타이밍 그림은 캡션이 2단이라 말하는데 그림이 4단이던 불일치**를 그림을 3단으로 갈아 해소했습니다. eli5 산문은 4,715자에서 4,868자로 밴드 안입니다
  - [x] **다이어그램 라벨의 선행 용어 도입을 없앴습니다.** §7.1 배치도 앞에 도입 산문을 새로 두어 `RGB-D`와 `라이다`를 약어 전개까지 붙여 정의했고, §6.1 계보도의 부제 라벨 아홉 개를 본문에 이미 있는 한국어로 바꿨습니다. `open-world 계층 추론`은 「처음 보는 환경에서 단계를 나눠 추론」이 되는 식입니다. §7.1의 세 라벨도 같은 원칙으로 정리해 **`RL` 두 곳이 모두 사라졌습니다**
  - [x] **`D_obs`를 `D_state`로 통일했습니다.** SVG와 `deep-dive.md`의 라벨을 바꿔 모듈 안 등장이 0건이 됐고, 표 정의인 proprioception 차원이 「관측 이력」 라벨과 의미상 맞는 것을 deep-dive §8.1의 L4 로봇 상태 표기와 대조해 확인했습니다. 모듈 밖 마지막 한 곳(`w2-policy-vla/01-imitation-learning-act/deep-dive.md:120`)은 오케스트레이터가 커밋 `fa8de30`으로 정정해 **저장소 전체 `D_obs`가 0건**입니다
  - [x] **`labs/worksheet.md`의 폐지된 비유 열을 걷어냈습니다.** 표를 2열로 줄이고 비유 작성을 강제하던 지시를 「정의는 lesson의 문장을 옮기지 말고 자기 말로 한 줄로 쓰세요」로 바꿨습니다. 파일 전체에 「비유」 문자열이 0건입니다
  - [x] **`practice/`와 `labs/`의 산문 줄표 72회와 가운뎃점 58회를 전부 0으로 내렸습니다.** 계획서 집계가 56이었는데 코드펜스 경계 판정 차이로 실제는 58이었고, `#`으로 시작하는 줄만 세느라 빠졌던 **트레일링 주석 3건**도 함께 고쳤습니다. 콜론 부제로 바꾼 자리는 없습니다
  - [x] 실측: `.py` 편집이 **주석 전용임을 AST 비교로 증명**한 뒤 `.py`에서 `.ipynb` 방향으로 동기화하고 왕복 일치까지 확인했습니다. 셀 id 재생성으로 생긴 노이즈 49줄은 HEAD 값으로 되돌렸습니다. `01`과 `02`는 스모크 완주했고 `03`은 `pandas` 부재로 실행 불가라 AST 동일성으로 대체했습니다
  - [x] 발견: **`M1-7`은 이미 적립돼 있었습니다.** 6-3이 「미적립」이라 판정한 것은 HEAD 기준이었고 작업 트리에는 `M1-7`부터 `M1-11`까지 있었습니다. 두 참조는 이미 유효했고, 대신 lesson 9번(§3.2 기호표의 오른쪽 열)만 대응 행이 없어 **`M1-12`로 적립**했습니다. 낡은 질문 범위 참조 3곳도 함께 맞췄습니다

> ⚠️ **동시 세션 사건: 다른 `physical-ai-study` 세션이 같은 워킹트리를 함께 편집했습니다** (2026-08-30)
>
> 6-3R이 도는 동안 다른 세션이 **「W1-M1 회사 스택 갱신 반영」** 작업을 진행했습니다. 사내 「GenP Navigation 모듈 진행 현황」을 근거로 `CLAUDE.md`의 회사 스택 표를 갱신하고 §7.4를 신설하고 `img/W1-M1-nav-substack.svg`를 만들고 **§9.20과 함께 Tier A 상한을 21,000에서 23,000으로 올린** 작업입니다.
>
> **결과가 셋입니다.** 첫째, 6-3R의 B 갈래가 밴드 상향을 근거로 **`deep-dive.md` 이관을 생략**했습니다. 사용자가 승인한 것은 「이관과 짝으로」였고 밴드 상향은 다른 세션의 결정이었습니다. 사후에 사용자가 상향 수용과 이관 불필요를 확인했고 산문은 21,805자로 상한까지 1,195자 여유입니다. 둘째, 그 세션이 `git add -A`로 커밋하면서 **6-3R 산출물 전부가 커밋 `c0c0adc`에 함께 담겼습니다.** 유실은 없고 내용은 온전합니다. 셋째, **6-3의 체크리스트 판정 110건이 스택 갱신 이전 본문 기준**이 됐습니다. §7.4와 `nav-substack.svg`는 검사된 적이 없어 6-3S로 추가 검증합니다.
>
> **배운 것.** 여러 세션이 한 저장소를 동시에 만질 때 `git add -A` 커밋은 남의 진행 중 작업을 자기 커밋에 쓸어 담습니다. 커밋 전에 `git status`로 자기 변경만 있는지 확인하거나 파일을 명시해 add해야 합니다.

- [x] **6-3S. 스택 갱신분 추가 검증과 하류 워크시트 정합** *(2026-08-30 추가, 2026-08-31 완료)*. 스택 갱신으로 새로 들어오거나 바뀐 자리에서 **44개 항목을 판정해 통과 36, 위반 8**입니다. 위반 6건을 4개 파일 21행 교체로 고쳤고 2건은 수정 범위 밖이라 보고만 했습니다
  - [x] **`img/W1-M1-nav-substack.svg`의 라벨 9종을 본문 어휘로 바꿨습니다.** `오도메트리`, `self / object filtering`, `hull`, `carry_on`, `HMSG`, `pcd`, `행동 트리`, `costmap 2D`, `depth`가 전부 lesson 본문 grep 0건이었고 deep-dive §12에만 정의돼 있었습니다. §3.8의 「다이어그램 라벨에서 용어를 처음 소개하지 않는다」 위반입니다. `hull 생성, carry_on 모드`가 「껍데기 형상을 만들어 가림」이 되는 식이고, 걷어낸 전문 어휘는 `notes/glossary.md` 184~192행과 deep-dive §12.1~§12.3에 그대로 살아 있어 **잃은 정보가 없습니다**
  - [x] **같은 유형이 `lesson.md` §7.1 Mermaid에서도 재발했습니다.** `3D 점군 맵(pcd)`의 `pcd`가 문서 전체에서 그 라벨에만 있었습니다. 6-3이 FAIL로 지목하고 6-3R이 고친 것과 **정확히 같은 위반이 새 노드에서 다시 난 것**입니다
  - [x] **`img/W1-M1-stack-5layers.svg`의 `2D 점유 격자`를 `2D 격자 지도`로.** 스택 갱신이 §3.2 그림의 L5 출력 라벨에 새로 넣은 말인데 lesson 어디에도 정의가 없고, 이 그림은 §7보다 **330줄 앞**에 놓입니다. §3.2 본문의 예외 문장은 「회사 도구 이름과 월드모델」만 유예하고 출력 열은 덮지 않습니다
  - [x] **§7.4 되짚기 표가 본문에 없는 용어 셋을 소개하고 있었습니다.** `점유 격자`를 본문과 같은 `2차원 격자`로, Nav2 정의에서 lesson 본문 grep 0건인 `ROS 2`를 삭제, FSR-VLN 정의의 `계층 장면그래프`를 본문 §7.1이 실제로 쓴 「방과 시점과 물체를 층으로 정리해 두고」로 바꿨습니다. 「이 절에서 나온 뜻」 표인데 절에 나오지 않은 뜻을 담고 있었습니다
  - [x] **§7.4 도입 산문에 2문장을 더했습니다**(+139자). SVG 가운데 줄의 걸러내기 상자가 본문에 한 글자도 없어 **그림을 보는 독자가 그 칸을 해석할 수 없었습니다.** 물건을 들고 걸을 때 그 물건이 장애물로 읽히는 문제와 껍데기 형상으로 몸의 일부로 치는 처리를 풀어 썼고, 도입 산문이 2문장에서 4문장이 됐습니다
  - [x] **SVG 텍스트 넘침 1건을 고쳤습니다.** `L4 를 거치지 않습니다`가 좌측 정렬이라 viewBox 1190을 약 17px 넘겼습니다. 우측 정렬로 옮기고 어색한 띄어쓰기를 없앴습니다. **SVG 9장을 전수 재측정해 넘침이 이 한 곳뿐이었고 지금은 0입니다**
  - [x] `deep-dive.md` 안내 표에서 §12 행이 §11 앞에 끼어 있던 순서를 바로잡았습니다
  - [x] 실측: 절 번호 참조 **381건을 전수 해석해 어긋난 것이 0건**입니다. §7 재배치가 걸린 `worksheet.md`의 `:90`, `:92`, `:282`, `:283`을 개별 대조했고 구 §7.2(G1 구성)를 가리키던 참조는 남아 있지 않습니다. **워크시트 채점 개수도 새 §7 내용과 맞습니다** (백지 표 6행에서 8행, 채점 `/5 → /7`과 `/6 → /8`)
  - [x] 실측: 하류 mermaid 라벨 정정이 **5/5 반영**됐고 바뀐 라벨에 가운뎃점이 0개입니다. 계획서에 없던 W1-M2 2곳도 함께 정정돼 있습니다. 저장소 전체에 구 라벨 `L5 · DualMap` 잔존은 `lesson-template.md:232` **한 곳뿐**이고 이것은 개선 권고로 남깁니다
  - [x] 실측: jupytext 3쌍이 모두 `--diff` 빈 출력에 `--sync` `Unchanged`이고, 동기 실행 후 `.py`와 `.ipynb` md5 6개가 전부 저장소 값과 같아 **역방향 덮어쓰기가 없었음이 증명됩니다.** 노트북 `outputs`는 3개 다 0개이고 저장소는 한 바이트도 바뀌지 않았습니다
  - [x] lint: W1-M1이 **PASS 51 · FAIL 0 · WARN 0**으로 6-1 기준선 그대로이고, 나머지 6개 파일을 `e74ddbe` 시점과 각각 대조해 **6/6 전부 한 칸도 다르지 않습니다.** 산문은 21,763에서 **21,902자**가 됐고 상한 23,000까지 여유 **1,098자**, 표 21블록과 불릿 21블록이 동수입니다
  - [x] 발견: **`--all` 기준선이 밴드 상향으로 조용히 이동했습니다.** 179/50/52에서 **180/49/52**가 됐는데 원인은 이번 편집이 아니라 **W2-M1의 산문 22,115자가 구 밴드(15,000~21,000)에서 F군 FAIL이었다가 상향(~23,000)으로 PASS가 된 것**입니다. §9.20은 「이번 실측은 Tier A 하나의 것」이라 적었지만 **밴드는 Tier A 모듈 8개 전체에 걸리고, 그중 한 모듈의 밴드 초과가 검토 없이 해소됐습니다.** Phase 8 밴드 재교정의 입력에 넣었습니다
  - [x] **미처리 1(6-3SR에서 해소): `practice/01_stack_frequency_budget.py`가 Nav2 「회사 실측」 행을 계산해 놓고 출력하지 않습니다.** `rows.append([...Nav2...])`가 `print_table(...)`보다 **뒤**에 있어 죽은 행이고, stdout의 분리비 표는 3행인데 lesson §3.3 표는 4행입니다. 뒤이어 찍히는 「마지막 행만 성격이 다르다」 안내문도 실제 마지막 행인 L4/L5를 가리키게 되어 오독을 부릅니다. **`docs/course-plan.md` §9.20의 「실행 검증했습니다. 출력이 lesson §3.3 표와 일치합니다」는 사실이 아닙니다.** 「practice와 labs의 내용은 손대지 않는다」는 지시 범위 밖이라 **6-3SR로 넘겼고 같은 날 해소됐습니다.** 나머지 12칸은 실행 출력과 한 자리도 다르지 않습니다
  - [ ] **미처리 2: 개선 권고 8건.** 6-3R의 결정을 따라 학습자 검토 뒤로 미룹니다. `stack-5layers.svg`가 §3.2에서 `RGB-D`와 `라이다 포인트클라우드`를 정의 330줄 앞에 보여주는 것(6-3이 지목했으나 6-3R이 §7.1과 §6.1만 고쳤습니다), 「2차원 격자」를 부르는 이름이 셋인 것, SVG의 라틴 뒤 조사 띄어쓰기 4곳, `Global Path`와 `Local Path`가 영문인 것, deep-dive 안내 표 12행이 §3.2 10행 상한을 넘는 것, `lesson-template.md:232`의 구 라벨이 그것입니다
- [x] **6-3SR. practice 죽은 행 결함 수정** *(2026-08-31 추가, 같은 날 완료)*. 사용자 승인 후 4개 파일을 고쳤습니다
  - [x] **`rows.append`를 `print_table` 앞으로 옮겼습니다.** 실물 줄 번호는 `print_table`이 297~301행, Nav2 블록이 302~313행이었고 6-3S 보고의 「297행 / 304~313행」과 달랐습니다. 로직과 값은 한 글자도 바꾸지 않은 **순수 이동**이고 diff가 5줄 교체입니다
  - [x] **안내문은 고칠 필요가 없었습니다.** 「마지막 행만 성격이 다르다」는 원래 4행 상태를 전제로 쓰인 문장이고, 이동으로 마지막 행이 Nav2가 되면서 **비로소 맞게 됐습니다.** 6-3S가 「오독을 부른다」고 본 것은 맞았지만 원인이 문장이 아니라 행 누락 쪽이었습니다
  - [x] **실행으로 4행 출력을 확인했습니다.** `--smoke`와 전체 모드 둘 다 exit 0이고 분리비 표가 4행입니다. 기존 12칸과 Nav2 3칸(20 / 2 / 10.0배)이 lesson §3.3 표와 일치합니다. **HEAD 판을 같은 조건으로 돌려 3행만 나오는 결함도 재현 확인**했습니다
  - [x] jupytext: 동기 전후 `.py` md5가 `11243e90...`으로 동일해 역방향 덮어쓰기가 없었음이 증명됩니다. `.ipynb` 실질 diff는 `.py`와 같은 5줄 교체 하나뿐이고 셀 19개와 id 19개가 보존되며 `outputs`는 0개입니다. `02`와 `03`은 손대지 않았습니다
  - [x] **`labs/worksheet.md` 2곳.** 채점 항목을 `/ 3`에서 `/ 4`로 고치고, **백지 표에 Nav2 행을 함께 더했습니다.** `/4`로 채점하는데 채울 칸이 3개면 새 불일치가 생기기 때문입니다. `labs/README.md`와 `practice/README.md`에는 분리비 개수를 3으로 적은 자리가 없어 미수정입니다
  - [x] **`docs/course-plan.md` §9.20의 허위 주장을 취소선으로 보존하고 정정 주석을 달았습니다.** 6-2R의 「보고 철회」와 6-3R의 「오탐 철회」 관례를 따랐습니다. **「한 번이라도 돌려봤으면 드러났을 차이이므로 실행 검증했습니다라는 서술 자체가 틀렸다」**고 명시했습니다
  - [x] lint 회귀 없음: 단일 **PASS 51 · FAIL 0 · WARN 0**, `--all` **180 / 49 / 52**로 6-3S 값 그대로입니다. `py_compile` 통과
  - [ ] **남긴 것: §3.3 표와 stdout의 표기 정밀도가 한 열 어긋납니다.** 「빠른쪽[Hz]」 열이 `,.1f`라 stdout이 `158.1`과 `3.2`를 찍는데 lesson은 같은 값을 `158.11`과 `3.16`으로 적습니다. **값이 아니라 표시 자릿수만 다릅니다.** `,.2f`로 바꾸면 세 행은 맞지만 `1,414.2`가 `1,414.21`이 되어 이번에는 첫 행이 어긋나므로 한 글자 수정으로 닫히지 않습니다. 승인 범위가 「위치만 이동」이었고 lesson 수정은 금지 대상이라 손대지 않았습니다. 선재 결함이고 HEAD 판에서도 같습니다

- [x] **6-4. 윤문은 하지 않습니다** *(2026-08-31 확인)*. §1 절차상 재집필 후에는 Phase 4에서 멈추고 학습자 검토를 받습니다. **결정이 그대로 지켜졌습니다.** 이번 회차에 `/humanize-korean`을 한 번도 돌리지 않았고, Phase 7-1이 `docs/course-plan.md` §5의 윤문 체크박스를 `[x]`에서 `[ ]`로 되돌려 **다음 세션이 윤문을 건너뛰지 않게** 했습니다. 구 본문 윤문 기록은 「구 본문 기준」임을 명시해 보존했습니다
- [x] **6-5. 학습자에게 `eli5.md`를 먼저, `lesson.md`를 그다음으로 읽어달라고 요청** *(2026-08-31 전달)*. 읽는 순서와 각 층이 무엇을 담는지, 그리고 **무엇을 봐 달라고 부탁하는지**를 함께 전했습니다. 이번 개정의 성패는 lint가 아니라 학습자가 「이제 이해된다」고 하는지에 달려 있고, 그 답이 Phase 8 확산의 착수 조건입니다. **회신은 아직입니다**

### Phase 7. 마무리

- [x] `docs/course-plan.md` §5 W1-M1 항목 갱신, 윤문 체크박스를 `[ ]`로 리셋 *(2026-08-31 완료)*. 5줄 교체이고 §0과 §9와 다른 모듈은 한 글자도 바뀌지 않았습니다
  - [x] `lesson.md 초안` 줄이 **1차 재집필(2026-08-09) 기준에 멈춰 있던 것**을 2차 개정 재작성 기준으로 교체했습니다. `eli5.md` 신설과 `deep-dive.md` 갱신을 §2 각주대로 이 줄에 함께 넣었고 1차 기록은 줄 끝에 보존했습니다
  - [x] 윤문을 `[ ]`로 되돌렸습니다. W1-M2~M5의 「재집필본 기준으로 미실시」 형식을 따르되 **이 모듈만 재작성이 두 번이라 윤문 기록이 두 세대 뒤처진다**는 차이를 명시했습니다
  - [x] `practice/`와 `labs/` 줄에 이번 회차에 손댄 내역을 적었습니다. 체크박스는 `[x]` 유지입니다. 재작성이 아니라 정정이고 SKILL.md의 리셋 규칙은 윤문에만 걸립니다. **`worksheet.md`의 채점 기준 변화(`/3 → /4`, `/5 → /7`, `/6 → /8`)는 학습자가 실행검증 때 직접 부딪히는 값**이라 기록이 없으면 다음 세션이 옛 기준으로 판단합니다
  - [x] 실행검증(5번)은 학습자 몫이라 `[ ]` 그대로입니다
  - [x] *(정정)* **계획서 §2.5의 「4.6배 불일치」 근거를 오케스트레이터가 잘못 전달했습니다.** 4.6배는 `est_reading_min` 39분과 §0 「이론 3h」(180분)의 비이고, 55~75분은 별개의 초독 실측 추정입니다. subagent가 §9.19와 §2.5를 대조해 잡아냈고 문서에는 올바른 근거로 적혔습니다
  - [x] 실측 재확인: subagent가 프롬프트의 숫자를 그대로 쓰지 않고 **재작성 직전 커밋 `3c445ac`을 현재 lint로 다시 재어** 산문 16,058자와 PASS 19 / FAIL 11 / WARN 9를 얻었습니다. 기존 §5가 적은 15,977자는 윤문 **전** 값이라 시작점이 달랐습니다
- [x] §0 대시보드 갱신 *(2026-08-31 완료)*. 「마지막 갱신」이 2026-08-12에 멈춰 있었고 그 뒤 일어난 것이 하나도 반영돼 있지 않았습니다. hunk 5개가 전부 §0 구간(10~31행) 안입니다
  - [x] **W1 행을 전면 재작성했습니다.** 1차(§9.11, 정보 배열)와 2차(§9.19, 설명 방식)를 구분하고, **W1-M1만 2차 규약이고 M2~M5는 아직 1차 규약 상태**라는 지금의 실제 모습을 적었습니다. 구본은 「M1은 윤문까지 끝났고」라고 적었는데 7-1이 그 체크박스를 되돌렸으므로 **다섯 개 전부 윤문 대기**로 고쳤습니다
  - [x] **lint를 파일별로 재측정했습니다.** M1이 51/0/0이고 M2~M5가 FAIL 8, 8, 9, 9이며 사유가 전부 2차 규약 항목입니다. 구 서술 「M4를 뺀 넷이 PASS 21 · FAIL 0」은 **Phase 4 lint 개정 이전 값이라 지금 값과 비교되지 않는다**는 것을 명시했습니다
  - [x] 실측: **`--all` 총계 180 / 49 / 52가 Phase 4 기준선 147 / 61 / 61과 정확히 화해됩니다.** 차이가 두 곳에서만 왔고 W1-M1 재작성(19/11/9 → 51/0/0)과 W2-M1의 밴드 상향 통과(20/9/9 → 21/8/9)입니다. 147+32+1=180, 61−11−1=49, 61−9=52
  - [x] **용어집 실측 139행 / 고유 137개.** 구 기록 「123행 / 고유 122개」는 W2-M1을 10, W2-M2를 22로 적어 **둘 다 틀렸습니다.** 중복은 `action chunking`과 `universal token space` 둘이고 관점이 달라 유지합니다
  - [x] **팀 질문 실측 53건.** 구 기록 「41건」은 **괄호 안 내역의 합이 46인데 총계를 41로 적어 자체 모순**이었습니다. 늘어난 7건은 전부 2026-08-29 스택 갱신분(`M1-7`~`M1-12`와 P0 6번)입니다
  - [x] 「데드락 방지선」 박스의 **「W1 집필 쪽에 남은 일은 없습니다」가 더 이상 맞지 않습니다.** 1차 재집필 직후에 쓴 문장이고 그 뒤 2차 개정이 왔습니다. 원문 취지를 남긴 채 남은 일 셋(윤문 5건, 학습자 검토, 검토에 달린 M2~M5 재작성)을 적고 방지선 판정 자체는 집필 개수만 보므로 여전히 해소라고 갈랐습니다
  - [x] 완료 기준 박스에 **§2가 2026-08-25에 산출물을 7종으로 늘렸는데 체크박스는 5칸 그대로**라는 것을 덧붙였습니다. `eli5.md`는 1번 칸으로 함께 추적하고 `deep-dive.md`는 조건부라 칸이 없습니다
  - [x] 발견: **`notes/glossary.md`의 W2-M2 절 마지막 9행이 실제로는 W1-M1 스택 갱신이 만든 내비 용어**입니다. 모듈 열도 `W1-M1`으로 시작하는데 파일 맨 끝 표에 이어 붙어 자리만 W2-M2입니다. **7-4 롤업에서 함께 처리합니다**
  - [x] 용어집 행 판단: 7-4 롤업이 이 숫자를 또 바꾸지만 **지금 값을 적고 롤업 예정을 드러내는 쪽**을 택했습니다. 기존 숫자가 이미 틀려 있어 기다리면 확실히 틀린 숫자가 남고, 롤업 증분(되짚기 표 8개 46행 중 미등재 10개)을 미리 재 뒀습니다
  - [ ] **범위 밖으로 보고된 것: §2 산출물 표 5번의 「클라우드 인스턴스에서 직접 확인」이 낡았습니다.** `CLAUDE.md` 실행 환경이 「로컬 RTX 3080 주력, 클라우드는 W3 대규모 학습만」으로 바뀌었습니다. §0은 §2 문구를 옮긴 것이라 §0만 고치면 두 절이 어긋나므로 손대지 않았습니다
- [x] `docs/progress.md` 집필 검증 섹션에 이 회차 블록 추가 *(2026-08-31 완료)*. 105줄이 들어갔고 `docs/progress.md` 한 파일만 바뀌었습니다
  - [x] **판단: 기존 W1-M1 블록에 덧붙이지 않고 별도 회차 블록을 세웠습니다.** 근거 셋입니다. 이번 회차가 검증한 것은 모듈 하나가 아니라 규약 SSOT와 집필 도구 5종과 `lint-lesson.sh`와 W1-M1 전체라 모듈 블록 제목이 내용을 잘못 말하게 됩니다. 섹션 머리말이 이 섹션을 「학습자 랩의 성공 판정 기준과 시간 예산의 출처」로 규정하는데 이번 값은 회귀와 결함 탐지 기록이라 성격이 다릅니다. 한 블록에 두 회차를 담으면 어느 값이 어느 시점인지 매번 되짚어야 합니다
  - [x] **「모듈당 1블록」 규정에 한 문장만 덧붙였습니다.** 「규약 개정처럼 한 회차가 여러 모듈과 집필 도구에 걸치면 모듈 블록에 넣지 않고 회차 블록을 따로 씁니다」입니다. 분리가 정보를 숨기지 않게 W1-M1 블록 끝에 되돌이 안내를 달았습니다
  - [x] **막힌 지점 10건을 세 갈래로 나눴습니다.** 잠복 4건(jupytext desync, README 상호 반박에서 틀린 쪽이 labs, 줄표 72와 가운뎃점 58, worksheet 비유 열), 이번 회차 산 4건(Nav2 죽은 행과 §9.20 허위 주장, eli5의 lesson SVG 재사용, 다이어그램 라벨 선행 도입 재발, `D_obs`), 규약 도구 2건(Tier A 1자 포화, 그 상향이 W2-M1 밴드 초과를 검토 없이 해소한 것)
  - [x] **메타 교훈 둘을 담았습니다.** 동시 세션 `git add -A` 사건과 그 대가(6-3 판정 110건이 구본 기준이 되어 6-3S 44건을 다시 판정), 그리고 **검증이 검증을 반증한 둘**(SVG font-family를 정규식 버그로 판정하고 철회, `M1-7`을 HEAD 대 작업 트리 차이로 판정하고 철회)
  - [x] **요약표는 갱신하지 않았습니다.** 표 제목이 「W1 5개 + W2-M1」이라 모듈 단위이고 W1-M1 행의 값은 2026-08-01 원 집필 검증 것으로 전부 유효합니다. 같은 칸에 겹쳐 쓰면 두 시점이 섞이므로 표 아래에 회차 블록으로 가라는 주석 한 줄만 달았습니다
  - [x] 실측 재확인: subagent가 커밋 34개와 `876428d`부터 `dfd8386`까지의 범위, lint 51/0/0과 `--all` 180/49/52, jupytext 3쌍 md5 6개 불변, `01 --smoke` 1.5초와 분리비 표 4행, **`e3a640e~1`을 돌려 3행 결함 재현**, `09548b3`이 `01_stack_frequency_budget.ipynb`를 건드리지 않은 것을 직접 확인했습니다. 재현 불가한 것(mermaid-cli 파싱, Chromium 육안 확인, 렌더 바이트 수)은 계획서 인용임을 블록에 밝혔습니다
  - [x] 발견: **6-3의 「세 스크립트를 스모크와 전체로 완주」 보고를 재현할 수 없습니다.** 저장소 `.venv`에 `pandas`가 없어 `03_player_landscape.py`가 돌지 않고, 6-3S와 6-3SR도 같은 이유로 `03`을 실행하지 못했습니다. 6-3이 어느 인터프리터를 썼는지 기록이 없어 **블록의 미기록 항목으로 남겼습니다.** 나머지 두 스크립트는 세 회차 모두 완주했습니다
- [x] `notes/glossary.md` 롤업 (되짚기 표 기준) *(2026-08-31 완료)*. 되짚기 표 8개 46항목을 개념 단위로 대조해 **139행에서 160행**, 고유 137개에서 **158개**가 됐습니다
  - [x] **신규 21개.** 전부 `비유 / 메모` 열이 빈 칸(`—`)이고 **비유를 새로 만들지 않았습니다.** 회사 스택 항목만 사실 메모를 넣었습니다
  - [x] 발견: **§1.1의 제어 기초 넷(`피드백 루프`, `setpoint`, `캐스케이드 제어`, `MPC`)이 기존에는 다른 항목의 비유 셀 안에서만 언급되고 정의가 없었습니다.** §3.9가 「제어는 앵커가 아니라 가르칠 대상」으로 바꾼 것이 용어집에서도 그대로 드러난 자리입니다. 같은 성격으로 `토크나이저`와 `코드북`도 파생어(`implied codebook`, `codebook collapse`, `코드북 사용률`)만 있고 원 개념이 없었고, **`ACT`는 W2-M1 절 전체가 ACT인데도 정작 행이 없었습니다**
  - [x] **병기 8건.** 문자열이 달라도 같은 개념이라 새 행 대신 기존 행에 붙였습니다. `다중 모드`를 `다봉성`에, `2차원 격자`를 `점유 격자`에(6-3S가 본문을 바꿔 이름이 갈렸습니다), `자기회귀`를 `autoregressive`에, `예측 지평`을 `예측 horizon`에, `액션 청크`를 `action chunking`에, `proprioception`과 `BPE`는 모듈 체인만 선행
  - [x] *(계획 변경)* **`GEAR-SONIC`은 새 행을 만들지 않았습니다.** `WBC 파운데이션 정책` 행의 메모가 이미 「SONIC = 42M 파라미터, 700시간 모캡, 21,000 GPU-hours」이고 모듈도 `W1-M1 → W3-M4`라 새 행이 중복이었습니다. 용어 셀에 병기해 검색만 되게 했습니다
  - [x] **7-2가 찾은 자리 잘못 9행을 옮겼습니다.** `FAST-LIO2`, `Nav2`, `FSR-VLN`, `HMSG`, `점유 격자`, `pcd`, `hull`, `self-filtering`, `두 갈래 경로`가 W2-M2 절 끝에 붙어 있었는데 모듈 열이 전부 `W1-M1`이었습니다. 순서를 유지한 채 W1-M1 절로 옮겨 W2-M2가 31행에서 22행, W1-M1이 10행에서 **40행**이 됐습니다
  - [x] **갱신 1건: `pcd`.** 6-3S가 §7.1 라벨에서 걷어내 본문이 지금은 「3차원 점군 지도」로만 부릅니다. 행은 남기되 메모에 그 관계를 적어 이름이 갈린 것이 드러나게 했습니다
  - [x] **머리말을 고쳤습니다.** 구 머리말이 「형식: **약어/용어** — 한 줄 정의 · (있으면) 제어·ML·LLM 대응 비유 · 처음 만난 모듈」이라 **§3.9가 폐지한 비유를 여전히 형식으로 요구**했고, 그 문장 자체가 **§3.10이 금지한 줄표와 가운뎃점**을 쓰고 있었습니다. 두 가지를 함께 고쳤습니다
  - [x] 판단: **열 이름 `비유 / 메모`는 그대로 뒀습니다.** SKILL.md Phase 7-4가 glossary 스키마를 그 이름으로 명시하고 있어 여기서 바꾸면 SSOT와 어긋나고, SKILL.md 수정은 이 작업 범위 밖입니다. 기존 137행의 비유 셀도 하나도 지우지 않았습니다. 과거 기록이기 때문입니다
- [x] 이모지 컨벤셔널 커밋 *(2026-08-31 완료)*. **현재 브랜치(`main`)에서 작업했고 새 브랜치를 만들지 않았습니다.** 항목마다 계획서를 갱신하고 커밋과 푸시를 거쳐 다음으로 넘어갔습니다
  - [x] `6b24a30` 6-3S, `e3a640e` 6-3SR, `25cbf00` 7-1, `dfd8386` 7-2, `9567c64` 7-3, `64df5b3` 7-4, 그리고 이 마감 커밋
  - [x] **`git add -A`를 한 번도 쓰지 않았습니다.** 계획서의 「동시 세션 사건」이 남긴 교훈대로 커밋마다 파일을 명시해 add했습니다

### Phase 8. 확산 (2026-09-01 착수)

> 🗂 **착수 조건이 충족됐습니다.** 6-5의 학습자 검토가 「읽었고 괜찮다, 그대로 확산」으로 회신됐습니다(2026-09-01). W1-M1 파일럿의 2차 규약을 그대로 적용합니다.
>
> **작업 단위와 순서.** Phase 6과 같은 방식입니다. 항목 하나를 subagent 하나가 맡고, 항목마다 이 계획서와 `course-plan.md` 갱신과 커밋과 푸시를 거쳐 다음으로 넘어갑니다. 모듈 하나가 네 항목(재작성 · 검증 · 윤문 · 마무리)이고, 8-2의 밴드 재교정만 오케스트레이터가 사용자 승인을 받아 직접 처리합니다.
>
> ⚠️ **이번 회차 범위는 W1-M2부터 W1-M5까지 넷이고, 2026-09-02에 밴드 최종 확정(8-7)과 W1-M1 윤문(8-8)이 더해졌습니다.** W2-M1과 W2-M2는 다음 회차로 미룹니다.
>
> ⚠️ **윤문을 모듈마다 완주합니다** (2026-09-01 사용자 결정). `SKILL.md` Phase 2.5는 「구조가 확정되기 전에 문체를 다듬으면 헛일」이라며 재집필을 Phase 4까지만 하도록 규정하고 W1-M1 파일럿도 그 순서를 따랐지만, 이번에는 규약이 파일럿으로 확정된 뒤라 그 유예 근거가 약합니다. 모듈당 재작성부터 보존 검증까지 닫고 넘어갑니다. ~~**W1-M1 자신의 윤문은 여전히 미실시이고 이번 범위 밖입니다.**~~ → **2026-09-02에 8-8로 범위에 들어왔습니다.**

#### 8-0. 왜 M2를 먼저 하고 밴드를 그다음에 고치는가

**네 모듈 전부 현행 밴드를 넘길 것이 산술적으로 예상됩니다.** W1-M1 파일럿은 2차 규약 재작성으로 산문이 16,058자에서 21,902자가 됐습니다(**+36.4%**). 같은 비율을 적용한 추정입니다.

| 모듈 | Tier | 현재 산문 | +36.4% 추정 | 밴드 상한 | 예상 초과 |
|---|---|---|---|---|---|
| W1-M2 | A | 17,477 | ~23,840 | 23,000 | +840 |
| W1-M3 | C | 15,597 | ~21,270 | 17,000 | +4,270 |
| W1-M4 | C | 18,387 | ~25,080 | 17,000 | +8,080 |
| W1-M5 | B | 17,486 | ~23,850 | 18,000 | +5,850 |

밴드를 지키려고 본문을 깎으면 **본문이 얇아진 것이 2차 불만의 원인이었으므로 같은 실패를 반복합니다**(§2.1, `course-plan.md` §9.19). 그렇다고 실측 표본 하나로 밴드를 고치면 §9.11과 §9.15가 두 번 겪은 재교정을 세 번째로 반복하게 됩니다.

**그래서 W1-M1과 같은 Tier A인 W1-M2를 먼저 재작성해 표본을 둘로 만든 뒤 밴드를 1회 교정합니다.** 이것은 §9.11과 §9.19가 이미 두 번 쓴 파일럿 절차의 반복이고, W1-M3과 W1-M4의 티어 재배정(§9.15 승인 대기)도 그 자리에서 함께 판단합니다. **M3·M4·M5는 교정된 밴드로 씁니다.**

#### 8-1. W1-M2 재작성 (Tier A, 밴드 파일럿)

- [x] **8-1a. 재작성** *(2026-09-01 완료)* — lint **PASS 21 · FAIL 9 · WARN 9 → PASS 50 · FAIL 1 · WARN 0**. 남은 FAIL은 **분량 하나뿐**입니다. `eli5.md` 신설(산문 4,535자, §2.1 밴드 통과), `deep-dive.md` 8,813 → 9,500자, SVG **8장** 신설(lesson 5, eli5 1, 공용 2). **§0~§7과 소절 번호를 100% 보존해 하류 참조 수정이 0건**이고 `practice/`와 `labs/`는 한 글자도 바뀌지 않았습니다(git status 확인). `est_reading_min` 150분이 §0 「소요」와 목차 표 합계와 **3중 정합**하고 lint 초독 추정 154분과 편차 3%입니다

  > 📏 **8-2에 넘기는 실측: 산문 지표가 규약이 명령한 변환을 순수 증가로 세고 있습니다** (2026-09-01)
  >
  > | 모듈 | 산문 | 총 문자 | 보호 구간 |
  > |---|---|---|---|
  > | W1-M1 재작성 전 → 후 | 16,058 → 20,991 (+30.7%) | 32,893 → 32,334 (**−1.7%**) | 51.2% → 35.1% |
  > | W1-M2 재작성 전 → 후 | 17,477 → **29,749** (+70.2%) | 36,683 → 42,354 (+15.5%) | 52.4% → **33.0%** |
  >
  > 두 표본 다 보호 구간이 약 51%에서 33~35%로 떨어집니다. 2차 규약은 3열 절 시작 용어표를 폐지하고 ASCII 블록도를 그림으로 옮기고 표를 불릿과 산문으로 풀라고 **명령**하는데, lint의 산문 정의(총 문자 − 코드펜스 − `$$` − 표 행 − frontmatter)는 그 변환을 전부 산문 증가로 셉니다. W1-M2는 산문 +12,272 중 **6,601자가 구조물 전환분**이고 순수 신규 저작은 5,671자입니다. 그 5,671자는 2차 규약이 신설을 요구한 구조물의 합(도입 산문 8개 ~1,840 + 워크드 예제 8개 ~2,600 + 헷갈림 박스 3개 ~390 + 그림 캡션 7개 ~770 + 자기 설명 프롬프트 ~250 ≈ 5,850)과 거의 일치합니다.
  >
  > **두 모듈의 총 문자 거동이 갈린 이유는 따로 봐야 합니다.** W1-M1은 총 문자가 줄고 W1-M2는 15.5% 늘었습니다. 8-1a 보고는 「W1-M1이 1차 개정 때 이미 9,184자를 덜어낸 상태였기 때문」이라 설명했지만 **W1-M2는 1차 때 20,371자를 덜어내 더 많이 뺐으므로**(§9.13) 그 설명은 성립하지 않습니다. 실제 차이는 모듈 규모입니다. W1-M2는 대절 8개에 소절 26개(W1-M1은 7개와 20개)이고 처음부터 가르쳐야 할 개념이 20개를 넘습니다
  >
  > **8-1a가 올린 제안 둘.** ① 밴드가 재는 대상을 산문 문자에서 총 문자로 바꾸거나 병기할 것. ② 산문 기준을 유지한다면 Tier A 상한은 최소 30,000이 필요할 것. **둘 다 8-2가 판단하며 아직 채택되지 않았습니다**
- [x] **8-1b. 검증** *(2026-09-01 완료)* — 재작성자가 아닌 제3자가 구본 대조로 독립 검증했습니다. **S1 5건, S2 6건, S3 12건**을 찾았고 오케스트레이터가 S1을 전수 재확인했습니다. 통과한 것도 넓습니다. arXiv와 URL 12개 전량 보존, 절 번호 실재성 100%, SVG 8장 XML과 `viewBox`와 다크 모드 전부 통과, 본문 비유 잔존 0건, 설명 회피 문장 0건, `practice/`와 `labs/` 실측값 40여 개 전량 일치, `sources_checked` 유지, `prereq` 정직성 확인입니다

  > 🔍 **「하류 수정 0건」 보고가 반증됐습니다. 절 번호는 맞았고 수식 번호가 틀렸습니다** (2026-09-01)
  >
  > 8-1a는 「§0~§7과 소절 번호를 100% 보존해 하류 참조 수정 0건」이라 보고했고 **절 번호에 대해서는 사실입니다.** 그런데 재작성이 §5.5에 PD 서보 식을 `eq. 2`로 새로 끼워 넣으면서 구본의 `eq. 2`였던 $k_v = 2\zeta\sqrt{k_p M_{eff}}$가 `eq. 3`으로 밀렸고, `practice/` 참조 4곳이 엉뚱한 식을 가리키게 됐습니다.
  >
  > **SKILL.md Phase 2.5의 grep 절차가 이 유형을 못 잡습니다.** 절차가 지시하는 것은 `grep -rn "§[0-9]"`와 `grep -rn "{모듈ID} §"` 둘인데 **수식 번호는 `§`를 달지 않습니다.** 같은 자리에서 「문구를 인용한 참조는 grep에 안 걸린다」고 경고해 두었지만 수식 번호는 그 경고에도 없었습니다.
  >
  > **더 나쁜 것이 하나 더 있습니다.** `practice/03_g1_sin_wave.py`는 파일 헤더에서 `\tag{1}`로 **자체 수식 번호 체계**를 선언하고 `eq.(1)~(4)`를 `practice/README.md`와 `labs/` 두 파일까지 퍼뜨립니다. lesson을 가리키는 것은 L230 하나뿐인데 **같은 표기가 두 개의 서로 다른 번호 공간을 뜻합니다.** 기계적으로 일괄 치환했다면 멀쩡한 참조 여덟 곳을 망가뜨릴 뻔했습니다.
  >
  > **제안: lesson을 가리키는 수식 참조는 `lesson §N eq.(M)`처럼 접두를 강제하고, 재집필 grep 절차에 수식 번호를 추가할 것.** 규약 개정이므로 승인 대기로 남깁니다

  > ⚠️ **산문이 70% 늘어난 자리에서 정확히 예상되는 실패가 났습니다** (2026-09-01)
  >
  > S1 5건 중 **4건이 재작성이 새로 만든 사실**입니다. ① §7.2에서 구본의 「G1과 H1 태스크가 4개」를 「사람 모양 로봇 태스크가 4개」로 일반화했는데 `deep-dive` §8.1의 19개 목록을 세면 휴머노이드는 **10개**라 틀린 문장이 됐습니다. ② 그림과 본문이 같은 메모리를 **약 100배 다르게** 말했고(본문 33 MB, 그림 수 GB) **구본에는 양쪽 다 없습니다.** 본문의 「mjData 하나에 실수 2000개라고 두면」이라는 임의 가정이 원인입니다. ③ 그림의 무릎 순번이 본문과 반대이고 괄호 안과도 자기모순이었습니다. ④ `gainprm[0]=kp`와 `biasprm=[0,-kp,-kv]` 설명이 이관 없이 사라졌는데 `practice/`가 그것을 가리켰습니다.
  >
  > **「밴드를 넘겨도 자르지 말라」는 지시가 날조를 막지 못합니다.** 규약이 요구하는 워크드 예제와 도입 산문을 채우려고 새 문장을 쓰는 자리가 곧 위험 지점입니다. **남은 세 모듈의 재작성 지시서에 「확인할 수 없는 수치는 쓰지 말고 구본 서술로 되돌릴 것」을 명시적 제약으로 넣습니다**

- [x] **8-1bR. 검증 지적 수정** *(2026-09-01 완료)* — S1 5건과 S2 6건을 전부 고치고 S3 12건도 전부 처리했습니다. 날조된 수치는 **되살리지 않고 삭제**했고(메모리 추정 양쪽), 워크드 예제는 본문 실측 배열 길이만 쓰는 검증 가능한 산술로 교체했습니다. 이관 없이 사라졌던 것 넷(`gainprm`과 `biasprm`, `unitree_mujoco` 지원 로봇 10종, `xpos`와 `xquat` 차원, MJX solver 표기)을 `deep-dive.md`로 이관했고, 퀴즈 하위 물음 5개와 팀 질문 근거 목록 2건을 복원했습니다. SVG는 8장 전부 텍스트 폭을 재서 넘침이 지목된 두 줄뿐임을 확인하고 분할했습니다.
  **`practice/`와 `labs/`는 참조 정정 4건뿐**입니다(`eq.(2)`→`eq.(3)` 둘, `lesson §5.5`→`deep-dive §11` 하나, `lesson §6.4`→`deep-dive §7` 하나). `jupytext`가 없어 `.ipynb` 문자열을 직접 맞추고 JSON 유효성과 `.py` 일치와 출력 셀 공백을 검증했습니다.
  lint는 **PASS 50 · FAIL 1 · WARN 0 유지**이고 남은 FAIL은 분량뿐입니다. **산문이 29,749 → 31,375자로 1,626자 늘었습니다**(퀴즈 복원 약 900, 연결 문장 약 400). `eli5.md`는 4,535 → 4,985자입니다
- [x] **8-1c. 윤문** *(2026-09-01 완료, run `2026-09-01-001`)* — v1.6 `register: course` · strict · `tier: A`. `lesson.md` **등급 A**(`accept_with_note`), `eli5.md` 등급 B. **S1 잔존 0, 신규 유입 0**이고 종합 burden 122 → 19(**84.4%↓**)입니다. `traceable_rate`는 둘 다 **100%**(24/24, 22/22)라 밴드를 채우려는 편집이 섞이지 않았습니다. 산문은 31,375 → **31,355자**, eli5는 4,985 → 4,953자입니다.
  **철칙 보존을 오케스트레이터가 독립 전수 검증했습니다.** 숫자 토큰 **141종이 빈도까지 전량 동일**, URL 12개 동일, 코드펜스 30개 동일, H2와 H3 헤딩 50줄 완전 동일, `(eq. 1)`부터 `(eq. 4)` 유지입니다. lint는 **PASS 50 · FAIL 1 · WARN 0**으로 베이스라인과 같고 **회귀 0**입니다. 수정 파일은 `lesson.md`와 `eli5.md` 둘뿐입니다.
  중단이 한 번 있었습니다. 호스트 절전으로 API가 끊겼는데 **Phase A 산출물이 `_workspace/`에 온전했고 파일은 미수정 상태**여서 같은 `run_id`로 Phase B부터 재개했습니다

  > ⚖️ **두 검증자가 정면으로 충돌했고 fidelity를 채택했습니다** (`eli5.md` L45, finding `f024`)
  >
  > `content-fidelity-auditor`는 「그래서」가 [명령 목록에서 몸통 자리가 빠진다] → [무릎 번호가 목록마다 다르다]는 **추론 단계의 유일한 표지**라 롤백이 필수(severity medium)라 했고, `naturalness-reviewer`는 같은 문단에 문두 접속부사가 3회 몰린다며 재적용을 권고(S2)했습니다. 적용하면 eli5도 등급 A였습니다.
  >
  > **스킬의 불문율이 「의미 불변이 최상위」이므로 fidelity를 택했습니다.** 자연스러움 S2가 medium 심각도의 의미 훼손을 덮지 못하고, **등급을 올리려고 의미를 깎는 것은 본말전도**입니다. 리뷰어의 대안(앞쪽 「그래서」를 대신 수정)은 finding이 없는 원문 구간이라 「탐지 없는 구간을 건드리지 않는다」 원칙에 걸려 하지 않았습니다. `f024`는 미해결로 기록해 남깁니다

  > 📊 **§9.12의 「재집필본은 라인율이 구조적으로 낮다」가 두 번째 표본으로 재현됐습니다**
  >
  > | 문서 | 원문 raw burden | 산문 라인 변경률 |
  > |---|---|---|
  > | W2-M1 (구 표준 문서) | 501.0 | 35.27% |
  > | W1-M1 (1차 규약 재집필본) | 61.7 | 17.67% |
  > | **W1-M2 (2차 규약 재작성본)** | **48** | **6.4%** |
  >
  > **원문 burden이 낮을수록 라인율이 낮습니다.** 라인 6.4%는 §1의 하한 20%를 크게 밑돌지만 **저윤문이 아닙니다.** §1이 정한 저윤문의 정의는 `주지표 하한 미달 그리고 S1 잔존`인데 S1이 0이고, 재집필본에는 하한 자체를 적용하지 않습니다. 오히려 **2차 규약 재작성본이 1차보다 원문 burden이 더 낮다**는 것은 집필 단계에서 AI 티가 더 걷혔다는 뜻입니다
  >
  > 리뷰어가 분류학자에게 신규 패턴 후보 **R-1 (rewrite-induced lexical echo)** 을 올렸습니다. 치환 어휘가 앞뒤 5줄 안의 기존 어휘와 부딪히는지 검사하는 가드이고, 이번에 `정작`과 `먼저` 충돌 두 건을 실제로 만든 실패 유형입니다. 후속 회차 검토 대상입니다
- [x] **8-1d. 마무리** *(2026-09-01 완료)* — `course-plan.md` §5의 W1-M2 다섯 칸 중 **네 칸이 `[x]`**가 됐습니다(lesson, practice, labs, 윤문). 남은 것은 학습자 실행검증뿐이라 **저장소에서 가장 앞선 모듈**입니다. §0 대시보드의 W1 트랙 행과 마지막 갱신일과 다음 작업 문단을 고쳤고, 회차 근거로 **§9.21을 신설**했습니다

#### 8-2. 밴드 재교정과 티어 판단 (오케스트레이터, 사용자 승인 필요)

- [x] **8-2. 밴드 재교정과 티어 판단** *(2026-09-01 완료, 사용자 승인)* — 승인받은 개정 다섯입니다. ① **밴드 잠정 상향** Tier A `21,000~32,000` · Tier B `18,000~26,000`이고 **2차 규약 재작성본(`eli5.md` 보유)은 초과를 WARN**으로 냅니다. 표본이 둘뿐이라 이 숫자로 FAIL을 내면 남은 모듈 집필이 미확정 숫자에 끌려가고, **밴드를 지키려고 설명을 깎는 것이 §9.19가 진단한 2차 불만의 원인**이었기 때문입니다. 7개 전부 재작성된 뒤 확정하고 FAIL로 복귀합니다. ② **Tier C 폐지.** W1-M3와 W1-M4와 W4-M1을 B로 옮겨 `A 8 + B 11 + 캡스톤 1 = 20`이 됐고, §9.15가 3주간 승인을 기다리던 재배정과 W1-M4에 붙어 있던 「판단이 날 때까지 남겨둔다」 FAIL이 함께 닫혔습니다. ③ **§1 윤문 판정 문턱 정리.** `course/**/lesson.md`는 보호 구간과 무관하게 산문 라인 판정으로 고정했습니다. ④ **수식 참조 접두 규약**(`lesson §N eq.(M)`)과 재집필 grep 절차 보완. ⑤ **밴드 숫자 3중 불일치 구조적 차단.** 근거는 `course-plan.md` §9.21

  > 🔧 **승인 범위 밖에서 둘을 더 했고 근거를 남깁니다** (2026-09-01)
  >
  > **첫째, 집필 도구 네 곳의 밴드 숫자를 새 값으로 고치지 않고 아예 지웠습니다.** 정본(lint)이 A 상한 23,000일 때 `SKILL.md`와 `pai-agent.md`와 `authoring-checklist.md`와 `lesson-template.md`가 전부 구 값 21,000을 들고 있었습니다. **3중 불일치의 세 번째 재발**이고 §9.19가 고친 지 한 주 만입니다. 특히 `lesson-template.md`는 「이 숫자는 편의용 사본이고 복사가 3중 불일치를 두 번 재발시켰다」고 경고하면서 같은 짓을 하고 있었습니다. **새 값으로 고치면 네 번째 재발이 예약되므로** 숫자를 빼고 lint를 가리키게 했습니다. 판정 지표 사본도 같은 구조여서 humanize 계열 네 곳을 함께 맞췄습니다.
  >
  > **둘째, lint의 셀 길이 검사가 바이트를 세던 것을 고쳤습니다.** macOS 기본 awk의 `length()`가 바이트를 세서 「120자 상한」이 한글에서 약 40자 상한으로 동작했고, **같은 파일이 환경마다 다른 판정**을 받았습니다. python3로 옮겼고 저장소 전체 `--all`이 **FAIL 45 → 40**이 됐습니다. 다섯 건이 전부 오탐이었습니다. **이 회차의 lint 수치는 전부 정정 후 값입니다.** 상세는 §9.21

- [ ] **8-2R. 마스터플랜의 다섯 번째 밴드 사본** *(승인 대기)* — `docs/physical-ai-4week-master-plan.md:449`가 `Tier A 15,000~21,000 / B 13,000~18,000 / C 12,000~17,000`을 들고 있습니다. **두 단계 stale**(2026-08-30 A 상향과 이번 개정 모두 미반영)이고 **폐지된 Tier C까지 명시**합니다. 개정 5와 같은 방식으로 숫자를 지우고 정본을 가리키게 하는 것이 일관되지만, **마스터플랜은 SSOT라 승인 없이 고치지 않았습니다**

#### 8-3. W1-M3 재작성 (Tier C, 재교정된 밴드 적용)

- [x] **8-3a. 재작성** *(2026-09-01 완료)* — lint **PASS 21 · FAIL 8 · WARN 9 → PASS 50 · FAIL 0 · WARN 1**이고 남은 WARN은 분량 하나뿐입니다. `eli5.md` 신설(4,336자), SVG **8장** 신설, **한글 정렬 ASCII 블록도 2개(36줄 adaLN-zero, 25줄 action-head)를 SVG로 대체**해 ASCII가 0개가 됐습니다. 절 번호와 수식 번호 모두 이동 없이 **하류 참조 수정 0건**이고 `deep-dive.md`와 `practice/`와 `labs/`가 무수정입니다. `est_reading_min`은 **38분 신고 대 lint 추정 119분(3.1배 어긋남)**이 150분 3중 정합으로 닫혔습니다. 「선수 지식」의 **「상태공간 모델과 공분산 전파, 칼만 필터를 보유 배경으로 가정」 행을 삭제**했습니다. §3.9 직접 위반이었고 제어 개념은 `<details>` 💡 박스 5개로 내려 각각 처음부터 설명합니다
- [x] **8-3b. 검증** *(2026-09-01 완료)* — **S1 3건, S2 6건, S3 8건**. 오케스트레이터가 S1을 전수 재확인했습니다. 통과 범위도 넓습니다. 자진 신고 수치 12종 전량 검산 통과, 미신고 신규 수치 0건, 구본에서 사라진 숫자 0건, 문구 참조 6종과 deep-dive 인용 2건 생존, W1-M4의 W1-M3 참조 20곳 중 17곳 정상, SVG 8장 XML과 넘침 전부 통과, ASCII의 텐서 shape 전량 이관, 근사 중복 실질 0건입니다

  > 🔴 **수식이 틀렸습니다. 이 회차 최악의 결함입니다** (2026-09-01)
  >
  > DDIM 갱신식의 노이즈 계수가 구본 $\sqrt{1-\bar\alpha_{t-1}}$에서 신본 $\sqrt{1-\bar\alpha_t}$로 바뀌었습니다. **display 수식 7개 중 구본과 달라진 것이 이 하나뿐**이라 우연한 잔존이 아니라 재작성이 도입한 단독 변경입니다.
  >
  > 근거가 넷입니다. ① DDIM(2010.02502) eq.(12)에 $\sigma_t=0$을 넣으면 $t-1$입니다. ② 같은 폴더 `deep-dive.md:111`의 일반형이 $\sqrt{1-\bar\alpha_{t-1}-\sigma_t^2}$입니다. ③ `practice/02_samplers_compare.py:213`이 `(1 - ab_prev - sigma**2).sqrt()`로 `t-1`을 씁니다. ④ **이 계수여야 $x_{t-1}$의 분산이 $1-\bar\alpha_{t-1}$이 되어 forward 주변분포가 보존되고, 그래야 §5.1 본문의 「학습된 신경망을 그대로 쓴다」가 성립합니다.**
  >
  > ⚠️ **윤문(8-3c)에 그대로 들어갔으면 통과했을 것입니다.** §1의 윤문 철칙이 LaTeX 수식을 「한 글자도 바뀌면 안 되는 것」으로 **보존**하라고만 규정하기 때문입니다. **보존은 정확성을 보장하지 않습니다.** 재작성이 수식을 건드릴 수 있다는 것이 이번에 처음 실증됐으므로, **재작성 단계에서 display 수식을 구본과 전수 대조하는 절차**가 필요합니다

  > 🔗 **내용이 사라져 다른 모듈이 깨졌습니다. 직전 회차와 같은 유형입니다** (2026-09-01)
  >
  > §5.1 비교표에서 SDE와 ODE 축이 삭제됐습니다(전 문서 기준 `DPM-Solver` 1→0, `Euler` 3→0, `probability flow` 4→1). **절 번호는 100% 보존됐는데 그 절의 내용이 사라졌고**, W1-M4가 정확히 그 자리를 셋이나 가리킵니다(`04-flow-matching/deep-dive.md:26`의 「ODE로 넘어오는 다리가 여기」, `lesson.md:110` mermaid 노드, `lesson.md:404` 비교표).
  >
  > **`deep-dive.md`가 무수정이라 본문에서 뺀 것이 갈 곳이 없었습니다.** 그 결과 사실 셋(`1D 시간 위치 인코딩`, in-context의 `최종 레이어 전 제거`, `DualMap`)이 저장소에서 소실됐습니다. 재집필 규약의 「삭제하지 말고 이관」이 **이관할 곳을 열어두지 않으면 지켜지지 않는다**는 뜻입니다

  > 📐 **Tier 역전의 원인을 규명했습니다. 티어가 모델링하지 않는 축이 둘 있습니다** (2026-09-01)
  >
  > | | W1-M1 (A) | W1-M2 (A) | **W1-M3 (B)** |
  > |---|---|---|---|
  > | 산문 | 21,902 | 31,355 | **37,250** |
  > | display 수식 | 4 | 4 | **7** |
  > | 워크드 예제 절 | 3 | 3 | **6** |
  > | 💡 비유 박스 | 1 | 1 | **5** |
  >
  > 검증이 신고 회계를 실측으로 검산해 **오차 0.1%**로 맞췄습니다(구조물 14,990 + §3.9 개념 설명 5,609 = 증가분 20,599). 근사 중복은 §9.15가 W1-M4에서 잡은 38건 유형이 **0건**이고 본문과 `deep-dive.md`의 축자 중복은 **95자**뿐입니다. 판정은 **「규약을 지킨 결과라 정당, 군살 1,230자」**입니다.
  >
  > M2 대비 M3의 초과분을 실측하면 💡 박스 4개 추가 +2,500, 워크드 예제 3개 +1,200, 수식 3개의 기호 정의 +940, 대절 1개 +400으로 **합계 약 +5,040자**이고 `31,355 + 5,040 = 36,395`가 실측과 거의 같습니다. **M3는 W1에서 유일하게 유도를 끝까지 따라가는 모듈이자 구본이 제어 앵커에 가장 크게 기대던 모듈**이라 두 축 모두에서 최댓값입니다.
  >
  > **티어는 「주제 중요도」 한 축만 갖고 있고 수식 밀도와 앵커 부채를 모델링하지 않습니다.** 검증이 밴드에 「수식 밴드」를 곱하자고 제안했고(display 수식 6개 이상이면 상한 +6,000) 표본 셋이 단조 관계입니다. **표본 3개로 밴드를 또 고치면 한 세션에 세 번째가 되므로 M4와 M5 실측을 모아 최종 확정 때 판단합니다**

- [x] **8-3bR. 검증 지적 수정** *(2026-09-01 완료)* — S1 3건과 S2 6건을 전부 고치고 S3 8건도 전부 처리했습니다. **display 수식 7개가 구본과 전량 바이트 동일**로 돌아왔고(프로그램 대조 확인), §5.1의 SDE와 ODE 축이 복원돼 `DPM-Solver` 2회 `Euler` 2회 `probability flow` 4회가 살았으며 W1-M4 참조 셋이 실재하는 본문을 가리킵니다. 되살린 용어는 §3.8에 맞춰 표에 넣기 전 산문 두 문단으로 정의했습니다. 소실된 사실 셋을 복원했고, **`attention`과 `MLP`와 `잔차 연결`이 정의 없이 표 셀에서 처음 등장하던 것**(구본부터 있던 위반)을 §6.1 끝에서 세 문장으로 풀었습니다. 군살은 지시한 네 곳에서 **652자**만 줄였습니다. lint **PASS 50 · FAIL 0 · WARN 1 유지**이고 산문은 36,196 → **37,250자**입니다. 필수 복원이 군살 정리보다 컸습니다
- [x] **8-3c. 윤문** *(2026-09-01 완료, run `2026-09-01-002`)* — strict · `register: course` · `tier: B` · **등급 B**(`accept_with_note`). **S1 잔존 0**, burden 149 → 11(**92.6%↓**), `traceable_rate` **100%**, 산문 라인 10.77%(하한 미적용), fidelity `conditional_pass`에 롤백 1건. 산문 37,250 → **37,196자**, eli5 4,336 → 4,299자.
  **이 회차의 무게중심은 수식 보존이었습니다.** 바로 앞 단계가 DDIM 계수 오류를 고쳐 놓은 상태라 윤문이 다시 깨뜨리는지가 관건이었고, 오케스트레이터가 독립 전수 검증했습니다. **display 수식 7개 바이트 동일, 인라인 LaTeX 154종 빈도까지 동일, 숫자 토큰 차이 0, 헤딩 전량 동일**입니다. 8-2가 복원한 `DPM-Solver`와 `Euler-Maruyama`와 `probability flow`와 `SDE`도 전건 보존됐습니다. lint 회귀 0.
  `eli5.md`가 전체 문자 4.73%로 하한을 0.27pt 밑돕니다. **저윤문이 아닙니다** — S1 잔존 0에 38건 전건 해소이고, 롤백 전에는 5.37%로 밴드 안이었는데 **fidelity 롤백이 텍스트를 원문 쪽으로 되돌려** 내려간 값입니다. 밴드를 채우려 편집을 더하면 `traceable_rate` 100%가 깨지므로 하지 않았습니다.
  두 검증자 충돌 1건도 **fidelity를 택했습니다**(eli5 L54, 「주제다」가 「다뤄진다」로 약화된 자리). 직전 회차와 같은 판단입니다.
  분류학자 에스컬레이션 3건 중 실질적인 것은 **`C-12b 쉼표 결핍`** 입니다. eli5의 쉼표 포함 문장이 19.1% → 4.1%로 인간 baseline의 6분의 1까지 떨어졌는데 **C-12에 하한이 없어 어느 지표에도 걸리지 않습니다.** 후속 회차 검토 대상입니다
- [x] **8-3d. 마무리** *(2026-09-01 완료)* — `course-plan.md` §5의 W1-M3 다섯 칸 중 **네 칸이 `[x]`**가 됐습니다. `practice/`와 `labs/`는 이번 회차에 **한 글자도 바뀌지 않아** 기존 실행검증 자산이 그대로 살아 있습니다. Tier 표기를 B로 갱신했습니다

#### 8-4. W1-M4 재작성 (Tier C, 밴드 초과가 이미 FAIL로 남아 있음)

- [x] **8-4a. 재작성** *(2026-09-01 완료)* — lint **PASS 22 · FAIL 8 · WARN 8 → PASS 50 · FAIL 0 · WARN 1**. §9.15가 「지우려고 문서를 자르지 말라」며 남겨둔 밴드 FAIL은 8-2의 Tier C 폐지로 닫혔습니다. `eli5.md` 신설 4,815자, SVG **8장**, `deep-dive.md` 6,703 → 7,592자. **`practice/`는 완전 무변경**이라 W1-M3 practice 재사용과 `artifacts/W1-M3/02_results.csv` 연결이 그대로입니다. `est_reading_min`은 **41분 신고 대 lint 추정 136분(3.3배 어긋남)** 이던 것이 180분 3중 정합으로 닫혔습니다
- [x] **8-4b. 검증** *(2026-09-01 완료)* — **S1 1건, S2 4건, S3 10건.** 재작성자가 자체 검증을 돌려 「S1 0건」이라 보고했으나 **틀렸습니다.** 자체 검증은 재작성자와 같은 맥락과 가정을 공유해 자기 맹점을 보지 못합니다. 통과 범위는 넓습니다. 구본 산문 418문장 전수 대조에 **소실 0건**, 신본 전용 숫자 34개 **전량 검산 통과에 날조 0건**, 하류 참조 **깨짐 0건**(W1-M3 practice 재사용 연결 생존, 타 모듈 인입 5건 유효), SVG 8장 XML과 넘침과 z-order 전부 통과, `prereq` 정직, `sources_checked` 유지입니다

  > 🔢 **「10배」가 두 종류였고 재작성자가 그 구분을 못 했습니다** (2026-09-01)
  >
  > 재작성자가 구본의 「한 자릿수」와 「10배」를 문서 자신의 값으로 재계산해 **2.60배와 3.81배**로 정정하고 세 곳에 반영했습니다. 재집필 규약이 금지한 「사실과 수치의 신규 생성」에 걸리는 행위라 오케스트레이터가 산수를 직접 검산했습니다.
  >
  > | $K$ | 루프 밖 $\tau_{VLM}+K\tau_{head}$ | 루프 안 $K(\tau_{VLM}+\tau_{head})$ | 비 |
  > |---|---|---|---|
  > | 4 | 112.8 ms | 292.8 ms | **2.596** |
  > | 10 | 192 ms | 732 ms | **3.812** |
  > | $\infty$ | — | — | **5.545 (상한)** |
  >
  > **$\tau_{head}=13.2$ ms에서는 어떤 $K$로도 10배가 나오지 않습니다.** 정정은 산수로 옳습니다. **그런데 구본의 「10배」에는 두 종류가 있었습니다.**
  >
  > - **(A) 고정 $K$에서의 지연비** (구본 §7.2, 오해 3) — **구본이 틀렸고 정정이 옳습니다**
  > - **(B) §8.2 NFE 상한 표의 오차 배율** (구본 §9.4⑤, M4-4) — 백본이 루프 안이면 상한식이 바뀌고, §8.1과 §8.2가 쓰는 **가정값 $\tau_{head}=3$ ms** 기준으로 L4 5 Hz가 46 → 3(15.3배), 10 Hz가 13 → 1(13배)입니다. $K\approx18$이면 정확히 10배입니다. **구본이 옳았습니다**
  >
  > 재작성자가 이 구분 없이 세 자리를 고치고 한 자리를 남겨 **문서 안에 서로 모순되는 수치 넷이 공존**하게 됐고, 그중 **핵심 아키텍처 그림이 「추론 비용이 K배로 뜁니다」**라고 단언했습니다. 구본의 10배도 아니고 새 계산의 2.6배도 아닌 **제3의 틀린 값**이며, 방금 고친 바로 그 양을 그림이 다시 틀리게 말한 것입니다.
  >
  > **교훈: 「구본이 틀렸다」는 판단은 무엇을 재는 값인지 먼저 확정한 뒤에 내려야 합니다.** 같은 문서 안에서 같은 이름의 수치가 서로 다른 양을 잴 수 있습니다

- [x] **8-4bR. 검증 지적 수정** *(2026-09-01 완료)* — S1 1건과 S2 4건을 고치고 S3 10건도 **전부** 처리했습니다. 수정 목표를 「한쪽으로 통일」이 아니라 **「각 자리가 무엇을 재는지 밝히기」**로 잡아, (A)는 2.6~3.8배로 두고 (B)는 「3 ms/NFE 기준」을 박아 살렸습니다. 「청크」의 역참조를 닫아 **첫 등장이 정의 줄 자신**이 됐고, 가정값 60 ms를 「실측값」이라 부르던 것을 고쳤습니다(이 모듈이 「무엇이 가정이고 무엇이 측정인가」를 가르치는데 §7.2가 그 구분을 무너뜨리고 있었습니다).
  **수정자가 범위 밖에서 하나를 더 잡았습니다.** `labs/README.md:1008`과 `labs/worksheet.md:795`가 A-1과 **똑같은 거짓 수치**(「예산이 10배로 뜁니다」)를 갖고 있었고 두 파일 다 자기가 lesson의 미러라고 선언합니다. 그대로 두면 **학습자가 답안에 틀린 수를 옮겨 적으므로** 참조 정정으로 판단해 고쳤습니다. `notes/questions-for-team.md`의 M4-4도 값은 (B)라 되돌리지 않고 기준만 명시했습니다.
  lint **PASS 50 · FAIL 0 · WARN 1 유지**, **display 수식 11개 구본과 전량 동일**, 산문 43,556 → **43,847자**입니다. `practice/`는 끝까지 완전 무변경입니다
- [x] **8-4c. 윤문** *(2026-09-02 완료, run `2026-09-02-001`)* — strict · `register: course` · `tier: B` · `lesson.md` **등급 A**(`accept_with_note`), `eli5.md` 등급 B. **S1 잔존 0, 신규 유입 0**이고 burden은 lesson 66 → 6(**90.9%↓**), eli5 92 → 25(72.8%↓)입니다. `traceable_rate`는 둘 다 **100%**(39/39, 32/32). 산문은 43,847 → **43,710자**, eli5는 4,815 → 4,735자입니다. fidelity는 1차 `conditional_pass`에서 **롤백 7건** 반영 후 재감사 **둘 다 `full_pass`**입니다. lint **PASS 50 · FAIL 0 · WARN 1 유지, 회귀 0**.
  **이 회차의 무게중심은 8-4bR이 방금 고쳐 놓은 「10배」 두 종류 구분이 살아남는지였습니다.** 오케스트레이터가 윤문 전 기준선을 떠 두고 독립 전수 대조했습니다. **display 수식 11개 바이트 동일, 인라인 LaTeX 223종 381회 동일, 숫자 토큰 252종 1,707회 다중집합 완전 일치, 헤딩 43줄 문자열 동일, arXiv 15종과 코드펜스 10개와 frontmatter 293자 동일**입니다. 「3 ms/NFE 기준」을 비롯한 구분 문구 12건이 전건 보존됐고 변경 파일은 `lesson.md`와 `eli5.md` 둘뿐이라 `practice/`는 8-4a부터 끝까지 완전 무변경입니다.
  **위험이 실현되지 않은 이유가 설계였습니다.** 탐지기가 「10배」 관련 8줄을 처음부터 위험 구간으로 전량 제외해 finding이 0건이었고, 그래서 정상 절차만 지키면 구분이 뭉개질 경로 자체가 없었습니다. 재현 가능한 안전장치이므로 남은 모듈에도 같은 방식을 적용합니다.
  **두 검증자 충돌 2건은 둘 다 fidelity를 택했고 8-1c와 8-3c에 이어 세 회차 연속입니다.** 하나는 인과 연결어미 소실 건이고 naturalness가 재검증에서 **자신의 권고를 철회**했습니다. 다른 하나는 eli5 볼드 건으로 **fidelity의 「볼드 집합 보존」과 J-1의 「볼드 밀도 자체가 AI 티」가 논리적으로 양립 불가**합니다. 라운드로 풀 수 없고 규약 결정이 필요합니다.
  `eli5.md` 등급 B의 **잔존 S2 6건 중 윤문가에게 귀속되는 것이 0건**입니다(2건은 fidelity 강제 롤백이 되살린 것, 2건은 리뷰어 자신의 보존 지시, 2건은 해소 불가한 구조적 충돌). eli5 전체 문자 3.07%가 하한을 1.93pt 밑돌지만 **S1 잔존 0이라 §1의 저윤문 정의에 해당하지 않고**, 밴드를 채우는 편집은 하지 않았습니다. 8-3c의 4.73%에 이어 두 회차 연속입니다.
  **분류학자 에스컬레이션 7건 중 실질적인 것은 셋이고 하나는 신규 패턴 승격 후보입니다.** `R-2 재작성 유발 지시 단절` — 윤문이 지운 문장이 뒤쪽 **미편집** 문장의 선행사나 개수를 유일하게 공급하던 경우입니다. C-6과 D-3과 C-7의 처방이 전부 「예고·선언 삭제」라 구조적으로 유발되고 유발 지점과 발현 지점이 떨어져 있어 국소 점검으로는 잡히지 않는데, 이번에 탐지기와 감사관이 **독립적으로 같은 자리를 지목**해 실증됐습니다. 둘째는 8-3c가 제기한 **`C-11` ↔ `C-12b` 처방 충돌의 실증**입니다(C-11 쉼표 삭제가 두 자리에서 C-12b를 직접 생성). 셋은 이 저장소가 아니라 `humanize-korean` 스킬 소관이라 **다음 회차 검토 대상으로 넘깁니다**
- [ ] **8-4d. 마무리**

#### 8-5. W1-M5 재작성 (Tier B, 회사 스택 L3 본체)

- [x] **8-5a. 재작성** *(2026-09-02 완료)* — lint **PASS 20 · FAIL 9 · WARN 10 → PASS 50 · FAIL 0 · WARN 1**입니다(이 자리에 적혀 있던 재작성 전 값 「FAIL 10 · WARN 9」는 FAIL과 WARN이 뒤바뀐 것이었고 8-4d가 잡았습니다). 남은 WARN은 **분량 하나뿐**입니다. 산문 17,486 → **39,907자**, `eli5.md` 신설 **4,970자**, `deep-dive.md` 9,351 → 13,632자, SVG **8장**(lesson 5, eli5 3)이고 36줄 ASCII 블록도가 0개가 됐습니다. `est_reading_min`은 **40분 신고 대 §0 「소요」 이론 180분(140분 어긋남)** 이던 것이 150분 3중 정합으로 닫혔고 lint 초독 추정 151분과 편차 1%입니다.
  **하류 227곳이 걸린 모듈인데 `practice/`가 완전 무변경입니다.** 절 번호는 §0~§7과 소절 전부 보존됐고 오케스트레이터가 헤딩 번호 집합을 프로그램 대조해 확인했습니다. 「흔한 오해」 1~4번과 퀴즈 7·8·9번의 묻는 것도 그대로입니다(하류가 번호로 참조하는 셋입니다). **8-1a가 밟은 수식 번호 지뢰는 이 모듈에 없었습니다** — lesson에 `\tag{}`가 0개이고 `eq.(1)`~`(8)`은 `practice/*.py` 헤더가 자기 네임스페이스로 정의하는 구조라 display 수식을 3 → 4개로 늘려도 하류가 무사합니다.
  **`labs/README.md` 1줄을 고쳤고 절 번호 이동 때문이 아닙니다.** L436의 `[§8 오해 1·2번]`이 **재작성 이전부터 깨져 있던 참조**였습니다. 2026-08-10 1차 재집필이 「흔한 오해」의 절 번호를 떼면서 이 한 곳만 남겨 뒀습니다.

  > 🔍 **재작성자가 제3자 둘을 붙여 자기 맹점을 스스로 열었습니다** (2026-09-02)
  >
  > 8-4b가 「자체 검증은 재작성자와 같은 맥락과 가정을 공유해 자기 맹점을 보지 못한다」로 남긴 교훈을 재작성 단계 안에서 먼저 적용한 첫 회차입니다. 내용 검증과 SVG 검증을 따로 붙였고 **둘 다 재작성자가 못 본 것을 냈습니다.**
  >
  > 내용 쪽에서 나온 것이 대응 트릭을 **「여섯 가지」라 쓰고 다섯만 나열**한 것, §7.1 표의 「공개 자료로 확인 완료」가 §7.4의 미확인 6행과 충돌해 보이던 것, `보조 손실`과 `코드북 사용률`과 `FSQ`의 산문 도입 부재, 용어 다섯의 소실, `H`와 `D` 미정의, 재구성 FID와 Sampling FID 구분 부재, 퀴즈 1·4에서 소리 없이 빠진 하위 질문 둘입니다. SVG 쪽에서는 **화살촉이 텐서 모양 라벨 위에 덧칠**되던 S1 2건과 `three-choices` 부제가 **그림과 모순**되던 것을 포함해 여러 건이 나왔습니다.
  >
  > **그래도 자체 검증의 한계는 남습니다.** 재작성자가 직접 잡은 것 하나가 §2 워크드 예제에서 `464/4 = 116배`를 구해 놓고 다음 문장을 **「줄어든 112개」**라 쓴 것입니다(460이 맞습니다). 비율과 개수를 섞은 종류라 lint가 잡을 수 없습니다. 8-5b는 이런 자리를 봅니다

  **새로 만든 수치는 전부 가정을 본문에 밝히고 손계산을 붙였습니다**(`116배`, `0.67도`, `6.25%`, `2,097,152`와 `8 MB`, `108`, `10번`, `24.4%`, `928 bits`, `1,485배`). **구본 수치는 하나도 고치지 않았습니다** — 8-4b의 「10배」 사고를 피하려고, 「2M vs 0」이 `practice`와 `labs` 세 곳에 인용된 것을 확인하고 2.1M으로 쓰던 것을 **2M으로 되돌렸습니다.** 팀 질문은 새로 적립한 것이 없습니다(M5-1~M5-6이 §7.4의 6행과 1대1로 이미 있습니다)
- [ ] **8-5b. 검증**
- [ ] **8-5c. 윤문**
- [ ] **8-5d. 마무리**

#### 8-6. 다음 회차로 미루는 것

- [ ] W2-M1, W2-M2 재작성 (이번 범위 밖)
- ~~W1-M1 윤문 (6-4의 유예분, 이번 범위 밖)~~ → **2026-09-02에 이번 회차로 끌어왔습니다. 8-8을 보세요**

#### 8-7. 밴드 최종 확정 (오케스트레이터, 사용자 승인 필요)

> 🗂 **2026-09-02 추가.** 8-3b가 「표본 3개로 밴드를 또 고치면 한 세션에 세 번째가 되므로 M4와 M5 실측을 모아 최종 확정 때 판단합니다」로 미뤄둔 항목입니다. 8-5d가 끝나면 W1 다섯 모듈이 전부 2차 규약 실측을 갖게 되므로 그 자리에서 판단합니다.

- [ ] **8-7a. 표본 5개 실측 집계** — W1-M1부터 M5까지 산문·총 문자·보호 구간·display 수식 개수·H3 개수를 한 표로 모읍니다. 8-1a가 올린 제안 둘(① 밴드가 재는 대상을 산문에서 총 문자로 바꾸거나 병기 ② 산문 기준 유지 시 Tier A 상한 최소 30,000)과 8-3b의 수식 밴드 제안(display 수식 6개 이상이면 상한 +6,000)이 표본 5개에서 성립하는지 검산합니다
- [ ] **8-7b. 제안서 작성과 승인** — §4 개정안을 올리고 승인을 받습니다. **승인 없이 §4를 고치지 않습니다**
- [ ] **8-7c. 반영** — 승인분을 `docs/course-plan.md` §4와 `scripts/lint-lesson.sh` **같은 커밋에서 함께** 고칩니다(§4 F군 주석이 정한 규칙). 8-2와 동일한 절차입니다. 반영 뒤 `--all` 회귀를 기록합니다

#### 8-8. W1-M1 윤문 (6-4의 유예분)

> 🗂 **2026-09-02에 이번 회차로 끌어왔습니다.** 6-4가 「재집필 후에는 Phase 4에서 멈추고 학습자 검토를 받는다」는 §1 절차를 근거로 유예했고, 그 검토가 6-5에서 회신됐으므로 유예 근거가 소멸했습니다. 이것을 닫으면 **W1 다섯 모듈이 전부 2차 규약 재작성 + 윤문 완료**로 균일해집니다.

- [ ] **8-8a. 윤문** — `/humanize-korean` `register: course` · strict · `tier: A`. 대상은 **2026-08-28 재작성본 + 회사 스택 갱신 + 검증 정정까지 반영된 현재 파일**입니다(산문 21,902자). `eli5.md`도 함께 돌립니다. 재작성이 없으므로 8-1c와 8-3c의 윤문 단계만 반복합니다
- [ ] **8-8b. 마무리** — 오케스트레이터가 철칙 보존을 독립 전수 검증하고(`display` 수식·인라인 LaTeX·숫자 토큰·URL·헤딩), `course-plan.md` §5의 W1-M1 윤문 칸을 `[x]`로 올립니다. **이 칸이 채워지면 W1 다섯 모듈이 전부 네 칸 완료**가 되고 남는 것은 학습자 실행검증뿐입니다

---

## 6. 수정 대상 파일

| 파일 | 성격 |
|---|---|
| `docs/course-plan.md` | 규약 SSOT. §2, §3.2, §3.7~§3.9, §4 개정과 §3.11, §9.19 신설 |
| `docs/physical-ai-4week-master-plan.md` | 커리큘럼 SSOT. §3, §11.1, 개정 이력만 |
| `CLAUDE.md` | 사용자 프로필, 작성 표준 요약 |
| `.claude/agents/course/pai-agent.md` | 집필 에이전트. 프로필, 사다리, 비유, 밴드, 산출물 |
| `.claude/skills/pai-course-author/SKILL.md` | 오케스트레이터. Phase 1, 2.5, 3, 7 |
| `.claude/skills/pai-course-author/references/lesson-template.md` | 골격 전면 개정 |
| `.claude/skills/pai-course-author/references/eli5-template.md` | 신규 |
| `.claude/skills/pai-course-author/references/authoring-checklist.md` | C4 재작성과 신규 항목 |
| `scripts/lint-lesson.sh` | 검사군 추가와 정정 |
| `course/w1-generative-core/01-physical-ai-landscape/eli5.md` | 신규 |
| `course/w1-generative-core/01-physical-ai-landscape/lesson.md` | 재작성 |
| `course/w1-generative-core/01-physical-ai-landscape/deep-dive.md` | 갱신 |
| `img/W1-M1-*.svg` | 신규 4장 |
| `docs/progress.md` | 집필 검증 로그 |

---

## 7. 재사용하는 기존 자산

- `scripts/lint-lesson.sh`의 `prose_metrics()` 산문 계산 함수는 그대로 씁니다. 과거 실측값과의 비교 가능성이 걸려 있습니다
- `SKILL.md` Phase 2.5의 재집필 절차(하류 참조 grep, `deep-dive.md` 이관 규칙, 보존 필수 목록)는 그대로 씁니다
- `docs/course-plan.md` §9.11의 파일럿 절차(1개 시범 후 밴드 교정)를 그대로 반복합니다
- `practice/`와 `labs/`의 excalidraw 워크시트 선례가 있으므로 SVG도 같은 자리에 둡니다

---

## 8. 알려진 위험

- **분량이 늘어납니다.** 친절하게 쓰면 같은 내용에 더 많은 글자가 듭니다. 완화책은 어려운 것을 `deep-dive.md`로 더 밀고 쉬운 것을 `eli5.md`로 앞당기는 3층 구조이지만, 시범 실측 전에는 밴드가 맞을지 모릅니다. 밴드를 잠정으로 선언하고 시범 후 1회 교정합니다
- **제어 비유를 지우면 설명이 길어집니다.** 캐스케이드 제어를 앵커로 쓰면 한 줄인 것이 처음부터 가르치면 한 문단입니다. 이것이 의도된 비용입니다
- **하류 참조가 깨질 수 있습니다.** `practice/`와 `labs/`가 절 번호로 링크합니다. Phase 5-5의 grep 절차를 반드시 거칩니다
- **SVG가 GitHub에서 렌더되지 않을 수 있습니다.** 폰트 임베딩과 다크 모드 대비를 확인해야 합니다. 실패하면 PNG 병행을 검토합니다
- **`eli5.md`가 lesson과 중복돼 읽기 부담만 늘 수 있습니다.** eli5는 요약이 아니라 다른 층위여야 합니다. 수식 0, arXiv 0, 그림 우선을 규약으로 못박습니다

---

*2026-08-25 작성. 학습자 2차 피드백 대응. Phase 0부터 7까지 2026-08-31 완료. Phase 8 확산 2026-09-01 착수.*

**진행 중인 것과 남아 있는 것입니다.**

1. ~~**학습자 검토** (6-5)~~ → **2026-09-01 회신 완료.** 「읽었고 괜찮다, 그대로 확산」이었습니다. **이번 개정의 성패는 lint가 아니라 이 답에 있다**고 적어 뒀고, 답이 왔으므로 Phase 8의 착수 조건이 풀렸습니다
2. **Phase 8 확산, 진행 중.** 이번 회차 범위는 W1-M2부터 W1-M5까지 넷이고 모듈마다 재작성부터 윤문과 보존 검증까지 완주합니다. 순서는 M2를 먼저 해서 Tier A 표본을 둘로 만든 뒤 밴드를 1회 교정하고 나머지 셋을 그 밴드로 쓰는 것입니다(8-0)
3. **W1-M1 윤문** (6-4의 유예). 재작성본을 대상으로 돌려야 하고 ~~**이번 회차 범위 밖**입니다~~ → **2026-09-02에 8-8로 범위에 들어왔습니다.** §5 체크박스는 이미 `[ ]`로 되돌려 뒀습니다
4. **W2-M1과 W2-M2 재작성.** Phase 8의 나머지 절반이고 다음 회차입니다
5. **범위 밖으로 미룬 자잘한 것 넷.** §4 티어표 Tier C 근거 셀(§9.15 승인 대기, **8-2가 판단합니다**), 6-3S 개선 권고 8건, `01`의 표기 정밀도 한 열, §2 산출물 표 5번의 「클라우드 인스턴스」 문구
