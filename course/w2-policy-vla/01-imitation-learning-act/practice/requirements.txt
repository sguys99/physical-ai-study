# W2-M1 practice 의존성
#
# ┌──────────────────────────────────────────────────────────────────────────────┐
# │  A층 (01 · 02 · 03) — 의존성 0. 설치할 것이 아무것도 없습니다.                │
# │      python3 01_latency_budget.py                                            │
# │  표준 라이브러리만 씁니다: math · random · statistics · csv · argparse ·      │
# │                            pathlib · unicodedata · dataclasses · time        │
# │  numpy 도 matplotlib 도 쓰지 않습니다. 그림은 CSV + ASCII 로 냅니다.          │
# └──────────────────────────────────────────────────────────────────────────────┘
#
# 집필·검증 환경 (여기서 실제로 설치·실행 검증한 값):
#   macOS 26 (darwin arm64) / Homebrew Python 3.14.6 / pip 26.1.2 / 2026-08-06
#   01 · 02 · 03 실행검증 완료 (의존성 0)
#   04 실행검증 완료 (CPU 경로만 — cuda / mps 경로는 미검증)
#
# ⚠️ W1 practice 는 WSL2 + RTX 3080 + Python 3.12 에서 검증했습니다(각 requirements.txt 참조).
#    이 모듈은 환경이 다릅니다. A층은 어느 쪽에서도 그대로 돕니다.

# ─── B층 (04_act_cvae_minimal.py) 만 필요한 것 ────────────────────────────────
# ACT형 CVAE 최소 구현. CPU로 충분합니다 (--smoke 16.5초 / 기본 2분 7초, Apple Silicon 실측).
# torch 가 없으면 04 는 크래시하지 않고 설치 안내를 낸 뒤 정상 종료합니다.
#
# 기본 인덱스(CPU 빌드)로 설치:
#   pip install torch==2.13.0
# CUDA 빌드가 필요하면:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu130
torch==2.13.0

# ⚠️ Python 3.14 에서도 torch 2.13.0 wheel 이 받아졌고 04 가 완주했습니다(위 실측).
#    다만 labs/ 의 LeRobot 은 Python 3.12 를 요구하므로 어차피 3.12 환경을 따로 만들게 됩니다.
#    그 환경에서 04 를 돌려도 동일하게 동작합니다.
#
# ℹ️ numpy 를 설치하지 않으면 torch 가 첫 import 에서 경고를 한 줄 냅니다
#    ("Failed to initialize NumPy"). 04 는 numpy 를 쓰지 않으므로 **무시해도 됩니다.**
#    거슬리면 `pip install numpy` 하면 사라집니다.

# ─────────────────────────────────────────────────────────────────────────────
# LeRobot 은 여기 없습니다 — 경계를 분명히 해둡니다.
#
#   설치 · 데이터셋 다운로드 · lerobot-dataset-viz · lerobot-train 은 전부
#   labs/ 의 몫입니다(lesson §8.1 이 정한 이 모듈의 실습 범위).
#   practice/ 는 "lesson 의 숫자를 손으로 검산하고 구조를 최소 구현으로 만져보는" 층이고,
#   labs/ 는 "실물 파이프라인이 돌기 시작하는 것을 확인하는" 층입니다.
#
#   LeRobot 은 Python 3.12 + PyTorch >= 2.10 을 요구하므로 별도 환경이 필요합니다:
#     python3.12 -m venv .venv-lerobot && source .venv-lerobot/bin/activate
#     pip install lerobot                    # 코어
#     pip install "lerobot[dataset_viz]"     # 데이터셋 시각화까지
#   착수 시점에 https://huggingface.co/docs/lerobot/installation 로 재확인하세요.
#
# 이 모듈은 jupytext 로 .py <-> .ipynb 를 동기화합니다(생성물도 커밋):
#   pip install jupytext==1.19.5
# ─────────────────────────────────────────────────────────────────────────────
