# W2-M1 워크시트, 제출물

> **이 파일이 W2-M1의 제출물입니다.** [`README.md`](README.md)의 8단계를 따라가며 채우세요.
> 시작 전에 사본을 만들어 쓰는 것을 권합니다: `cp worksheet.md worksheet-$(date +%F).md`
>
> 🔴 **⑥과 ⑦이 이 모듈의 본체입니다.** ①~⑤는 전부 그 둘을 쓰기 위한 재료입니다.
> ⑥은 lesson [§3.6](../lesson.md) 표를 **자기 기기 숫자로 다시 세우는 것**이고,
> ⑦은 lesson 상단의 **완료 기준** 그 자체입니다.
>
> 🔴 **모르는 칸은 `?`로 남기세요.** 특히 회사 내부 구현은 lesson §7.3에도 답이 없습니다(6개 항목 전부 미확인).
> **그 `?`가 팀 질문의 원재료입니다.** 채워진 칸보다 `?`가 온보딩의 산출물에 더 가깝습니다.

---

## ⓪ 기본 정보

| 항목 | 값 |
|---|---|
| 작성일 | |
| 총 소요 시간 (계획 2시간 5분, CPU면 2.5시간 대비) | |
| 선수 모듈 완주 여부 | **W2-M1 practice ☐** (`01` ☐ · `02` ☐ · `03` ☐ · `04` ☐) · W1-M1 ☐ · W1-M2 ☐ |
| lesson 정독 완료 | ☐ |

### 실행 환경

| 항목 | 값 | 확인 명령 |
|---|---|---|
| **가상환경** | `.venv-lerobot` ☐ / 다른 것 ______ | `verify` 출력 `[1] 환경` 표의 python 경로 |
| Python | | `.venv-lerobot/bin/python --version` |
| `lerobot` | | `[1]` 표 |
| `torch` + cuda | 버전 ______ / `cuda_available` = ______ | `[1]` 표 |
| GPU | 있음(모델 ______ / VRAM ____ GB) ☐ / **없음(CPU 단독)** ☐ | `[1]` 표 |
| CPU 코어 수 | | `verify [6](b)`의 load average 줄 |
| `ffmpeg` | 있음 ☐ / **없음(E1 폴백)** ☐ | `which ffmpeg` |
| 디스크 여유 (시작 전) | ______ GB | `df -h .` |

**집필 검증 환경과 다른 점** (README §0.5는 WSL2 / Python 3.12.12 / lerobot 0.6.1 / torch 2.11.0+cu130 / RTX 3080 12GB / ffmpeg 없음 / 2026-08-08~09):

```
(여기에 작성 — 없으면 "동일")
```

### Step별 실측 소요 시간

| Step | 계획 | 명령 실행(초) | 실측 소요 | 비고 |
|---|---|---|---|---|
| Step 1 — 환경 관문 | 5분 | | | 집필 실측 설치 42초 |
| Step 2 — 기본값 21개 대조 ★ | 8분 | | | 집필 실측 약 7초 |
| **Step 3 — 데이터셋 메타 ★★** | 7분 | | | 집필 실측 첫 실행 40.6초 / 이후 약 10초 |
| Step 4 — `dataset-viz` | 5분 | | | 집필 실측 8.8초 |
| **Step 5 — 학습 스모크 ★** | 10분 | | | 집필 실측 GPU 31.3초 / CPU 약 9.7분 |
| **Step 6 — 추론 실측 ★★** | 5분 | | | 집필 실측 약 12초 |
| **Step 7 — 백지 아키텍처 ★** | **30분** | (코드 없음) | | |
| **Step 8 — 제출물 두 개 ★★** | **55분** | (코드 없음) | | 8.A 28 · 8.B 22 · 기록 5 |
| **합계** | **약 2시간 5분** | **GPU 약 3분 / CPU 약 14분** | | |

> Step 7+8이 전체의 **68%** 여야 정상입니다. 이 비율이 뒤집혔다면 명령만 돌리고 끝낸 것입니다.

---

## ① Step 1, 환경 관문

### 1.1 설치와 분리 확인

| 항목 | 기대 | 내 결과 | 판정 |
|---|---|---|---|
| 설치 소요 | 약 42초 | ______초 | ☐ |
| Python | **3.12.x** | | ☐ |
| `torch` | 2.10 이상 | | ☐ |
| `cuda.is_available()` | `True` 또는 `False` **둘 다 정상** | | ☐ |
| `lerobot-*` CLI 개수 | **18개** | ______개 | ☐ |
| ★ `.venv/bin/python -c "import lerobot"` | **`ModuleNotFoundError`** ← 이게 정상 | | ☐ |

**Q. 왜 W1의 `.venv`와 분리했습니까?** (키워드: torch 버전)

```

```

**Q. 그런데도 두 환경을 합치고 싶어진다면, 합쳤을 때 무엇이 깨집니까?**

```

```

### 1.2 `--policy.type` 선택지에서 본 것

`lerobot-train --help | grep -A3 "policy.type"` 의 선택지 중 **W2에서 다룰 것**을 표시하세요.

| 선택지 | 어느 모듈 | 목록에 있었나 |
|---|---|---|
| `act` | **W2-M1 (이 모듈)** | ☐ |
| `diffusion` | W2-M2 | ☐ |
| `pi0` / `pi0_fast` / `pi05` | W2-M4 | ☐ |
| `groot` | W2-M4 | ☐ |
| `lerobot-rollout --strategy.type=dagger` | lesson §2.3 | ☐ |
| `lerobot-train-tokenizer` | **정체 미확인 — W2-M5 / FSQ와 접점?** | ☐ |

**Q. 마지막 행을 보고 무엇을 추측하고 싶어집니까? 그리고 왜 지금 추측하면 안 됩니까?**

```
추측하고 싶은 것:
추측하면 안 되는 이유:
→ ⑧ 확인 항목으로 옮겼는가: ☐
```

---

## ② Step 2, ACT 기본값 21개 대조

### 2.1 🔴 먼저 채우고 나서 돌리세요

**스크립트를 보기 전에** lesson [§6.4](../lesson.md)를 읽고 아래 셋을 기억나는 대로 채우세요. 출력을 보고 채우면 검산이 아니라 받아쓰기입니다.

| 필드 | 내 기억 | 실측 | 일치 |
|---|---|---|---|
| `chunk_size` / `n_action_steps` | ______ / ______ | | ☐ |
| `n_decoder_layers` | ______ | | ☐ |
| `kl_weight` | ______ | | ☐ |

### 2.2 대조 결과

| 항목 | 기대 | 내 결과 | 판정 |
|---|---|---|---|
| **하이퍼파라미터 대조** | **`21/21 PASS`** | ______ / 21 | ☐ |
| 인용 Q1 (`chunk size size`) | PASS | | ☐ |
| 인용 Q2 (앙상블 docstring) | PASS | | ☐ |
| 인용 Q3 (`n_decoder_layers`) | PASS | | ☐ |
| 예외 클래스 | **`NotImplementedError`** | | ☐ |
| 종합 | **FAIL 0 · WARN 0** | FAIL ___ · WARN ___ | ☐ |
| exit code | **0** | | ☐ |

**FAIL이 하나라도 났다면 어느 필드입니까? (있으면 ⑨에도 적으세요)**

```

```

### 2.3 ★ 판정, 이 스크립트가 실제로 잡아낸 것

README 2.3의 사연입니다. 집필 검증 중 Q3가 **WARN**이었습니다.

| | 문장 |
|---|---|
| 설치본 원문(정본) | `...there is a bug in the code` **`that means only`** `the first layer is used.` |
| 당시 lesson 「흔한 오해」 | `...there is a bug in the code` **`that only`** `the first layer is used.` |

**Q. `means` 한 단어가 빠졌을 때 lesson 「흔한 오해」의 주장 자체는 바뀝니까?**

```
☐ 바뀐다   ☐ 안 바뀐다
근거:
```

**Q. 바뀌지 않는데도 고쳐야 하는 이유는?** (힌트: lesson 「흔한 오해」가 그 인용문으로 무엇을 주장하고 있었는가)

```

```

**Q. 이 스크립트가 lesson 문구를 상수로 베끼지 않고 `../lesson.md`를 매번 읽는 이유는?**

```

```

### 2.4 예외 클래스, 두 문장 구분

| | 문장 |
|---|---|
| **docstring** (Q2) | |
| **예외 메시지** ([4]) | |

**Q. 둘을 섞어 인용하면 무엇이 문제입니까?**

```

```

---

## ③ Step 3, 데이터셋 메타 ★★

### 3.1 🔴 예상 먼저 (돌리기 전에 적을 것)

**ACT 논문의 ALOHA 데이터가 몇 Hz로 기록됐을 것 같습니까?**

```
예상: ______ Hz
근거:
```

**PushT의 `repo_id`는 무엇일 것 같습니까?** ______

### 3.2 실측 표

| repo_id | fps | 에피소드 | 프레임 | 총 시간 | 카메라 | `D_action` | 캐시 크기 |
|---|---|---|---|---|---|---|---|
| `lerobot/aloha_sim_transfer_cube_human` | | | | | | | |
| `lerobot/aloha_mobile_cabinet` | | | | | | | |
| `lerobot/pusht` | | | | | | | |

> 집필 실측: **50 / 50 / 20,000 / 6.7분 / 1대 / 14 / 69.9 MB**, **50 / 85 / 127,500 / 42.5분 / 3대 / 14 / 1,741.6 MB**, **10 / 206 / 25,650 / 42.8분 / 1대 / 2 / 7.7 MB**

**3.1의 예상과 얼마나 달랐습니까?**

```
fps 예상 ______ → 실측 ______
```

### 3.3 ★★ 판정, §3.6 표의 두 열은 각각 무엇인가

| | **@50 Hz 열** | **@30 Hz 열** |
|---|---|---|
| 어디서 온 값인가 | | |
| 무엇을 말하는가 | | |
| 200 ms 문턱과 맞대는 것은 어느 쪽 | | |
| **실측인가 가정인가** | | |

**Q. "기록 FPS"와 "배포 $f_2$"는 같은 양입니까? 다르면 무엇을 해야 합니까?**

```

```

**Q. 이 질문이 팀 질문 `W2M1-2`인데, 방금 두 항 중 어느 쪽 값을 손에 넣었습니까?**

```
손에 넣은 것:
아직 모르는 것:
```

### 3.4 "적은 데이터"의 기준점

`aloha_sim_transfer_cube_human` = ______ 에피소드, ______ 분.
ACT 초록의 "10분 분량 시연, 50 시연"과 **자릿수가 같은가**: ☐ 예 ☐ 아니오

**Q. LLM 파인튜닝 감각으로 보면 이 규모가 이상하게 작습니다. 로봇에서 이것이 정상인 이유는?** (키워드: 사람이 로봇을 몬다, 시간에 선형)

```

```

**Q. 팀 질문 `W2M1-1`의 답이 이 자릿수와 같을 때 / 크게 다를 때, W2 실습의 방향이 어떻게 갈립니까?**

```
같을 때:
다를 때:
```

---

## ④ Step 4, 5, 6 실측 기록

### 4.1 Step 4, `dataset-viz`

| 항목 | 기대 | 내 결과 | 판정 |
|---|---|---|---|
| 붉은 traceback | **떠도 정상** | 떴다 ☐ / 안 떴다 ☐ | |
| 마지막 줄 | **`Falling back to 'pyav'`** | | ☐ |
| `.rrd` 크기 | **약 25 MB** | ______ MB | ☐ |
| 소요 | 8.8초 | ______초 | |
| **뷰어 창** | **뜨면 안 됨** | | ☐ |

**`.rrd`를 실제로 열어봤습니까?** ☐ 예 (방법: ______) ☐ 아니오

열었다면: **`observation.state`와 `action` 두 곡선이 거의 겹칩니까?** ☐ 예 ☐ 아니오
**겹치는 이유를 한 줄로** (lesson §2.1):

```

```

### 4.2 Step 5, 학습 스모크

| 항목 | 기대 | 내 결과 | 판정 |
|---|---|---|---|
| `num_learnable_params` | **51613582 (52M)** | | ☐ |
| loss (첫 로그 → 마지막) | **24.4 → 3.8** | ______ → ______ | ☐ |
| `grdn` | 388 → 101 | ______ → ______ | ☐ |
| `mem_gb` | 약 2.10 | | ☐ |
| `updt_s` | GPU 0.074 / CPU 2.86 | | ☐ |
| 벽시계 | GPU 31.3초 / CPU 약 9.7분 | ______ | ☐ |
| 체크포인트 크기 | **592 MB** | ______ MB | ☐ |
| 재개 후 `checkpoints/` | **`000200` · `000260` · `last`** | | ☐ |
| `outputs/` 총량 | **1.2 GB** | ______ GB | ☐ |

### 4.3 ★ KL 기여율, 손으로 계산

$$\mathcal{L}_{\text{ACT}} = \|\hat a - a\|_1 + \beta \cdot D_{\mathrm{KL}}, \qquad \beta = \texttt{kl\_weight} = 10.0$$

| step | `l1_loss` | `kld_loss` | $\beta \times$ `kld_loss` | 합 | `loss` 열 | **KL 기여율** |
|---|---|---|---|---|---|---|
| 첫 로그 (25) | | | | | | ______ % |
| 마지막 (200) | | | | | | ______ % |

> 집필 실측: **0.784 / 2.365 / 23.65 / 24.43 / 24.430 / 96.8 %**, **0.528 / 0.323 / 3.23 / 3.76 / 3.756 / 86.0 %**

**검산 ① $\ell_1 + 10\times$KL 이 `loss` 열과 맞습니까?** ☐ 예 ☐ 아니오
**검산 ② lesson §3.4의 loss 조립식이 실물에서 성립합니까?** ☐ 예 ☐ 아니오

**Q. KL 기여율이 96.8%입니다. "그러므로 `kl_weight=10`이 너무 크다"는 결론이 왜 틀립니까?**

```

```

**Q. practice `04`는 3,000 스텝에서 7.0%까지 내려갔습니다. 내 곡선은 몇 스텝쯤에서 역전될 것 같습니까?**

```
예상: 약 ______ 스텝
근거(두 곡선의 모양 비교):
```

### 4.4 Step 6, 추론 실측

| 항목 | 기대 | **내 결과** | 판정 |
|---|---|---|---|
| 청크 shape | **정확히 `(1, 100, 14)`** | | ☐ |
| 카메라 1~4대 파라미터 증분 | **전부 `+0`** | | ☐ |
| `use_vae=True` / `False` | 51,613,582 / 34,232,142 | | ☐ |
| 차이(VAE posterior 인코더) | **17.4M** | | ☐ |
| **τ_infer (GPU)** | 배율로 판정 | **______ ms** | |
| **τ_infer (CPU)** | 배율로 판정 | **______ ms** | |
| CPU/GPU 배율 | 10~15배 | ______배 | ☐ |
| 측정 시점 load average | **낮을수록 좋음** | ______ | |
| `select_action` 1회차 / 이후 | 20배 이상 | ______ / ______ = ______배 | ☐ |
| **z=0 최대차** | **정확히 `0.000e+00`** | | ☐ |
| 앙상블 스텝당 | τ_infer와 비슷 | ______ ms | |
| 추론 VRAM peak | 0.26 GB | | |

> 집필 실측(한 번의 실행): τ_infer GPU **12.8 ms** / CPU **132.5 ms**(10.3배), 큐 배율 **23배**, 앙상블 **11.7 ms**, VRAM **0.26 GB**.
> ⚠️ **절대값을 맞추려 하지 마세요.** 같은 GPU에서 6.5 ~ 66.0 ms까지 나왔습니다. **판정은 README 6.2 아래의 부등호 3개로.**

### 4.5 ★ 판정, 51.6M, 52M, 80M

| 값 | 출처 | 실측과의 관계 |
|---|---|---|
| ______ | 이 설치본 실측 | — |
| 약 ______ M | lesson 본문 | ☐ 일치 ☐ 불일치 |
| 약 ______ M | HF `docs/lerobot/act` | ☐ 일치 ☐ 불일치 |

**Q. 카메라를 늘려도 파라미터가 안 느는 이유를 파일:라인으로 적으세요.**

```
생성:  modeling_act.py:____
호출:  modeling_act.py:____
한 줄 설명:
```

**Q. "HF 문서가 왜 80M인가"에 대해 지금 쓸 수 있는 문장은 무엇입니까?** 🔴 **추측을 쓰면 안 됩니다.**

```

```

**Q. ★ "수치가 틀렸다"와 "결론이 틀렸다"를 가르세요.** 파라미터 수가 52M이 아니라 80M이었다면 lesson §3.6의 결론("지연은 제약이 아니다")이 뒤집힙니까?

```
☐ 뒤집힌다   ☐ 안 뒤집힌다
근거(무엇을 재보면 알 수 있는가):
```

### 4.6 ★ 판정, 큐 소비

```
1회차:  ____________________  →  ______ ms
2회차~: ____________________  →  ______ ms
배율:   ______배
```

**Q. 이것이 `chunk_size`와 `n_action_steps`가 왜 별개 필드인지를 어떻게 설명합니까?**

```

```

**Q. `chunk_size`를 늘리는 것과 `n_action_steps`를 늘리는 것은 비용 구조가 어떻게 다릅니까?**

```
chunk_size ↑ :
n_action_steps ↑ :
```

### 4.7 ★★ 판정, 앙상블이 통과한 것을 어떻게 읽는가

| | GPU | CPU (데스크톱 x86) | Jetson급 온보드 |
|---|---|---|---|
| τ_infer | ______ ms | ______ ms | **미측정** |
| 기본값 예산 2,000 ms @50 Hz | ☐ ✅ ☐ ❌ (____배) | ☐ ✅ ☐ ❌ (____배) | ? |
| **앙상블 예산 33 ms @30 Hz** | ☐ ✅ ☐ ❌ (____배) | ☐ ✅ ☐ ❌ (____배) | ? |

**Q. lesson §7.2이 L3 설계 변수로 셋을 들었습니다. 이 표가 무엇을 하나 더 얹습니까?**

```
셋:
넷째:
```

**Q. "우리 로봇이 온보드 추론이라면 temporal ensembling을 켤 수 있는가?"**
🔴 **답을 쓰지 마세요. 답하려면 무엇을 알아야 하는지를 쓰세요.**

```
알아야 할 것 ①:
알아야 할 것 ②:
관련 팀 질문 번호:
```

---

## ⑤ Step 7, ★ 백지 ACT 아키텍처 (excalidraw)

> 🔴 **lesson §4.1, §4.2를 닫고 채우세요.** 보면서 채우면 베끼기이지 백지 연습이 아닙니다.
> 워크시트 파일: [`act_architecture_worksheet.excalidraw`](act_architecture_worksheet.excalidraw), [excalidraw.com](https://excalidraw.com)에서 열기

### 5.1 채우기 기록

| 항목 | 값 |
|---|---|
| lesson을 닫고 채웠는가 | ☐ 예 ☐ 아니오 |
| 소요 시간 | ______분 |
| 물음표로 남긴 칸 수 | ______개 |
| 대조에서 틀린 칸 수 | ______개 |

### 5.2 ★ 채점 체크리스트 A군, shape

**이 6개가 이 문항의 핵심입니다.**

| # | 항목 | 맞음 | 내가 쓴 것 (틀렸을 때만) |
|---|---|---|---|
| A1 | **출력 청크 `[B, 100, D_action]`** — Step 6 (a) 실측 `(1, 100, 14)`와 일치 | ☐ | |
| A2 | VAE 인코더 입력 시퀀스 = **`[B, 102, 512]`** (`[CLS]` 1 + 상태 1 + 액션 100) | ☐ | |
| A3 | latent = **`[B, 32]`** · split 전은 `[B, 64]` | ☐ | |
| A4 | 이미지 토큰 = **2대 480×640에서 600개** ($2\times15\times20$) | ☐ | |
| A5 | 메인 인코더 입력 = **`[B, 602, 512]`** (이미지 600 + 상태 1 + latent 1) | ☐ | |
| A6 | 디코더 쿼리 = **`[100, 512]`**, 개수가 곧 **`chunk_size`** | ☐ | |

### 5.3 ★ 채점 체크리스트 B군, 구조

| # | 항목 | 맞음 | 내가 쓴 것 |
|---|---|---|---|
| B1 | 스택 **셋**, 층수 **4 / 4 / 1**. 디코더 1층인 이유는 **원 구현 버그를 재현성 때문에 따라간 것** | ☐ | |
| B2 | **자기회귀가 아니다** — 한 번의 forward로 100스텝 **병렬** | ☐ | |
| B3 | 관측은 **단일 프레임**(`n_obs_steps=1`) → **입력은 마르코프, 출력은 비마르코프** | ☐ | |
| B4 | **언어 입력이 없다** → 태스크당 정책 하나. L5가 "해당 없음" | ☐ | |
| B5 | 어텐션 비용의 거의 전부가 **이미지 토큰** → 지연 손잡이는 **카메라 수·해상도** | ☐ | |

### 5.4 ★ 채점 체크리스트 C군, 추론 경로

| # | 항목 | 맞음 | 내가 쓴 것 |
|---|---|---|---|
| C1 | **VAE 인코더 스택이 통째로 빠진다** (정답 액션이 없으므로) | ☐ | |
| C2 | **$z = 0$** (`torch.zeros`) — prior 평균으로 고정 | ☐ | |
| C3 | 결론 ① **배포 모델은 결정론적** (Step 6 (d)의 `0.000e+00`) | ☐ | |
| C4 | 결론 ② **$z$는 실행 시 모드를 고르는 장치가 아니다** — 학습 시 변동 흡수용 정규화 | ☐ | |
| C5 | 안 쓰이는 파라미터 = **17.4M** | ☐ | |
| C6 | 실행 정책 3갈래의 `n_action_steps`와 **새 관측 반영 주기** | ☐ | |

### 5.5 검산, 자기가 쓴 것만으로

| # | 검산 | 내 답 | 맞음 |
|---|---|---|---|
| 1 | 카메라 2대 → 4대면 메인 인코더 입력 토큰이 몇 개? | | ☐ |
| 2 | 그때 파라미터는 몇 개 늘어나나? | | ☐ |
| 3 | 1과 2가 동시에 성립하는 것이 이상하지 않은가? | | ☐ |

**3번을 한 문단으로** (키워드: 모델 크기 ≠ 추론 비용):

```

```

**Q. 그래서 팀 질문 `W2M1-4`(카메라 구성)가 왜 "지연 질문"입니까?**

```

```

---

## ⑥ ★★ Step 8.A, lesson §3.6 표를 자기 숫자로 다시 계산

> **lesson [§3.6](../lesson.md)의 표에는 $\tau_{\text{infer}}$ 열이 없습니다.** 자기 기기 값으로 다시 세우는 것이 이 문항입니다.

$$T_{\text{replan}} + \tau_{\text{infer}} + \tau_{\text{comm}} \;\le\; \frac{\texttt{n\_action\_steps}}{f_2}$$

### 6.1 표, 두 열을 함께

**내 $\tau_{\text{infer}}$ = ______ ms (GPU), ______ ms (CPU)**

| 설정 | `n_act` | 예산 @**50 Hz**(ALOHA 실측) | 예산 @**30 Hz**(우리 로봇 가정) | **τ_infer 대비 여유** (@30 Hz) | **최악 반응 지연** (@30 Hz) | **200 ms 문턱 대비** |
|---|---|---|---|---|---|---|
| LeRobot 기본값 | 100 | ______ ms | ______ ms | ______배 | ______ ms | ______배 밖 |
| docstring 예시 | 50 | ______ ms | ______ ms | ______배 | ______ ms | ______배 밖 |
| 짧은 receding horizon | 10 | ______ ms | ______ ms | ______배 | ______ ms | ______ |
| temporal ensembling(강제) | 1 | ______ ms | ______ ms | ______배 | ______ ms | 안전 |

### 6.2 다섯 문장, 🔴 숫자가 안 붙으면 lesson 요약을 옮겨 적은 것입니다

| # | 문장 | **내 숫자** | 출처 |
|---|---|---|---|
| 1 | **"지연은 제약이 아니다"** — 기본 설정의 예산은 τ_infer의 **______배** | | Step 6 (b) |
| 2 | **"그러므로 `chunk_size=100`은 지연 논거로 정해진 값이 아니다"** — 예산 **______ ms** vs 필요한 것 **______ ms** | | Step 6 (b) |
| 3 | **"최악 반응 지연이 200 ms 문턱의 ______배 밖"** — 이 계산의 $f_2$는 ☐ 실측 ☐ **가정** | | Step 3 · 3.3 |
| 4 | **"앙상블은 예산을 ______분의 1로 조인다"** — 그 예산에 내 GPU는 **______**, 내 CPU는 **______** | | Step 6 (f)·[6-b] |
| 5 | **"청크 100개 중 모델을 부르는 것은 ______번, 나머지 ______번은 ______배 싸다"** | | Step 6 (c) |

### 6.3 검산, practice `01`과 대조

```bash
cd ../practice
python3 01_latency_budget.py --sweep            # 30 Hz
python3 01_latency_budget.py --f2 50 --sweep    # 50 Hz
```

| $f_2$ | 200 ms 문턱을 넘지 않는 `n_action_steps` 상한 | 그때 추론 빈도 |
|---|---|---|
| 30 Hz | ______ | ______ Hz |
| 50 Hz | ______ | ______ Hz |

**손계산과 스크립트가 맞습니까?** ☐ 예 ☐ 아니오 (다르면 어디가)

```

```

**Q. ★ lesson §3.6의 세 결론 중, 내 실측으로 값이 바뀐 것은 몇 개이고 결론 자체가 뒤집힌 것은 몇 개입니까?**

| 결론 | 값이 바뀌었나 | **결론이 뒤집혔나** | 근거 |
|---|---|---|---|
| ① 지연은 제약이 아니다 | | | |
| ② 최악 반응 지연 3.3초 = 문턱의 16배 밖 | | | |
| ③ 앙상블은 예산을 100분의 1로 조인다 | | | |

> 🔴 **③이 함정입니다.** GPU에서는 통과하고 CPU에서는 안 됩니다. **뒤집힌 것이 lesson의 주장인지, 아니면 하드웨어라는 조건이 추가된 것인지** 구분해서 쓰세요. lesson 「흔한 오해」는 애초에 "온보드 추론이면"이라는 조건을 달고 있습니다.

```
③에 대한 내 판정:
```

### 6.4 우리 로봇이라면, 세 팀 질문을 하나의 숫자로

practice README의 과제 A입니다. `W2M1-2`(기록 FPS ↔ 배포 $f_2$)와 `W2M1-5`(가장 빠른 실패 모드의 시간상수)의 답이 왔다고 **가정**하고 채우세요.

| 가정한 값 | 내가 넣은 값 |
|---|---|
| 우리 로봇의 $f_2$ | ______ Hz |
| 가장 빠른 실패 모드의 시간상수 | ______ ms |
| → **`n_action_steps` 상한** | ______ |
| → **추론 호출 빈도** | ______ Hz |
| → 그 빈도를 내 하드웨어가 감당하는가 | ☐ 예 ☐ 아니오 (τ_infer ______ ms 기준) |

**Q. 세 질문의 답이 하나의 숫자로 수렴합니까? 그렇다면 `M4-5`를 던질 준비가 된 것입니다.**

```

```

---

## ⑦ ★★ Step 8.B, 완료 기준 한 문단 (최종 제출물)

> **lesson 상단의 완료 기준 그 자체입니다.**
>
> > LeRobot ACT 기본값 `chunk_size=100`, `n_action_steps=100`, `temporal_ensemble_coeff=None`을 놓고 **(a)** 이 설정이 청크를 끝까지 개방루프로 실행한다는 뜻임을 지적하고 **(b)** 30 Hz 기준 3.33초의 최악 반응 지연을 계산하고 **(c)** 그 값이 [W1-M1 §3.2](../../../w1-generative-core/01-physical-ai-landscape/lesson.md)의 "자세가 무너지기까지 200 ms" 문턱과 왜 충돌하지 않는지(ALOHA는 고정 베이스라 낙상 모드가 없다)를 **한 문단으로 쓸 수 있다.**
>
> 🔴 **lesson과 ⑥을 닫고 한 번에 쓰세요.** 다 쓴 뒤에 열어서 대조합니다.
> 🔴 **(a)(b)(c)를 세 개의 분리된 답으로 쓰면 안 됩니다. 한 문단입니다.**

### 7.1 조각별 재료 확인 (쓰기 전에)

| 조각 | 내 재료 (숫자·근거) |
|---|---|
| **(a) 개방루프 완주** | |
| **(b) 3.33초 계산** | |
| **(c) 충돌하지 않는 이유** | |

### 7.2 ★★ 한 문단, 백지로

```
[여기에 한 문단]




```

### 7.3 반드시 덧붙일 두 줄

**① "그러므로 이 하이퍼파라미터를 우리 로봇으로 그대로 옮기면 안 된다"**, lesson §6.2 표의 세 행으로.

| 태스크 | 가장 빠른 실패 모드의 시간상수 | 허용 개방루프 창 | 기본값 3,333 ms는? |
|---|---|---|---|
| 고정 베이스 양팔 조작 | | | |
| 이동 중 조작 | | | |
| 휴머노이드 전신 균형 | | | |

```
한 줄 결론:
```

**② "옮길지 판단하려면 무엇을 알아야 하는가"**, 팀 질문 두 개를 지목.

```
`W2M1-__` :
`W2M1-__` :
```

### 7.4 자가 채점

| ☐ | 항목 |
|---|---|
| ☐ | (a)(b)(c)가 **한 문단 안에** 들어 있다 |
| ☐ | (b)에 **30 Hz 값과 50 Hz 값이 둘 다** 있고, 어느 쪽이 **실측**이고 어느 쪽이 **가정**인지 밝혔다 |
| ☐ | (c)가 **"ACT가 잘못 설계됐다"가 아니라 "태스크가 다르다"** 로 되어 있다 |
| ☐ | 7.3의 두 줄이 붙어 있다 |
| ☐ | **자기 실측 숫자가 최소 3개** 들어 있다 (fps · τ_infer · 문턱 대비 배수) |

### 7.5 셀프 체크 퀴즈, 틀린 것 기록

lesson [「셀프 체크 퀴즈」](../lesson.md)의 10문항. **틀린 것만** 적으세요.

| # | 왜 틀렸나 | 다시 읽을 절 |
|---|---|---|
| | | |
| | | |

---

## ⑧ 팀 질문 / 모르는 것 ★

lesson [「팀에 물어볼 것」](../lesson.md)의 `W2M1-1`~`W2M1-6`.
→ [`../../../../notes/questions-for-team.md`](../../../../notes/questions-for-team.md)의 `### W2` 섹션으로 옮길 것.

| # | 질문 | 어느 Step에서 급해졌나 | 우선순위 |
|---|---|---|---|
| `W2M1-1` | 시연 데이터를 실제로 무엇으로 수집하는가? 에피소드 수와 시간 규모는? | | |
| `W2M1-2` | 시연 기록 FPS와 배포 $f_2$가 같은가? 다르면 리샘플링인가 환산인가? | | |
| `W2M1-3` | 액션 벡터에 하체가 들어가는가? $D_{\text{action}}$의 정체는? | | |
| `W2M1-4` | 카메라 구성(대수·해상도·손목 카메라)과 시각 백본은? | | |
| `W2M1-5` | 타깃 태스크의 가장 빠른 실패 모드와 그 시간상수는? | | |
| `W2M1-6` | ACT 또는 BC baseline을 돌려본 적이 있는가? 실패 모드는? | | |

### 8.1 ★★ 자기 스택에서 가장 급한 것 둘

> 🔴 **여섯 개를 다 물어보면 하나도 답을 못 받습니다.** 둘을 고르고 **왜 그 둘인지** 쓰세요.

| 순위 | 고른 질문 | **왜 이것이 가장 급한가** (내 상황 기준, 한 문단) |
|---|---|---|
| **1순위** | `W2M1-__` | |
| **2순위** | `W2M1-__` | |

**이 둘의 답이 오면 ⑥의 어느 칸이 채워집니까?**

```
1순위 →
2순위 →
```

### 8.2 이 랩에서 닫히지 않은 것, 그대로 두세요

| 항목 | 상태 | 왜 안 닫혔나 |
|---|---|---|
| **HF 문서 80M vs 실측 51.6M의 원인** | 🔴 미확인 | |
| **Jetson급 온보드 추론 지연** | 📎 **미측정** | |
| **`lerobot-train-tokenizer`의 정체** | 📎 미확인 | |
| **`ffmpeg` 설치 처방(E1)** | ⚠️ 미검증 | |

**Q. 이 넷 중 "돌려보면 알 수 있는 것"과 "장비나 사람이 있어야 알 수 있는 것"을 가르세요.**

```
돌려보면 알 수 있는 것:
그렇지 않은 것:
```

### 8.3 실습을 하고 나서야 생긴 질문 (이게 제일 값집니다)

```

```

---

## ⑨ 내가 발견한 불일치 (문서 vs 실제 출력)

> 문서와 출력이 어긋나면 **출력을 믿고** 여기 적으세요. 자료 수정의 입력이 됩니다.
> 단, **하이퍼파라미터 21개**, **z=0 결정론성(정확히 0)**, **청크 shape `(1, 100, 14)`** 는 예외입니다. 결정론적이라 어긋나면 환경을 먼저 의심하세요.

| 위치 | 문서의 서술 | 실제 출력 | `verify`가 잡았나 |
|---|---|---|---|
| | | | ☐ |
| | | | ☐ |

**`verify_act_install.py`가 WARN이나 FAIL을 냈다면 CSV 행을 그대로 붙이세요.**

```
artifacts/W2-M1/labs/verify_act_install.csv 에서:

```

---

## ⑩ 막힌 지점과 해결 방법 (→ `docs/progress.md`로 그대로 옮길 것)

> 이 칸이 다음 주 자료의 입력이 됩니다. **지우지 마세요.**

| # | 증상 | 원인 | 해결 | 소요 |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

**에러 표(README §흔한 에러 7개)에 없던 새로운 에러:**

```

```

**어느 에러에 걸렸습니까?** (해당 항목에 체크)

☐ **E1** torchcodec traceback, 알아채는 데 ______분 걸림
☐ **E2** `NotImplementedError` ☐ **E3** `--policy.repo_id` 요구 ☐ **E4** HF 미인증 경고
☐ **E5** `--episode-index` 누락 ☐ **E6** 디스크 ☐ **E7** 인터프리터

**Q. E1에서 "설치 실패"로 오해했습니까?** ☐ 예 ☐ 아니오
오해했다면 **무엇을 보고 정상이라고 판단하게 됐습니까?**

```

```

---

## ⑪ 용어집 (5개)

→ [`../../../../notes/glossary.md`](../../../../notes/glossary.md) W2-M1 절에 추가.
후보: behavior cloning, compounding error, covariate shift, DAgger, action chunking, `chunk_size` vs `n_action_steps`, temporal ensembling, CVAE latent $z$, `kl_weight`($\beta$), LeRobotDataset, receding horizon

| 용어 | 내 말로 정의 | 제어·ML·LLM 비유 |
|---|---|---|
| | | |
| | | |
| | | |
| | | |
| | | |

---

## ⑫ 실행 로그 (→ `docs/progress.md`)

```
날짜:
환경: .venv-lerobot / lerobot ____ / torch ____ / GPU ____ (또는 CPU 단독)
돌린 것:
  - 설치 → ___초
  - verify --quick → 21/21 ____ · FAIL ___ · WARN ___ · ___초
  - verify --quick --with-datasets → fps ___ / ___ / ___ · 다운로드 ___초
  - lerobot-dataset-viz --save 1 → .rrd ___ MB · ___초
  - lerobot-train --steps=200 → loss ___ → ___ · KL 기여율 ___% → ___% · ___초
  - 재개 --resume=true --steps=260 → exit ___ · 체크포인트 ___개 · outputs ___ GB
  - verify --compare-cpu → τ_infer GPU ___ ms / CPU ___ ms · 큐 배율 ___배
  - Step 7 백지 아키텍처 → 맞힌 칸 ___ / 틀린 칸 ___ / 물음표 ___
  - Step 8.A 표 재계산 → 200 ms 문턱 대비 ___배 · 8.B 완료 기준 문단 작성
막힌 지점:
소요 시간: Step 1~6 ___분 / Step 7~8 ___분
GPU 비용: 로컬이므로 $0
디스크: 시작 ___ GB → 끝 ___ GB (outputs 정리 여부: ___)
미검증 배지: ffmpeg 처방 ___ / Jetson 측정 ___ (둘 다 그대로면 "변동 없음")
다음에 할 것:
```

---

## ⑬ 최종 제출 체크

- [ ] **G1**, `verify --quick`이 **`21/21 PASS`** 와 **`FAIL 0`** 를 찍었다 *(② 2.2)*
- [ ] **G2 ★★**, **`fps=50`** 을 확인했고, §3.6 표의 **두 열이 각각 무엇인지** 썼다 *(③ 3.3)*
- [ ] **G3**, `.rrd` **25 MB**가 생겼고 **뷰어를 한 번도 띄우지 않았다** *(④ 4.1)*
- [ ] **G4 ★**, **loss 24.4 → 3.8**을 봤고 **KL 기여율을 손으로 계산**했다 *(④ 4.2, 4.3)*
- [ ] **G5 ★★**, **τ_infer**를 측정했고 **큐 소비 배율**을 설명했다 *(④ 4.4, 4.6)*
- [ ] **G6 ★**, excalidraw를 **lesson을 닫은 채** 채웠고, **A군 6개**를 맞혔다 *(⑤ 5.2)*
- [ ] **G7 ★★**, ⑥에 **§3.6 표를 자기 숫자로** 다시 채웠고, ⑦에 **완료 기준 한 문단**을 썼다 *(⑥, ⑦)*
- [ ] **G8**, ⑧ 8.1에 **팀 질문 둘을 고르고 이유를 썼다**
- [ ] **G9**, `docs/progress.md`, `notes/glossary.md`, `notes/questions-for-team.md`, `docs/course-plan.md` 갱신
- [ ] **G10**, **`outputs/train/act_w2m1_smoke`를 지웠다**(1.2 GB) 또는 남겨둘 이유를 적었다
- [ ] **[P1]** `notes/papers/`에 ACT(2304.13705) 2-pass 노트 착수

> ⑥ 6.2의 다섯 문장에 **숫자가 안 적혀 있으면 G7은 미완입니다.** 숫자 없는 "지연은 제약이 아니다"는 lesson 요약을 옮겨 적은 것이지 자기 답이 아닙니다.
> ⑦ 7.2가 세 문단이면 G7도 미완입니다. 완료 기준이 **한 문단**을 요구합니다.
