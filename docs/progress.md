# 학습 실행 로그

> **이 문서의 역할**: 실제로 **무엇을 돌렸고 무엇이 막혔는지** 기록합니다.
> 집필 현황(체크박스)은 [course-plan.md](course-plan.md)에서 추적합니다 — 역할이 다르니 섞지 마세요.
> 여기 쌓인 실측값(에러·소요시간·GPU 비용)이 다음 주 교육자료의 입력이 됩니다.

---

## 기록 규칙

- **이 문서에는 두 종류의 로그가 들어갑니다.** §주간 요약·§일자별 기록은 **학습자**가 커리큘럼을 실행한 로그이고, §집필 검증 실행은 **집필자**가 자료를 쓰면서 코드를 돌려 확보한 실측 로그입니다. 블록 템플릿도 완료 기준도 다르므로 두 섹션을 섞지 않습니다.
- 하루에 한 블록. 실습을 돌린 날만 씁니다(이론만 읽은 날은 생략 가능).
- **막힌 지점과 해결 방법은 반드시 남깁니다** — 그대로 해당 모듈 `labs/`의 "흔한 에러" 섹션으로 옮겨집니다.
- GPU 비용은 인스턴스 타입 × 시간으로 기록. 주간 예산 상한을 넘기면 §주간 요약에 표시합니다.
- `> ⚠️ 미검증(GPU 필요)` 배지가 달린 절차를 실행했다면, 검증 결과를 남기고 lesson에서 배지를 제거합니다.

### 블록 템플릿

```markdown
## YYYY-MM-DD (W_-M_ / 토픽 슬러그)

**환경**: 제공자 / 인스턴스 타입 / GPU / CUDA
**돌린 것**:
- (명령 또는 스크립트) → 결과

**막힌 지점**:
- 증상 → 원인 → 해결 (labs 반영 여부)

**소요**: 실습 _h / 학습 _h
**GPU 비용**: $_ (인스턴스 _h)
**미검증 배지 해제**: (모듈/절차) 또는 없음
**다음 액션**:
```

---

## 주간 요약

| 주차 | 모듈 완주 | 실습 산출물 | GPU 시간 | 비용 | 비고 |
|---|---|---|---|---|---|
| W0 | — | 환경 셋업 | — | — | |
| W1 | 0 / 5 | | | | |
| W2 | 0 / 5 | | | | |
| W3 | 0 / 5 | | | | |
| W4 | 0 / 5 | | | | |

---

## 일자별 기록

<!-- 최신 기록을 위에 추가합니다 -->

_(아직 없음)_

---

<!-- ══════ 여기부터 집필자 로그. 위쪽 학습자 로그와 섞지 마세요 ══════ -->

# 집필 검증 실행 (집필자)

> **위 §주간 요약·§일자별 기록과 성격이 다릅니다.** 여기 있는 것은 학습자가 커리큘럼을 완주한 기록이 아니라, **자료를 쓰면서 집필자가 코드를 돌려 확보한 실측값**입니다. 학습자 실행검증(`course-plan.md` §2의 산출물 5번)은 여전히 미완이며, 그 체크박스는 이 섹션과 무관합니다.
> 여기 적힌 소요·에러·환경은 **학습자 랩의 성공 판정 기준과 시간 예산의 출처**입니다. 학습자가 같은 절차를 돌려 값이 다르면, 그 차이를 §일자별 기록에 남기고 이 표를 갱신하세요.
> **모듈당 1블록**입니다(학습자 로그의 "하루 1블록"과 다릅니다). 다만 규약 개정처럼 한 회차가 여러 모듈과 집필 도구에 걸치면 모듈 블록에 넣지 않고 **회차 블록**을 따로 씁니다. 지금 그런 블록이 맨 아래 「2차 개정 회차」 하나 있습니다.

## 요약표 — W1 5개 + W2-M1

| 모듈 | 검증 환경 | 스모크 (실측) | 본 실행 (실측) | 막힌 지점 | 미검증 배지 | 비용 |
|---|---|---|---|---|---|---|
| **W1-M1** | Python 3.12.3 / CPU 전용 / 2026-08-01 | 3종 각 **1.3초** | 3종 각 **1.3초** (합계 1분 미만) | **6건** | 없음 | **$0** (클라우드 미사용) |
| **W1-M2** | Python 3.12.12 / WSL2 / **CPU** / mujoco 3.11.0 · playground 0.2.0 · jax 0.11.0(CPU 빌드) / 2026-08-01 | 03 스모크 5초 | 4종 합계 **약 90초** (03만 정밀 실측 27초) | **10건** | **2건** (GPU JAX 경로) | **$0** |
| **W1-M3** | Python 3.12.12 / WSL2 / i7-12700K(20스레드) / **RTX 3080 12GB** / torch 2.13.0+cu130 / 2026-08-03 | GPU **31초** / CPU **61초** | GPU **2분 55초** / CPU **약 10분** | **8건** | 없음 | **$0** |
| **W1-M4** | 위와 동일 (torch 2.13.0+cu130 / RTX 3080 / WSL2) / 2026-08-04 | GPU **42초** / CPU **247초** | GPU **6분 30초** / CPU 미측정(추정 30~40분) | **8건** | 없음 | **$0** |
| **W1-M5** | Python 3.12.12 / WSL2 / **RTX 3080 12GB** / torch 2.13.0+cu130 / mujoco 3.11.0 / 2026-08-02 | 02 **9.6초** · 03 **7.9초** | `02 --sweep` **165초** · 03 **68.9초** (전체 약 5분) | **6건** | 없음 | **$0** |
| **W2-M1** | ⓐ practice: macOS / Python 3.14.6 / CPU / torch 2.13.0 / 2026-08-06 <br> ⓑ **labs: WSL2 / Python 3.12.12 / RTX 3080 12GB / lerobot 0.6.1 · torch 2.11.0+cu130 / 2026-08-09** | A층 3종 합 **4.4초** · `04 --smoke` 16.5초 <br> **학습 스모크 200스텝 31초** | `04` **2분 7초** <br> **설치 42초 · viz 8.8초 · 재개 18초** | **6건** | 없음 (배지 제거) | **$0** |

> ⚠️ **W2-M1은 검증 환경이 둘입니다.** practice(ⓐ)와 labs(ⓑ)를 다른 기기·다른 파이썬·다른 torch에서 검증했습니다. 상세는 아래 W2-M1 블록.
>
> 🔁 **W1-M1 행은 2026-08-01 원 집필 검증의 값이고 지금도 유효합니다.** 2026-08-28에 이 모듈을 새 규약으로 재작성하고 검증도 다시 했지만, 그 회차는 규약 SSOT와 집필 도구와 lint에도 걸쳐 있어 모듈 행이 아니라 아래 「2차 개정 회차」 블록에 적었습니다. **이 표는 갱신하지 않았습니다.**

**W1 합계**

| 항목 | 값 |
|---|---|
| 클라우드 GPU 시간 | **0시간** — W1 집필 전 구간을 로컬 RTX 3080 12GB(WSL2)와 CPU로 처리 |
| 클라우드 GPU 비용 | **$0** |
| 로컬 GPU wall-clock | **약 15~16분** (문서에 남은 정본 실행 1회분의 단순 합. 집필 중 반복 실행 횟수는 **미기록**) |
| 30분을 넘긴 절차 | **0개** — 마스터플랜 부록 A.0의 "W1~W2는 로컬로 완결"이 실측으로 뒷받침됨 |
| 미검증 배지 잔존 | **2건** (전부 W1-M2의 GPU JAX 경로) |

### 블록 템플릿 (집필 검증용)

```markdown
## W_-M_ (토픽 슬러그) — 집필 검증

**집필 기간 / 커밋**: YYYY-MM-DD ~ YYYY-MM-DD · 커밋 _개
**검증 환경**: Python / CPU / GPU / OS / 주요 패키지 버전 / 측정일
**돌린 것과 실측 소요**: 스모크와 본 실행을 구분. GPU·CPU 양쪽을 잰 경우 둘 다
**막힌 지점**: 증상 → 원인 → 해결 (어느 문서에 반영했는지)
**GPU 비용**: 인스턴스 타입 × 시간, 또는 로컬이면 $0
**미검증 배지**: 남은 배지와 그 사유, 또는 없음
**미기록**: 재지 않고 지나간 측정 항목과 그 이유
```

---

## W1-M1 (physical-ai-landscape) — 집필 검증

**집필 기간 / 커밋**: 2026-08-01 ~ 2026-08-02 · 커밋 8개 (`1cd8765` lesson → `18231dd` practice → `2ef75d9` artifacts 정리 → `09548b3` labs → `28803f4` 윤문 → `74f0b15` 마무리 → `bed3ca0` Tier A 승급 + 재윤문 → `5cf7113` gantt 교체)

**검증 환경**

| 항목 | 값 |
|---|---|
| Python | **3.12.3** (W1-M2 이후의 3.12.12와 다릅니다 — 아래 §수치 불일치 ④) |
| GPU | **해당 없음.** 이 모듈의 실습은 matplotlib 시각화 전용이라 GPU·MuJoCo·시뮬레이터를 쓰지 않습니다 |
| 패키지 | matplotlib 3.11.1 · numpy 2.5.1 · pandas 3.0.5 · jupytext 1.19.5 |
| 렌더 | matplotlib `Agg` 강제(`plt.show()` 0건), 한글 폰트는 글리프 보유 여부로 판정 후 영문 폴백 |
| 측정일 | 2026-08-01 |

**돌린 것과 실측 소요**

- `01_stack_frequency_budget.py` · `02_data_pyramid_plot.py` · `03_player_landscape.py` — `--smoke`와 전체 모드 **전부 완주, 각 1.3초**(CPU). 세 개를 다 돌려도 1분 미만.
- Jupyter 커널 argv 조건에서도 완주(argparse `SystemExit` 회피 확인).
- jupytext `.py` ↔ `.ipynb` 왕복 불변, 노트북 출력 비움 상태로 커밋.
- PNG 4종 육안 확인(한글 폰트 정상, 라벨 겹침 없음). excalidraw JSON 검증 — 37 elements, 필수 필드 누락 0, dangling ref 0.
- lesson §3 부등식의 예시값(H=16)을 스크립트가 `assert`로 검증. Step 2 세 시나리오는 손계산과 스크립트 값이 일치(S3의 80% vs 78.72% 차이는 정상상태 duty vs 유한구간 실측 — 양쪽 모두 맞음).
- 렌더 검증: markdown-it로 볼드 실측, mermaid 파서로 3블록 통과.

**막힌 지점 (6건)**

*실행·환경 2건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 1 | `--ascii-labels`로 뽑으면 한글판 그림이 사라짐 | `01`만 같은 파일명에 덮어쓰고 `02`·`03`은 `*_ascii.png`로 분리 저장 — 규약이 갈려 있었음 | `01`도 `_ascii` 접미사 저장으로 통일 (`09548b3`). labs **E1**에 잔여 주의사항으로 남김 |
| 2 | `artifacts/W1-M1/`에 `_ascii` 파일이 함께 커밋됨 | `--ascii-labels`로 언제든 재생성 가능한 중복 산출물 | `.gitignore` 추가 + 정본 4종만 유지 (`2ef75d9`) |

*집필·검증 4건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 3 | 볼드가 리터럴 `**`로 렌더됨 | 윤문이 만든 `**"액션의 BPE"**라는` 형태가 CommonMark에서 볼드를 닫지 못함 | markdown-it 실측 후 수정. **`28803f4`와 `bed3ca0`에서 2회 재발** — 윤문 후 렌더 확인을 고정 절차로 |
| 4 | §4.3 ASCII 타임라인이 뷰어마다 다르게 깨짐 | 한글 라벨 폭 편차(18~21칸) + Ambiguous-width 박스 문자(CJK 2칸 / 서양 1칸). 세로선이 서양 폰트 9열 · CJK 18열로 흩어짐 | Mermaid `gantt`로 교체 (`5cf7113`). 같은 방식으로 측정한 §3.1 블록도는 1열로 일관이라 **유지** |
| 5 | lesson "각 경계에서 대략 한 자릿수 이상의 비율이 유지됩니다"가 사실이 아님 | L1/L2 ×8.9, L2/L4 ×50이지만 **L4/L5는 대표 ×2.0, 최악 ×0.20으로 역전** | 실측 표로 교체 + "이 경계는 대역폭이 아니라 기능으로 갈렸다"는 해석을 오해 2번·퀴즈 3번까지 연결 (`bed3ca0`). **practice 스크립트(`01_stack_frequency_budget.py:299-301`)가 이미 본문을 반박하고 있었음** |
| 6 | practice README §5 "L1↔L2가 가장 넓다" | 실측은 L2↔L4(×50) > L1↔L2(×8.9) | 정정 + "가장 넓은 그 틈에 L3가 놓인다"는 해석 보강 (`09548b3`) |

**GPU 비용**: **$0.** 클라우드 인스턴스 미사용, 로컬 CPU만.

**미검증 배지**: **없음.**

**미기록**

- GPU 측정 — **해당 없음**(CPU 전용 시각화 모듈).
- 집필에 든 사람 시간(wall-clock) — 집필 당시 기록하지 않음.

> 🔁 **이 모듈은 2026-08-28에 새 규약으로 재작성됐고 검증도 다시 했습니다.** 위 실측은 2026-08-01 원 집필 검증의 것이고 그대로 유효합니다. 재작성 회차의 실측과 그때 드러난 결함은 아래 「2차 개정 회차」 블록에 있습니다. 특히 **위 1번을 고친 커밋 `09548b3`이 `.py`만 고치고 `.ipynb`를 재생성하지 않아** 노트북 쪽에 같은 결함이 2026-08-29까지 잠복해 있었다는 것이 그때 드러났습니다.

---

## W1-M2 (simulator-bootcamp) — 집필 검증

**집필 기간 / 커밋**: 2026-08-01 ~ 2026-08-02 · 커밋 5개 (`d013b2f` lesson → `aaac668` practice → `2f1c575` labs → `383bc90` 윤문 → `8720f4c` 마무리)

**검증 환경**

| 항목 | 값 |
|---|---|
| Python | **3.12.12** (`jax`가 `requires_python >= 3.12`) |
| OS | Ubuntu on **WSL2** |
| GPU | **쓰지 않음.** 01~04 전부 CPU로 완주 — 이 모듈에는 GPU 필수 단계가 없다는 것이 실측 결론 |
| 패키지 | mujoco **3.11.0** · numpy 2.5.1 · playground **0.2.0**(import명 `mujoco_playground`) · jax **0.11.0 CPU 빌드** · imageio 2.37.4 · mediapy 1.2.7 |
| 렌더 백엔드 | `MUJOCO_GL=egl` ✅ / `glfw` ✅ / **`osmesa` ❌**(libOSMesa 미설치) |
| 로봇 모델 | mujoco_menagerie `--depth 1` 클론 **2.3 GB** |
| 측정일 | 2026-08-01 |

**돌린 것과 실측 소요**

- `01`~`04` 스모크·본 실행 **전부 EXIT=0**. 스크립트 실행 시간 합계 **약 90초**.
  - `01_mujoco_basics.py` 1분 이내 / `02_g1_inspect.py` 수 초 / `03_g1_sin_wave.py` **20~30초**(스모크 5초, `--joints arms` 정밀 실측 **27초** = 6초 시뮬 + 180프레임 렌더) / `04_playground_smoke.py` 1~2분(JAX 첫 컴파일 대기 포함).
- 실측으로 확보한 값(전부 lesson·labs 본문의 출처):
  - G1 `scene.xml` **nq=36 · nv=35 · nu=29**, `stand` 키프레임 골반 **0.79 m**, 총질량 **33.341 kg**
  - position 액추에이터 `kp=500 dampratio=1` → `kv`가 관절별로 다름(43.01 / 15.85 / 16.73 / 9.43)
  - timestep 스윕 수렴차수 **Euler 0.91 / RK4 3.36**
  - `02`의 세 인덱스 정렬(`ctrl i ↔ qpos i+7 ↔ qvel i+6 ↔ jnt i+1`) **assert 29개 통과**
  - `03` 낙상 스펙트럼 — `arms` 유지 / `waist` **t=2.66 s**(골반 0.790→0.062 m) / `legs` **t=0.88 s**
  - `04` `G1JoystickFlatTerrain` — state(103,) / privileged(216,) / action 29 / 50 Hz 정책 · 500 Hz 물리 · `n_substeps` 10 · 보상 24항
- 디스크 실측: menagerie 2.3 GB · `jaxlib` 339 MB · `mujoco` 74 MB.

**막힌 지점 (10건)**

*실행·환경 6건 — labs "흔한 에러" 10개 중 E1·E2·E3·E5·E7·E8이 집필 환경에서 실제 재현된 메시지입니다*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 1 | `MUJOCO_GL=osmesa` → `glGetError` AttributeError | **libOSMesa 미설치** | `egl`/`glfw`로 진행. 백엔드 폴백 순서도(Mermaid) + labs **E2** |
| 2 | 종료 시 EGL traceback | `Exception ignored in:` — 파이썬이 이미 무시한 예외. 프레임은 정상 저장됨 | **무해**로 판정. `renderer.close()` 명시 호출로 빈도를 줄이고 labs **E1**을 맨 위에 배치 |
| 3 | `AttributeError: 'MjData' object has no attribute 'qM'` | mujoco 3.11에서 **`data.qM` → `data.M`**으로 이름 변경(`mj_fullM` 시그니처도 변경) | lesson 오류 정정 + **API 주의 박스 신설** + labs **E3** (`aaac668`) |
| 4 | `pip install mujoco_playground` → 그런 패키지 없음 | **배포명은 `playground`, import명이 `mujoco_playground`** | lesson §7.1 · requirements 주석 · labs **E4**에 3중 기재 |
| 5 | `MUJOCO_GL`을 바꿔도 백엔드가 안 바뀜 | 백엔드가 **`import mujoco` 시점에 결정**됨 | `os.environ.setdefault`를 import 이전에 배치. 노트북은 커널 재시작 필요 — labs **E6** |
| 6 | `04`가 멈춘 것처럼 수십 초 정지 | `jax.jit(env.reset)` 첫 호출의 추적 + 컴파일(CPU) | 대기 전 안내 문구 선출력 + labs **E8**. LLM 서빙 웜업 비유로 설명 |

*집필·검증 4건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 7 | `scene_with_hands.xml`에서 `ctrl i ↔ qpos i+7` 규칙이 손가락 4개에서 깨짐 | **actuator 선언 순서 ≠ 관절 트리 순서** | 29 DoF `scene.xml`에서는 성립함을 확인하고, `02`가 assert 대신 **위반 표로 출력**하도록 변경 — "손으로 세지 말라"의 실물 증거 |
| 8 | lesson 수치 4건이 실측과 불일치 | 집필 시점 자료 기준으로 적은 값 | 정정 — `data.qM`→`data.M` · classic `ls_iterations` 기본 **50** · MJX `kp` 75이되 **발목4·손목6은 20** · 위 반영해 퀴즈 5번 정답 갱신 (`aaac668`) |
| 9 | 심화 과제가 `--joints waist`를 "안 넘어짐"으로 암시 | 실측은 **t=2.66 s 낙상** | arms(유지) → waist(2.66 s) → legs(0.88 s) **안정 여유 스펙트럼**으로 재작성 (`2f1c575`) |
| 10 | 윤문이 의미를 훼손 — fidelity **롤백 9건** | I-4(권고형 축소) 적용이 격식체 **의무 서술까지** 걷어냄 | 복원. "절대 손으로 세지 마세요"(자기 표와 모순되는 진술로 바뀜) · "기본적으로 지연이 0"(조건부→무조건) · "반드시 이걸 써야 합니다"(silent failure 경고 소실). 부수로 원문 오류 2건도 정정(§1.3 상호참조 §4.4→§5.6, §3.3 비문) |

**GPU 비용**: **$0.** 클라우드 미사용. **이 모듈은 GPU 자체를 쓰지 않았습니다** — labs §0.2에 "GPU 필수 단계는 없다"를 표로 못박은 근거가 이 실측입니다.

**미검증 배지**: **2건 잔존.**

| 위치 | 대상 절차 | 검증된 범위 |
|---|---|---|
| `course/w1-generative-core/02-simulator-bootcamp/lesson.md:774` | `mujoco_playground` **GPU 학습 경로**(`jax[cuda12]` 설치 + MJX 병렬 롤아웃) | CPU JAX 기준 import · 레지스트리 조회 · `env.reset`/`env.step` 1회까지 |
| `course/w1-generative-core/02-simulator-bootcamp/labs/README.md:1004` | 같은 GPU JAX 경로 (Step 5) | 동일 |

> 이 두 배지는 **W3-M1(PPO 병렬 보행 학습) 착수 시점에 해소**하는 것이 자연스럽습니다. 해소하면 여기와 lesson·labs 양쪽에서 배지를 제거하세요.

**미기록**

- `01`·`02`·`04`의 초 단위 정밀 실측 — 범위 표기(1분 이내 / 수 초 / 1~2분)만 남기고 개별 계측은 하지 않음.
- GPU 인스턴스에서의 재실행 — 로컬 CPU로 전 절차가 통과해 GPU 경로를 열지 않음(그 결과가 위 미검증 배지 2건).

---

## W1-M3 (diffusion-ddpm-dit) — 집필 검증

**집필 기간 / 커밋**: 2026-08-03 (하루) · 커밋 4개 (`e855892` lesson → `4e7e87e` practice → `2747e5c` labs → `18f507f` 윤문)

**검증 환경**

| 항목 | 값 |
|---|---|
| Python | **3.12.12** |
| OS | Ubuntu on **WSL2** |
| CPU | **i7-12700K** (20 스레드) |
| GPU | **RTX 3080 12GB** (capability 8.6) — 이 모듈에서는 선택 사항 |
| 패키지 | torch **2.13.0+cu130**(`cuda.is_available() == True`) · numpy 2.5.1 · scikit-learn 1.9.0 · mujoco 3.11.0 · matplotlib 3.11.1 |
| 시드 | 세 스크립트 전부 `--seed 0` |
| 측정일 | 2026-08-03 |

**돌린 것과 실측 소요** — GPU·CPU 양쪽을 각각 계측했습니다.

| 명령 | GPU | CPU | 비고 |
|---|---|---|---|
| `01 --smoke` | **12.7초** | **20.1초** | 600 스텝. 표 검산은 스모크에서도 그대로 |
| `02 --smoke` | **6.6초** | **9.0초** | NFE 집합 `{1,4,20,250}`로 축소 |
| `03 --smoke` | **11.9초** | **31.6초** | 300 스텝, DiT-XL 벤치 생략 |
| **스모크 합계** | **31초** | **61초** | |
| `01` (본 실행) | **97초** | **279초** | 12,000 스텝 + ancestral 4,096개(NFE 1000) |
| `02` (본 실행) | **9초** | **60~100초** | NFE 8종 × 샘플러 2종 = 16회 샘플링 |
| `03` (본 실행) | **68초** | **275초** | 3,000 스텝 + DiT-XL급 forward 벤치 |
| **본 실행 합계** | **약 2분 55초** | **약 10분** | |

검증된 주장:

- lesson **§3.3 표 5행 + §4.4 표 6행이 인쇄된 자릿수까지 `assert` 통과**(스케줄은 학습 파라미터 0개라 결정론적).
- `03`의 adaLN-Zero 항등 검증 — $\alpha=0$에서 오차 **0.000e+00**, 대조군 **2.9e-01**. 항등을 만드는 것이 $\gamma\beta$가 아니라 $\alpha$ 하나임이 bit 단위로 확인됨.
- `[B,32,29] → patchify → [B,16,d] → unpatchify` shape 관통, G1 `scene.xml`에서 `model.nu=29` 직접 확인.
- **30분 넘는 절차 0개** → lesson §13의 "CPU로 완주되고 로컬 RTX 3080이면 몇 분"이 실측으로 확인됨.

**막힌 지점 (8건)**

*실행·환경 3건 — labs 흔한 에러 6종 중 E1·E5·E6이 실제 재현*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 1 | `jupytext --execute`로 노트북을 검증할 수 없음 | 집필 환경에 **`nbclient`/`ipykernel` 부재** | 노트북과 같은 조건(`__file__` 미정의 · argparse 무인자 · 단일 네임스페이스 셀 순차 실행)으로 `.py`를 돌려 대체하고, **검증 방식을 practice README에 명시**. 실제 Jupyter Run All은 학습자 몫으로 남김 |
| 2 | `03` forward 벤치의 ms가 문서와 최대 2배 다름 | **배치 1 · 토큰 16개는 커널 실행 오버헤드가 연산량을 지배.** WSL2 GPU 패스스루·스레드 스케줄링·캐시 상태로 흔들림. 같은 명령 3회에 d=128이 **1.2 / 1.35 / 2.36 ms** | 판정 기준을 절대값에서 **세로 방향 배율**로 변경. labs **E6** + practice README §5.3에 명시. `02`의 ms 열도 ±30% 흔들리며 **energy 열은 시드 고정이라 재현**됨을 대비로 기재 |
| 3 | loss 텐서 `float()` 변환 UserWarning 3곳 | 0-dim 텐서 암묵 변환 | `.item()`으로 수정 (`4e7e87e`) |

*집필·검증 5건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 4 | §3.3·퀴즈 2의 SNR=1 교차점 `t≈250`이 실측과 불일치 | 근사 서술 | **t=259**($\bar\alpha=0.500$), 158배 → **157배**로 정정 (`4e7e87e`) |
| 5 | §5.1·§9의 "부분 격자에서 품질 급락"이 과장 | 실측은 NFE 4~20에서 DDIM이 **1.2~1.8배 우세**하고 NFE 50 이상에서는 차이 소멸 | 실측값 + practice 링크로 교체 (`4e7e87e`) |
| 6 | 윤문이 의미를 훼손 — fidelity **롤백 3건** | 문체 규칙의 기계적 적용 | 전부 복원. §6.1 "파라미터 수는 상관이 적습니다"(범주적 배제 → 정도 주장, **§9와 문서 내부 모순**) · §8.4 "칸이 다섯입니다" 삭제(**§12가 "5개 항목"으로 참조 중**이라 상호참조 파손) · §8.4 "미확인" → "정해지지 않았습니다"(인식 상태를 세계의 사실로 단언, §3.6 위반) |
| 7 | 한글 수사 4건 삭제가 자가검증을 통과 | 윤문가의 수치 자가검증이 **아라비아 숫자만 계수**(944/944 통과) | 보존 검증에 **한글 수사 계수** 추가. 다음 회차부터 "개수어를 지우기 전에 그 수가 다른 절에서 참조되는지 확인" 지시 (course-plan §9.3). W1-M4에서 이 처방이 실제로 작동 |
| 8 | 분량이 Tier C 상한을 약 37% 초과 | 마스터플랜이 한 모듈에 재해석 대상 6개를 묶음 | 중복 제거 1,238자가 한계임을 확인하고 **초과 상태로 확정 + 근거 기록**(course-plan §9.2). `est_reading_min` 25 → **40** 정정 |

**GPU 비용**: **$0.** 로컬 RTX 3080. 클라우드 인스턴스 미사용.

**미검증 배지**: **없음.** lesson·practice·labs 전 절차가 집필 시점에 실행 검증됨(labs §0.5에 "미검증 절차가 없습니다"로 명시).

**미기록**

- 집필에 든 사람 시간 — 기록하지 않음.
- `02` CPU 본 실행은 **60~100초 범위**로만 남기고 단일 확정값을 잡지 않음(실행 편차 때문).

---

## W1-M4 (flow-matching) — 집필 검증

**집필 기간 / 커밋**: 2026-08-04 ~ 2026-08-05 · 커밋 5개 (`7c07bd1` lesson → `f55575b` practice → `32b839d` labs → `6a10cee` 윤문 → `88b3311` 마무리)

**검증 환경**

| 항목 | 값 |
|---|---|
| Python | **3.12.12** |
| OS | Ubuntu on **WSL2** |
| CPU | **i7-12700K** |
| GPU | **RTX 3080 12GB** (capability 8.6) |
| 패키지 | torch **2.13.0+cu130** · numpy 2.5.1 · scikit-learn 1.9.0 · mujoco 3.11.0 · matplotlib 3.11.1 — **W1-M3와 완전히 동일. 신규 의존성 0개** |
| 시드 | 세 스크립트 전부 `--seed 0` |
| 측정일 | 2026-08-04 |

**돌린 것과 실측 소요**

| 명령 | GPU | CPU | 비고 |
|---|---|---|---|
| `01 --smoke` | **10초** | **26초** | 800 스텝 |
| `02 --smoke` | **8초** | **49초** | reflow 1회 · 600 스텝 |
| `03 --smoke` | **24초** | **172초** | 400 스텝 × 2 objective |
| **스모크 합계** | **42초** | **247초** (전부 완주) | |
| `01` (본 실행) | **112초** | 미측정 | 12,000 스텝 + NFE 8종 스윕 |
| `02` (본 실행) | **137초** | 미측정 | reflow 2회 × 8,000 스텝 + 3모델 × NFE 8종 |
| `03` (본 실행) | **138초** | 미측정 | 3,000 스텝 × 2 + DiT-XL 벤치 |
| **본 실행 합계** | **약 6분 30초** | **미측정** (스모크 비 환산 추정 30~40분) | |

검증된 주장:

- **W1-M3가 막혔던 NFE 1~2를 뚫음** — sliced $W_1$ 기준 NFE=1에서 ancestral 0.5648 / DDIM 0.5648 / FM 1-rect 0.8509 → **RF 2-rect 0.0254**(노이즈 바닥 0.0261 이하).
- 직선성 $S(Z)$ **0.9075 → 1.489e-05**(6.4만 배).
- `03`이 W1-M3 코드를 **모듈로 import해 재사용**(재사용 376줄 / 신규 83줄). `difflib`로 학습 스텝 diff가 **정확히 3줄**임을 스크립트가 확인. 같은 백본 1,275,450 파라미터에서 DDPM vs FM = 같은 품질에 **NFE 50 vs 20(2.5배)**, 시작 포즈 오차 **72.74° vs 2.59°**.
- 겹쳐 그리기 자격을 **4중 검사**(분포 bit 대조 · 표본 수 · 지표 함수 import 재사용 · CSV 무재계산)하고 하나라도 어긋나면 자동으로 분리 패널로 강등.

**막힌 지점 (8건)**

*실행·환경 4건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 1 | 노트북 경로에서 `OSError` | **`inspect.getsource()`가 `exec` 환경에서 소스를 못 찾음.** `01`의 §1(손실 본문 출력)과 `03`의 `objective_diff()`가 그걸 쓰고 있었음 | 원본 `.py`를 **`ast`로 파싱하는 폴백 `source_of()`** 추가. **셀 단위 exec 시뮬레이터 검증이 실제로 잡아낸 버그** |
| 2 | 겹쳐 그리기가 **에러 없이 조용히** 생략됨 (점선 2개가 그냥 없음) | 자격 검사가 `artifacts/W1-M3/01_model.pt`의 `data_ref` bit 대조를 요구하는데 **`.pt`는 `.gitignore` 대상**(`artifacts/**/*.pt`). CSV는 추적되므로 **클론 직후가 "곡선은 있는데 자격이 없는" 상태** | labs Step 1을 **두 파일 동시 확인 관문**으로 세우고 **E2**를 별도 에러 항목으로 문서화 (`32b839d`) |
| 3 | W1-M3 코드를 재사용했는데 학습이 진행되지 않음 | objective diff는 3줄이 맞지만 **시간 임베딩 대역 정합(`t×1000`) 한 줄**이 더 필요 | lesson §7.1에 실측 각주 추가 (`f55575b`) |
| 4 | CPU 본 실행 시간을 확정할 수 없음 | `nbclient`/`ipykernel`/`nbconvert` 부재(M3와 동일) + CPU 본 실행 미측정 | 노트북은 셀 exec 시뮬레이터로 검증(01: 셀 11개 · 02: 7개 · 03: 9개, 각 약 100~125초). **CPU 본 실행은 추정임을 labs §0.3에 ⚠️로 명시** |

*집필·검증 4건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 5 | `course-plan`의 practice 소요가 practice README 실측과 불일치 | 집필 중 수치가 갱신됐는데 상위 문서가 따라가지 않음 | practice README 실측 **6분 30초(112+137+138초)** 로 통일 (`32b839d`) |
| 6 | 윤문의 C-8("A가 아니라 B") 해체 **3건이 전부 훼손** | **"A가 아니라 B"가 범주적 배제를 운반**하므로 대구를 풀면 비교급으로 내려가거나 층위가 오름 | 3/3 전부 복원. **W1-M3에 이어 2회 연속**이라 C-8을 **자동 적용 대상에서 제외하고 수동 검토로 고정**할 것을 스킬 개정 제안에 등록 (course-plan §9.5) |
| 7 | 윤문가가 🔴·⚠️를 이모지 과다(C-5)로 삭제 | 이 저장소에서 두 기호는 **§3.5(미검증 배지)·§3.6(모르는 것은 모른다)의 표기 규약**이지 장식이 아님 | 복원 + `register: course`의 **Do-NOT 목록 등재** 제안 |
| 8 | 윤문가가 F-5 3건을 "해소"로 보고했으나 실제로는 무변화 | **계획만 하고 치환 목록에 넣지 않음.** 자가검증에 어휘 빈도 대조가 없어 통과 | 롤백 재검증에서 윤문가 자신이 발견해 **burden 12 → 30으로 정정**. 자가검증에 "계획한 edit이 실제 치환 목록에 있는가" 대사 항목 추가 제안 |

**그 외 기록**

- 분량 **35,165자** — 사전에 정한 목표 밴드 28,000~31,000자도 초과. 초과 기록으로 확정하고 `est_reading_min` **45** · 소요 "이론 **2~2.5h**"로 정직하게 표기(마스터플랜 Day 4 배정 2h 대비 30분 초과).
- 출처 검증(2026-08-04)에서 걸러낸 것 — pi0의 action expert를 **DiT로 쓰지 않음**(MoE형 별도 트랜스포머) / RF의 $k$별 1-step FID는 **2차 출처 충돌로 미검증** / GR00T N1.5는 **arXiv 논문 부재**(모델 카드만).

**GPU 비용**: **$0.** 로컬 RTX 3080. 클라우드 인스턴스 미사용.

**미검증 배지**: **없음** (labs §0.5에 명시). 단, lesson §11에 **출처 미검증 3건**이 별도로 표기돼 있으며 이는 실행 배지가 아니라 인용 신뢰도 표시입니다.

**미기록**

- **CPU 본 실행 3종** — 스모크만 실측(247초)했고 본 실행은 돌리지 않음. 30~40분 추정치는 스모크 비 환산.
- 집필에 든 사람 시간 — 기록하지 않음.

---

## W1-M5 (latent-discrete-fsq) — 집필 검증

**집필 기간 / 커밋**: 2026-08-02 (하루) · 커밋 5개 (`5f0def5` lesson → `b138b54` practice → `338e495` labs → `ffc95af` 윤문 → `3c5001a` 마무리 + FSQ 논문노트)

**검증 환경**

| 항목 | 값 |
|---|---|
| Python | **3.12.12** |
| OS | **WSL2** |
| GPU | **RTX 3080 12GB** (capability 8.6) · `torch.cuda.is_available() == True` |
| 패키지 | torch **2.13.0+cu130** · mujoco **3.11.0** · numpy 2.5.1 · imageio 2.37.4 · mediapy 1.2.7 · matplotlib 3.11.1 |
| 로봇 모델 | mujoco_menagerie `--depth 1` 클론 **2.3 GB** |
| 시드 | `01`·`02` seed 0 / `03` seed 7 (`--demo-seed 789`) |
| 측정일 | 2026-08-02 |

**돌린 것과 실측 소요**

| 명령 | GPU | CPU | 비고 |
|---|---|---|---|
| `01_fsq_minimal.py` | **3.4초** | **3.4초** | torch만 쓰고 GPU 커널을 안 씀 — 같은 시간이 정상 |
| `02 --smoke` | **9.6초** | **9.6초** | 모델이 작아 차이 없음 |
| `02` (기본, 2 구성) | **31~57초** | **183초** | GPU 편차가 큼 (3회 측정 31 / 55.7 / 57.4) |
| `02 --sweep` (6 구성) | **165초** | 미측정(약 9분 추정) | **이 실습의 본체** |
| `03 --smoke` | **7.9초** | — | mp4 12프레임 |
| `03` (기본) | **68.9초** | — | 대부분이 mp4 렌더(480프레임 × 2회) |
| `03 --no-video` | **3.7초** | **3.1초** | GPU 이득이 없는 구간 |
| `03 --physics --demo-len 240` | **약 40초** | — | 물리 롤아웃 + mp4 |
| **전체** | **약 5분** | — | 30분 넘는 절차 0개 |

검증된 주장:

- `01` — `nn.Parameter` **0개**(코드북이 없다는 코드 수준 증거). assert 5종(왕복·인덱스범위·전단사 유일성·STE 해석해 대조·채널별 레벨수)이 짝수 L 포함 8개 구성에서 통과. Table 1 곱 240/1000/4375/15360/64000 재현(prod/target 0.938~1.068).
- `02` — 코드북 사용률 실측: **|C|=240/256 FSQ 100.0% vs VQ 83.6%**(MSE는 VQ 우세) / **1000/1024 FSQ 96.8% vs VQ 21.1%** / **4375/4096 FSQ 71.9% vs VQ 4.5%**. 복원 성능이 $2^{10}$에서 교차 — **논문 Fig 3과 lesson §5.5의 공정성 주장이 자체 데이터로 재현됨.**
- VQ 트릭 실측(|C|=1024) — 바닐라 21.1 / init-data 19.5 / EMA 18.8 / 둘 다 18.3. **도움이 되지 않았으므로 실측표만 남기고 해석 문구는 쓰지 않음.**
- `03` — 480프레임 → 정수 30개, 복원 RMSE 평균 **5.88°**(최악 `right_shoulder_yaw` 12.00°), 압축률 14,848 bits / 9.97 bits = **1,489.9배**(lesson 퀴즈 9번과 일치, 스크립트가 assert로 검증).
- **시드를 고정한 채 2회 실행해 표의 값이 재현**되는 것을 확인.

**막힌 지점 (6건)**

*실행·환경 3건 — labs 흔한 에러 9개 중 E2·E4·E9가 실제 관찰*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 1 | `[device] CUDA를 못 찾아 CPU로 폴백합니다` | 인덱스 지정 없이 `pip install torch`를 하면 환경에 따라 **CPU 휠**이 받아짐 | `--index-url .../cu130`으로 재설치. requirements 주석 + labs **E2**. CPU여도 완주는 됨(`02 --sweep`이 약 9분) |
| 2 | `02_results.csv`가 2행뿐 / 사용률이 이상하게 낮음 / `03_tokens.txt`에 토큰 4개 | **`--smoke`가 본 실행 png·csv를 그대로 덮어씀**(체크포인트만 `_smoke` 접미사로 분리) | 본 실행 재실행으로 복원 가능함을 labs **E4**로 문서화 + 심화 실행 전 백업 명령 제시 |
| 3 | 같은 명령의 실행 시간이 31 / 55.7 / 57.4초로 들쭉날쭉 | **WSL2 GPU 패스스루 오버헤드 + 첫 CUDA 컨텍스트 생성 + 커널 캐시 상태** | 대처 없음(정상). **벽시계를 성공 판정 기준에서 제외**하고 출력 숫자로 판정하도록 labs **E9**에 명시 |

*집필·검증 3건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 4 | 윤문 워크스페이스 사고 — **W1-M1 윤문 회차의 `01_input.txt`를 덮어씀** | 오케스트레이터가 `run_id` 재사용 규칙을 위반(`_workspace/2026-08-02-001`을 W1-M5가 재사용) | 발견 후 폴더 분리(W1-M5를 `-002`로 이동) + 사고 기록. **최종 산출물 영향 없음**(W1-M1 회차는 검증까지 끝난 상태였음) |
| 5 | 윤문이 의미를 훼손 — fidelity **필수 롤백 2건** | 문체 규칙의 기계적 적용 | §5.3 귀속 표지 "직관적으로는" 복원(**논문이 근거를 대지 않았다는 문장 바로 뒤**라 필자 추론과 논문 주장의 경계 표지가 중요) · §2 semantic compression **수식 범위 중의성** 해소(오독하면 이 문서의 1급 개념이 뒤집힘). naturalness 선택 수정 4건 추가 적용 |
| 6 | 변경률 2.32%로 Tier B 밴드(5~30%) 하한 이탈 | Tier B 문서도 보호 구간(코드·표·수식) 비율이 높음 | **저윤문 조건(하한 미달 AND S1 잔존)에 미해당** — S1 잔존 0이므로 accept. 산문 라인 변경률 **44.3%**를 병기 (course-plan §9.1 근거 적용) |

> **W1-M2에서 발생한 윤문 사고 유형 3종은 이 모듈에서 재발하지 않았습니다** — ①의무·금지 표지 삭제로 자기 표와 모순 ②한정어 삭제로 조건부 참이 무조건 참으로 ③`suggested_fix` 직접 적용으로 비문. 공정성 한정어 6/6 생존 확인.

**GPU 비용**: **$0.** 로컬 RTX 3080. 클라우드 인스턴스 미사용 — **"이번 주 가장 중요한 실습"(마스터플랜)이 로컬 5분으로 끝났다**는 것이 이 모듈의 비용 측면 결론입니다.

**미검증 배지**: **없음** (labs §0.5 "미검증 절차가 없습니다").

**미기록**

- `02 --sweep`·`03`의 **CPU 실행** — 미측정. `02 --sweep`의 "약 9분"은 `02` 기본 구성의 GPU/CPU 비(31~57초 vs 183초)에서 환산한 추정.
- 집필에 든 사람 시간 — 기록하지 않음.

---

## W2-M1 (imitation-learning-act) — 집필 검증

**집필 기간 / 커밋**: 2026-08-06 (lesson · practice) → **2026-08-09 (labs · lesson 패치)**

**이 모듈만의 특징: 검증 환경이 두 번 바뀌었습니다.** lesson·practice는 macOS + Python 3.14에서 썼고(그래서 표준 라이브러리 층과 torch 층으로 갈라 설계했습니다, `course-plan` §9.7·§9.8), labs는 **WSL2 + RTX 3080으로 돌아와 LeRobot을 실제로 설치**해 썼습니다. W1과 같은 기기입니다.

### 검증 환경

| 항목 | practice (2026-08-06) | **labs (2026-08-09)** |
|---|---|---|
| OS | macOS 26 (darwin arm64) | **WSL2** (Linux 6.6.87.2-microsoft-standard-WSL2) |
| Python | Homebrew 3.14.6 | **3.12.12** (uv 관리) |
| 가상환경 | 시스템 | **`.venv-lerobot/`** — 기존 `.venv`(W1용, torch 2.13)와 **분리**. `.gitignore` 등재 |
| GPU | 없음 (CPU) | **RTX 3080 12GB** · `torch.cuda.is_available() == True` |
| 주요 패키지 | torch 2.13.0 | **lerobot 0.6.1** · torch **2.11.0+cu130** · torchvision 0.26.0 · rerun-sdk 0.33.1 · pyav 15.1.0 (torchcodec 0.11.1은 로드 실패, 아래 ②) |

**환경을 분리한 이유**: LeRobot이 torch 2.11을 끌고 오는데 W1 실습 5개가 torch 2.13.0+cu130에서 검증된 상태입니다. 한 venv에 섞으면 W1 실행검증이 무효가 됩니다. **`.venv`는 W1~W1-M5용, `.venv-lerobot`은 W2 이후 LeRobot용**으로 고정합니다.

### 돌린 것과 실측 소요

| 절차 | GPU (RTX 3080) | CPU | 비고 |
|---|---|---|---|
| `uv pip install "lerobot[training,viz]"` | **42초** | — | `.venv-lerobot` 최종 **5.8 GB** |
| 데이터셋 `aloha_sim_transfer_cube_human` (69.9 MB) | 수 초 | — | fps **50** · 50 에피소드 · **6.7분** · action 14 |
| 데이터셋 `aloha_mobile_cabinet` (1.74 GB) | **40.6초** | — | fps 50 · 85 에피소드 · 카메라 **3대** |
| 데이터셋 `pusht` (7.7 MB) | 수 초 | — | fps 10 · action 2 · **`repo_id` 최초 확인** |
| `lerobot-dataset-viz --save 1` | **8.8초** | — | `.rrd` **25.0 MB** · 뷰어 spawn 없음 |
| **`lerobot-train` ACT 스모크 200스텝** | **31초** | **약 9.7분** | batch 8 · 손실 24.43 → **3.76** · `mem_gb 2.10` |
| 학습 1스텝 (`updt_s`) | **0.074초** | **2.86초** | **39배** |
| 재개 (`--resume=true --steps=260`) | 정상 (exit 0) | — | `checkpoints/000260` 생성 |
| **τ_infer** (청크 1회, batch 1) | **7.7 ms** | **112.1 ms** | **14.6배** |
| 앙상블 모드 스텝당 | **8.1 ms** | (112 ms) | 33 ms 예산: GPU 충족 / **CPU 초과** |
| `labs/verify_act_install.py --quick` | 수 초 | — | **PASS 27 · FAIL 0** |

**100k 스텝 외삽**: GPU **약 2.1시간** / CPU 약 80시간(비현실적). 공식 문서의 "단일 GPU 수 시간"과 정합.
**30분을 넘긴 절차: 0개.** 마스터플랜 부록 A.0의 "W1~W2는 로컬로 완결"이 W2 첫 모듈에서도 유지됐습니다.

### lesson 대조 결과 — 문서가 실물과 얼마나 맞았나

| 대조 항목 | 결과 |
|---|---|
| §6.4 하이퍼파라미터 **21개** | **21/21 일치** — 문서가 정확했습니다 |
| §7 영문 축자 인용 3건 | 2건 일치 · **1건 불일치**(아래 ①) |
| §10 신뢰도 각주 "미확인" 4건 | **3건 해소** · 1건은 새 불일치로 전환(아래 ③) |
| practice `01`·`02`의 lesson 표 검산 | lesson 패치 후에도 **16/16 · 20/20 PASS 유지** |

### 막힌 지점 · 발견 (6건)

**① lesson §7 축자 인용에 단어 하나 누락** — `there is a bug in the code that only the first layer is used` → 원문은 `that means only`. 하필 이 오해의 논지가 *"파일 경로:라인을 인용하라"* 라서 인용이 부정확하면 자기모순입니다. lesson을 정정하고 `configuration_act.py:108-109`를 명시했습니다.

**② `torchcodec` 로드 실패 → pyav 폴백** — `OSError: libavutil.so.56: cannot open shared object file`. **근본 원인은 시스템에 ffmpeg이 아예 없는 것**(`which ffmpeg` 없음, `ldconfig -p | grep libavutil` 없음). torchcodec이 libavutil `.so.56~.60`을 차례로 찾다 전부 실패합니다. **동작에는 영향 없음** — pyav 15.1.0으로 폴백해 데이터셋 로드·시각화·학습이 전부 정상이었고, 이 로그의 모든 실측이 그 상태에서 나온 값입니다. 다만 **첫 실행에서 수십 줄 빨간 traceback이 쏟아져** 입문자가 설치 실패로 오인하기 쉬워 labs 흔한 에러 1순위로 올렸습니다. 처방(`sudo apt install ffmpeg`)은 **sudo가 필요해 실행 검증하지 않았습니다.**

**③ 파라미터 수 불일치 — HF 문서 80M vs 실측 51.6M** — `ACTPolicy`가 실제로 만드는 학습가능 파라미터는 **51,613,582개**이고 **카메라를 1→4대로 늘려도 변하지 않습니다**(백본 공유, `modeling_act.py:334`에서 `self.backbone` 하나만 생성, `475`에서 카메라별 반복 호출). `use_vae=False`면 34,232,142개라 VAE posterior 인코더가 약 17.4M입니다. lesson이 8곳에서 쓰던 "약 80M"을 **52M으로 정정**하고, **왜 다른지는 확인하지 못했으므로**(원 ACT 구현을 읽어야 함, 이번에 읽지 않음) §10에 🔴 확인 대상으로 남겼습니다.

**④ `--policy.push_to_hub=false` 없이는 `--policy.repo_id` 요구** — lesson §8.2 커맨드는 HF 네임스페이스를 주는 형태라 계정 없이 돌리면 막힙니다. 스모크용 플래그를 lesson·labs 양쪽에 추가했습니다.

**⑤ 체크포인트가 1개당 592 MB** — 200스텝 + 260스텝 재개만 돌렸는데 `outputs/train/act_w2m1_smoke`가 **1.2 GB**가 됐습니다(`model.safetensors` 206.8 MB + optimizer state). 기본 `save_freq`로 100k를 돌리면 수십 GB입니다. `outputs/`는 `.gitignore` 231행에 이미 있어 커밋되진 않고 디스크만 먹습니다.

**⑥ `verify_act_install.py`가 lesson 문구를 하드코딩해 드리프트** — 스크립트가 lesson의 인용문과 파라미터 주장값을 상수로 복사해 뒀는데, lesson을 패치하자 **정상 문서를 WARN으로 잡았습니다.** "lesson이 박아둔 값이 실물과 맞는가"를 검사하는 도구가 lesson 쪽을 복사해 두면 lesson이 바뀔 때 조용히 어긋납니다. `../lesson.md`를 읽어 대조하도록 구조를 바꿨습니다.

### 윤문 (2026-08-09)

run `2026-08-09-001` · v1.6 `register: course` · strict · **등급 A**. 상세는 `course-plan` §9.10.

| 지표 | 값 |
|---|---|
| 산문 라인 변경률 (주지표) | **35.27%** (91/258행) · 밴드 20~50% 내 |
| 잔존 burden (주지표) | **28.0** (501.0에서 **94.4%↓**) |
| 전체 문자 변경률 | 0.76% (기록만 — 보호 구간 39.8%) |
| S1 잔존 | **0** |
| fidelity | `conditional_pass` → **롤백 4건** 반영 후 승인 |
| naturalness | `accept_with_note` · **과윤문 0 시그널** |

**이 회차의 새 장치**: 윤문 후 **하류 검산기 3종을 재실행**해 보존을 기계로 확인했습니다 — practice `01` 16/16 · `02` 20/20 · `labs/verify_act_install.py` PASS 28 / FAIL 0 / WARN 0. 특히 verify 스크립트가 `lesson.md`의 영문 축자 인용을 설치본 소스와 글자 단위로 대조하므로, **"윤문이 인용을 건드리지 않았다"를 사람이 아니라 기계가 보증**했습니다.

### GPU 비용

**$0.** 로컬 RTX 3080. 클라우드 인스턴스 미사용. **로컬 GPU wall-clock 약 2분**(스모크 31초 + 재개 18초 + 추론 측정 + viz 8.8초). CPU 측정에 별도로 약 12분.

### 미검증 배지

**lesson §8.2의 `⚠️ 미검증(GPU 필요)` 배지를 제거**하고 실행 검증 블록으로 교체했습니다. **남은 미검증 항목 3개**는 배지가 아니라 §10 각주로 명시했습니다.

- `sudo apt install ffmpeg` 처방 (sudo 필요, 미실행)
- **Jetson급 온보드 장치의 추론 지연** — 데스크톱 GPU/CPU 두 경우만 측정
- `--policy.device=mps` 경로 (기기 없음)

### 미기록

- **100k 본학습을 완주하지 않았습니다.** lesson §8.1이 이 모듈의 종료 지점을 "파이프라인이 돌기 시작하는 것까지"로 정했고, 완주는 W2-M2의 몫입니다. 2.1시간은 13 step/s에서 환산한 **외삽**입니다.
- **정책 성공률 미측정** — `lerobot-eval`·`lerobot-rollout` 미실행(시뮬 환경 extras 미설치).
- CPU 200스텝 학습은 **20스텝 68초에서 환산**한 값입니다(9.7분). 전량 실측 아님.
- 집필에 든 사람 시간 — 기록하지 않음.


---

## 2차 개정 회차 (규약 개정과 W1-M1 재작성) 검증 실행

**모듈 블록이 아니라 회차 블록입니다.** 위 여섯 개는 「새로 쓴 실습 코드가 도는가」를 잰 기록이고, 이 블록은 「규약을 갈아엎고 문서를 다시 쓴 것이 기존 산출물을 깨뜨리지 않았는가」를 잰 기록입니다. 대상도 한 모듈이 아니라 규약 SSOT와 집필 도구 5종과 lint 스크립트와 W1-M1 전체입니다. 위 W1-M1 블록의 2026-08-01 실측(Python 3.12.3, 세 스크립트 각 1.3초, 막힌 지점 6건)은 그대로 유효하고 이 블록이 대체하지 않습니다.

**집필 기간 / 커밋**: 2026-08-25(계획 수립)부터 2026-08-31까지, 커밋 **34개**. `876428d`(계획서 이관과 규약 SSOT 개정)에서 시작해 `38affbc`부터 `965ff7b`까지가 집필 도구 5종과 그 후속 정정, `3e1e8b3`부터 `9e10041`까지가 lint 검사군 8개, `50ffda6`부터 `94eb4db`까지가 W1-M1 재작성, `cba2356`부터 `e3a640e`까지가 검증 7단계(6-1, 6-2, 6-2R, 6-3, 6-3R, 6-3S, 6-3SR)와 그 사이에 끼어든 회사 스택 갱신, `25cbf00`과 `dfd8386`이 마무리입니다. 이 블록을 더하는 커밋은 세지 않았습니다. 단계별 기록은 [`course-author-update-plan.md`](course-author-update-plan.md), 규약 근거는 [`course-plan.md`](course-plan.md) §9.19와 §9.20입니다.

**검증 환경**

| 항목 | 값 |
|---|---|
| 저장소 | 브랜치 `main` · 검증 시점 HEAD `dfd8386` · 워킹트리 clean |
| Python | `.venv`의 **3.12.12**. 시스템 인터프리터는 3.12.3이고 W1-M1 원 집필 검증은 그쪽이었습니다(§수치 불일치 ④) |
| 패키지 | matplotlib 3.11.1 · numpy 2.5.1 · jupytext 1.19.5. **`pandas`는 `.venv`에 없습니다** (아래 §미기록) |
| Node | nvm의 **v20.20.2** |
| Mermaid | mermaid-cli **11.16.0**(mermaid 11.17.2)을 스크래치패드에 임시 설치. 기본 `securityLevel: strict`라 GitHub와 같은 조건 |
| SVG 렌더 | mermaid-cli에 딸려 온 Chromium. 라이트와 다크 배경 PNG, 의심 구간은 2배 배율 |
| GPU | **쓰지 않았습니다.** 이 모듈 실습은 matplotlib 시각화 전용이고 이번 회차에 학습도 시뮬레이션도 없습니다 |
| 측정일 | 2026-08-29부터 2026-08-31 |

**돌린 것과 실측 소요**

lint(`scripts/lint-lesson.sh`)는 Phase 4에서 검사군 8개를 개정했으므로 재작성 직전 상태를 같은 개정판으로 다시 재어 기준선을 잡았습니다.

| 지표 | 재작성 직전 (같은 lint로 재측정) | 2026-08-31 |
|---|---|---|
| W1-M1 단일 | PASS 19 · FAIL 11 · WARN 9 | **PASS 51 · FAIL 0 · WARN 0** |
| `--all` 총계 | 147 · 61 · 61 | **180 · 49 · 52** |
| W1-M1 본문 산문 | 16,058자 | **21,902자** (당시 Tier A 밴드 15,000~23,000, 여유 1,098자) |
| `eli5.md` 산문 | 파일 없음 | **4,868자** (§2.1 독립 밴드 3,000~5,000) |

표 21블록과 불릿 21블록이 동수이고, lint 초독 추정 128분이 frontmatter `est_reading_min` 120분과 편차 6%입니다.

> ⚠️ **위 표의 밴드 표기는 2026-08-31 시점 값입니다.** 그 뒤 두 번 바뀌었습니다. 2026-09-01에 Tier A가 21,000~32,000이 됐고, **2026-09-03에 문자 상한이 폐지돼 하한만 남았습니다**(A 21,000 이상). 상한은 `est_reading_min` 180분으로 옮겨졌습니다. 「여유 1,098자」는 지금 성립하지 않는 계산입니다. 근거는 [course-plan.md](course-plan.md) §4의 「상한을 문자에서 초독 시간으로 옮깁니다」 박스. `--all` 총계 변화는 두 곳에서만 왔습니다. W1-M1 재작성이 32칸을 옮겼고 W2-M1이 밴드 상향으로 FAIL 하나를 PASS로 바꿨습니다(147+32+1=180, 61−11−1=49, 61−9=52).

렌더와 참조는 구조 검사로 대체하지 않고 실물 파서와 브라우저를 돌렸습니다.

| 대상 | 방법 | 결과 |
|---|---|---|
| Mermaid 5블록 (lesson 4 · deep-dive 1) | mermaid-cli 실제 파서 | **5/5 파싱**. 가운뎃점 치환 후보 2종을 다시 넣어 **10/10 재파싱** |
| Mermaid 라벨 치환의 레이아웃 영향 | 렌더 SVG 바이트 비교 | 42,010 → **41,972**, 28,753 → **28,728**. 오히려 줄어 폭 증가로 깨질 여지가 수치로 배제됨 |
| SVG 9장 (lesson 5 · eli5 4) | Chromium PNG, 라이트와 다크 | XML 적격 **9/9**, 텍스트 넘침 1건을 찾아 고쳐 **0건**, 글자 잘림과 겹침 0, 한글 정상 |
| 상대 링크 68개 | 로컬 66개 전수 해석 | 깨진 것 **0** |
| 절 번호 참조 381건 | 전수 해석 | 어긋난 것 **0** |
| 체크리스트 110개 (6-3) | 육안 전수 판정 | 통과 86 · 위반 24. **위반이 전부 lint가 보지 않는 자리에서 나옴** |
| 체크리스트 44개 (6-3S, 스택 갱신분) | 육안 전수 판정 | 통과 36 · 위반 8 |

`practice/`는 저장소를 건드리지 않으려고 스크래치패드로 복사해 돌렸습니다.

- 세 스크립트를 `--smoke`와 전체 모드로 완주(6-3). **lesson의 9.6퍼센트와 78.7퍼센트와 41스텝이 실행 결과와 일치**합니다.
- `01_stack_frequency_budget.py --smoke` 실측 **1.5초**. 원 집필 검증의 1.3초와 같은 자리입니다.
- 분리비 표가 **4행**이고 Nav2 행의 20과 2와 10.0배가 lesson §3.3 표와 일치합니다. 고치기 전 판(`e3a640e~1`)을 같은 조건으로 돌리면 **3행만 나오는 결함이 그대로 재현**됩니다.
- jupytext 3쌍 모두 `--diff` 빈 출력, `.ipynb` `outputs` 전부 0개, 실행 전후 `.py`와 `.ipynb` md5 6개가 불변입니다.

**막힌 지점 (10건)**

*재작성 이전부터 잠복해 있던 것 4건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 1 | `01`의 `.ipynb`가 `--ascii-labels`에서 한글판 PNG를 덮어씀 | **위 W1-M1 블록 1번을 고친 커밋 `09548b3`이 `.py`만 고치고 `.ipynb`를 재생성하지 않음.** `.ipynb`의 git 로그에 그 커밋이 아예 없어 2026-08-01부터 잠복 | `.py` 기준으로 `.ipynb` 재생성. 차이는 cell 17 한 곳이고 셀 19개와 metadata와 `outputs` 0개가 보존됨 (`34df8ba`) |
| 2 | `practice/README.md`와 `labs/README.md`가 서로 반박 | 같은 결함이 문서 두 곳으로 번짐. `labs/README.md:592-593`이 옛 동작을 적으며 practice 쪽을 명시적으로 부정했고 **틀린 쪽이 labs**였음 | 세 스크립트가 전부 `suffix` 로직을 갖고 있음을 확인하고 labs의 옛 4줄을 실제 파일명 넷을 적은 3줄로 교체 (`34df8ba`) |
| 3 | `practice/`와 `labs/` 산문에 줄표 72회와 가운뎃점 58회 | 커밋 `9b7a617`이 「문장부호 규약 적용」이라는 이름으로 같은 파일들을 손대고도 `lesson.md`와 `deep-dive.md`만 0으로 만든 뒤 멈춤 | 전부 0으로. 계획서 집계는 56이었으나 코드펜스 경계 판정 차이로 실제는 58이었고 트레일링 주석 3건도 함께 (`bb171c7`) |
| 4 | `labs/worksheet.md:331`에 폐지된 「제어·ML·LLM 대응 비유」 열 | §3.9가 이 열을 폐지한 근거가 **다름 아닌 W1-M1 실측**(비유 지점 50개 중 36개가 이 열)인데 당사자 모듈에 남음. lint E군은 `lesson.md`만 봄 | 표를 2열로 줄이고 비유 작성 강제를 자기 말 정의로 교체. 파일 전체 「비유」 0건 (`bb171c7`) |

*이번 회차가 새로 만든 것 4건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 5 | `01`의 stdout 분리비 표는 3행인데 lesson §3.3 표는 4행 | Nav2 행을 만드는 `rows.append`가 `print_table` 호출보다 **뒤**에 있어 계산해 놓고 버림. **`course-plan.md` §9.20의 「실행 검증했습니다. 출력이 lesson §3.3 표와 일치합니다」가 사실이 아니었습니다.** 한 번이라도 돌렸으면 드러날 차이입니다 | 순수 이동 5줄. 두 모드 실행으로 4행 확인, jupytext 재동기, `worksheet.md` 채점을 `/3`에서 `/4`로 고치고 백지 표에 Nav2 행 추가, §9.20 허위 주장에 취소선과 정정 주석 (`e3a640e`) |
| 6 | `eli5.md`가 `lesson.md`용 SVG 4장을 그대로 재사용 | lint I군이 eli5 **산문**의 약어만 세고 SVG 파일을 열지 않음. 라벨에 eli5 본문이 한 번도 쓰지 않은 층 번호와 `q_des`와 `H_chunk`와 `setpoint`와 `RGB-D`와 회사 도구명이 노출 | eli5 전용 축약 SVG 4장 신설. `0.5 ~ 5 Hz`가 「1초에 한 번에서 다섯 번」이 되는 식. 캡션은 2단이라 말하는데 그림이 4단이던 불일치도 함께 해소 (`bb171c7`) |
| 7 | 다이어그램 라벨이 본문보다 먼저 용어를 소개 | §3.8이 이 규칙을 만든 근거가 **구본 W1-M1의 48줄 ASCII 블록도**인데, 블록도를 없애고도 같은 용어가 Mermaid 라벨과 SVG 라벨로 살아남음. 컨테이너만 바뀌고 위반이 생존 | §7.1 앞에 도입 산문 신설, §6.1 계보도 부제 9개와 `nav-substack.svg` 라벨 9종을 본문 어휘로 교체. **6-3S에서 새 노드의 `pcd`로 재발해 두 번 정정** (`bb171c7`, `6b24a30`) |
| 8 | `D_obs`가 §3.2 기호표에 없음 | 그림은 `관측 이력 [T_hist, D_obs]`인데 표는 `D_state`. 이름을 고정하겠다고 선언한 표가 자기 그림의 이름을 놓침 | `D_state`로 통일. 모듈 밖 한 곳까지 정리해 **저장소 전체 `D_obs` 0건** (`bb171c7`, `fa8de30`) |

*규약과 도구 쪽 2건*

| # | 증상 | 원인 | 해결 (반영처) |
|---|---|---|---|
| 9 | 산문을 한 글자만 더해도 lint FAIL | 재작성본 20,991자가 Tier A 상한 21,000에 **9자** 남았고, 「더 깊이」 목록 한 줄을 보완하자 여유가 **1자**가 됨. 후보 셋을 실측해 21,014자와 21,005자를 버리고 범위 형식(20,999자)만 통과 | 2026-08-30 사용자 승인으로 상한을 21,000에서 **23,000**으로 상향(근거는 §9.20). **2026-09-03에 문자 상한 자체가 폐지돼 근본 해소**(course-plan §4) |
| 10 | 그 상향이 **W2-M1의 밴드 초과 22,115자를 검토 없이 해소** | 밴드는 Tier A 모듈 8개 전체에 걸리는데 §9.20은 「이번 실측은 Tier A 하나의 것」이라 적음. `--all` 기준선이 179/50/52에서 180/49/52로 조용히 이동 | **2026-09-03 해소.** update-plan 8-7이 표본 7개(W1 다섯 + W2 대조 둘)를 함께 재어 밴드를 확정했고, 문자 상한을 폐지하고 상한을 `est_reading_min` 180분으로 옮겼습니다(course-plan §4) |

**메타 교훈 둘**

다음 회차에 그대로 걸릴 것이 둘입니다.

**① 동시 세션이 같은 워킹트리를 편집했고 `git add -A`가 남의 작업을 쓸어 담았습니다.** 6-3R이 도는 동안 다른 `physical-ai-study` 세션이 「W1-M1 회사 스택 갱신 반영」을 진행했습니다. 그 세션이 `git add -A`로 커밋하면서 6-3R 산출물 전부가 커밋 `c0c0adc`에 함께 담겼습니다. **유실은 없었지만 대가가 있었습니다.** 6-3의 체크리스트 판정 110건이 스택 갱신 이전 본문 기준이 되어 신설된 §7.4와 `nav-substack.svg`가 한 번도 검사되지 않은 상태가 됐고, 그것을 메우려고 6-3S를 따로 돌려 44개를 다시 판정해야 했습니다. 6-3R의 한 갈래가 다른 세션의 밴드 상향을 근거로 `deep-dive.md` 이관을 생략한 것도 같은 사건의 갈래입니다. **커밋 전에 `git status`로 자기 변경만 있는지 확인하거나 파일을 명시해 `add`해야 합니다.**

**② 검증이 검증을 반증한 사례가 둘이고 두 번 다 실물을 다시 본 쪽이 맞았습니다.** 6-2가 「SVG 4장 전부 `font-family` 미선언」이라 보고했는데, 6-2R이 파일을 열어 보니 최상위 `<g>`에 한글 폰트 스택이 이미 있었고 그 `<g>` 하나가 `<text>` 전량을 감싸며 개별 재정의가 없었습니다. **파일이 아니라 검사 정규식이 틀렸고**(여는 큰따옴표를 먹고 작은따옴표에서 빈 매치를 냄) 한 글자도 고치지 않은 채 보고를 철회했습니다. 6-3이 「팀 질문 `M1-7`이 적립되지 않았고 존재하지 않는 항목을 가리키는 참조가 2곳」이라 판정한 것도 6-3R이 철회했습니다. **6-3은 HEAD를 봤고 작업 트리에는 `M1-7`부터 `M1-11`까지 이미 있었습니다.** 대신 진짜로 빠져 있던 것 하나를 찾아 `M1-12`로 적립했습니다. 두 보고를 그대로 적용했다면 한 번은 멀쩡한 파일을 고치고 한 번은 없는 문제를 고쳤을 것입니다.

**GPU 비용**: **$0.** 클라우드 인스턴스 미사용이고 **로컬 GPU도 쓰지 않았습니다.** 이번 회차에 돌린 것은 lint 셸 스크립트와 mermaid 파서와 헤드리스 Chromium 렌더와 CPU 파이썬 스크립트뿐입니다.

**미검증 배지**: **없음.** W1-M1의 `lesson.md`, `eli5.md`, `deep-dive.md`, `practice/`, `labs/`에 「미검증」 문자열이 0건입니다. 다만 배지와 별개로 **닫히지 않은 것이 둘** 있습니다. 윤문(update-plan 6-4)은 절차상 학습자 검토 전에 하지 않기로 해서 미실시이고, 학습자 검토(6-5)는 요청 대기입니다.

**미기록**

- **집필과 검증에 든 사람 시간.** 이번에도 재지 않았습니다. 검증만 일곱 단계가 사흘에 걸쳤는데 단계별 소요를 남기지 않았습니다.
- **`02`와 `03`의 초 단위 재실측.** 6-3에서 완주 여부만 확인하고 시간을 잡지 않았습니다. 이 블록을 쓰며 `01 --smoke`만 1.5초로 다시 쟀습니다.
- **`03_player_landscape.py`는 `.venv`에서 돌지 않습니다.** `03`이 `pandas`를 import하는데(`03_player_landscape.py:67`) `.venv`에 그 패키지가 없습니다. 6-3R은 `01`과 `02`만 스모크로 완주시키고 `03`은 실행 대신 AST 동일성 비교로 대체했습니다. 6-3은 세 개를 다 완주했다고 보고했으므로 **두 회차의 실행 환경이 달랐다는 뜻인데 어느 환경이었는지가 기록에 없습니다.**
- **GitHub의 실제 렌더 화면.** 로컬 Chromium과 mermaid 파서로 대체했고 `securityLevel: strict` 조건은 같지만 GitHub 페이지에서 눈으로 본 것은 아닙니다.
- **`01`의 표기 정밀도 한 열.** stdout의 「빠른쪽[Hz]」이 `,.1f`라 `158.1`과 `3.2`를 찍는데 lesson §3.3 표는 같은 값을 `158.11`과 `3.16`으로 적습니다. **값이 아니라 표시 자릿수만 다릅니다.** `,.2f`로 바꾸면 이번에는 `1,414.2`가 어긋나 한 글자로 닫히지 않고, 선재 결함이라 승인 범위 밖으로 남겼습니다.

---

## Phase 8 확산 회차 (W1-M2~M5 재작성, 밴드 최종 확정, W1-M1 윤문) 검증 실행

**모듈 블록이 아니라 회차 블록입니다.** 위 「2차 개정 회차」가 규약을 갈아엎고 W1-M1 하나를 시범 재작성한 기록이라면, 이 블록은 **그 규약을 W1 나머지 넷으로 확산하고, 다섯의 실측으로 분량 밴드를 확정하고, 마지막으로 W1-M1 자신을 윤문한** 기록입니다. 대상은 `course/w1-generative-core/` 다섯 모듈 전부와 `docs/course-plan.md` §4와 `scripts/lint-lesson.sh`입니다. 위 모듈 블록들의 실습 실측(2026-08-01부터 08-05까지)은 그대로 유효하고 이 블록이 대체하지 않습니다. **이번 회차에 실습 코드를 새로 돌린 것은 없습니다.** `practice/`는 네 모듈에서 사실상 무변경이었고 검증의 무게가 문서 대조 쪽에 있었습니다.

**집필 기간 / 커밋**: 2026-09-01부터 2026-09-03까지, 커밋 **20개**. `55240cf`(확산 착수)에서 시작해 `5d8cb9d`부터 `abb6992`까지가 W1-M2 네 단계(8-1a, 8-1b와 8-1bR, 8-1c, 8-1d), `687f018`이 밴드 재교정과 Tier C 폐지(8-2), `aaa722e`와 `badfa82`가 W1-M3(8-3a부터 8-3d), `b3ee72d`와 `e554fea`와 `41387d3`이 W1-M4(8-4a부터 8-4d), `ff0cef8`부터 `4070ea2`까지가 W1-M5 다섯 단계(8-5a, 8-5b, 8-5bR, 8-5c, 8-5d), `9d59d36`과 `f323c26`이 밴드 최종 확정(8-7a부터 8-7c), `0d08904`가 W1-M1 윤문(8-8a)입니다. 중간에 `3cfe05d`가 회차 범위를 둘 늘렸습니다(8-7과 8-8 편입). 이 블록을 더하는 커밋(8-8b)은 세지 않았습니다. 단계별 기록은 [`course-author-update-plan.md`](course-author-update-plan.md) Phase 8, 규약 근거는 [`course-plan.md`](course-plan.md) §9.21입니다.

**검증 환경**

| 항목 | 값 |
|---|---|
| 저장소 | 브랜치 `main` · 8-8b 착수 시점 HEAD `0d08904` · 워킹트리 clean |
| Python | 8-8b 재측정 셸의 `python3`는 **3.14.6**. `scripts/lint-lesson.sh`가 검사 대부분을 python3에 위임하므로 lint 판정은 이 인터프리터가 낸 값입니다 |
| awk | `awk version 20200816` (BWK awk). §9.21이 기록한 「셀 길이를 바이트로 세던」 검사는 이미 python3로 옮겨져 이 환경에서도 문자 기준입니다 |
| OS | macOS 26.5.2 |
| GPU | **쓰지 않았습니다.** 이번 회차에 학습도 시뮬레이션도 없습니다 |
| 측정일 | 2026-09-01부터 2026-09-03까지, 최종 전수 재측정은 **2026-09-03** |

**돌린 것과 실측 소요**

`scripts/lint-lesson.sh`를 모듈별로 그리고 `--all`로 돌린 것이 이번 회차의 주 계측입니다. 8-7c가 lint를 개정했으므로 개정 전후를 같이 적습니다.

| 모듈 | 재작성 전 | 8-7c lint 개정 직전 | **2026-09-03 재측정** |
|---|---|---|---|
| W1-M1 | 이번 회차 이전에 재작성됨 (2026-08-28) | PASS 51 · FAIL 0 · WARN 0 (윤문 전) | **PASS 52 · FAIL 0 · WARN 0** (8-8a 윤문 후, 회귀 0) |
| W1-M2 | PASS 21 · FAIL 9 · WARN 9 | PASS 51 · FAIL 0 · WARN 0 | **PASS 52 · FAIL 0 · WARN 0** |
| W1-M3 | PASS 21 · FAIL 8 · WARN 9 | PASS 50 · FAIL 0 · WARN 1 | **PASS 52 · FAIL 0 · WARN 0** |
| W1-M4 | PASS 22 · FAIL 8 · WARN 8 | PASS 50 · FAIL 0 · WARN 1 | **PASS 51 · FAIL 0 · WARN 1** |
| W1-M5 | PASS 20 · FAIL 9 · WARN 10 | PASS 50 · FAIL 0 · WARN 1 | **PASS 52 · FAIL 0 · WARN 0** |
| W2-M1 (대조, 미재작성) | PASS 21 · FAIL 8 · WARN 9 | — | **PASS 22 · FAIL 9 · WARN 8** |
| W2-M2 (대조, 미재작성) | PASS 22 · FAIL 7 · WARN 10 | — | **PASS 22 · FAIL 8 · WARN 9** |
| **`--all` 총계** | — | 294 · 15 · 22 | **303 · 17 · 18** |

W1-M4에 하나 남은 WARN은 분량이 아니라 **`est_reading_min` 180분이 권장 상한 150분을 넘는 것**입니다. FAIL 문턱은 180분 초과라 걸리지 않습니다. W2 둘의 FAIL이 하나씩 는 것은 **의도한 순환 차단의 결과**입니다(`est_reading_min` 신고값과 lint 초독 추정의 30% 대조를 WARN에서 FAIL로 승격했습니다. M1은 46분 신고 대 추정 144분, M2는 43분 대 100분).

⚠️ **update-plan 8-7c 표의 W2-M2 행 「PASS 23」은 오기입니다.** 2026-09-03 재측정이 **22**이고, 같은 항목이 적은 `--all` 총계 303에서 W1 다섯의 합 259를 빼도 22입니다. `course-plan.md` §0의 W2 행은 8-8b가 정정했습니다.

산문 실측입니다. 8-7a가 `prose_metrics()`를 복제해 재고 실제 lint 함수로 전건 대조했습니다.

| 모듈 | tier | 재작성 전 산문 | 재작성·윤문 후 산문 | `eli5.md` | SVG | 3층 합계 |
|---|---|---|---|---|---|---|
| W1-M1 | A | 16,058 (1차 윤문본) | **21,876** | 4,858 | 9장 | 55,656 |
| W1-M2 | A | 17,477 | **31,355** | 4,953 | 8장 | 46,333 |
| W1-M3 | B | 15,597 | **37,196** | 4,299 | 8장 | 48,198 |
| W1-M4 | B | 18,387 | **43,710** | 4,735 | 8장 | 54,767 |
| W1-M5 | B | 17,486 | **41,014** | 4,948 | 8장 | 57,882 |

**막힌 지점, 그리고 회차 전체에서 배운 것 셋**

**① 독립 검증이 네 회차 연속 S1을 냈습니다.** 8-1b 5건, 8-3b 3건, 8-4b 1건, 8-5b 3건입니다. 8-5a가 8-4b의 교훈(「자체 검증은 재작성자와 같은 맥락과 가정을 공유해 자기 맹점을 보지 못한다」)을 받아 **재작성 단계 안에 제3자 둘(내용과 SVG)을 미리 붙였는데도** 이어진 독립 검증에서 S1이 3건 더 나왔습니다. 재작성 단계에 검증을 앞당기는 것은 잡는 건수를 늘리지만 **뒤에 붙는 독립 검증을 대체하지 못합니다.**

**② 마지막 두 회차는 S1의 무게중심이 그림으로 옮겨갔습니다.** 2차 규약(§3.2)이 ASCII 블록도를 SVG로 옮기라고 **명령**하는데, **그림의 숫자는 산문과 떨어져 있어 본문을 고쳐도 따라오지 않고 lint는 SVG 안을 보지 않습니다.** 8-4b에서는 핵심 아키텍처 그림이 방금 정정한 바로 그 양을 제3의 틀린 값으로 단언했고, 8-5b에서는 **그림이 문서 자신이 적어둔 경고를 지운 사례가 둘**이었습니다. `usage-curve.svg`의 가로축 눈금 `4,000`은 FSQ도 VQ도 아니고 저장소 어디에도 코드 수로 등장하지 않는데, 두 줄 아래 본문이 굳이 「세 쌍이 코드 개수가 정확히 같지는 않습니다」라고 경고하는 자리였습니다. `eli5-dead-words.svg`는 색칠 칸을 10 → 5 → 3으로 그렸는데 `labs/README.md:506`의 실측은 214 → 216 → 185로 평평하고, 그 자리가 바로 **「VQ는 사용률이 떨어진다는 표현은 오해를 부릅니다」**라고 경계하라 한 곳이었습니다. 랩이 조심하라고 한 오해를 그림이 그대로 그린 셈입니다.

**③ 밴드가 재던 것은 모듈의 크기가 아니라 집필자의 라우팅 결정이었습니다.** 8-7a의 최대 발견입니다. **3층 합계의 산포가 lesson 단독의 2.6분의 1**입니다(CV 0.096 대 0.248). 다섯이 만든 내용의 총량은 46,333부터 57,882까지로 상당히 균일한데 lesson 산문만 21,876에서 43,710까지 두 배로 벌어졌고, 그 차이는 거의 전부 「어디에 뒀는가」입니다. **밴드 안에 남은 두 모듈이 곧 `deep-dive.md`를 쓴 두 모듈**이었습니다(Pearson −0.794). 그래서 8-7b는 문자 상한을 올리는 대신 **폐지하고 상한을 `est_reading_min` 180분으로 옮기는** 안을 올려 승인받았습니다.

**다음 회차로 넘기는 미결 넷**

1. **`humanize-korean` 규약 개정 안건 둘.** `C-11`(연결어미 뒤 쉼표) 처방과 `C-12b`(쉼표 포함 문장 비율) 처방이 서로를 직접 생성하는 충돌이고 **표본이 넷**입니다(8-3c 제기, 8-4c 실증, 8-5c 진단 좁힘, 8-8a에서 처음으로 「방어 성공」 계측). 신규 패턴 후보 `R-2 재작성 유발 지시 단절`도 8-4c에서 탐지기와 감사관이 독립적으로 같은 자리를 지목해 실증됐습니다. **이 저장소가 아니라 스킬 소관**입니다.
2. **8-7c의 문턱 설계.** W1-M4가 FAIL이 아니라 WARN인 것은 **판정 입력이 집필자 자기 신고값 `est_reading_min`(180)이고 문턱이 「초과」라 정확히 180은 통과**하기 때문입니다. lint 초독 추정 183분은 판정에 쓰이지 않습니다. **문턱을 lint 추정치로 옮기면 M4는 FAIL이 되고 순환 차단(30% 대조 FAIL 승격)도 불필요해집니다.** 어느 쪽이 나은지는 표본이 더 필요합니다.
3. **`img/W1-M1-*.svg` 다크 모드 판정 충돌.** 8-5b가 범위 밖 참고로 5장에 「배경 rect가 없고 어두운 텍스트를 써 다크 모드에서 글자가 사라질 위험」을 보고했는데, 8-5bR이 다시 재니 **9장 전부 불투명 배경을 갖고 `fill` 없는 `<text>`가 0개**로 재현되지 않았습니다. **두 검증자의 판정 축이 서로 다를 수 있습니다.** 그때까지 「위험 있음」으로도 「없음」으로도 단정하지 않습니다.
4. **범위 밖으로 확인만 한 둘 (W1-M5).** `labs/README.md`의 베이스라인 val MSE가 `0.02173`과 `0.02181`로 갈리는데 `git log -S`로 **둘 다 최초 집필 커밋 `338e495`에서 들어온 것**이 확인됐고 재작성과 무관합니다. `practice/01`과 `02`가 인용하는 `ground-truth` 문서는 **저장소에 존재하지 않습니다**(`find -iname "*ground*truth*"` 0건). 둘 다 `practice/` 무변경 원칙 때문에 손대지 않았습니다.

**용어집 미롤업**

W1-M2부터 W1-M5까지 넷의 **절 끝 되짚기 표 33개 168행이 아직 `notes/glossary.md`에 반영되지 않았습니다**(M2 7개 34행, M3 8개 40행, M4 10개 51행, M5 8개 43행). W1-M1 롤업은 커밋 `64df5b3`(Phase 7-4)이 이미 했고 그때 139행이 160행이 됐습니다. 이번 회차가 만든 168행은 그 뒤에 생긴 것이라 아직 들어가지 않았습니다.

**GPU 비용**: **$0.** 클라우드 인스턴스 미사용이고 **로컬 GPU도 쓰지 않았습니다.** 이번 회차에 돌린 것은 lint 셸 스크립트와 문서 대조 파이썬 스크립트뿐입니다.

**미검증 배지**: **없음.** W1 다섯 모듈의 `lesson.md`와 `eli5.md`와 `deep-dive.md`에 「미검증」 문자열이 0건입니다. 다만 배지와 별개로 **다섯 모듈 전부 학습자 실행검증(산출물 5번)이 남아 있습니다.**

**미기록**

- **집필과 검증에 든 사람 시간.** 이번에도 재지 않았습니다. 모듈 하나가 네다섯 단계이고 사흘에 스무 커밋이 났는데 단계별 소요를 남기지 않았습니다. 세 회차 연속 같은 항목입니다.
- **재작성된 넷의 `practice/` 재실행 시간.** W1-M3와 W1-M4와 W1-M5의 `practice/`는 이번 회차에 사실상 무변경이라(각각 무수정, 완전 무변경, 완전 무변경) **기존 실행검증 자산을 그대로 신뢰했고 다시 돌리지 않았습니다.** W1-M2만 참조 4건이 바뀌었고 `jupytext` 부재로 `.ipynb`를 문자열 대조와 JSON 유효성 검사로 대체했습니다.
- **SVG 33장의 GitHub 실제 렌더 화면.** 이번 회차가 W1-M2부터 M5까지 SVG 32장을 신설했는데(M1의 9장을 더하면 41장) 각 회차의 구조 검사와 넘침 검사로 대체했고 GitHub 페이지에서 눈으로 본 것은 아닙니다.
- **W1-M1 SVG 9장의 다크 모드 실렌더.** 위 미결 3번과 같은 항목입니다. 8-5b와 8-5bR이 서로 다른 결론을 냈고 8-8 회차에 실렌더로 결착 짓지 않았습니다.

---

## 자료 간 수치 불일치 (집필 검증 중 발견)

이 로그를 작성하며 문서 간 값이 어긋난 곳입니다. **이 문서에서는 고치지 않고 어느 쪽이 정본인지만 기록합니다**(해당 파일 수정은 별도 작업).

| # | 위치 | 어긋난 값 | 정본 판단 |
|---|---|---|---|
| ① | W1-M3 `labs/README.md` §0.3 "본 실행 합계" | **약 2분 40초** vs `practice/README.md` §2.1의 **약 2분 55초** | **2분 55초**가 맞음 — 항목 합 97+9+68 = 174초 = 2분 54초. `course-plan` §5도 2분 55초. labs 표만 어긋남 |
| ② | W1-M4 practice 커밋 `f55575b` 본문 | "**RTX 3080 본 실행 4분 9초**" vs practice README **약 6분 30초**(112+137+138 = 387초) | **6분 30초**가 정본. 후속 커밋 `32b839d`가 `course-plan`을 이 값으로 정정함. 커밋 메시지는 과거 기록이라 정정 불가 |
| ③ | `course-plan` §9.2 표 vs §9.4 표의 W1-M3 문자 수 | **31,421자** vs **31,568자** | 윤문 전후 재측정 시점 차이로 보이나 문서에 명시가 없음. 차이 147자 |
| ④ | W1-M1 Python 버전 | **3.12.3** (requirements·labs §0.2) vs 나머지 4개 모듈 **3.12.12** — 같은 날(2026-08-01) 작성된 W1-M2도 3.12.12 | **양쪽 다 실제 값.** W1-M1은 다른 인터프리터에서 검증했다는 뜻이므로 그대로 둡니다. W1-M1 실습은 순수 CPU·matplotlib이라 영향 없음 |
| ⑤ | W2-M1 lesson 8곳의 ACT 파라미터 수 | **약 80M**(HF 문서 인용) vs **51,613,582(약 52M)**(LeRobot 0.6.1 실측) | **실측이 정본.** lesson을 52M으로 정정했습니다(2026-08-09). **HF 문서가 왜 80M인지는 미확인** — 원 ACT 구현을 읽어야 알 수 있고 읽지 않았습니다 |
| ⑥ | W2-M1 lesson §7 영문 축자 인용 | `a bug in the code **that only** the first layer is used` vs 설치본 원문 `**that means only**` | **설치본이 정본.** lesson 정정 + `configuration_act.py:108-109` 명시 |
